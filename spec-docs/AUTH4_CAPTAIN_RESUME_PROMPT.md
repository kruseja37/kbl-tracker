# AUTH-4 Captain Resume Prompt → now the `kbl-captain` skill

This protocol is now the canonical **`kbl-captain`** skill:
`.claude/skills/kbl-captain/SKILL.md` (mirrored to `.agents/skills/kbl-captain/` for Codex).

**Invoke it:** `/kbl-captain` in Claude Code, or tell a fresh session:
> read `.claude/skills/kbl-captain/SKILL.md` and follow it exactly

Kept as a pointer (not a copy) to avoid two diverging sources — the spec-assembler
duplicate-divergence lesson (SESSION_RULES "Pending cleanup").
