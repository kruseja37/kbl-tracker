# C4 BUILD AUDIT LOG — 2026-07-02 (Opus, sole auditor; builder = Codex, builder≠auditor)

Running audit log for the C4 Codex builds. Each lane: Codex builds → Opus audits the diff +
runs its own gate → verdict here. Branch-only; nothing pushed. Isolation: my remaining builds
run in worktree `opus/c4-builds` (`/Users/johnkruse/Projects/kbl-tracker-opus-c4`, off clean HEAD
`76d0b6de`) to avoid colliding with Fable's concurrent chem-potency work on the main tree.

---

## LANE 3 — CODEX-ASSTGM-LEGALITY — VERDICT: CLEAN (1 MINOR should-fix) — built on MAIN tree

**Files (confirmed scope, isolated from Fable's concurrent changes):** `src/engines/rosterAnalyzerEngine.ts`,
`src/src_figma/app/components/TradeFlow.tsx` + tests `rosterAnalyzerEngine.test.ts`,
`TradeFlow.franchiseTransactions.test.tsx`, and adapted existing tests `rosterAnalyzerDraftAdapter.test.ts`,
`TeamHubContent.franchiseReads.test.tsx`. No creep into Fable's files.

**L3-1 (analyzer never advises illegal) — CORRECT.** Canonical rules imported from `rosterConstruction`/`rosterNeed`;
the ad-hoc `DEFAULT_POSITION_MINIMUMS` (catcher min **1**) and `PITCHER_POSITIONS` are removed and replaced by
`LEGAL_ROSTER`-derived values (catcher min **2**, 13-14/8-9 flex, SP≥4/RP≥4, `canCover`/`isLegalRoster`/`depthReport`).
`sendDownLegalityAssessment` (`rosterAnalyzerEngine.ts:493`) computes `need = rosterNeedBreakdown(projectedSlots)`
on the correct post-removal roster; `safe` gates on `need.catcherCoverNeed===0` (and the other need floors), so an
illegal catcher send-down is suppressed/downgraded before any `send_down_advice` is emitted. Recommendations stay
`read_only`.

**L3-2 (non-blocking send-down warning) — CORRECT.** `TradeFlow.tsx` builds the projected post-move roster,
warns via the existing `setRosterStatusMessage` channel (new `kind:"warning"`), and STILL calls
`sendDownFranchisePlayer` — the eligibility gate is untouched, nothing blocks. The named only-catcher case is caught
by the explicit `catcherCoverers < LEGAL_ROSTER.minCatchers` check.

**MINOR (should-fix; NOT a blocker) — `wouldStrandRoster` misused for the removal case (both files).**
`rosterAnalyzerEngine.ts:505` and `TradeFlow.tsx` call `wouldStrandRoster(projectedPlayers.map(id), candidateId, positions)`
where `projectedPlayers` already EXCLUDES the candidate — so `wouldStrandRoster` re-adds the candidate and reconstructs
the ORIGINAL legal roster, making `strands`/`stranded` **always false**. Contract: `wouldStrandRoster` is a bid-time
ADD guard (does winning this player strand you), not a removal guard. Impact: `strands` is dead (the `need.*`/catcher
checks carry all correctness, hence 74/74 green), BUT `rosterAnalyzerEngine.ts:1170` emits a MISLEADING evidence line
`postSendDownWouldStrandRoster: <always false>`, and a future maintainer could wrongly trust it. **Fix (cheap):** drop
the `strands`/`wouldStrandRoster` computation + its evidence line in both files (the post-removal `need`/catcher checks
already cover stranding correctly). Fold into the lane-3 commit/cleanup.

**Test changes are legit (not weakening).** `rosterAnalyzerDraftAdapter.test.ts` + `TeamHubContent.franchiseReads.test.tsx`
moved a fixture/expectation from a generic `OF` bucket to canonical `CF` — correct, because `LEGAL_ROSTER.fieldPositions`
is `LF/CF/RF` (no `OF`); the tests still assert a real coverage finding is produced.

**Gate (my own run):** `npm run build` exit 0 (worktree sanity + Codex); lane-3 targeted tests **74/74 pass** (my run:
`rosterAnalyzerEngine` 11, `TradeFlow.franchiseTransactions` 9, `rosterAnalyzerDraftAdapter` 7, `TeamHubContent.franchiseReads` 47).
Codex full-suite report: 3 reds (`wpaRuntimeBoundary` known-red; `franchiseManualSmokeFixture` + `archetypeBalanceSimulator`
timeout-in-full/solo-pass) — all in the characterized/flaky set, 0 lane-3-related. NOTE: that full-suite run was on the
MIXED main tree (Fable's chem work present), so it is NOT a clean lane-3 gate — the clean signal for lane 3 is the
74/74 targeted pass above.

**COMMIT STATUS:** NOT committed. Lane-3's 6 files are cleanly separable from Fable's concurrent work in the main tree;
they can be committed by explicit path once (a) the MINOR is fixed and (b) JK sets the branch-reconciliation order.
Lane-3's TradeFlow warning is USER-VISIBLE → also awaits JK browser sign-off per the model.

---

## QUICK WINS — CODEX-QUICKWINS-B1 — VERDICT: CLEAN — built on MAIN tree (isolation note below)

**Files (confirmed disjoint from Fable + lane-3):** `AppHome.tsx`, `EndOfDraftStaffing.tsx`, `FranchiseHome.tsx`,
`LeagueBuilderLeagues.tsx`, `LeagueBuilderFarmAuctionDraft.tsx`. No test files touched (pure cleanups; existing coverage).

**All 8 items correct:** QW-1 Builder→/league-builder; QW-2 EXHIBITION casing + dead lucide import gone + fractional-px
normalized; QW-4 dead `recap` toggle+state+`Newspaper` gone; QW-5 fake E/W leaders toggle removed — verified the two
branches aliased the SAME object (`battingLeadersDataAL === ...NL === franchiseData.battingLeaders`), so collapsing the
ternaries to the AL alias is behavior-preserving; QW-6 duplicate Home icon button gone (branded logo kept, same
`handleLogoClick`); QW-7 duplicate View row-action + `ChevronRight` import gone (Edit kept); QW-8 farm CTA →"Continue to
Staff Your Clubs" (nav unchanged); QW-9 staffing CTA →"Confirm Staff and Continue to Franchise Setup" (nav unchanged).

**Gate (my own run):** `FranchiseHome.test.tsx` **12/12 pass** (the riskiest — toggle removal). Codex: build exit 0;
`LeagueBuilderLeagues.test.tsx` 30/30; full suite 2 reds (`wpaRuntimeBoundary` known-red; `franchiseManualSmokeFixture`
solo-pass) — 0 new. No test-weakening. **COMMIT STATUS:** NOT committed; USER-VISIBLE → awaits JK browser sign-off.

---

## ISOLATION NOTE (correction) — the worktree didn't take; disjointness is the real guard

Intended: run my Codex builds in worktree `opus/c4-builds` to isolate from Fable's concurrent main-tree work. RESULT:
Codex `--cd <worktree>` did NOT redirect its file edits with the sandbox bypassed — both lane-3 and quick-wins landed in
the MAIN tree (worktree diff empty; Codex's own diff paths were main-tree absolute). NO HARM: every one of my jobs is
file-disjoint from Fable's (Fable = chem/potency/ivEngine/franchiseInitializer/leagueBuilderPoolBuilder; mine = analyzer/
TradeFlow/UI pages/App.tsx/new kit files). The ONLY overlap is `CODEX-C4A-GUARD` ↔ Fable's `franchiseInitializer` →
that job stays HELD. Real residual risk = concurrent build/test flakiness in one tree → detect via anomalous (non-
characterized) reds + re-run solo. Go-forward pending JK: keep all C4 UI work in one tree for coherent browser review,
commit per-feature after his sign-off, hold C4A-GUARD.

---

## CHEM-POTENCY (Fable #4, rulings 4-6) — VERDICT: CLEAN (1 doc-only nit) — CONFIRMED (calibration green)

**Builder:** Fable 5. **Auditor:** Opus (cross-model). Scope (new): `chemistryTierValue.ts`+test,
`chemistryIntelligence.ts`+test; (modified) `ivEngine.ts` (+50 additive), `chemistryFitValue.ts`,
`derivedTraitPotency.ts`, `franchiseInitializer.ts` (captain tilt), `leagueBuilderPoolBuilder.ts`
(lockLeaguePool axis regen), + tests.

**Economy-freeze = proven at SOURCE.** `auctionMarketModel.ts`, `auctionStateMachine.ts`,
`cpuShillBidding.ts`, `auctionPoolSizing.ts`, `trueValue.ts`, `rosterEngineConstants.ts` are ALL
byte-identical (not in the diff; trueValue verified by hash match). The premium is an
intelligence-only layer by construction.

**My gate (run by me, not trusted from the report):** `npm run build` exit 0; full suite
**2 reds / 8723 passed — both reds characterized** (`wpaRuntimeBoundary` hard-fail +
`franchiseManualSmokeFixture` order-flake, which I re-ran SOLO → 4/4 pass), **0 new**. C3 pool
sizing sweep **3/3, SWEEP_RC=0** (completion guarantee intact — lockLeaguePool didn't perturb
sizing). C2B market calibration **1/1, CALIB_RC=0** — second-price bands still cover true clearing
prices (coverage 0.98–1.0 at the spec gate). CONFIRMED: CPU/market path behaviorally unchanged.

**My inline read:** `chemistryTipPremium` partition is correct by construction — teamLift iterates
existing roster only (candidate excluded), ownContext iterates candidate traits only → no
double-count; crossings gate on real 2→3 / 6→7 tier change; removal path skips exactly the
departing player and prices the down-tier as a loss. `chemistryIntelligence` reads only
chemistry/traits/ratings.

**3-lens adversarial pass (Opus Workflow) — ALL HOLDS, zero defects:**
- L1 (IV-dollar primitive + byte-stability): `traitPotencyDollarDelta` = clean priceAt(to)−priceAt(from)
  reusing holder curves; flat/multipliers excluded (proven: flat-fee/mult-only traits price 0 across
  all tier pairs); negative traits yield positive delta via `scaleDelta` (no manual polarity flip);
  SP/RP negative-block rule verbatim; L2→L2 = 0; **computeIV byte-identical (oracle 11/11: G1/G3/G4/G10
  green)**; POTENCY_SCALE canonical 0.5/1/3 & inverted 3/1/0.5, unchanged.
- L3 (single-math + trueValue): `chemistryFitTier` delegates to `derivedPotencyTier` (3/7); no divergent
  4/8 copy remains; ±8% scout-mult SHAPE preserved (farm test self-referential — real shift, not a
  rubber-stamped pin); **trueValue.ts byte-identical (hash match)**; only `derivedTraitPotency` change is
  a `readonly` type widening (runtime-inert); tsc clean.
- L4 (rulings + privacy): captain tilt bands/monotonic/non-finite→0 correct, **age-28 fixtures → 0 tilt
  so existing captain pins unaffected** (9/9 franchiseInitializer green), span-12 breaks near-ties but
  not clear gaps; lockLeaguePool regen byte-identical for auction, new-but-deterministic for snake, and
  **does NOT disturb the IV/market snapshot** (toSalaryPlayer/buildSalaryIvInput never read chemistry/
  personality); **privacy holds — grep-verified the new math never reads personality/hiddenPersonalityModifiers**;
  advice functions consumer-less BY PLAN (C4-B + analyzer are named future consumers).

**ONLY FINDING — MINOR, doc-only:** design §3.3 says the farm-price shift is "at counts 3 and 7"; it
actually shifts the multiplier at counts 2,3,4,6,7,8 (boundary moved down one). Prose understatement;
code correct. No fix required (optional doc touch-up).

**VERDICT: CLEAN.** No code defect found across gate + inline + 3-lens adversarial. COMMIT STATUS: NOT
committed (awaiting calibration confirm + JK commit-order call; chem-potency files are disjoint from
lane-3/quick-wins → committable by explicit path).

---

## C4A-GUARD — CODEX-C4A-GUARD — VERDICT: CLEAN — built on MAIN tree

**Scope:** `src/utils/franchiseInitializer.ts` (+25) + `src/utils/tests/draftPipeline.integration.test.ts` (+236, 3 new tests)
+ `src/src_figma/__tests__/franchiseMode/franchiseInitializer.test.ts` (mock adaptation — see note). Disjoint from
quick-wins/lane-3/chem.

**Logic CORRECT.** New `assertAuctionDraftReadyForFranchise(leagueId)` (`franchiseInitializer.ts:113`) throws the
block message if the MLB auction session EXISTS and `state !== 'AUCTION_COMPLETE'` (and same for a present-but-incomplete
FARM session); returns (allows) when the session is ABSENT. Called at `:688` BEFORE `createFranchise` (`:690`) → no orphan
metadata on block. Seeding gate tightened `:782` to `mlbSession?.session?.state === 'AUCTION_COMPLETE'`.

**KEY-CORRECTNESS verified (the sharp risk):** the precheck uses `franchiseLeagueId`, seeding uses `config.league` — I
confirmed `const franchiseLeagueId = config.league` (`:678`), so BOTH read the SAME session key. The mock unit test can't
prove this (its mock ignores the arg), but the REAL-DATA integration tests do (they'd fail if the key were wrong) — and
they pass.

**Third-file note (scope):** the guard also edited `src/src_figma/__tests__/franchiseMode/franchiseInitializer.test.ts`
(`mockResolvedValueOnce`→`mockResolvedValue` for `getAuctionSession`/`getAuctionSessionById`). LEGIT adaptation, not
weakening: the new precheck adds a second `getAuctionSession` call, so the once-only mock had to persist (same complete
session returned; same behavior asserted). Slightly beyond the contract's stated file scope but necessary + benign.

**Gate (my own run):** guard targeted tests **17/17 pass** (draftPipeline.integration 9/9 incl. the 3 new block/allow
cases on REAL data; src_figma franchiseInitializer mock 8/8). Codex: build exit 0; full suite 3 reds (wpaRuntimeBoundary +
franchiseManualSmokeFixture + archetypeBalanceSimulator — all characterized timeouts/known, solo-pass), 0 new.

**COMMIT STATUS:** NOT committed. Files disjoint from quick-wins/lane-3 → committable by explicit path. Minimal UI surface
(only a block message on incomplete-draft franchise creation) — low browser-review burden.

---

## BALLPARK-KIT — CODEX-BALLPARK-KIT — VERDICT: CLEAN (code) — Fable §9 conformance + JK browser sign-off PENDING

**Scope (correct):** NEW `src/src_figma/styles/ballpark-kit.css` (CSS vars, imported in `main.tsx:9` — the LIVE
Tailwind-v3 path, not the inert theme files) + `src/src_figma/app/components/ballpark/BallparkKit.tsx` (5 primitives:
BallparkShell / PressButton / PanelWithHeaderStrip / ChunkyModal / LeftAccentFeedCard). Adopted by the 6 league-builder
page headers (LeagueBuilder + Leagues/Teams/Players/Rosters/Rules). **GameTracker.tsx + AuctionStage.tsx untouched (verified
git diff empty).** No creep into Fable's taxonomy files.

**Gate (my own run):** `NODE_ENV= npm run build` **exit 0** (✓ built 20.8s); `tsc --noEmit` 0 errors; LeagueBuilder test
dir **237/237 pass** (12 files). Codex: `git diff --check` clean; full suite 2 reds characterized (wpaRuntimeBoundary +
franchiseManualSmokeFixture), 0 new; D11 strings + icons/colors/right-slots preserved.

**⚠ CONCURRENT-TREE NOTE (worth remembering):** during the kit build the shared-tree build was transiently RED from
Fable's UNCOMMITTED `prospectScoutingDraftEngine.ts:667` TS7053 (her in-flight taxonomy) — NOT the kit. It cleared when
she fixed it. Lesson: with Fable building concurrently, `npm run build` red on the shared tree may be HER in-progress
code — verify error ownership (`tsc --noEmit` + file attribution) before attributing to my diff.

**COMMIT STATUS:** NOT committed. This is a UI reskin → Fable is the design authority (§9 conformance review pending) +
JK browser sign-off on the wired whole. Kit + C4-B slice 1 will be reviewed together as a coherent UI unit, then committed.

---

## C4-B SLICE 1 — CODEX-C4B-SLICE1 — VERDICT: CLEAN (code) — Fable §9 + JK browser sign-off PENDING

**Scope (slice-1 files):** `AuctionStage.tsx`, `LeagueBuilderAuctionDraft.tsx`, `auction-theme.css`, `AuctionStagePreview.tsx`
+ tests (`LeagueBuilderAuctionDraft.test.tsx`, `useAuctionDraft.test.ts`). Disjoint from the kit headers + Fable's taxonomy.

**Correct:** wired `buildLotViewFromSession` + `estimateMarket` into the VM; public price band {low/median/high} + CONTESTED
(counts/prose) render on the lot card; old `draftGuideAffordability`/`scoutInsight` push-cap-pass advisory REMOVED + coach
voice retired; R-IA3 setup inputs (seed/CPU/bid-increment) removed (dev seed → `?devSeed=`); §6 copy grep clean; mobile
band-stacking fix.

**TOP RISK RESOLVED — the unproven adapter lit up end-to-end.** Codex real-data browser check on a locked MLB league:
session `OPEN_BIDDING`, lot `pos` populated, `buildLotViewFromSession` returned a view, `estimateMarket` band
`$43,944 / $54,836 / $64,054`, CONTESTED `rivalCount: 12`. First real session→view→React run works.

**SLICE BOUNDARY HELD (verified):** grep of the diff shows ZERO slice-2 symbols added (no `projectBidVsPass`, whisper,
`chemistryAdvice`, verdict, nominationOdds). Bid-vs-pass + advice panel correctly deferred to slice 2.

**Gate (my own run):** `npm run build` exit 0 (✓ 9.16s); LeagueBuilderAuctionDraft + useAuctionDraft **20/20**; GameTracker
untouched. Full suite: 3 reds — 2 characterized (wpaRuntimeBoundary, franchiseManualSmokeFixture) + **EliminationTeamHub
(NEW but a FLAKE — solo 6/6, not in the slice-1 diff, loading-state timing under full-suite load)**. 0 real new regressions.

**COMMIT STATUS:** NOT committed. UI reskin → Fable §9 conformance + JK browser sign-off (reviewed together with the kit as
the coherent auction UI). Both remain UNREVIEWED-by-Fable/JK — accumulating UI review-debt (kit + slice-1).

---

## PLAYER-ARCHETYPE TAXONOMY (Fable #5) — VERDICT: BLOCK (fix round) — 2 MAJOR + minors; safety dims CLEAN

**Builder:** Fable 5. **Auditor:** Opus (cross-model). Scope: `playerArchetypeTaxonomy.ts` (data + 17 lifted families
+ 14 extended shapes + menus + personality groups), `playerArchetypeClassifier.ts` (reverse classifier + tags +
alignment), `rosterDesignFeasibility.ts` (design-feasibility evaluator), `prospectScoutingDraftEngine.ts` (relocation),
+ tests + sweep.

**CLEAN dimensions (my gate + 3-lens adversarial, all HOLDS):**
- **PRIVACY (ruling-5) ✓** — classifier + tags read VISIBLE primary personality only; grep-confirmed ZERO reads of
  `hiddenPersonalityModifiers`/hidden fields; polluted personalities → UNKNOWN (no crash); the feasibility consumer
  threads no hidden data.
- **NO-DH (ruling) ✓** — TaxonomyPosition excludes DH; no-DH pin test passes.
- **Relocation byte-identical ✓** — the 17 generator families diff IDENTICAL to the pre-move array; generator imports
  them; its 39 seeded tests pass (no generation drift).
- **No-value-parity (re-sync ruling) ✓** — zero balancing/normalization pass; weak shapes genuinely weak.
- **Honest alignment (trap guard) ✓** — `shapeAlignmentScore` takes no IV/value arg; pure identity cap-shift fit.
- **Additivity ✓** — feasibility evaluator is a NEW file; every frozen completion/market source untouched; 59/59
  frozen suite. **Shape-not-level ✓**, tilt-is-a-weight-not-a-filter ✓.
- **My gate:** build exit 0; full suite 3 reds all characterized (wpaRuntimeBoundary + franchiseManualSmokeFixture +
  franchiseOffseasonGuards.component — all solo-pass), 0 new; engine tests 22/22; sweep S1/S2/S4 3/3.

**MAJOR 1 (CONFIRMED by me — functional bug) — the feasibility evaluator OVER-REJECTS.** `evaluateRosterDesign`
(`rosterDesignFeasibility.ts:201-253`) is greedy + non-backtracking: slots processed in fixed kind order, each takes
the cheapest eligible unused match (`used.add`, no re-assignment). Classic greedy bipartite-matching failure — an
earlier loose/no-ask slot consumes a scarce player a later specific-ask slot uniquely needs → returns `feasible=false`
on a design that IS completable. Undermines the evaluator's core promise ("blockers fire exactly when true"); the test
suite is green but has NO greedy-defeated case. FAILING CASE: pool = 3 generic SP + 1 cheap Effectively-Wild arm;
design asks Effectively-Wild on the 2nd SP slot (1st loose) → SP1 grabs the cheap special arm → SP2 falsely blocked.

**MAJOR 2 (design-principle violation + drift risk) — personality groups are a HAND-TABLE, not engine-derived.**
Design §2.2 claims membership is DERIVED from the engine sets (relationshipFormation/traitAcquisition) "instead of
duplicating a hand table." As-built `PERSONALITY_GROUPS` (`playerArchetypeTaxonomy.ts:336-344`) is a standalone
literal; NO engine imports; the only pin checks it against the canonical 7, not the engine semantics. Values agree
TODAY but a future edit to the engine sets leaves the table silently stale (breaking avoid-fragile/prefer-steady).

**MINORs:** allowRunnerUp defaults true → a runner-up match reports the PRIMARY shape (asks Complete-Bat, gets a
Slugger labeled "Slugger") — slightly-generous feasible + mislabeled board (`rosterDesignFeasibility.ts:250`); a second
weaker (greedy) feasibility calc sits beside the canonical exact completion floor (two verdicts possible); budget-culprit
market basket recomputes with pitchersUsed=0 (cosmetic premium-text skew); doc drifts (generator at src/utils not
src/engines; "14/14" vs 15 tests; alignment named archetypeFitScorer but code uses archetypeCapShift).

**COMMIT STATUS: HELD pending fix round (FABLE-TAXONOMY-FIX below).** The classifier/data/relocation are commit-ready;
the leg commits as a unit after MAJOR-1 (matching fix + greedy-defeated test) + MAJOR-2 (derive or pin-test) land and
I re-audit the delta.

### TAXONOMY — ROUND 2 (FABLE-TAXONOMY-FIX) — VERDICT: CLEAN (delta re-audit by Opus)
F1 FIXED+VERIFIED: `evaluateRosterDesign` now Kuhn's max bipartite matching (augmenting paths, most-constrained-first;
feasible IFF complete matching); my exact greedy-defeat repro pinned as a regression test (SP2 Effectively-Wild → feasible=true)
and passes; saturated-pool slot-permutation subtlety documented+tested (verdict/cost/tilt are the promises). Frozen completion
machinery still untouched (matching stays a consumer). F2 FIXED+VERIFIED: relationshipFormation + traitAcquisition export
their own named sets (STEADY_SET/ANTAGONIST_SET/COMPOSURE_NEGATIVE_IMAGE_DRIVERS; pure refactor — both live-engine suites
green), and a pin test asserts PERSONALITY_GROUPS STEADY==STEADY_PERSONALITIES / FRAGILE==COMPOSURE_NEGATIVE_IMAGE_DRIVERS →
drift fails the build. F3: runner-up matches report the ASKED shape flagged. F4: docs corrected. GATE (my run): build 0;
fix-surface + both engines 158/158; full suite 3 reds all characterized order-flakes (0 new); sweep 3/3. COMMITTED.

---

## DRAFT ROOM SETUP-MERGE — CODEX-DRAFTROOM-MERGE — VERDICT: my-code-gate CLEAN — awaits Fable §2/§9 design-review (+ watchdog caveat)

**WATCHDOG CAVEAT:** Codex exit rc=143 — my 45-min watchdog killed it AFTER the code + full-suite finished (the suite ran to completion inside the log, my build passes), so Codex's SELF-REPORT (its §9 checklist / any STOP-IF) was truncated. I therefore verified the build DIRECTLY rather than trusting a report. (Lesson for big UI merges: raise the watchdog to ~60min OR tell Codex to skip its own full-suite run — I run the suite myself.)

**Structural merge DONE + coherent:** `DraftSetupHubPreview.tsx` DELETED (merged into `LeagueBuilderDraftSetup.tsx`);
`/draft-config` → `<Navigate to="/league-builder/draft-setup" replace />` (Navigate imported); `ScoutHire.tsx` link
updated; `useLeagueBuilderData.ts` wired. GameTracker + AuctionStage untouched (verified).

**PERSISTENCE done right (the one real risk):** additive `DraftSetupSeat` + `draftSeats?`/`gmSeatId?`/`gmSeatName?`
fields on the EXISTING `LeagueTemplate` (`leagueBuilderStorage.ts`) — NO new IndexedDB (grep: no new `indexedDB.open`/
`createObjectStore`), so NO 4-registry gotcha. The merge's own test "persists a changed GM seat name through the existing
league and team records" (`LeagueBuilderDraftSetup.test.tsx:251`) PASSES.

**Gate (my own run):** `npm run build` exit 0; the merge's test file **7/7 SOLO** (incl. seat-name persistence + the
saved-auction crash-guard/resume). Full suite 4 failing FILES, ALL attributed: wpaRuntimeBoundary (known) +
franchiseManualSmokeFixture (known timeout) + `LeagueBuilderDraftSetup.test.tsx` (the merge's tests TIMING OUT under
full-suite load — pass 7/7 solo) + `identityPreferenceBoost.test.ts` (FABLE's concurrent taxonomy-polish WIP, NOT the
merge). **No real merge regression.**

**MINOR (for Fable's §9 review):** possible duplicate "Draft Room" title — `LeagueBuilderDraftSetup.tsx:722` (BallparkShell
title="Draft Room") + `:733` (title "Draft Room — {league}"). Verify they don't both render (the §2 kill wanted ONE plate).

**NEXT GATE: FABLE design-review against §2 + §9** (her stated role for this build) — then commit (explicit paths, isolated
from her polish WIP). Deferred JK+Fable readability pass (handoff §9) follows the chalk-and-ash flip + full wiring.
COMMIT STATUS: HELD for Fable's design-review.

---

## TAXONOMY-POLISH (Fable §5c leg) — VERDICT: CLEAN (delta re-audit by Opus)
Two items. **Project age-qualification** (`playerArchetypeClassifier.ts`): Project classes now key on age/rawness not
geometry — `projectEligible = ageBand∈{null,rookie,rising} OR potentialGap ≥ TAXONOMY_TUNING.projectMinPotentialGap`;
unknown markers don't disqualify (permissive); strong-gap (≥2) boost so a declared prospect beats its geometric
near-twin star shape (the S4 finding). **Preference-aware builder** (`archetypeBalanceSimulator.ts`): an OPTIONAL
`slotBonus?` threaded through identityGreedyStart/identityShortlist/the fit objective — every use is
`+ (slotBonus?.(...) ?? 0)`, so ABSENT → adds EXACT 0 → byte-identical no-preference builds; the module stays
classifier-free (caller computes the bonus) so its calibrated dependency surface is untouched. BYTE-STABILITY holds by
construction + the archetypeBalanceSimulator calibration test (1/1). Gate (my run): build 0; classifier 17/17,
identityPreferenceBoost 2/2 (steers the asked slot without breaking feasibility), archetypeBalanceSimulator 1/1; sweep
S1/S2/S4 3/3; full suite 2 reds (both characterized), 0 new. (S4 gate passes; the exact recovery-lift is Fable's
measured tuning story.) COMMITTED.

---

## POOL-FROM-DEMAND (Fable §6.1, Mode A) — VERDICT: CLEAN (cross-model audit by Opus)
One new engine `poolFromDemand.ts` (+test); frozen C3 completion/sizing machinery UNTOUCHED (additive). §6-faithful:
classify universe → aggregate HUMAN design demand cells → reserve ceil(asks×contest[2]) with price-spread → UNION the
C1B archetype-floors pool → re-verify every design against the FINAL pool. **Completion guaranteed BY CONSTRUCTION:**
the extracted pool = cell-reservations ∪ `extractDraftPool` (C1B) output — a SUPERSET of the audited floors pool that
alone guarantees completion, so demand cells can only add, never break it. DETERMINISTIC (salary+id order, no RNG).
Reason-carrying shortfalls (correctly drive the design verdict to blocked when the universe can't meet a cell). PRIVACY:
pure/machine-side engine, no cross-GM leakage. Documented v1 choices: CPUs ride the floors (no shape cells — floors
guarantee their completion), no trim-to-target (oversupply safe, owner edits before lock). Gate (my run): build 0;
poolFromDemand 4/4 (contest multiplicity + feasibility + shortfall-blocks + determinism + price-spread); full suite
2 reds both characterized, 0 new. Test coverage strong. COMMITTED.

---

## COLOR-FLIP (chalk-and-ash) — CLEAN — 2026-07-02

**Builder:** Codex (from FABLE_COLOR_FLIP_SPEC_2026-07-02.md). **Auditor:** Opus.
- Diff is EXACTLY the 5 token value swaps Fable specified, one file (`ballpark-kit.css` :root), `git diff --numstat` = 5/5. No class rule, font, shadow, press token, or .tsx touched.
  - `--ballpark-page-bg` #2d3d2f→#CBB89C · `--ballpark-panel` #556B55→#3d4a42 · `--ballpark-panel-border` #4A6844→#3d5240 · `--ballpark-action-green` #4A6844→#3d5240 · `--ballpark-action-green-hover` #5A8352→#4A6844
- Destructive-red `#DD0000` UNCHANGED (Fable: signal-red split is next-rev; #DC3545 absent from GameTracker so introducing it would break match-the-reference).
- Opus's own `NODE_ENV= npm run build` exit 0 (not trusting Codex's claim).
- The 3-token aliasing onto the named field-green palette values is DELIBERATE per Fable's spec (panel-border serves both panel-rim and well-edge roles) — not a false-positive.
- VISUAL acceptance = Fable design-review (she authored the exact values) + JK browser look. Deferred kit-gaps (texture, well-scroll, ALL-CAPS labels, signal-red) are next-rev, out of scope.
**VERDICT: CLEAN — committed.**

---

## PAYLOAD (RosterIntelligencePayload backbone) — CLEAN after FIX-1 — 2026-07-02

**Builder:** Codex (CODEX-PAYLOAD). **Auditor:** Opus (gate + 3-lens adversarial Workflow).
Two new files only (`src/engines/rosterIntelligencePayload.ts` + test); no other source touched.

### Opus gate (all my own runs, not trusting Codex's claims)
- `NODE_ENV= npm run build` exit 0 with the new files present.
- Full suite: 3 red files, ALL characterized (wpaRuntimeBoundary hard-fail; franchiseManualSmokeFixture timeout-flake; archetypeBalanceSimulator order-flake — CONFIRMED green solo). 0 new reds; 8764 passed.
- Targeted `rosterIntelligencePayload.test.ts` 6/6 (my run).
- Code read: pure (no store writes/React/RNG), deterministic (id tie-breaks), privacy-safe (visible trait1/trait2 via public TRAIT_PRICING only; consumes caller-supplied iv so callers can pass fogged farm values), type matches ASST_GM_DESIGN §6.

### 3-lens adversarial Workflow (verdict-logic · engine-semantics · purity-privacy)
- HELD: market-present verdict ladder; exactMinimumGap === (only on the amber branch, so surplus-depth correctly not flagged); chemistryLight boundaries; budgetLight NaN-safety; shapeLight flow; assembleBoard sort; ALL engine-semantic assumptions verified against dependency source (identityEmbodiment.boostZ higher-is-better; FamilyChemistryProfile.tier 'L1'=base; TRAIT_PRICING polarity literals exactly 'negative'/'positive'; distanceToNextTier); purity; determinism; privacy/fog; no side-channel.
- BROKEN (1, minor/latent): `worthVerdict` no-market fallback returned 'push' whenever worth>0, ignoring capValue magnitude → would recommend PUSH on an unaffordable player (capValue=0). Latent (no live caller; whisper always passes a market; branch untested). **Routed to Codex FIX-1**: no-market path never 'push' — capValue<=0/null → 'pass', else 'cap'; + tests for the no-market branch.
**VERDICT: gate CLEAN; committing after FIX-1 re-verify.**

---

## DESIGNER-ZONE ("THE TWENTY-TWO" roster designer) — CLEAN after FIX-1 — 2026-07-02

**Builder:** Codex (CODEX-DESIGNER-ZONE, from FABLE_ROSTER_DESIGNER_LAYOUT §2-§7). **Auditor:** Opus.
Touched: LeagueBuilderDraftSetup.tsx (zone 3), new RosterDesigner.tsx (873 lines), leagueBuilderStorage.ts (+rosterDesign field), ballpark-kit.css (+--ballpark-status-warn). GameTracker untouched.

### Opus gate (own runs)
- Build exit 0. Full suite: 4 red files ALL characterized (wpaRuntimeBoundary hard-fail; franchiseManualSmokeFixture + archetypeBalanceSimulator + franchiseOffseasonGuards.component flakes — the last CONFIRMED green solo). 0 new reds; 8764 passed. rosterDesignFeasibility engine test 9/9.
- Opus code read: verdictTone precedence correct (red>legality>budget>green>quiet); chipCopy guarded by tone; alignment tag filtered to score>0 (top-3 POSITIVE); tilt NOT used to filter the pool (anti-starve); arsenal only feeds the classifier (not a control); persistence additive.

### 2-lens adversarial Workflow (Fable §7 conformance + defect hunt) — 20 HOLDS, 2 BROKEN
- HELD (independently verified vs code, not Codex's self-report): all §7 engine-binding, layout, states, persistence, kit/copy items; no fabricated candidate counts; no crash on empty pool/no identity/all-ANY; first-open does not write the seeded default.
- BROKEN 1 (major): designer budget used STATIC TIER_CAPS[tier].tierCap, but the auction floor seeds POOL-RELATIVE computePoolTierCap(pool, tier) → a curated pool can read GREEN in the designer yet be over-budget at auction. Fable §2.4 named pool.tierCap as the match. **FIX-1**: use computePoolTierCap on the same pool the designer evaluates against.
- BROKEN 2 (major): switching clubs within the 200ms save-debounce drops the pending edit (team-load effect resets dirtyRef + reseeds; debounce only saves when dirty) — proven by RTL probe. **FIX-1**: flush the pending save before loading a new team; don't clear dirty until onSave resolves. + tests.
**VERDICT: gate CLEAN; committing after FIX-1 re-verify.**

---

## WHISPER-PANEL (C4-B slice 2 Asst-GM whisper) — CLEAN after FIX-1 — 2026-07-02

**Builder:** Codex (CODEX-WHISPER, from FABLE_WHISPER_PANEL_LAYOUT §1-§8). **Auditor:** Opus.
Touched: new WhisperPanel.tsx + test, AuctionStage.tsx (mount + key), LeagueBuilderAuctionDraft.tsx (adapter +229). Scout lot card + GameTracker untouched.

### Opus gate (own runs)
- Build 0. Full suite: 2 red files, BOTH characterized (wpaRuntimeBoundary hard-fail; franchiseManualSmokeFixture timeout-flake). 0 new reds; 8772 passed. WhisperPanel test 3/3.
- Opus code read of the adapter (704-865): secrecy holds — payload built ONLY from the active human seat's roster/budget + PUBLIC shared state (available pool, lot, public ivs/names); no other seat's private data read. worthToYou/scorecard pieces gated on rosterShapeClean/remainingPoolClean → degrade to absent, no fabrication. Panel body conditional-render (closed = no DOM).

### 3-lens adversarial Workflow (secrecy-leak · conformance+fog · adapter-correctness) — 17 HOLDS, 1 BROKEN, 2 UNCERTAIN(minor)
- HELD: ALL 6 secrecy checks (active-seat-only, closed=no DOM, keyed collapse-then-swap, closed-face neutral, CPU→null, no precomputed per-seat payloads); farm fog (no true ratings/grades reach DOM — only money + verdict/light sentences); market partition (no band/CONTESTED in panel); five lights + hollow + default selection; verdict colors §6; help layer; no fabrication.
- BROKEN 1 (critical): identity light passed team.capIdentity.rawShift (ModStat-keyed 'POW'/'RVEL') but identityEmbodiment looks up 'group/stat' keys ('hitters/POW') → all miss → boostZ=0 → identity light CONSTANT AMBER (never green/red), misleading. **FIX-1**: build the identity SimArchetype via the canonical historicalToSimArchetype/archetypeCapShift (group/stat-keyed) the sibling draftabilityRanker uses; gate on the resolved HistoricalArchetype (else hollow).
- UNCERTAIN 2 (minor): shape/chemistry lights mislead on an incomplete roster (missing player record). **FIX-1**: gate chemistry + shape-gap detail on rosterPlayersClean (degrade to unknown, no fabrication).
- LESSON (recurring): adapters must build engine inputs via the CANONICAL builder (single-math), not hand-construct — the designer's budget bug + this identity bug are the same class (wrong-namespace input to an engine).
**VERDICT: gate CLEAN + secrecy airtight; committing after FIX-1 re-verify.**

---

## MODE-A ("Design first" pool flow, Draft Room zone 4) — CLEAN after FIX-1 — 2026-07-02

**Builder:** Codex (CODEX-MODEA, from FABLE_MODEA_POOL_LAYOUT). **Auditor:** Opus.
Touched: LeagueBuilderDraftSetup.tsx (zone-4 rewrite +754), poolFromDemand.ts (+countCellMatches, +test), leagueBuilderStorage.ts (+poolExtractedAt).

### Opus gate (own runs)
- Build 0. Full suite: 3 red files ALL characterized (archetypeBalanceSimulator flake — green solo 2x this session; wpaRuntimeBoundary hard-fail; franchiseManualSmokeFixture timeout). 0 new reds; 8774 passed. poolFromDemand test 5/5.
- Opus code read: countCellMatches uses the engine's own demandCellMatches (UI count == engine reservation); profile+IV from canonical buildRosterDesignPool/computePlayerIv; extractor inputs (locked human designs, resolved archetypes, tier, computePoolTierCap budget) correct; gating/stale/persistence sound.

### 2-lens adversarial Workflow (engine-fidelity + conformance/gating) — 9 HOLDS, 1 BROKEN + 1 UNCERTAIN (same root)
- HELD: A1-A4 states reload-safe; extract/lock/stale gating; shortfalls-never-block; poolExtractedAt additive (no new IDB); host corrections; no fabrication; shuttle reuse.
- BROKEN (major, + the UNCERTAIN completion-guarantee finding shares this root): demandPlayerFromLeaguePlayer derived SimPlayer.role via buildRosterDesignPool's roleForPlayer (P/TWO-WAY -> 'SP/RP') instead of the canonical toFeasibilitySimPlayer/toRosterSlotPlayer (P/TWO-WAY -> undefined). A TWO-WAY player thus counted toward BOTH arm floors + rotation-scored, so Mode-A's extracted pool + draftability verdicts DISAGREE with the app's canonical shape and over-credit staff minimums. **FIX-1**: reuse toFeasibilitySimPlayer so Mode-A universe == canonical shape (role P/TWO-WAY -> undefined); + a canonical-role-parity test.

### RECURRING PATTERN (3rd occurrence this arc — capture durably)
designer budget (static vs computePoolTierCap) · whisper identity (ModStat vs group/stat keys) · Mode-A role (roleForPlayer vs toFeasibilitySimPlayer). SAME class: a UI/adapter HAND-BUILT an engine input that diverged from the canonical mapper. RULE: adapters must build engine inputs by REUSING the canonical mapper (single-math), never re-derive. Each was caught by the adversarial engine-fidelity lens, not the gate (build+tests pass on skewed-but-valid data).
**VERDICT: gate CLEAN; committing after FIX-1 re-verify.**

---

## DJ-01 (auction board frame) — ROUND 1: BLOCK (Opus audit 2026-07-03)

Codex built DJ-01 from FABLE_P0_DESIGN §1 (new `src/engines/auctionBoardFrame.ts` + AuctionStage
rewire + splice-drop deletion). Fidelity GOOD: canonical `toRosterSlotPlayer` used at the call site
(no hand-map), splice-and-drop deleted, group headers + overflow rail + test ids + depth-note all
present, kit tokens (no raw hex), LEGAL_ROSTER constants (no bare literals). Build exit 0; Codex's
5-case property test + AuctionStage RTL green.

**BLOCK — board invariant violated (found by Opus fuzz, 6000 random rosters):** a LEGAL 22 renders
with a body STRANDED in overflow. Repro (13H/9A, isLegalRoster true): the roster's 2nd catcher-coverer
is a SECONDARY-C hitter (SS/C) whose PRIMARY is SS, so it seats at the SS field seat; backupC step-4a/4b
then find no UNSEATED C-coverer; §1.3-4c only rescues the SEATED TWO-WAY-(C) ARM corner, NOT the seated
secondary-C HITTER corner → backupC left open (correctly no glow: catcherCoverNeed 0) → a leftover plain
hitter strands in overflow. Root cause = a GAP in Fable's §1.3 step 4 (built faithfully by Codex), not a
build defect. Routed to Fable to amend the design (backupC = capacity seat whenever catcherCoverNeed === 0;
never strand a legal body; + a random-legal-roster property test added to §1.7). Codex re-builds to the
amendment; Opus re-fuzzes. The fixed 5-case property corpus MISSED this class — hence the fuzz.

## DJ-01 — ROUND 2: CLEAN + COMMITTED 1a2d456e (Opus audit 2026-07-03)
Fable amended §1.3 step-4 (backupC → capacity seat when catcherCoverNeed===0, truthful depth note);
Codex applied. Re-audit: build 0; board+stage+page tests 20/20; Opus independent 6k fuzz GREEN (the
iter-1630 strand gone) + Codex permanent 5k+ fuzz GREEN (1599 legal rosters). Board invariant holds:
legal roster => zero glow + empty overflow; illegal => >=1 glow. Canonical toRosterSlotPlayer at the
call site (no hand-map). Committed by explicit paths (no sweep of launch.json/CLAUDE.md).
DESIGN-FIDELITY flag for Fable's landed-frame review (non-blocking): gap glow styled with
--ballpark-ground (ash) rather than the semantic --ballpark-status-warn (amber) her "dashed-amber,
restyled to kit tokens" wording likely intended — Fable's call on final gap color.

## DJ-02 (CPU panel valuation leak) — CLEAN (Opus audit 2026-07-03)
Codex removed valuation/maxBid from CpuDecisionVM + the "Read/Cap" render spans; rewrote all four
buildCpuDecisionVm reasons to plain language (no $ from valuation), de-leaked cpuPassReason. Gate:
build 0; AuctionStage+AuctionDraft tests 11/11 (added a no-leak assertion); grep confirms no residual
cpuDecision.valuation/maxBid or "read values" reader. Walled-internals (F4) restored: the CPU turn
preview shows only the MOVE (bid/pass) + a plain reason, never the internal valuation or fold-point.

## HARD-CAP PHASE 1 — CLEAN (Opus audit 2026-07-03)
Codex built Phase 1 per FABLE_HARD_CAP_DESIGN §1/§2/§3.2/§4/§5. Static read: one resolver
(resolveLeagueSalaryCap), registerPool stamps salaryCap ?? computePoolTierCap, all 7 switch sites
correct, engines take budget as a PARAMETER (no storage import), §4.2 migration via sessionPool
override (real teams + shills + tax + players read the resolved cap; stale stamp never leaks), snake
+ designer + banner switched. Grep: NO live path calls computePoolTierCap (only documented fallbacks
+ the offline simulator). Ratings/IV untouched (TIER_RATING_SCALES dormant, guard test passes).
Gate: build 0; targeted cap suites all green (resolver, registerPool stamp, designer-respects-cap,
budgetPerTeam threading, auction session budget=cap, snake, migration, validation, banner, ratings
invariant); FULL suite 2 failed/8804 passed = the characterized reds only (wpaRuntimeBoundary +
franchiseManualSmokeFixture), NO new red. Adversarial 2-lens pass (enforcement-migration + residual/
edges): 12 HOLDS, 0 confirmed defects. Phase 2 (cap-aware pool selection) deferred to a later contract.

## DJ-03 (stable CPU club bidding identity) — CLEAN (Opus audit 2026-07-03)
Codex built per FABLE_P0_DESIGN §2. Verified: resolveClubBandPriorities priority chain exact
(bandPriorities>0 → mlbArchetypeKey→archetypeBandPriorities [the ONE bridge, reused] → rawShift lift
→ null); buildClubCpuProfile stable personality via hash(leagueId:teamId:club-personality), NO
shillMaxWins for real clubs; init adds clubs to cpuShills (ai-only); resume heal idempotent. Market
coherence: bandPriorities PUBLIC via resolver, personality WALLED — call sites pass bandPrioritiesByTeamId
+ humanTeamIds but NEVER personalityByTeamId, so CPU clubs → undefined → MEAN spread/bias (lines 294/301
truthy-check treat undefined==null==MEAN; line 304 human unknownBidders count unchanged). No import cycle
(cpuShillBidding does not import archetypeIdentity). Gate: build 0; focused 57/57; FULL suite 2 chars reds
only (no new). Adversarial checks done INLINE (import cycle · undefined-handling · personality-walling ·
humanTeamIds wiring · resolver reuse — the recurring-bug-class lens). Fable design-review of the landed
auction room (DJ-01 gap color + DJ-02 + DJ-03 derivation) queued as a batch.

## DJ-07 (START blocks on post-lock design staleness) — CLEAN (Opus audit 2026-07-03)
Codex hoisted the EXISTING modeAStaleTeams rule (single source) above startReady; design-first START now
requires !designsStale (no locked design with lockedAt > poolExtractedAt); plain blocker "re-extract the
pool — some designs changed after it was drawn". Pool-first unaffected; readOnly unchanged (START-gate
approach, not freeze-designs). Gate: build 0; DraftSetup 10/10 (2 new: stale-block + happy path); full
suite 2 characterized reds only.

## FABLE DESIGN-REVIEW — landed auction room (DJ-01/02/03) = design-CLEAN (2026-07-03)
Fable reviewed the committed board frame + CPU identity against her design: faithful on both. One ruling:
gap glow must be AMBER (--ballpark-status-warn), not ash (--ballpark-ground) — a gap is a law-deficit
warning; ash is the neutral accent. Swap in auction-theme.css .slot.gap rules + aucGapGlow keyframes +
.gap-label + overflow-rail border/title; leave .needline + .depth-note as ash (informational). Applied as
a DJ-01 follow-up.

## DJ-05 (design-first pool-lock membership) — CLEAN (Opus audit 2026-07-03)
Codex built per FABLE_DJ0506_DESIGN §1. Verified all 5 changes exact: mode-aware Source-B union
(includeRosterUnion gated on draftPoolMode; pool-first/capless byte-identical), regen membership param,
lockLeaguePool REORDER (register → regen over frozen pool.players ids → stamp; header updated),
reconcile-gate comment, useAuctionDraft re-stamp (load pool first, regen locked ids), + listRosteredButUnassigned
+ design-first-only non-blocking stray notice. Design-first frozen pool = the reviewed assignment set exactly;
regen now covers the frozen membership (closes the latent pool-first hole too). Gate: build 0; new dj05 test
5/5 (T1-T5 incl. T3 regen-coverage: pool-first B gets hidden modifiers, design-first B excluded+untouched);
full suite 2 characterized reds only (no new red).

## DJ-06 (draft-exit legality gate "THE HANDOFF CHECK") — CLEAN (Opus audit 2026-07-03)
Codex built per FABLE_DJ0506_DESIGN §2. Verified: pure auctionExitGate engine — legal is authoritative
(isLegalRoster(shapes) && count===22; blockers via describeRosterLawGaps are advisory only, gate never
rides on them); unknown=blocked; describeRosterLawGaps is the one law voice (buildLawNeedLine delegates —
drift-kill). Page gating: exitReport over CONTROLLED clubs (deriveShillTeamIds excludes shills),
exitPositionMap from STORED records (position-blind resumes get sighted), canProceedToFarm gates BOTH nav
sites + the legacy banner, transient explicit override (re-gates on reload). Franchise-side upgrade runs
isLegalRoster after the count check (throw stays); fixtures updated to genuinely-legal 22s (LEGAL_MLB_POSITIONS
= 14 hitters incl 2 C + 4 SP + 4 RP — verified legal). Gate: build 0; auctionExitGate 8 + page 13 (E1-E7/P1-P4);
full suite 2 characterized reds only (no new). Adversarial 2-lens: 9 HOLDS, 1 CONFIRMED-MINOR (below), 0 blockers.

**MINOR (pre-existing, ticketed, NOT a DJ-06 break):** toRosterSlotPlayer maps a bare 'TWO-WAY'/'P' PRIMARY
position → role undefined, so isLegalRoster credits it to pitcher headcount only (not the SP/RP minimums),
while the rest of the app maps a 'TWO-WAY' primary → 'SP/RP'. A legacy/custom/imported player with such a
primary would be blocked by the gate. NOT reachable in the draft flow (generators emit SP/RP primary + a
'Two Way' TRAIT, never a TWO-WAY primary), pre-existing (DJ-06 only USES the law), arguably intended (invalid
data). Ticketed TWO-WAY-PRIMARY-ROLE (Fable, near DJ-29's territory).

## POOL-SIZING PHASE 2A (cap-aware sizing engine) — CLEAN (Opus audit 2026-07-03)
Codex built Phase 2A per FABLE_POOL_SIZING_DESIGN §8. Verified faithful: resolvePoolSizingTarget (demandBase =
teams×22 + expectedShillWins; hard ceiling ceil(1.5×); hardFloor; effectiveTarget), trimPoolToTarget
(fit ASC → salary DESC within-tie → id ASC; protects reservedIds ∪ claimedIds ∪ floorIds), constructive G1
(N disjoint legal-22-under-cap via the all-no-ask evaluator; returns assemblies), fit-aware bounded repair
(fit-qualified → cheapest-among-fit → last-resort NOTE; additive-only; never hard-fails), sizing/g1 result
surface (both optional). No-dial extraction byte-identical (sizingEnabled gate). buildModeAResult threads the
PERSISTED multiplier + RESOLVED cap (adapter-fidelity). Reuses archetypeFitScorer + historicalToSimArchetype
(canonical mappers — recurring-bug-class avoided). FIT-FIRST LAW verified: no selector's primary key is price.
Gate: build 0; poolFromDemand 22/22 (incl. shuffled-input determinism); GameTrackerLaunchState = order-flake
(solo 9/9, not in my full run, GameTracker untouched); full suite 2 characterized reds only (no new).
Adversarial 2-lens (fit-first-law + correctness/determinism/fidelity): 13 HOLDS, 0 confirmed defects.
Phase 2B (dial UI + RE-CHECK panel + hand-edit preservation per the §5.2 amendment) is the next contract.

## POOL-SIZING PHASE 2B (dial UI + RE-CHECK panel + hand-edit preservation) — CLEAN (Opus audit 2026-07-03)
Codex built Phase 2B per FABLE_POOL_SIZING_DESIGN §5/§6 + all [AMENDED 2026-07-03] hand-edit blocks (JK
override: hand-edits preserved). Verified: foldHandEditLedger matches the §5.2 set algebra EXACTLY
(dAdd/dRem; handAdds = ((prevAdds∪dAdd)−dRem)∩universe; handRemoves = ((prevRemoves∪dRem)−dAdd)∩universe;
disjoint; first-extraction empty; migration/cancellation fall out of the algebra). Engine reconcile
(§4.3 step 5): pins force-included + protected from trim (§3.2) + kept out of nothing; excludes dropped from
union AND repair (§4.2); result pinnedHandPicks/excludedHandRemoves report what the engine DID; gated on
pins/excludes presence → byte-identical when absent. Recalc wiring folds → passes pinnedIds/excludedIds +
persisted multiplier + resolved cap → converges → persists modeAExtractedIds + ledgers; modeAManualEdits
derived; pool-first clears the ledgers. Dial = 7-stop stepper (persists poolSizeMultiplier). RE-CHECK panel
reuses evaluateRosterDesign + describeRosterLawGaps (the DJ-06 exit-gate words — ONE law, read-only). Nice
extra: a "your hand-remove would qualify — re-add" note when a removal blocks buildability. Gate: build 0;
targeted 40/40 (fold algebra + engine pin/exclude + dial persist + re-check law wording); full suite reds
ALL flakes (wpaRuntimeBoundary + franchiseManualSmokeFixture + AwardsWatchlist + EliminationTeamHub — last
two solo-green 9/9, no Phase-2B import path). Adversarial 2-lens (fold-algebra + reconcile/recheck/identical):
14 HOLDS, 0 confirmed defects. ⇒ POOL-SIZING FEATURE COMPLETE (2A engine + 2B dial/panel/hand-edits).

## ITEM ③ AUCTION-SETTLE-FROM-SHILLS (in-draft repair) — CLEAN (Opus audit 2026-07-03)
Codex built per FABLE_SETTLE_FROM_SHILLS_DESIGN (R1-R15). Verified: NEW pure auctionSettleFromShills.ts —
fit-first BY CONSTRUCTION (rankCandidates: fit DESC → openingAsk ASC → id ASC → price=rank fed to
cheapestLegalCompletion, which is UNCHANGED; charged price uniform team.minSalary so cheap can't outbid fit);
double-entry (buyer pays pickIds×minSalary + roster++; source shill refunds assignment.salary + slots++;
result SOLD+settled:true; saleCount +1 for PASSED→SOLD only); guards (non-COMPLETE→rejected; ≤0→already-complete;
unreadable club skipped; no-legal-completion; insufficient-budget); determinism (nominationOrder, shared pool
consumed per club); idempotent at fixpoint. clubArchetypeFit = single-math (market model's own bandFitMultiplier/
cachedBandLift/MEAN_PERSONALITY_SPREAD). Hook adapter reuses deriveShillTeamIds (SAME as commit) + toRosterSlotPlayer
(stored records) + resolveClubBandPriorities + clubArchetypeFit — adapter-fidelity. GATE PURITY: canProceedToFarm
gains NO settle term (settled club passes via exitReport isLegalRoster recompute). Settle block only in
HandoffCheckPanel (DJ-25 note). Gate: build 0; targeted 25/25 (S1-S10 + P1-P5 incl. the fit-law pin S2 + gate-purity
P3 + law-identity S10); full suite 2 characterized reds only (no new). Adversarial 2-lens (fit-law/legality +
double-entry/gate/adapter/determinism): 10 HOLDS, 0 confirmed defects. Fable-rec'd v1.1 but JK ruled BUILD NOW.
⇒ THE WHOLE JK-APPROVED BUILD LIST IS DONE: P0 train + hard cap + pool-sizing (2A+2B) + settle-from-shills.

## DJ-AUCTION-POLISH batch (DJ-09/19/20/21/24-auction/25) — CLEAN (Opus audit 2026-07-03)
Codex fixed the auction-page minors. Verified: DJ-09 fake 30% identity-tax meter DELETED (no real signal;
honest removal) + farm scout band bar now COMPUTES from the real range span (span/anchor → bandWidthPct;
mid positioned in-range) — the fixed 30%/42% fake is gone; DJ-19 reason→plain-English on the human bid path;
DJ-20 lot-counter no +1 during SOLD/PASSED; DJ-21 stale "Inherited from Draft Setup" dropped; DJ-24 auction
copy (engine→plain noun, Shill→Market Shill naming, farm tutorial behind ?); DJ-25 deleted the unreachable
legacy MLB session-gated JSX (-260 net). SCOPE GUARDS PASS: DraftRosterBoard.tsx UNCHANGED (farm still renders
it live); the DJ-06 exit gate (buildAuctionExitReport/exitControlledClubs/canProceedToFarm/AuctionStage) SURVIVES
the deletion + still wired on the new path; designer/engine files untouched. Gate: build 0; auction+farm+hook+stage
tests 21/21 (my run) + 37/37 (Codex focused); full suite reds all flakes (wpaRuntimeBoundary + franchiseManualSmoke
+ EliminationTeamHub + LeagueBuilderDraftSetup — all solo-green). Low-risk copy/delete/compute batch — no adversarial pass.

## DJ-08 BEST-22 ENGINE (round 1) — BLOCK, CONFIRMED MAJOR defect → fix dispatched (Opus audit 2026-07-03)
Codex built CODEX-B22-ENGINE per FABLE_BEST22_DESIGN §1 (best22Target.ts + askSatisfaction/rankPoolForSlot on
rosterDesignFeasibility.ts + canonical leaguePlayerAdapter.ts consolidation + DJ-24(4) $-format). Gate GREEN
(build 0; targeted 19/19; full suite 2 characterized reds only; adapter consumers 18/18). Refactor is byte-faithful
(functions moved verbatim, re-exports keep the public API, no import cycle). Fit-first math VERIFIED sound: the
slotPreferenceBonus is ADDED to buildIdentityRoster's FIT objective (archetypeBalanceSimulator.ts:680), additive-only,
bounded (min(bonusCap,bonus)×u in fit-σ units), price only enters as the budget constraint + value floor — cheap can
NEVER outrank fit. Test A3 proves an unaffordable ask stays advisory + leaves the FLOOR verdict untouched.

BUT the 4-lens adversarial workflow found (3 of 4 lenses, independently) a CONFIRMED MAJOR: **buildBest22Target
labels picks by ARRAY POSITION (build.players[index] → slots[index]) but buildIdentityRoster returns players in the
order of whichever internal seed won.** The value-seeded branch (idFromValue) is in SLOT_PLAN order, re-labeled via
VALUE_TO_IDENTITY_SLOT (rewrites .slotIndex, KEEPS array position), so at every index ≥9 the player does NOT belong
to slots[index]. When that branch wins (reachable with any real archetype — reproduced in 1832/6400 = ~28% of a
seeded sweep), pitchers get labeled FLEX*, hitters get labeled SP*/RP*, and slotId/honorsAsk/asksHonored are all
attributed to the WRONG slot. Second (minor) case: candidate-starved pools return a SHORT players array → same
positional map index-shifts every pick after the gap. SCOPE: advisory board ONLY — the FLOOR verdict is independent
& untouched; the BUILD math is correct (bonus applied at the true slotIndex); no illegal/insolvent picks; no live UI
consumer yet (best22Target imported only by tests). But it would corrupt the exact GM-facing board the DESIGNER slice
renders. Test A3 masked it because NEUTRAL_ARCHETYPE forces a zero-fit tie that always selects the IDENTITY-ordered
seed. Ask-satisfaction lens: 0 findings (clean). ⇒ NOT COMMITTED. Fix CODEX-B22-ALIGN dispatched: additive slotPicks
{slotIndex,player} on IdentityRosterResult (frozen engine, additive-only, zero risk to bag-iterating consumers
draftPoolExtractor/draftabilityRanker) + best22Target keys by TRUE slotIndex (fixes both the permutation AND the
starved short-array case) + B1/B2 regression tests. Re-audit on completion.

## DJ-08 BEST-22 ENGINE (round 2, CODEX-B22-ALIGN fix) — CLEAN, COMMITTED (Opus audit 2026-07-03)
Codex applied CODEX-B22-ALIGN. Verified the archetypeBalanceSimulator.ts diff is EXACTLY additive (new slotPicks
{slotIndex,player} field + evaluate preserves picks + return maps chosen.picks — `players` order & every other field
byte-identical; buildBestRoster/frozen path untouched). best22Target reporting loop now keys by TRUE slotIndex
(playerBySlotIndex map over build.slotPicks; maps over slots so length===22 always; unfilled slot → empty pick,
asked-but-not-honored). Bonus closure unchanged (was already correct). B1 = 72-seed real-archetype sweep asserting a
STRONG structural invariant per pick (pos → non-pitcher AND exact position; backupC → canCover C; sp → canStart; rp →
canRelieve; flex → non-pitcher; swing → non-pitcher|canRelieve) + feasibleBuilds>0. B2 = starved-catcher pool → C slot
empty (no index-shift), feasible false, length 22.
DECISIVE before/after PROBE (my own, 400 seeds, then deleted): valueBranchBuilds=251, OLD positional labeling violated
ALL 251 (100% correlation with the value branch), NEW slotIndex labeling violated 0. The fix eliminates the defect
completely and B1's seed space genuinely exercises the buggy branch. Gate: build 0; targeted 35/35 (best22 + feasibility
+ the 3 frozen consumers draftabilityRanker/archetypeIdentityEmbodiment/identityPreferenceBoost — proving additive change
is inert to calibration); full suite 2 characterized reds only (franchiseManualSmokeFixture solo-green 4/4 + wpaRuntimeBoundary
solo-fail = the documented pair), no new red. ⇒ COMMITTED (best22Target + test, rosterDesignFeasibility + test,
leaguePlayerAdapter, RosterDesigner/LeagueBuilderDraftSetup re-export edits, archetypeBalanceSimulator additive). NEXT:
C-B22-DESIGNER (render the TARGET board) → C-B22-PAGE → C-B22-WHISPER → DJ-26.

## DJ-08 C-B22-DESIGNER (round 1) — BLOCK, 2 real defects → fix dispatched (Opus audit 2026-07-03)
Codex built C-B22-DESIGNER per FABLE_BEST22 §1.3/1.4/1.7/§3/§4. Verified GOOD: canonical wires reused everywhere
(simPool=demandUniverseFromPlayers, classifiedById=classifyPlayerArchetype(profile), archetype=historicalToSimArchetype,
shortlist=rankPoolForSlot, target=buildBest22Target — NO hand-building); DJ-15 shared designVerdict module's FLOOR
tone+copy is BYTE-IDENTICAL to the removed verdictTone/chipCopy.state (the "EST." cost line intentionally killed);
target copy matches §1.3/§1.5 exactly; DJ-16 (RP1 avoid-fragile default + "RP1 · CLOSER" label, no retro-migration);
DJ-17 (slotKindAllowsTwoWay gate + seed-time sanitizePreferenceForSlot strips twoWay on pos/flex); DJ-14 (dedicated
unmount useEffect flushes latestPendingSaveRef via persistDirtySave — no double-flush: debounce cleanup only flushes on
team CHANGE, unmount only via the new effect); slot-card line correctly hidden when targetPick.playerId==='' (unfilled).
Gate: build 0; targeted 27/27 (designer+verdict+feasibility); full suite reds ALL solo-green flakes (wpaRuntimeBoundary,
franchiseManualSmokeFixture, archetypeBalanceSimulator, LeagueBuilderDraftSetup 12/12 solo, RosterDesigner 8/8 solo — the
new tests are order-flaky-in-batch but green solo = the repo's characterization bar). Adversarial 3-lens (12 agents):
8 CONFIRMED, clustering into 2 real defects:
  • NAME (findings 1/2/4/5/6, MAJOR): buildBest22Target picks derive playerName from the SimPlayer, but
    demandPlayerFromLeaguePlayer omits name → every slot-card target line renders the raw id (player-<ts>-<rand>) while
    the shortlist beside it shows real names (it reads designPool). One root cause; fix at source = adapter carries name.
  • STALE FLOOR (finding 7, MINOR): team-switch effect resets target but not `result` → old team's floor verdict/blockers
    flash under the new team for ~300ms. Fix = setResult(null) in that effect.
  Non-defects: finding 8 (clubCheckTargetCopy "orphan") is INTENTIONAL forward-provisioning for C-B22-PAGE; finding 3
  (shortlist ≈ glyph vs rankPoolForSlot when allowRunnerUp:false) = minor defensible cosmetic, TICKETED B22-SHORTLIST-GLYPH.
⇒ NOT COMMITTED. CODEX-B22-DESIGNER-FIX dispatched (adapter name + DemandUniversePlayer name? field + setResult(null) +
regression tests closing the name-test gap). Re-audit on completion.

## DJ-08 C-B22-DESIGNER (round 2, FIX) — CLEAN, COMMITTED (Opus audit 2026-07-03)
Codex applied CODEX-B22-DESIGNER-FIX. Verified: (1) NAME — DemandUniversePlayer gains optional `name?`;
demandPlayerFromLeaguePlayer sets `name: playerName(player)` (the file's existing "First Last" helper); best22Target.ts
UNCHANGED (its cast now resolves). Test-pinned end-to-end: best22Target.test asserts pick.playerName === 'Simone Short'/
'Avery Ace' when the sim player carries a name (closes the finding-6 gap); poolFromDemand.test asserts the adapter sets
name. (2) STALE FLOOR — team-switch effect now also `setResult(null)` (alongside setTarget/setTargetState) → the floor
shows the honest quiet state until the debounce recomputes, no cross-team flash. Gate: build 0; targeted 79/79 (7 files
incl. the frozen consumers draftPoolExtractor + draftabilityRanker — the additive name? is inert); full suite 2
characterized reds only (wpaRuntimeBoundary + franchiseManualSmokeFixture), no new red. Findings 3 (≈-glyph, minor
cosmetic) ticketed B22-SHORTLIST-GLYPH; finding 8 (clubCheckTargetCopy) is intentional forward-provisioning consumed by
C-B22-PAGE. ⇒ COMMITTED (RosterDesigner + designVerdict + test, LeagueBuilderDraftSetup, poolFromDemand + test,
leaguePlayerAdapter, best22Target.test). ⇒ THE DESIGNER BOARD IS LIVE (chip/strip/slot-picks/shortlist + DJ-14/16/17).
NEXT: SILENT-REFRESH (JK-flagged scroll-to-top on save — refresh() flips full-page isLoading → unmount/remount) →
C-B22-PAGE (CLUB CHECK target + DJ-11 picker) → C-B22-WHISPER → DJ-26.

## SILENT-REFRESH (JK-flagged scroll-jump-to-top on every save) — CLEAN, COMMITTED (Opus audit 2026-07-03)
JK observed: setting an identity archetype or a positional priority scrolls the league-builder page to the top every
time. ROOT CAUSE (Opus, definitive): useLeagueBuilderData.ts loadData() always setIsLoading(true→false); refresh()=
loadData(); EVERY mutation (updateTeam/updateLeague/createTeam/updatePlayer/registerLeaguePool/…) awaits refresh(); the
page gates ALL content on {isLoading ? <Loading…/> : content} (LeagueBuilderDraftSetup.tsx:1567). So every save flips
isLoading → UNMOUNTS the whole content subtree (→spinner) → refetch → REMOUNTS fresh → browser discards scroll → jump
to top. Not a browser reload — the app's own full-page data reload. FIX (Codex, root-cause, minimal 12-line): loadData
gains `{silent?}`; when silent it skips BOTH setIsLoading calls (still setError/refetch/setState/catch); refresh() →
loadData({silent:true}); initial-mount load stays loud (spinner once). Fixes EVERY screen using the hook, not just this
one. Verified: the additive-only diff (no other behavior change); no existing test asserted isLoading toggling on refresh
(Codex grep + full suite). New renderHook test PROVES the guarantee: after initial load, refresh() updates teams
(scroll-safe-team appears) while isLoading NEVER re-enters true (tracked across renders). Gate: build 0; hook test 1/1;
full suite 2 characterized reds only (wpaRuntimeBoundary + franchiseManualSmokeFixture), no new. Low-risk 2-line logic
change + solid regression test — no adversarial pass. ⇒ COMMITTED (useLeagueBuilderData.ts + test).

## DJ-08/DJ-11 C-B22-PAGE (round 1) — 1 minor found → fix dispatched (Opus audit 2026-07-03)
Codex built C-B22-PAGE per FABLE_BEST22 §1.5 + §2. Verified GOOD: (a) CLUB CHECK per-club TARGET segment — pool basis =
inPoolPlayers (SAME as the floor's inPoolDesignPool; no third pool), simPool+classifiedById built ONCE, per-club
buildBest22Target keyed on (id+mlbArchetypeKey+slots per club, inPoolDesignPool, inPoolPlayerIdsKey, tier, budget),
250ms debounce + cancelled guard; render adds a 2nd dim segment via clubCheckTargetCopy/targetVerdictState. FLOOR-ONLY
invariant HOLDS (nonGreenClubCount + toneDotClass + lock-confirm read row.tone, not target — adversarially confirmed no
gate/dot/count leak). (b) DJ-11 picker — draftability effect = rankAllArchetypesForPool(demandUniverseFromPlayers(
rosterDesignerPlayers), tier, {budgetOverride:tierBudget}) → Record[archetypeId]{band,reason:reasons[0]}; ArchetypeCard
GREEN silent / YELLOW ▲ / LOCKED gray+desat-chip+✕; per-slot disable = disabled||(slot==='mlb'&&LOCKED) (farm stays
pickable — adversarially confirmed); grid order = TEAM_ARCHETYPES catalog order (no re-sort); pending line gated on empty-
not-absent; keep-previous-on-recompute. Key alignment verified: TEAM_ARCHETYPES=HISTORICAL_ARCHETYPES.map(key:id) so
a.key===archetypeId. Gate: build 0; targeted 33/33 (picker+page+verdict+ranker); full suite 3 reds all solo-green flakes
(wpaRuntimeBoundary + franchiseManualSmokeFixture + EliminationTeamHub 6/6 solo). Adversarial 3-lens (floor-only/disable/
staleness): 1 CONFIRMED MINOR — the DJ-11 effect keys on player-IDS-ONLY, so editing an in-pool player's RATINGS (which
move the ranker's iv/bat inputs) doesn't retrigger the recompute → stale bands until next add/remove (advisory-only,
self-heals; the sibling CLUB CHECK effect keys on inPoolDesignPool so it's fine). ⇒ NOT COMMITTED. CODEX-B22-PAGE-FIX
dispatched (content-signature pool key sensitive to ratings/salary but NOT slot asks + B5 rating-edit test). Re-audit on
completion.

## DJ-08/DJ-11 C-B22-PAGE (round 2, FIX) — CLEAN, COMMITTED (Opus audit 2026-07-03)
Codex applied CODEX-B22-PAGE-FIX. Verified: the DJ-11 effect key is now `rosterDesignerPoolKey` — a content signature over
(id + power/contact/speed/fielding/arm/velocity/junk/accuracy + salary) of rosterDesignerPlayers, derived ONLY from player
fields (never slot asks). So a rating/salary/membership change of an in-pool player retriggers the recompute; a slot-ask/
design edit does NOT (B5 holds). Old ids-only `rosterDesignerPlayerIdsKey` REMOVED; the CLUB CHECK effect's separate
`inPoolPlayerIdsKey` UNTOUCHED; CLUB CHECK effect still keys on inPoolPlayers/inPoolDesignPool; floor-only nonGreenClubCount
intact. B5 test extended with the rating-edit retrigger case. Gate: build 0; targeted 33/33 (page+picker+verdict+ranker);
full suite 2 characterized reds only (wpaRuntimeBoundary + franchiseManualSmokeFixture), no new. ⇒ COMMITTED
(LeagueBuilderDraftSetup + test, ArchetypePicker + test + snapshot). ⇒ THE DRAFT-SETUP PAGE IS LIVE (per-club CLUB CHECK
target + DJ-11 draftability picker). NEXT: C-B22-WHISPER (§1.6 auction board need/fit) → DJ-26.

## DJ-08 C-B22-WHISPER (§1.6 auction board need+fit) — CLEAN (1 minor gated) — Opus audit 2026-07-03
Codex built C-B22-WHISPER. Verified GOOD: assembleBoard sort = needTier→fitTier→worth desc→id asc; with need/identityZ
absent it collapses to today's worth desc→id EXACTLY (back-compat). worth UNCHANGED (iv+chemistry premium; no invented
money). boardNeedTag first-match order matches §1.6 via canonical canCover/canStart/canRelieve; boardFitTag reuses
PAYLOAD_TUNING.identityGreenBoostZ (0.35). Caller: comparisonPool/identityRoster/identityArchetype MOVED UP + identityTier
extracted (existing identity payload receives identical values — verified); identityZ = z-score of archetypeFitScorer over
comparisonPool (σ=0 guarded →0), reuses playerToSimPlayer (canonical); shape=auctionPlayer.pos, need=rosterNeedBreakdown
(rosterShapes). WhisperPanel: NEED chip (brass) then IDENTITY chip (gold) after the name, render-only-when-present.
Gate: build 0; targeted 32/32 (payload+whisper+auction); full suite 2 characterized reds only. Adversarial 2-lens
(sort/back-compat + caller-refactor/z-score): 1 CONFIRMED MINOR — the board's `need` was passed UNGATED while every sibling
whisper read (worthToYou/scorecard/budget) gates on rosterShapeClean; a position-blind roster member truncates rosterShapes
→ false FILLS/deficit tags. FIXED (Opus, 1-line, matches the sibling pattern + adversarial verifier pre-validated the exact
fix): `need: rosterShapeClean ? rosterNeedBreakdown(rosterShapes) : undefined` (undefined → boardNeedTag null → back-compat).
Re-gate + commit.

## LB-UI-CLEANUP (JK rulings 2026-07-03) — CLEAN, COMMITTED (Opus audit + browser-verified)
JK-ruled UI cleanup (not audit findings). Codex built: (1) AppHome — "FRANCHISE"→"Living Season Mode" (w-220→320),
"BUILDER"→"League Builder" (w-220→280); (2) LeagueBuilder hub — removed the farm-draft "DRAFT" ModuleCard (+ its now-unused
farmDraftRouteForLeague import/local), renamed "MLB DRAFT"→"Draft Setup"; (3) LeagueBuilderDraftSetup — identitiesReady now
requires BOTH mlbArchetypeKey AND farmArchetypeKey (JK: farm identity also required to START), copy updated ("MLB and a farm
identity"; "✓ every club has both identities"). Gate: build 0; targeted 49/49 (LeagueBuilder + LeagueBuilderDraftSetup incl.
the new farm-gate test); full suite 2 characterized reds only. BROWSER-VERIFIED (preview :5199): both renamed home buttons
render without clipping ("LIVING SEASON MODE" 280px in a 320px button; "LEAGUE BUILDER" 240px in 280px). ⇒ COMMITTED.
NEXT (JK ruled "remove snake entirely"): CODEX-RETIRE-SNAKE (auction-only routing + drop the format option + de-route both
snake screens). Then: wire needAwareCompletion (JK-approved), ticket nominationOdds/projectBidVsPass/chemistryRemovalAdvice,
fix the franchise personality-seed nit.

## RETIRE-SNAKE (JK ruled "remove snake entirely" 2026-07-03) — CLEAN, COMMITTED (Opus audit)
Codex removed snake as a draft format; auction is now the only draft style. Verified: draftRouting.mlbDraftRouteForFormat
→ ALWAYS "/league-builder/auction-draft"; farmDraftRouteForFormat → ALWAYS "/league-builder/farm-auction-draft" (legacy
draftFormat:'snake' leagues fall back to auction gracefully — new draftRouting.test pins this). DRAFT_FORMAT_OPTIONS
narrowed to auction-only + form defaults draftFormat 'auction' (LeagueBuilderLeagues). Snake routes de-registered in BOTH
route registries (App.tsx + routes.tsx); the two live navs to /league-builder/draft (Builder.tsx fallback +
FranchiseManualSmokeSetup link) repointed to farm-auction-draft; the snake-redirect effect removed from
LeagueBuilderFarmAuctionDraft. Snake SCREEN .tsx files KEPT on disk (reversible); their tests deleted
(LeagueBuilderSnakeDraft.test + LeagueBuilderDraft.test). GREP-PROVEN: no live route/nav to /league-builder/snake-draft or
the farm-snake /league-builder/draft remains. Gate: build 0; targeted 38/38 (draftRouting + leagues + franchise smoke);
full suite 3 reds all solo-green flakes (wpaRuntimeBoundary + franchiseManualSmokeFixture + RosterDesigner D1). ⇒ COMMITTED.

## OPEN TAIL ledger (2026-07-03) — deferred items + JK decisions pending
IN FLIGHT: CODEX-CPU-NEEDAWARE (JK-approved: enable needAwareCompletion at the 6 live cpuBidOnLot/cpuDecideLoneSurvivor
sites — MLB×4 + farm×2; flag CPU-behavior change for JK's browser validation).

DEFERRED / TICKETED (JK ruled "ticket, not build"):
- Dormant asst-GM feature helpers (need own UI surface + design): nominationOdds + projectBidVsPass (auctionMarketModel.ts),
  chemistryRemovalAdvice (chemistryIntelligence.ts) — orphaned (own file + test only). Future features.
- franchiseInitializer.ts:288 personality-seed uses bare player.id vs canonical ${leagueId}:${player.id}
  (leaguePoolAxisRegen.ts:98); GUARDED backfill branch only (normal locked path unaffected) — minor consistency nit.
- Mode-A pool-extraction receipt re-derives on reload (LeagueBuilderDraftSetup.tsx ~:1002-1020) instead of being persisted —
  works, wasteful. DJ-26 orphan family (needAwareCompletion now being wired; the rest ticketed).
- Prior: DJ-29 + TWO-WAY-PRIMARY-ROLE (two-way frame-vs-law, Fable), B22-SHORTLIST-GLYPH (≈ vs allowRunnerUp cosmetic).

DECISIONS PENDING JK (surfaced 2026-07-03, from his flow questions — NO build until he rules):
- (a) REDO A FINISHED DRAFT: no UI reset for a completed draft (deleteAuctionSession exists, unexposed). Build a "re-run
  this draft" button? 
- (b) LEAGUE RULES are VESTIGIAL: LeagueBuilderRules + presets collected but NEVER read; the season hardcodes settings
  (FranchiseSetup bypasses defaultRulesPreset). SeasonRulesPreview sketches intent (keep season length/innings/playoffs,
  cut the rest) but is unwired. Wire a slim rules set into the season, or cut the obsolete rules UI? (Opus lean: cut to the
  few that matter + wire those.)
- (c) ASSISTANT GM is UNNAMED: whisper panel labels it "ASST GM"; Staff Hire names Manager + Reporter but not the Asst-GM.
  Give it a name (in Staff Hire) or leave anonymous?
CONFIRMED-GOOD (JK flow questions): drafted rosters DO persist per-league to team rosters (leagueBuilderAuctionPipeline
commit* → saveTeamRoster, leagueAssignments MLB/FARM); league→franchise handoff wired (FranchiseSetup picks a league →
deepCopyLeagueToFranchise seeds from the drafted rosters). Teams are GLOBAL/reusable (not per-league) — JK aware.

## CPU-NEEDAWARE (JK-approved smarter CPU bidding) — CLEAN, COMMITTED (Opus audit 2026-07-03)
Codex enabled the built-but-dormant needAwareCompletion (FABLE-C3 anti-wedge) at all 6 live cpuBidOnLot/cpuDecideLoneSurvivor
sites (LeagueBuilderAuctionDraft×4 + useFarmAuctionDraft×2) via one shared CPU_BID_OPTIONS const per file; cpuShillBidding.ts
untouched; the override is self-gating (completing team + all slots hard-required + candidate fills one + affordable → forces
a bid). KEY: NO existing CPU decision expectation changed + no test rewritten (the override only fires in the genuine
anti-wedge case existing fixtures don't hit); the C2B auction calibration sim + auctionTuningSim (100 runs) passed = no draft
balance regression. Gate: build 0; cpuShillBidding 12/12 + focused CPU/auction 54; full suite reds all solo-green flakes
(archetypeBalanceSimulator 1/1, GameTrackerLaunchState 9/9, wpaRuntimeBoundary, franchiseManualSmokeFixture, RosterDesigner
D1 — all confirmed solo). ⇒ COMMITTED. CPU-behavior change — FLAG for JK browser validation (CPU teams now complete legal
rosters instead of wedging).

## SLIM RULES scope (rules-consumption map, 2026-07-03) — season length + innings ALREADY LIVE; extra-innings needs a launch shim
Evidence map: FranchiseSetup season config is the LIVE rules surface. gamesPerTeam → CONSUMED (processCompletedGame
seasonMetadata → warOrchestrator seasonGames → runsPerWinForSeason). inningsPerGame → CONSUMED (franchiseAdaptiveStandards
→ milestone thresholds/scaling). extraInningsRule → SET in FranchiseSetup but NEVER READ = VESTIGIAL. The LEAGUE Rules tab
(LeagueBuilderRules/RulesPresets) is SAFE TO RETIRE (never read for behavior, only a name-ref in the leagues dropdown).
"Runner on second": GameTracker already HAS a ghost-runner (navigationState.extraInningRunner + extraInningRunnerDelay 1|2,
GameTracker.tsx ~:1720-1795) but it does NOT read the franchise extraInningsRule → operationalizing it = a TRANSLATION SHIM
at franchise game-launch (franchise rule + delay → GameTracker nav-state), NOT a GameTracker edit (GameTracker is SET). ⇒
#4 slim-rules work = (a) retire the obsolete LeagueBuilderRules tab; (b) add a delay (1|2) to the franchise extra-innings
config + wire the launch shim; (season length + innings already done). Deferred behind the iterate-loop.

## ITER-LOOP slice 1 C-ITER-ENGINE (pins, NOVEL math) — round 1: math APPROVED, 2 fixes → dispatched (Opus audit 2026-07-03)
Codex built the pin-constrained BEST-22 recompute per FABLE_ITERATE_DRAFT_DESIGN §2.2. archetypeBalanceSimulator additive:
normalizeIdentityPins + identityGreedyStart pinned-seed path + constrainedIdentityClimb pinnedSlots skip + BuildIdentityOptions.pinned;
best22Target validatePins + Best22Target.pins{honored,dropped} + pick.pinned; rosterDesignFeasibility isDesignPlayerEligibleForSlot.
Gate: build 0; targeted 43/43 (P1-P6 + 3 frozen consumers inert); full suite flakes only. FABLE DESIGN-REVIEW (novel-math
gate): items 1-5 APPROVE — pins seeded-first + fixed both directions (climb skip + used-set), byte-identical when absent, drop
taxonomy correct; the idFromValue seed substitution (value-greedy honoring pins vs the pin-blind baselinePicks re-seed) is
FORCED-correct (baselinePicks would freeze wrong occupants into pinned slots = a pin-violating bug); the value FLOOR anchor
(baselineIv, pin-blind) untouched. Item 6 CHANGE-REQUIRED: toDesignPoolPlayer fabricates a false ClassifiableProfile (C4
hand-built-input law). Adversarial 2-lens (both independently): 1 CONFIRMED MINOR (latent) — design gate (eligibleForSlot
sp/rp `role ?? primaryPosition`) vs engine gate (canStart/canRelieve role-only) DIVERGE for {position:'SP',role:undefined} →
honored-but-not-placed lie; no canonical adapter builds that shape + no caller wires pins yet. ⇒ NOT COMMITTED.
CODEX-ITER-ENGINE-FIX dispatched (delete toDesignPoolPlayer + narrow SlotEligibilityPlayer to a Pick-typed door; derive
pins.honored from ACTUAL placement so it can't lie; doc nits). Re-audit on completion.

## ITER-LOOP slice 1 C-ITER-ENGINE (round 2, FIX) — CLEAN, COMMITTED (Opus audit 2026-07-03)
Codex applied CODEX-ITER-ENGINE-FIX. Verified: (FIX1) toDesignPoolPlayer DELETED; eligibility now via the narrowed
exported `SlotEligibilityPlayer = {profile: Pick<...,'isPitcher'|'primaryPosition'>; slotPlayer: RosterSlotPlayer}` door
(eligibleForSlot + isDesignPlayerEligibleForSlot retyped; internal DesignPoolPlayer callers are supersets → compile-clean,
build 0); best22Target passes real fields `{profile:{isPitcher,primaryPosition:player.position}, slotPlayer:player}` — zero
fabrication (C4 law satisfied). (FIX2) pins.honored + pick.pinned now DERIVED FROM ACTUAL PLACEMENT (playerBySlotIndex from
build.slotPicks: a buildPin is honored iff its player truly occupies its slotIndex, else dropped 'ineligible') — the
gate-divergence honored-but-not-placed lie is impossible by construction. New test pins the role-undefined-SP → dropped case.
Gate: build 0; targeted 44/44 (P1-P6 + honored-from-placement + 3 frozen consumers inert); full suite reds all solo-green
flakes (wpaRuntimeBoundary + franchiseManualSmokeFixture + archetypeBalanceSimulator). ⇒ COMMITTED (archetypeBalanceSimulator
additive, best22Target + test, rosterDesignFeasibility). ⇒ THE PIN ENGINE IS LIVE (dark — no UI caller yet; slice 2 wires it).
NEXT: C-ITER-DESIGNER (edit-after-extract + pin UI) → C-ITER-PAGE → C-ITER-RERUN → C-ITER-COPY.

## ITER-LOOP slice 2 C-ITER-DESIGNER (round 1) — 1 MAJOR (adapter divergence) → fix dispatched (Opus audit 2026-07-03)
Codex built the edit-after-extract affordances + pin UI + persistence per FABLE_ITERATE_DRAFT_DESIGN §1.3/2.3/2.4. Verified
GOOD: RosterDesignSave.pins additive+migration-safe (cleanPins + savePayload omits empty; legacy loads clean); pins threaded
through the debounce→buildBest22Target(pinMap) + memo key + persistDirtySave + the DJ-14 unmount flush; unlock affordances
(header UNLOCK & EDIT + consequence line, SlotEditor lock row + inline onUnlock gated on lockedByDesign-not-disabled); PIN/UNPIN
move semantics, target strip pin segment. Gate: build 0; targeted 46/46 (W1-W2/P7/P9 + designVerdict + best22 + feasibility);
full suite reds solo-green flakes (RosterDesigner 12/12 solo). Also folded the engine slice's MISSED rosterDesignFeasibility.test.ts
(isDesignPlayerEligibleForSlot "P4 seam" test — a commit miss from c070c0be). Adversarial 2-lens: 1 CONFIRMED MAJOR —
CROSS-ADAPTER ELIGIBILITY DIVERGENCE: buildRosterDesignPool (leaguePlayerAdapter.ts, roleForPlayer) maps a bare 'P'/'TWO-WAY'
primary → role 'SP/RP' (so he's in the SP/RP shortlist WITH a PIN button + design-eligible), but the canonical
toRosterSlotPlayer (used by the sim pool + engine + isLegalRoster) maps it → role undefined (ineligible for sp/rp). Pinning
him → validatePins/engine DROP him ('ineligible'), and pinDisplayBySlot's orphaned = !poolPlayer || Boolean(dropped) flips true
→ the slot card + editor falsely say "OUT OF THE POOL / RE-EXTRACT CAN BRING HIM BACK" (he IS in the pool; re-extract can't
help). This is the pre-existing roleForPlayer-vs-toRosterSlotPlayer divergence (flagged at the ENGINE-slice adapter
consolidation) now biting via pins; it ALSO means the FLOOR disagrees with isLegalRoster for 'P'/'TWO-WAY' pools (the
TWO-WAY-PRIMARY-ROLE ticket). Live reachability is EDGE (DJ-06: draft generators emit SP/RP primaries + a 'Two Way' TRAIT,
never a bare 'P'/'TWO-WAY' primary), but it's a real lie on the shipped feature. ⇒ NOT COMMITTED. CODEX-ITER-DESIGNER-FIX
dispatched: (1) ROOT — buildRosterDesignPool builds slotPlayer via the canonical toRosterSlotPlayer (kills roleForPlayer;
aligns 'P'/'TWO-WAY' everywhere + fixes floor-vs-legality); (2) HARDEN — orphaned = !poolPlayer only, a dropped-but-in-pool
pin shows an honest "can't pin here" message. Re-audit on completion.

## ITER-LOOP slice 2 C-ITER-DESIGNER (round 2, FIX) — CLEAN, COMMITTED (Opus audit 2026-07-03)
Codex applied CODEX-ITER-DESIGNER-FIX. Verified: (FIX1 ROOT) buildRosterDesignPool now builds slotPlayer via the CANONICAL
toRosterSlotPlayer (roleForPlayer DELETED) — 'P'/'TWO-WAY' primaries now map to role undefined consistently with the sim
pool + engine + isLegalRoster; the divergence is dead + the FLOOR now agrees with isLegalRoster for those pools. NO regression
for normal pitchers: best22Target 14/14 solo + rosterDesignFeasibility 16/16 solo + LeagueBuilderDraftSetup 15/15 solo (SP/RP
fixtures unchanged — only bare 'P'/'TWO-WAY' role differs, which no fixture uses). (FIX2 HARDEN) pinDisplayBySlot splits
orphaned (!poolPlayer → "OUT OF THE POOL"/"LEFT THE POOL. RE-EXTRACT…") from dropped-in-pool (poolPlayer && dropped →
"CAN'T PIN HERE"/"CAN'T PIN TO THIS SLOT") — no more false out-of-pool lie. Gate: build 0; solo-confirmed the suspects
(the targeted+full runs hit a heavy ambient IndexedDB-teardown flake burst — all reds solo-green: LeagueBuilderDraftSetup,
RosterDesigner, best22, feasibility, franchiseManualSmoke, archetypeBalanceSimulator, wpaRuntimeBoundary). ⇒ COMMITTED
(RosterDesigner + test, leaguePlayerAdapter canonical-mapper alignment, leagueBuilderStorage rosterDesign.pins type, +
folded the engine-slice's missed rosterDesignFeasibility.test P4-seam). ⇒ THE PIN UI + EDIT-AFTER-EXTRACT AFFORDANCES ARE
LIVE (dark until slice 3 passes poolDrawn + rides pins into extraction). Also resolves the pre-existing
roleForPlayer-vs-canonical divergence (TWO-WAY-PRIMARY-ROLE ticket partially closed for the design pool).
NEXT: C-ITER-PAGE (rail + cap + basis + pins→extraction) → C-ITER-RERUN → C-ITER-COPY → slim-rules.

## ITER-LOOP slice 3 C-ITER-PAGE — CLEAN, COMMITTED (Opus audit 2026-07-03)
Codex built CODEX-ITER-PAGE (re-plan rail + THE MONEY cap + poolExtractedBasis + pins→extraction). Full static audit
(builder Codex ≠ auditor Opus). VERIFIED:
- FEASIBILITY-SOFT LAW HELD: canModeALock byte-identical (`!modeAStale`, LeagueBuilderDraftSetup.tsx:1486-1492 — NOT touched);
  the ONLY gate move is startReady's join `designsStale` → `poolTrailing = designsStale || basisStale` (:1013) + the matching
  startBlocker message softening (predicate parallel). modeAStale NOT orphaned (still the canModeALock input). handleExtractPool
  lock-req + FLOOR + nonGreenClubCount + CLUB CHECK dot untouched.
- SHARED salaryCapInput.ts extraction clean: LeagueBuilderLeagues repoints via `as get*` aliases (no local-const collision);
  the parse/format/floor/error/advisory logic is one owner now. Cosmetic: hard-error strings uppercased in the move; the
  Leagues test was updated to derive from the module (`salaryCapHardError(SALARY_CAP_FLOOR-1)`), not weakened.
- poolExtractedBasis staleness: closes all 3 silent holes (cap / pool-size dial / MLB-identity change post-draw); written by
  handleExtractPool, cleared on mode→pool-first; MIGRATION-SAFE (poolBasisStaleLines returns [] when either basis absent → a
  pre-build drawn pool is CURRENT, no retro-nag; asserted by M4-M6). APPLY/RESET run through runAction → refresh() (:1140), so
  the cap change propagates → tierBudget → basisStale → rail lights → START blocks → RE-EXTRACT clears it (loop closes).
- pins→extraction: lockedDesignPinPlayerIds (locked designs' pins ∩ universe) folded into pinnedIds; excludedIds filters out
  pinned ids → PIN-WINS over hand-remove (P8 asserts extractPoolFromDemand receives the id + pin beats hand-remove).
- TESTS STRENGTHENED not weakened: 8 new blocks (W3/W5 rail, M1-M7 cap+basis, P8 pins) / 27 assertions; the 2 deleted lines
  were only the old amber-banner copy (banner → rail redesign).
GATE (Opus-run, not Codex-trusted): build exit 0; TARGETED 124/124 (LeagueBuilderDraftSetup + LeagueBuilderLeagues +
poolFromDemand + RosterDesigner + best22Target + rosterDesignFeasibility all green); FULL suite 4 reds ALL characterized
flakes (archetypeBalanceSimulator provenance, wpaRuntimeBoundary allowlist, franchiseManualSmokeFixture batch, and
LeagueBuilderDraftSetup "pool-size dial" — which passed SOLO in this run's targeted battery ⇒ batch teardown flake, not a
regression). No NEW red. ⇒ COMMITTED (LeagueBuilderDraftSetup.tsx + LeagueBuilderLeagues.tsx + new salaryCapInput.ts +
leagueBuilderStorage.ts poolExtractedBasis type + both tests). ⇒ THE EDIT-WALL LOOP IS NOW WHOLE: unlock → edit/pin →
re-extract (basis + designs staleness both surfaced) → START; and THE MONEY cap is live on the draft-setup screen in both modes.
NEXT: C-ITER-RERUN (run it back) → C-ITER-COPY (deep duplicate) → slim-rules.

## ITER-LOOP slice 4 C-ITER-RERUN — CLEAN, COMMITTED (Opus audit 2026-07-03)
Codex built CODEX-ITER-RERUN. Round 1: Codex correctly STOPPED (no files touched) on the missing farm-session deleter — the
farm session is created/read by a composed id but the only deleter (deleteAuctionSession) derives the MLB id internally. Opus
authorized the symmetric fix (UNBLOCK in the contract) + named the franchise-guard scan seam; re-dispatched. Round 2 built full.
VERIFIED (full static audit, builder Codex ≠ auditor Opus):
- STORAGE: deleteAuctionSessionById(id) added mirroring getAuctionSessionById; deleteAuctionSession now DELEGATES to it (parity
  with getAuctionSession→getAuctionSessionById, byte-same behavior for existing callers). No store added, no TRACKER_DB_VERSION
  bump → no version-pin test impact.
- GUARD: leagueHasLinkedFranchise(leagueId) scans listFranchises()→getAllFranchiseTeams(id)→team.leagueIds.includes(leagueId).
  Lives in franchiseManager.ts; no import cycle (franchisePlayerStorage does not import franchiseManager — Codex grep-confirmed).
- resetCompletedDraftArc: guard-first (throws named ResetCompletedDraftLinkedFranchiseError); READ-FIRST (Promise.all of mlb/farm/
  startup sessions + registeredPool) before any delete; FARM un-commit deletes only players in the farm session's SOLD results
  AND holding a FARM assignment for THIS league (both conditions, league-scoped); MLB un-commit rewrites the league assignment →
  {teamId:'', FREE_AGENT} (pool membership survives — keyed on leagueId), restores salary from the RegisteredPool ask, strips
  settledSalary; clears mlbRoster/farmRoster; deletes farm+mlb+startup sessions + scout profiles; KEEPS registered pool + designs
  + pins + identities + seats + league fields. IDEMPOTENT (2nd run: sessions gone → empty sets → early returns, no throw).
  Farm-id correctness traced: farmProspectToPlayer spreads ...prospect (keeps prospect id) → minted player.id === farm SOLD
  result.playerId → deletion matches.
- PAGE: purely ADDITIVE — RUN IT BACK affordance (badge split "Drafted ✓" + destruct button → inline SURE? confirm with the
  exact consequence copy), a franchise-link probe effect (FAIL-SAFE: on read error it disables the button — the conservative
  default for a destructive op), disabled while the probe is in flight or a franchise is linked (exact guard string). NO gate/
  predicate changed — FEASIBILITY-SOFT law held (canModeALock/startReady/poolTrailing/handleExtractPool/FLOOR untouched).
  Completed-drafts-only (badge gated on hasCompletedDraft; in-progress saved draft keeps its resume gate).
- TESTS real + strong, nothing weakened: integration R1/R6 (full arc → un-commit; FREE_AGENT, settledSalary stripped, pool
  membership survives, rosters emptied, registered pool byte-identical), R2 idempotent, R3 partial-arc, R4 guard throws;
  UI R5 (RUN IT BACK → resetCompletedDraftArc called) + R4-UI (linked franchise → disabled, not called).
- ORTHOGONALITY: L-SIM harness does not import leagueBuilderAuctionPipeline (Codex grep-confirmed) → L-SIM not required.
GATE (Opus-run): build exit 0; TARGETED 57/57 (draftPipeline.integration + LeagueBuilderDraftSetup + franchiseManager.contract);
FULL suite 2 reds — wpaRuntimeBoundary allowlist (franchiseAnalyticsTrust/StadiumRecords — files slice 4 never touched, red
since before this slice) + franchiseManualSmokeFixture (Opus solo-reran → 4/4 GREEN ⇒ batch flake). No NEW red. ⇒ COMMITTED
(leagueBuilderAuctionPipeline.ts + leagueBuilderStorage.ts + franchiseManager.ts + LeagueBuilderDraftSetup.tsx + both tests).
⇒ RUN IT BACK IS LIVE: a completed draft can be wiped back to scout-hire, same pool/prices/designs/pins intact, guarded against
an already-running franchise. NEXT: C-ITER-COPY (deep duplicate) → slim-rules.

## ITER-LOOP slice 5 C-ITER-COPY (+ FIX round) — CLEAN, COMMITTED (Opus audit 2026-07-03)
Codex built CODEX-ITER-COPY: duplicateLeague upgraded from a shallow record copy to a deep copy (mints new league id; deep-copies
every team via saveTeam→new ids; rosterDesign copied as {slots structuredClone, pins} with lockedAt DROPPED = copy starts editable,
killing the shared-record wall JK hit; lineups/rotation/optimal cleared; teamIds + divisions[].teamIds + conferences remapped;
extraction state poolExtractedAt/basis/modeA* reset; pool-first copies mlbRoster/farmRoster as the auto-importer re-seed source;
design-first copies no rosters; pool membership/registered pool/sessions/scouts left behind by leagueId-keying). Static audit
resolved the pool-first roster-copy (correct: importRosteredPlayersToLeaguePool re-seeds the pool FROM team rosters; verified via
the auto-importer at LeagueBuilderDraftSetup.tsx:1151) and confirmed RosterDesign is exactly {slots,lockedAt?,pins?} so dropping
lockedAt is the wall fix. ADVERSARIAL WORKFLOW (ultracode, wf_687bcb56-276, 3 lenses → 4 findings, all verified): CONFIRMED (1)
RIVALRIES STALE-ID (in-scope, blocks commit) — copied teams kept rivalries[].opponentTeamId pointing at ORIGINAL team ids (remap
skipped the per-team field) → phantom in the editor + inert (intensity-0) in the reporter; hits even a pre-draft copy. (2) POST-DRAFT
POOL-FIRST LEAK (out-of-scope edge) — copying an ALREADY-DRAFTED pool-first league copies post-draft output rosters (won players +
farm-minted prospects) which the auto-importer then leaks into the copy's pool; design §5 scopes copy to PRE-draft, §4 owns
re-draft → UNRULED edge, needs a Fable/JK ruling. ⇒ TICKETED (task_3dda5d4e); NOT fixed here (would need a design decision).
FIX (CODEX-ITER-COPY-FIX) for #1: rivalries stripped from the first-pass spread; a SECOND PASS after teamIdMap is complete remaps
each opponentTeamId via the map and DROPS unmapped ones (re-saveTeam on the existing id = in-place update, no duplicate). Verified
correct + test-covered (mutual rivalry remapped to copy ids, foreign-team rivalry dropped, originals unchanged). Full deep-copy
tests C1-C5 present (distinct ids, lockedAt undefined, slots/pins copied, extraction cleared, knobs kept, sessions/scouts/pool NOT
copied, pool-first auto-import). ORTHOGONALITY: hook-only, not in the L-SIM import graph (grep-confirmed).
GATE (Opus-run): build exit 0; TARGETED useLeagueBuilderData test green; FULL suite 2 reds — wpaRuntimeBoundary allowlist +
franchiseManualSmokeFixture (Opus solo-reran → 4/4 GREEN ⇒ batch flake). No NEW red. ⇒ COMMITTED (useLeagueBuilderData.ts + test).
⇒ COPY-LEAGUE FIX LIVE: a copied league starts fully editable (shared-record wall dead), memberships + rivalries remapped to the
copy's own teams, extraction reset — JK's "couldn't edit the copy league" is solved. Post-draft-copy edge ticketed.
⇒ ALL 5 ITERATE-LOOP SLICES DONE. NEXT: CODEX-SLIM-RUNNER (runner-on-second, the last AUTH-4 item).

## SLIM-RULES CODEX-SLIM-RUNNER (runner-on-second) — CLEAN, COMMITTED (Opus audit 2026-07-03)
Built to Fable's BINDING spec FABLE_SLIM_RULES_RUNNER_ON_SECOND_2026-07-03.md. Recon (Explore agent) had corrected two stale
premises: (a) GameTracker ALREADY has the full ghost-runner-on-second logic wired (consumes extraInningRunner + extraInningRunnerDelay
from nav-state, places at regulationInnings+delay) → runner-on-second is a PURE nav-shim, no frozen-tracker edit; (b) the RULES
screen is NOT obsolete (only editor of RulesPreset) — JK RULED "leave it alone", so slim-rules = the runner-on-second wiring only
(season length + innings already worked). VERIFIED (builder Codex ≠ auditor Opus):
- TYPE: one optional field `extraInningsRunnerDelay?: 1 | 2` added to FranchiseConfig.season (franchise.ts:162); StoredFranchiseConfig
  extends it → inherited, not duplicated; extraInningsRule kept as string (not renamed/narrowed). Additive, migration-safe, no DB bump.
- MAPPING (the point): exported pure resolveFranchiseExtraInnings(config) beside resolveFranchiseGameUseDH — switch: "Runner on 2nd"
  → {true, delay ?? 1}; "Standard"/"Sudden Death"(labeled future-seam no-op)/legacy-lowercase/null → {false, 1}. Applied at the
  franchise-game launch: `...resolveFranchiseExtraInnings(franchiseConfig)` spread beside totalInnings. useGhostRunner NOT passed
  (GameTracker mirrors extraInningRunner via its documented fallback). Matches Fable §3 table exactly.
- CONTROL: FranchiseSetup EXTRA INNINGS RULE box gains a sub-choice row rendered ONLY under "Runner on 2nd" (divider + "GHOST RUNNER
  ARRIVES" + two radio dots 1st/2nd extra inning, pixel-matching the parent options); selecting "Runner on 2nd" seeds delay=(prev ?? 1),
  switching away RETAINS it (inert by the §3 gate). The ℹ️ hint is now context-sensitive: Runner on 2nd → "Ghost runner takes second
  starting the {ordinal} inning" (inline formatOrdinal, live-updates with innings); Sudden Death → honest v1 no-op line; Standard →
  BYTE-IDENTICAL to the prior string. Every element in the existing chalk/brass language; no new box/tooltip/(?)-button.
- FROZEN-FILE PROOF (Opus-run git diff): GameTracker.tsx / useGameState.ts / LeagueBuilderRules / ExhibitionGame.tsx — ZERO changes.
- TESTS (§4 A-E, real + comprehensive): A resolver units (5 cases incl. missing-delay MIGRATION + legacy lowercase 'standard' + null);
  B launch nav-state per rule; C config round-trip incl. migration (delay round-trips only when provided; rule preserved); D FranchiseSetup
  show/hide (hidden under Standard default, reveals with 1st selected, writes delay 2 + shows 9-inning "11th", Sudden Death hides +
  its hint, Standard hint byte-identical after toggle-back); E frozen-file diff proof.
GATE (Opus-run): build exit 0; TARGETED 3 franchiseMode files green; FULL suite — characterized flakes only (wpaRuntimeBoundary
allowlist + franchiseManualSmokeFixture batch + any figma IndexedDB-teardown batch flake, each solo-green). No NEW red. ⇒ COMMITTED
(types/franchise.ts + FranchiseSetup.tsx + FranchiseHome.tsx + 3 tests). ⇒ RUNNER-ON-SECOND IS LIVE: the league picks the rule +
1st/2nd-extra-inning delay in franchise setup; it rides into the (frozen) GameTracker at launch, which already places the ghost runner.
⇒ ALL 6 AUTH-4 ITEMS DONE (edit-wall, salary cap, CPU bidding, scroll/home/hub, run-it-back, copy-league, slim-rules). One out-of-scope
edge ticketed (post-draft pool-first copy leak, task_3dda5d4e).

## FLOOR-DIAGNOSTIC-FIX (JK browser-pass finding) — CLEAN, COMMITTED (Opus audit 2026-07-03)
JK's browser pass surfaced a confusing recheck panel: 4 green per-club "BUILDS · $602,731 to spare" rows + a red "Cleveland Indians ·
LEAGUE ✗ … Priciest asks vs their market: C (+$0), backupC (+$0), 2B (+$0)". Investigated read-only (Explore recon + a general-purpose
root-cause subagent, NO Fable per JK). Findings: (Q1) panel-1 CLUB CHECK "TO SPARE" = per-club FLOOR headroom (identical across clubs =
correct, same full pool + cheapest-first matching); "TARGET $X" = identity-optimized target cost (differs) — CONFUSING-BUT-CORRECT.
(Q2) panel-1 vs panel-2 Cleveland number drift ($602,731 vs $570,729) = the recheck panel was STALE (its "pool changed — re-check" flag
was showing) — resolves on RE-CHECK; not a code bug. (Q3) the 5th row is NOT a duplicate team — it's the sequential shared-pool FLOOR
row (buildLeagueSeatabilityRow) that named leagueTeams[pass-1]; LATENT bug: it iterated leagueTeams (incl CPU) while per-club rows are
human-only → could blame an unseen CPU club. (Q4) 4 green + 1 red reads contradictory: per-club rows check each club ALONE vs the FULL
pool (no contention); the FLOOR row drains a SHARED pool — both true, display misleading. (Q5, the real bug) the FLOOR reused the
per-team budget blocker's "Priciest asks vs their market" tail, whose premium is computed vs the DRAINED remaining pool → all +$0 when
only expensive players remain → actively misleading (blames prices; real cause = pool exhaustion/contention).
FIX (CODEX-FLOOR-DIAGNOSTIC-FIX, ALL in LeagueBuilderDraftSetup.tsx — rosterDesignFeasibility.ts per-team message left intact, correct
in its own context): FLOOR failure message is now case-split — law-gap keeps its "short a body/position" detail; budget-overflow states
"the cheapest legal 22 left costs $X against the $Y cap ($Z over) — the affordable players are used up. Raise the cap or add players."
(NO "Priciest asks" tail); fallback = result.blockers. The FLOOR row is relabeled pool-level (`ALL CLUBS · ONE POOL` / `SHARED POOL`),
count-framed ("The shared pool seats N of M clubs, then can't seat the next: …") — no longer names an arbitrary/CPU club (closes Q3),
success = "Seats all M clubs." A caption clarifies the two questions ("Each club is checked drafting alone from the full pool; the last
line checks all clubs sharing one pool") (closes Q4). Verified: constants correct; 3-case message logic; leagueTeams kept as the
contention set (CPU clubs DO draft) but no longer named. TEST added (design-first fixture WITH a CPU team forcing the budget-overflow
FLOOR path): asserts SHARED POOL tag + ALL CLUBS·ONE POOL label + "seats 1 of 3 clubs" + "affordable players are used up", and NOT
"Priciest asks" / NOT the CPU team name / NOT "club 2"; per-club "BUILDS · to spare" rows still render (no regression); panel title
unchanged. GATE (Opus-run): build exit 0; TARGETED LeagueBuilderDraftSetup green; FULL suite — characterized flakes only. No NEW red.
⇒ COMMITTED (LeagueBuilderDraftSetup.tsx + its test). NOT changed: panel-1 CLUB CHECK (Q1/Q2 correct-but-could-clarify — left per scope).

## POOL-SEATABILITY-SYNC (JK "pool builds at 1.78x and still not enough") — CLEAN, COMMITTED (Opus audit 2026-07-03)
JK flagged the core value prop: design-first sizes a pool at 1.78× and the FLOOR still says "can't seat all clubs" — "is the build
process broken?" Root-caused by an adversarial workflow (wf_a9772ea7-6e7, 4 lenses + repro): NOT a sizing-math error. The sizer's
guarantee runG1Check and the UI FLOOR buildLeagueSeatabilityRow are the SAME greedy-sequential drain but were TWO copies (drift
risk), and the on-screen sizing receipt (modeAReport "added N for affordability") RE-DERIVES LIVE over the full universe (re-heals)
on every input change while the FLOOR checks the PERSISTED pool — so a stale healed "buildable" sat next to a failing FLOOR. Repro
proved: on the SAME set the two checks ALWAYS agree; divergence only when fed different sets (a live-healed hypothetical vs the stale
persisted pool). Team-count / budget / mapper all ruled out in the shown case. Trigger: JK changed THE MONEY (cap) after extracting
without re-extracting → healed receipt lied, FLOOR checked the old-cap pool. FIX (CODEX-POOL-SEATABILITY-SYNC, Codex-built, Opus-audited):
(A) ONE shared seatability primitive seatAllClubs(designPool, clubs, budget) in rosterDesignFeasibility.ts; runG1Check + the FLOOR
are now thin wrappers (byte-same behavior; can never algorithmically drift). (B) the stale healed sizing receipt is HIDDEN when
poolTrailing (sizingSummaryLine `&& !poolTrailing`) — no healed "buildable" shown beside a failing FLOOR; the RE-PLAN RAIL is the sole
guidance when stale. Staleness signal COMPLETED: poolExtractedBasis now stamps `shills` (migration-safe optional) → a shill change
joins basisStale. (C) team-count hardened: extractPoolFromDemand throws PoolTeamsForSizingMissingError if options.teams is undefined
(no silent designs.length fallback); the only prod caller passes teams: league.teamIds.length (verified — no other prod caller).
AUDIT VERIFIED: seatAllClubs is a faithful extraction; runG1Check wrapper byte-same; FLOOR wrapper preserves the CODEX-FLOOR-DIAGNOSTIC-FIX
copy (seats N of M, overrun/law-gap detail); basis.shills migration-safe; throw safe (sole caller passes teams); stale gating leaves
the fresh case untouched. ⭐ DEFINITIVE: the new seatAllClubs.test.ts SAME-SET AGREEMENT test (extract → seatAllClubs on the persisted
result.players HOLDS whenever g1 holds) PASSES + the equivalence invariant (shared.holds === floor.holds) PASSES ⇒ FRESH EXTRACTS ARE
SOUND; JK's "pool not enough" was the STALENESS, not a sizer bug. The build process works. GATE (Opus-run): build exit 0; TARGETED
seatAllClubs + poolFromDemand + rosterDesignFeasibility + LeagueBuilderDraftSetup green (72/72); FULL suite — characterized flakes only
(wpaRuntimeBoundary allowlist + franchiseManualSmokeFixture batch, solo-green). No NEW red. ⇒ COMMITTED (rosterDesignFeasibility.ts +
poolFromDemand.ts + LeagueBuilderDraftSetup.tsx + leagueBuilderStorage.ts + poolFromDemand.test + LeagueBuilderDraftSetup.test + new
seatAllClubs.test). NEXT: CODEX-ROTATION-ORDER (the queued display fix) → then JK decides #3 (best-players-start + require-a-closer).

## ROTATION-ORDER (JK browser-pass: rotation in a strange order, SP/RP over SP) — CLEAN, COMMITTED (Opus audit 2026-07-04)
JK: the BEST-22 rotation/bullpen slots showed no quality order (ace could read as SP3) and pure-vs-hybrid placement was a fill-order
artifact. Confirmed by the #2 investigation: SP1-4/RP1-4 are filled in fill-order and rendered in fixed slotId order — no value sort.
FIX (CODEX-ROTATION-ORDER, Codex-built, Opus-audited) — PRESENTATIONAL ONLY, scoped to best22Target.ts: after picks are built, a pure
reorderArmSlotsByValue() sorts each same-kind arm group (sp, rp) BEST-FIRST by player kblIV (empty picks last; tiebreak salary desc,
playerId asc), PRESERVING pinned picks in their exact slot (only non-pinned arms reorder around them); slotIds stay in position (SP1..SP4).
Selection, totals, allIn, feasibility, asksHonored, and all non-arm slots are UNCHANGED — verified. TEST (best22Target.test A5): pins SP2,
asserts free arms sort IV-desc around the held pin, arm order flips best-first, pinned pick stays put, non-arm slots + full player set +
totalSalary + allIn + feasible all byte-identical. GATE (Opus-run): build exit 0; TARGETED best22Target + RosterDesigner green (29/29);
FULL suite 3 reds ALL characterized flakes (archetypeBalanceSimulator provenance + franchiseManualSmokeFixture — both Opus solo-reran
GREEN; wpaRuntimeBoundary allowlist, unrelated files). No NEW red. ⇒ COMMITTED (best22Target.ts + its test). ⇒ ACE READS AS SP1.

## OPEN — POOL AFFORDABILITY/QUALITY (JK: expensive RP-heavy pool needs ~50% more cap despite ~1000 players)
JK browser-pass (2026-07-04): design-first pool = 168 of 998; sorted by salary the TOP is ALL relievers ($143k-$218k RP/SP-RP); recheck
only passes at ~$1.5M cap (then >$1M/team leeway); apparent duplicate player (Gasser Morris ×2, Ace von Acesson ×2). This is SEPARATE
from + BIGGER than the staleness sync (which only made the readouts honest, not the pool cheaper) — the sizer is failing its core job:
build the CHEAPEST viable pool per identity under the cap from the whole DB. Opus corrected the earlier "it was just staleness"
reassurance to JK. Adversarial workflow wf_fd89a48d-9a8 running (lenses: RP demand/pricing, fit-vs-affordability, repair-gap, duplicates).
Likely a Fable MATH/DESIGN gap (poolFromDemand never optimizes for affordability, only fit) — awaiting synthesis before proposing the fix.

## OVERNIGHT AUTONOMOUS PLAN (AUTH-4, caffeinate on, 2026-07-04) — pool-affordability + full valuation audit
Fable design = spec-docs/FABLE_POOL_AFFORDABILITY_DESIGN_2026-07-04.md (4 steps). SP/RP reconciled to 0.80×SP (JK: SP/RPs ARE valuable
+ reconcile with IV engine → 0.80 = pure production, flexibility premium deferred to v1.1). RP 0.55, CP 0.65 unchanged. Build order:
- STEP 1 arm reprice (ivCurves.ts, SP unchanged / SP-RP 0.80 / CP 0.65 / RP 0.55) + MANDATORY oracle re-bless — IN FLIGHT (codex bbtl6ir51).
- STEP 2 balanced seatAllClubs · STEP 3 cheap-depth floors · STEP 4 repair swap-down. Re-measure symptom after 1,2,4.
- ⭐ JK REQUEST (2026-07-04, good-night): COMPREHENSIVE valuation audit — check EVERY position/role (SP/RP/CP/SP-RP + C/1B/2B/3B/SS/
  LF/CF/RF/DH) for mispricing like the reliever bug. RUN AFTER Step 1 commits (computing live IV during Step 1's ivCurves edit = garbage);
  safe to run PARALLEL to Steps 2-4 (they don't touch ivCurves). Method: cross-position computeIV at identical ratings + inspect every
  ivCurves block for outliers + resolve where positional scarcity value lives (curves vs position multiplier — gotchas memory says C 1.15/
  SS 1.12/CF 1.08/... but the earlier workflow saw salaryCalculator multipliers = 1.0; RECONCILE). Any mispricing → Fable design → Codex
  repricing (batch into ONE further re-bless if possible). Report all findings + before/after numbers to JK in the morning.

## STEP 1 — ARM REPRICING — CLEAN, COMMITTED (Opus audit 2026-07-04)
Codex built CODEX-ARM-REPRICE (SP/RP=0.80 after JK reconciliation). Full static audit + Opus-run gate. VERIFIED:
- ivCurves.ts: SP block UNCHANGED (diff starts at SP/RP block :170); SP/RP → 0.80×SP, CP → 0.65×SP, RP → 0.55×SP — sal100 = SP×mult
  with SP's SHAPE params (so the ratio holds at EVERY rating, not just anchors); SP/RP batting sal100 inflation (200k/160k/200k) fixed
  (copies SP's batting rows).
- ivEngine.ts: removed the old SP/RP pricing path blend(SP,RP)×SP_RP_FLEX_PREMIUM → SP/RP now reads its own 0.80 curve block directly.
  This IS Fable's "pull the flexibility premium out of the curve" (the FLEX_PREMIUM was the baked-in optionality). SP_RP_FLEX_PREMIUM +
  SP_RP_INNINGS_ALPHA constants removed (rosterEngineConstants.ts + effectiveRatings.test.ts — these are STEP-1 work, Codex mislabeled
  them "pre-existing"; grep-confirmed ZERO remaining refs).
- Oracle RE-BLESSED (deliberate, hand-checked): ivEngine.test.ts re-pinned + iv_oracle.json + IV_ENGINE spec + DECISIONS_LOG updated.
  Before/after on real players (the drop is on relievers/swingmen, exactly as intended): Buzz Pastimm SP/RP (the $199,126 arm topping
  JK's screenshot pool) → $122,198; Jon Gray → $46,607; wpg-drake → $56,490. SP-role players unchanged.
- ⚠️ FLAG FOR JK — HDH ROYALS PARITY RETUNE: to keep value-parity in-band after relievers got cheaper, the HDH Royals archetype's
  boost/nerf spec was softened (PEN_ACC 1.5→0.3, POW −1.5→−0.5, ROT_ACC −1→−0.25) in historicalArchetypes.ts. AUTHORIZED by Fable's spec
  (adjust the archetype, NEVER re-inflate curves) and parity holds 24/24 across juiced/standard/nerfed. BUT it noticeably softens HDH's
  "lockdown bullpen" identity — JK should confirm this is acceptable; if not, a more faithful HDH re-tune (preserve bullpen character
  while hitting parity) is the follow-up. This is the one design-visible side effect of the repricing.
GATE (Opus-run): build exit 0; TARGETED 32/32 (ivEngine.test + archetypeBalanceSimulator + salarySeam.t5 + effectiveRatings);
FULL suite 3 reds ALL solo-green batch/characterized flakes (archetypeBalanceSimulator parity SOLO 1/1 GREEN — parity confirmed post-
reprice; franchiseManualSmokeFixture SOLO 4/4; wpaRuntimeBoundary allowlist, unrelated). No NEW red. ⇒ COMMITTED (ivCurves.ts +
ivEngine.ts + rosterEngineConstants.ts + historicalArchetypes.ts + ivEngine.test + salarySeam.t5 + effectiveRatings.test + iv_oracle.json
+ IV_ENGINE spec + DECISIONS_LOG). ⇒ ⭐ ARM MISPRICING FIXED: relievers/swingmen no longer top the pool; ordering SP>SP/RP>CP>RP restored.
NEXT: launch the every-position valuation audit (JK's ask) + Step 2 balanced seating (concurrent, disjoint). Full pool re-measure after Steps 2-4.

## EVERY-POSITION VALUATION AUDIT (JK's overnight ask) — FINDING: positional scarcity absent (2026-07-04)
Fable-model audit (empirical computeIV across every position/role, scratch deleted). RESULT: the arm reprice is CONFIRMED sane
(SP 1.00 / SP-RP 0.80 / CP 0.635 / RP 0.547 empirically). But it found a SECOND real mispricing, SAME CLASS as the reliever bug:
- ⭐ POSITIONAL SCARCITY ENTIRELY ABSENT (MEDIUM-HIGH, NEEDS JK MAGNITUDE SIGN-OFF). Identical hitter (all-70 and all-85) prices the
  SAME kblIV at EVERY position — C/1B/2B/3B/SS/LF/CF/RF/DH all $45,389 (70) / $80,998 (85). All 8 hitter blocks in ivCurves.ts:49-120 are
  byte-identical AND salaryCalculator POSITION_MULTIPLIERS (:246-263) are all 1.0. Reconciliation: the old ladder (C 1.15/SS 1.12/CF 1.08/
  2B 1.05/3B 1.02/RF 0.98/LF 0.95/1B 0.92/DH 0.88) was RETIRED from the multiplier table (IV spec §3.8 "value lives in the curves") but
  NEVER landed in the curves → the defensive-scarcity premium lives NOWHERE. Premium C/SS/CF underpriced, bat-first corners overpriced.
  FIX DIRECTION (Fable): a positional ladder in the KBL LAYER ONLY (rawIV stays oracle-frozen, mirror the pitcher-usage-layer pattern) —
  per-position kbl multiplier (the old ladder is a defensible starting point) OR position-differentiated FLD/ARM curves.
- DH gets NO bat-only discount (MEDIUM-LOW, subset): ivEngine.ts:203 maps DH→1B block → DH charged full FLD/ARM (~7-10% overprice).
  FIX: a DH kbl treatment zeroing/discounting FLD/ARM.
- Structural scan: no other sibling-outlier blocks; pitcher batting cliffs are uniform across roles + oracle-fitted (no action).
⇒ DECISION (AUTH-4): this is a fresh VALUE RULING (ladder magnitudes are JK's call, like SP/RP) + a high-stakes oracle re-bless →
NOT built tonight (audit's own recommendation). SURFACE TO JK in the morning for magnitude sign-off; on approval Fable firms the spec →
Codex builds → its own re-bless. Separate re-bless from the arm fix regardless (different value dimension), so deferring costs no batching.

## STEP 2 — BALANCED SEATING — CLEAN, COMMITTED (Opus audit 2026-07-04)
Codex built CODEX-BALANCED-SEATING. seatAllClubs (rosterDesignFeasibility.ts, the shared primitive both G1 + UI FLOOR call) rewritten
from greedy-sequential to: global most-constrained-first Kuhn max-cardinality matching over T×22 slots with SNAKE club tie-breaks
(scarce cheap bodies stripe across clubs), then a budget-balancer (re-solve the max club vs unused players via evaluateRosterDesign at
currentCost−ε, else swap an expensive max-club body to an UNDER-budget donor of the same slot-class keeping both legal + donor ≤ budget).
Returns per-club assemblies + costs. AUDIT (static): TERMINATES — both moves strictly reduce total over-budget excess (donor stays ≤
budget → no oscillation), + a maxIterations backstop; matching is standard augmenting-path; legality gated by isLegalRoster (explainIllegality);
deterministic (candidateOrder + snakeRank + reduction tie-breaks, no randomness). Heuristic (not global-optimal) balancer — allowed by
Fable §Step 2 — and it directly kills the greedy-stranding artifact. Both consumers (runG1Check + buildLeagueSeatabilityRow) updated;
SeatAllClubsResult gains assemblies+costs. TESTS: STRANDING (2 clubs, budget 412k where old greedy stranded club 2 → now holds:true,
seated:2, 44 distinct, max ≤ budget) + SYMMETRY (order-invariant holds + max cost) + PARTITION (holds at budget, fails at budget−1 with
overrun 1) + existing equivalence/same-set preserved. PAGE (LeagueBuilderDraftSetup FLOOR): leeway-readout ONLY — overrun detail uses
balanced per-club failingCost; success line adds "tightest club has $X to spare"; NO gate/predicate change (FEASIBILITY-SOFT held).
GATE (Opus-run): build exit 0; TARGETED 75/75 (seatAllClubs + rosterDesignFeasibility + poolFromDemand + LeagueBuilderDraftSetup);
FULL suite — characterized flakes only (wpaRuntimeBoundary allowlist, franchiseManualSmokeFixture batch, historicalArchetypes/archetypeBalance
big-batch — solo-green). No NEW red. ⇒ COMMITTED (rosterDesignFeasibility.ts + seatAllClubs.test + LeagueBuilderDraftSetup.tsx + its test).
⇒ ⭐ FAIR SEATING: the "$1.5M / then $1M leeway" artifact is dead — the pool's cheap arms are shared across clubs, not hoovered by club 1.
NEXT: Step 3 cheap-depth floors → Step 4 repair swap-down → full pool re-measure.
