# V1 BUILD STATUS — SINGLE SOURCE OF TRUTH

> **THIS is the one status doc for v1. Scope: A-to-Z, LEAGUE SETUP → END OF REGULAR SEASON.**
> Playoffs/offseason DEFERRED (Decision E). In-season GM tools (dead-cap display, call-ups, trades advisor) = a SEPARATE thread, excluded here.
> **As-of: 2026-07-07**, git-grounded (trunk HEAD `7b5214ca` + docs commits); **§0 items 2c-2h below carry it forward to 2026-07-09, HEAD `677a7607`.** Update this doc IN PLACE as work lands — do not spawn a new status doc.
> Every other v1 "status/roadmap/plan" doc is SUPERSEDED (see §5). When in doubt, this doc + the code win.

---

## §0. 2026-07-07 UPDATE (Fable, evidence-classed — read this before the body)

**Evidence legend:** [git]=command run 2026-07-07 · [read]=doc/log read in full · [browser]=seen rendering · [UNVERIFIED]=assume broken.

1. **ASSEMBLY EXECUTED (corrects §2 below):** `claude/v1-draft-ui` AND `claude/lineups-fenway-hub` are both MERGED into trunk [git: merge-base --is-ancestor both ✓]; trunk advanced 74 commits 2026-07-02→04 [git]: C2B second-price market + C3 pool sizing/FS-3 launch fix [read: CURRENT_STATE 07-02 header], asst-GM B-series (B1/B2/B5, bid-vs-pass board wired), require-a-closer, the pool-affordability arc (repricing/seating/cheap-depth/swap-down), D17 positional scarcity, AUTH-4 finale, hard-cap Phase 1 + legality-by-construction fix a20ff1a6 [read: DECISIONS_LOG 07-02..07-04].
2. **Trunk baseline MEASURED [git, executed]:** build exit 0; full suite **8,983 pass / 1 fail / 8 skip (589 files)**. The 1 failure's identity to be named in the next gate run.
3. **LINEAGE RULING (JK 2026-07-07):** this trunk = THE PRODUCT. GitHub PRs #8–#18 (all opened+merged 2026-07-07 [git: gh pr list]) built a parallel line on a May-21 base: PR #9 was a content snapshot OF this trunk [git: core files byte-identical]; PRs #10–#17 added genuinely-new draft-economy work (pool balance/quality/source presets, cap-fit diagnostic, liquidity-aware bidding, a 19-file headless auction-sim harness) which is being PORTED here (port map in `PATHWAY_TO_V1_2026-07-07.md`; files verified ABSENT on trunk [git]). The GitHub line will be archived after the port; trunk becomes published main (Pathway Phase 0c).
4. **UI TRUTH DEMOTION (JK ground-truth, browser 07-03/04):** the RUNNING app is an old/new amalgamation. Every "[UI] DONE" in §3 hereby means "exists in tree", NOT "is what renders". Proven instance [git]: `/franchise/:id` still routes the OLD FranchiseHome (App.tsx:334) while the lens/fenway hub sits at `/__preview/franchise-lens` only (App.tsx:419) — merged, never routed. NO UI status is trusted until the Phase-1.5 **UI TRUTH MAP** (route-by-route browser walk, row-count == router-count, screenshot per row) exists and JK rules the cut-over. `UX_NORTH_STAR.md` §3's binding disposition table (KEEP/RESKIN/FOLD/KILL incl. R-IA6 lens-is-the-hub, R-IA7 dev-gate previews) is the design layer the map inherits.
1b. **PORT LANDED (PR #19, 2026-07-07, main @ 292112ad):** the #10–#17 economy work is ON MAIN (pool presets, cap-fit, liquidity bidding, auction-sim harness). Post-port suite 9,079 pass; failures = the named wpa pre-existing + DraftSetup batch flakes (solo-green). Opus audit: APPROVE.
1c. **CUT-OVER LANDINGS (2026-07-07):** PR #20 baseline fully GREEN (wpa allowlist traced: 4 read-only materializers, guard intact). PR #21 dev-gated 16 leaked /__preview routes (browser-verified prod 404) + archetype help harvest (15→24). PR #22 floor fixes: in-session recompute + shills-advisory floor per JK ruling — 30-club room locks with ZERO hand-adds (browser-proven: pool 660 = exact demand). Hub flip + farm fold STOPPED at parity gates → tickets LENS-PARITY (building) + FARM-STAGE (queued). Economy: measurement campaign committed (pool FIXED, money BROKEN, evidence → Lever A); JK ruled Fork A; FABLE_RESERVE_PRICE_DESIGN committed; Lever A building. Escalated: CPU identity auto-assign (gates lock UX for big rooms; queued behind Lever A — same page surface).
1d. **THE FENWAY HUB IS LIVE (PR #24, 2026-07-07):** /franchise/:franchiseId renders FranchiseLens; old FranchiseHome unrouted (file retained until the walk). Full loop browser-proven (setup wizard → lens → add game → SCORE → GameTracker w/ lineups). Ballpark kit + dev-speak copy kills landed (PR #23). CARRY-FORWARD: lens seasonNumber reads URL param (default 1) — derive from franchise metadata BEFORE any multi-season work; manual lineup-benchmark button unwired (auto path intact).
1e. **JK RULING (2026-07-07): ELIMINATION MODE IS OUT OF v1** — a separate path from Mode 1/Mode 2 entirely. No v1 work on it (no builds, no hides, no reskins); its screens are exempt from the zero-old-screens walk, which covers the league-build → draft → franchise → season journey only. Manifest rows touching elimination = OUT-OF-SCOPE/leave-alone.
1f. **LEVER A MERGED (PR #25, 2026-07-07):** reserve prices live (k dial default 0.65, k=0 = old economy bit-identical). Audit arc: REJECT (k>0 infinite loop, reproduced) → remediation (bounded renomination, falsification-proofed harness) → APPROVE (independently verified). Measured: free talent dead, fairness 0→7/11 scenarios; RESIDUAL for next lever: spot-11 pacing + big-league completion pressure. Exhaustion amendment recorded in the design doc. **JK feel-pass on a real draft = pending gate.**
1g. **ORPHAN/WIRING MATRIX COMMITTED (2026-07-07):** spec-docs/ORPHAN_WIRING_MATRIX_2026-07-07.md — 137 rows from 15 tracers + critic: 28 wired-live, 28 built-dark-by-design, 28 partial, 21 orphaned, 12 missing, 8 spec-drift; **50 pre-tuning blockers** (§2, headline = production flag-flip mechanism for the 11 soul flags); §3 = 7 JK decisions; §4 = retirement list. CAVEAT: some UI verdicts cite the pre-lens-flip truth map — re-walk required (matrix item 7).
1h. **MODE-1 PUNCH LIST + GAUNTLET (2026-07-08):** spec-docs/MODE1_PUNCHLIST_2026-07-08.md — JK Friday findings F1-F5 (deterministic nomination order, farm bands missing/wrong-source, generic scout pool, inflated prospect curve, style mismash) + P1-P12 + the gauntlet grid + the new ILLOGIC-PASS audit lens (behavioral coherence, GM-eyes). Mode 1 validated to error-free CONCURRENTLY with the Mode-2 wiring wave (disjoint file surfaces). Definition-of-done for JK A-Z draft testing = punchlist §5.
1i. **F4 PROSPECT CURVE LANDED (2026-07-08, lane/m1b-curve):** farm prospect generation now uses the `PROSPECT_GENERATION_SPEC` §3.2 grade curve as a seeded pool-level quota sequence before the existing `scoreSmb4Player` inverse rating solver. Permanent opt-in invariant added at `scripts/farmProspectDistribution.test.ts` (`RUN_FARM_PROSPECT_DISTRIBUTION=1`, N=500 through real farm auction/prospect pool path). Before: B- +2.0pp, C +2.2pp, total abs 8.8pp exceeded the chosen max bucket 1.5pp / total 8.0pp tolerance. After: exact N=500 curve, total abs 0.0pp; existing 40k §13 prospect distribution check also 0pp grade deviations. No MLB pool shaping, IV curves, or oracle changes.
1j. **M2A PHASE-2 ACTIVATION MECHANISM LANDED (2026-07-08):** the 11 default-OFF soul flags now resolve `test override > persisted activation > compiled default`. Persistence reuses existing `kbl-app-meta.appSettings` key `franchisePhase2Activation` (no new DB/store/version bump). `/__preview/phase2-activation` is DEV/test-gated for tuning with global/per-flag controls; production UI stays dark until the Phase-5 migration-check/flip sequence. Gates: tsc pass; build pass; focused flag/soul suite pass 59/59; L-SIM smoke 24g all CRITICAL invariants green. Full suite has zero M2a reds; remaining known unrelated red is `LeagueBuilderDraftSetup.test.tsx` CUT2-2 30-club shill pressure, still failing solo on disabled `START THE DRAFT`.
1i. **MODE-1 P1 CPU IDENTITY AUTO-ASSIGN LANDED (2026-07-08, lane/m1c-autoassign):** Draft Setup THE CLUBS now has deterministic CPU identity auto-fill plus per-club reroll. It fills only missing slots by default, preserves user-set identities, excludes LOCKED draftability archetypes, defaults human clubs out unless explicitly included, and uses a visible leagueId:nonce seed. Gates: tsc pass, build pass, DraftSetup solo 60/60, constrained full suite 9,125 pass / 9 skip.
1k. **M2B ARM RATING LAST MILE LANDED (2026-07-08, lane/m2b-arm):** `armThrowingRate` now receives real expected-stats samples/actuals. Catcher arms use the existing RA-8 catcher CS-rate aggregator over stored `caughtStealingAgainst` / `stolenBasesAllowed`; OF arms use stored `outfieldAssists + baserunnersHeld` over LF/CF/RF games; players with no C/OF arm exposure omit the category so no adjustment fires. Pitchers still emit no arm signal. Gates: tsc pass; build pass; focused expectedStats/checkpoint suite 106/106; full suite showed only known `LeagueBuilderDraftSetup.test.tsx` batch reds, and the solo file rerun passed 60/60. Commit `00466697`.
1l. **M1J AUCTION COMPLETION TERMINAL FIX LANDED (2026-07-08, lane/m1j-completion):** nomination exhaustion and other terminal paths now route through the existing FABLE-C3 cleanup before `AUCTION_COMPLETE`, reusing Lever A's exhaustion affordability rule instead of bypassing `backfillFromPassedLots`. Enriched MLB sessions that remain legally uncompletable reject with `auction-uncompletable` plus `terminalShortfall` instead of silently completing short. Opt-in matrix `RUN_M1J_COMPLETION_MATRIX=1` covers {6-team,8-team} x {0,2 shills} x {k0,k065}, seeds `m1e-s1..s3`; fixed run is 100% legal 22-man rosters, zero flags, and the temporary direct-completion falsification fails every row. Gates: tsc pass; build pass; focused auction/farm/UI suites 105/105; full suite only known DraftSetup CUT2-2 batch reds, solo rerun 60/60.
1m. **M1K FARM AUCTIONSTAGE FOLD LANDED (2026-07-08, lane/m1k-farmstage):** the routed farm auction now uses the shared `AuctionStage` farm tier instead of the legacy draft-floor register, with no engine/hook edits. The fold preserves M1D farm bands/fog, M1H strict 4-field lot card privacy, M1G no-shill/human-safe behavior, M1a resume/seed persistence, and Lever A reserve/renomination semantics. Setup controls are off the floor; tests/dev use URL `leagueId`/`devSeed` only. Gates: tsc pass; build pass; focused farm/page/hook/AuctionStage suite 17/17; full suite reds cleared in exact isolated rerun 12/12. Browser verification was environment-blocked by Browser/node_repl sandbox metadata and Chromium Mach-port permission failures.
1m. **M1L POST-FREEZE SUMMARY LANDED (2026-07-08, lane/m1l-freeze):** FranchiseSetup now shows a read-only persisted freeze summary immediately after successful franchise initialization and before entering the lens. Displayed fields are loaded from existing franchise config/player/team/morale/True Value stores only: team payroll baselines, roster counts, settled salary counts, draft-baseline contract rows, player and fan morale baseline summaries, plus not-displayable gaps for exact freeze-engine team payroll aggregates and slot/pay class because those artifacts are not persisted as readable summary data. No schema, freeze logic, or recomputation path changed. Gates: tsc pass; build pass; focused FranchiseSetup/freeze-summary tests 34/34; franchiseInitializer-adjacent suites 25/25; full suite reds under unconstrained load all passed solo (`LeagueBuilderDraftSetup` 60/60, `RosterDesigner` 19/19, `prospectScoutingDraftEngine` 42/42, `draftPipeline.integration` 13/13).
1n. **M1N/P8 CONFERENCE EDITOR LANDED (2026-07-08, lane/m1n-conference):** League Builder's league create/edit modal now exposes conference assignment only (no division editor): single-conference default action, balanced split, add/remove, rename, and one-conference-per-team assignment validation. The untouched default path remains the old empty `conferences: []` / `divisions: []` shape; edited leagues persist names/membership through existing `LeagueTemplate.conferences` and hidden one-division-per-conference `LeagueTemplate.divisions`. `useFranchiseData` now reuses valid source league conference/division membership for franchise standings, while FranchiseLens standings are documented/tested as still league-wide. Gates: tsc pass; build pass; focused conference/LeagueBuilder/franchise consumer tests 43/43; League Builder surface suite only known CUT2-2 batch reds; full `NODE_ENV= npx vitest run` had one known CUT2-2 batch red and the exact solo rerun passed.
1n. **M1M P7 RULES PRUNE LANDED (2026-07-08, lane/m1m-rulesprune):** visible rules/season setup controls are now either wired or removed. Removed the decorative standalone League Builder rules preset editor/card, dev/test season-rules preview controls, Franchise Setup All-Star Game/Trade Deadline/Mercy Rule controls, and the unwired Sudden Death extra-innings option. Persisted rules/config fields remain for old saves; old-save launch still tolerates legacy values. Wired season controls left visible: quick presets, games per team, innings per game, Standard/Runner on 2nd extra innings, and runner-delay selection. Playoff setup controls are ambiguous and left untouched because consumer code exists while v1 playoffs remain deferred. Gates: tsc pass; build pass; focused rules/setup/launch suites 75/75; full suite only known `LeagueBuilderDraftSetup.test.tsx` CUT2-2 batch reds, solo rerun 60/60. Browser smoke was environment-blocked by Browser/node_repl `sandboxCwd must be an absolute file URI`.
1n. **M1O/P11 SCOUT REVEAL PLACEMENT LANDED (2026-07-08, lane/m1o-scoutplace):** the farm-only ScoutHire reveal moved from before the MLB auction to between the MLB auction and farm auction. The draft arc is now `draft-setup -> MLB auction -> scout reveal -> farm auction -> staff hire -> franchise setup`. This is navigation/order only: ScoutHire stays deep-linkable at `/league-builder/scout-hire`, M1D scout derivation/content stays untouched, Draft Setup starts the MLB auction directly, MLB auction completion proceeds to ScoutHire, and ScoutHire confirm proceeds to the farm auction with back returning to the MLB auction. Gates: tsc pass; build pass; focused draft-routing/page-nav/farm-scout suites 127/127; full suite had the known `LeagueBuilderDraftSetup.test.tsx` CUT2-2 batch red, and the solo file rerun passed 60/60.
1o. **WT-A AUCTION SCOUT-REPORT CLICK-TOGGLE + HONEST PASS COPY LANDED (2026-07-08, merge `c7d4688e`, lane `b051af24`):** JK's post-walkthrough morning wave. The `AuctionStage` scout-report cover control (shared by both MLB and farm tiers post-M1K) is now a real `onClick` toggle — supersedes the prior `onPointerDown`/`onPointerUp` press-and-hold. Passed lots gain an honest `UNSOLD` overlay for first-pass-recycled players under reserve pricing, distinct from the terminal `GONE`; farm floor recycling stays off. Auction spec docs synced in-lane (`AUCTION_DRAFT_SPEC_V2.md` §2.2 dated supersession note; long-press wording fixed in 4 other docs). Audit: APPROVE-WITH-NOTES (test-hardening follow-ups only, non-blocking).
1p. **WT-C RULES PAGE DELETED + CUSTOM SEASON/INNINGS ENTRY LANDED (2026-07-08, merge `a2a66956`, lane `892f53ae`; cleanup `b532e3a7`):** the dead `/league-builder/rules` route/component/nav-card is deleted entirely (JK: "remove dead page entirely and simplify customizable, wired setup"). FranchiseSetup Season step gained free-entry custom games-per-team (8-200, spec C-071 bound) and innings-per-game (3-9) beside the presets; the extra-innings rule control was restyled radio-dots → box-buttons (JK's walkthrough missed the old control — it lacked the step's usual selected/unselected contrast). Audit-verified: no live schedule generator exists in v1 (`franchiseInitializer` hardcodes `generatedSchedulesAllowed:false`), so games-per-team is safe season-length metadata at any bound value. `b532e3a7` swept stale `/league-builder/rules` rows from two journey fixtures + the button-audit fixture (audit note, not a defect). Audit: APPROVE-WITH-NOTES.
1q. **WT-B PERSONALITY TAXONOMY + WEIGHTED DRAW LANDED (2026-07-08, merge `a4de48c7`, lane `bcc47014`):** JK-ruled weighted personality draw (Droopy 0.10 / Timid 0.10 / other five 0.16 each) at both live assignment sites (prospect generation + league-pool axis regen), seeded distribution tests lock it (N=600: Droopy 9.17%, Timid 9.83%, both below every other personality). Canonical-7 taxonomy fixed: `Builder.tsx` missing "Competitive" restored; `leagueBuilderStorage`'s `Personality` union narrowed 11→7 with `normalizeStoredPersonality()` reusing the `masterMoraleMatrix` reconciliation table; edit forms (`LeagueBuilderPlayers`, `franchisePlayerProfileEdit`, `TeamHubContent`) normalize on load; dark offseason `DraftFlow.tsx` no longer hardcodes personality/chemistry — real seeded draws, display==persisted. Audit: APPROVE-WITH-NOTES (weighted-draw math re-derived clean; golden-hash re-pin verified non-masking; DraftFlow's pre-existing `Math.random` display-name gen stays dark, a separate issue).
1r. **DRAFT COCKPIT DESIGN AUTHORED (2026-07-08, commit `6cb97e0b`) THEN RATIFIED SAME SESSION (commit `ecce2d9c`):** `spec-docs/DRAFT_COCKPIT_DESIGN_2026-07-08.md` — Fable's 3-tier Assistant-GM redesign (THE CALL / THE READ / THE BOARD), grounded in a 3-tracer sweep (B3 live board + B4 posture dial from `ASST_GM_DRAFT_INTELLIGENCE_SPEC_2026-07-04` never dispatched; drag-to-rank alive at setup but never carried to the live floor; luxury tax computed but never rendered; `nominationOdds`/`gradeBandPrice` built with zero callers; BALANCE light a permanent stub; farm whisper mostly hollow). JK ratified ("good ideas on the draft cockpit: ratify") and resolved all 3 forks (§4): BALANCE-light removal RESOLVED YES, posture-dial timing RESOLVED PARK until Wave 2 is felt, farm chem chip build-dark RESOLVED YES. Ratification added a farm-bridge directive (§2.5 — the farm Asst GM must reason about MLB-roster coverage/flexibility, not mimic the MLB floor) and an anti-generic design principle (8 — nothing that reads identically for every team belongs above the fold). This is now the plan of record for finishing S5 below — see the reconciliation note there. WT-D (clickable profile popovers, extends WT-A) dispatched same session, in flight, not yet merged. Companion follow-up `22f3fe5b` (WT-B lane): a full merged-tree suite run caught a second stale personality golden (`prospectChemistryRebalance.test.ts`) that WT-B's own focused gates missed — re-pinned same session; a new SESSION_RULES lesson requires a cross-directory golden-fixture grep on any future seeded-draw change.
1s. **WT-D LANDED (2026-07-08, merge `b4936b8d`, lane `f4466cd2`):** the popover lane from item 1r merged. Clickable won-player/overflow/farm-lot profile popovers on both auction floors, extending WT-A's click-toggle pattern. Audit: APPROVE-WITH-NOTES (privacy attack refuted — reveal state is a required literal `'hidden'`, the band branch structurally cannot render true ratings/trait names; revealFull defense-in-depth + a card-vs-popover chip asymmetry queued for W1d, item 1u below).
1t. **UNIVERSE + UNIVERSE-FIX1 LANDED (2026-07-08):** `0537bcf9` (lane `788cce31` + captain-ruled default rework `242d0ffa`) — draft-available player universe: per-league source checkboxes in Draft Setup (own league un-checkable, all leagues listed with counts, `sourceLeagueIds` persisted on the league record). Captain rework after audit Finding 1: an absent field now resolves to unfiltered (`null`, byte-identical to pre-feature behavior via same-array-reference proof), not own-league-only — the earlier default would have silently excluded other leagues' seed players from a new league's first pool extraction. Audit: APPROVE (delta re-verified; empty-assignments exploit refuted; cut players re-enter free-agent supply). `aaf5e0ff` (lane `507721ed`) — a JK browser finding that archetype auto-fit still pulled from the whole player DB fixed across three automatic paths (RosterDesigner candidate feed via new `candidatePlayers` prop; feasibility/draftability/reroll consumers; design-first identity-critical-id extraction); manual pins/hand-adds stay deliberately unrestricted with "LEFT THE POOL" surfacing. Audit: APPROVE-WITH-NOTES (independent consumer sweep found zero remaining automatic full-set paths).
1u. **COCKPIT WAVE 1 (W1a/b + W1d) LANDED (2026-07-08) — WAVE 1 NOW COMPLETE:** `a66d00ed` (lane `7df3e08d` + captain-ruled grade-chip rework `427d2e2c`) — MLB Tier-1 verdict strip (VERDICT word, YOUR NUMBER, TRUE COST after tax via `auctionMarginalTax` — closes the tax-wiring half of RB-3) + Tier-2 promoted reads (bid-vs-pass permanent, `nominationOdds` WAIT/CHASE chip, a grade-sanity chip reading the grade's own `GRADE_SALARY_BOUNDS` floor/ceiling directly, replacing a deleted arbitrary ±1-step window), lights→icons, the permanent BALANCE stub deleted, within the design doc's ≤60-word verdict-strip budget. Audit: APPROVE-WITH-NOTES (grade rework verified table-verbatim; notes: live tax/odds values need a JK browser confirm; the farm BALANCE stub icon's disappearance via the shared `LIGHT_ORDER` constant is intentional, formalized in W1d below; a latent divergence between `auctionMarginalTax`'s global cap tables and `projectedTax`'s pool-specific caps is ticketed, fires only if a league ever customizes caps). `28a4ec9e` (lane `e32b42d6` + audit-driven rework `838cbd2a`; fog-math correction doc `77d4feb5`) — the farm bridge: MLB-need-driven farm valuation via a new `depthAwareNeedNudge` (`rosterNeed.ts` `DEPTH_NEED_NUDGE`: covered 0.92 / adequate 1.00 / thin 1.12; a bullpen/closer-short roster separately hits the existing 1.35 need-multiplier ceiling), an honest farm whisper (BUDGET+SHAPE only, SHAPE un-stubbed against the real MLB roster), a real fogged farm board, a team-conditioned bridge headline (seat-guarded). Farm chemistry-fit stays wired-but-inert behind `FARM_CHEM_FIT_ENABLED=false` per the ratified build-dark-first fork. **Audit story:** initial REJECT — the farm board ranked/displayed the scout band's raw midpoint, which algebraically equals the true opening ask, leaking the fog (new general rule recorded in `77d4feb5`: no derived quantity on a fogged surface may be an exact deterministic function of a true-value anchor); reworked to a seeded `displayedEstimate` with a regression lock catching both a midpoint AND a raw-ask revert. Delta audit: APPROVE-WITH-NOTES (a near-zero-jitter seed can still money-round a single row near its true reserve — a model characteristic, ordering stays fogged; a jitter floor is an optional model-wide follow-up). B3/B4 status and the Wave-1-complete framing are reconciled into S5 below.
1v. **DRAFT SKIN STANDARD AUTHORED + FORKS RESOLVED (2026-07-08, commits `a8d7f794` + `f4ea3ffa`):** `spec-docs/DRAFT_SKIN_STANDARD_2026-07-08.md` — one premium-retro skin standard A-to-Z for the draft journey (ballpark-kit token canon, component recipes, a ruled gold-CTA spec = the `EndOfDraftStaffing` treatment, a conformance/sweep-scope map, guardrails). JK resolved the doc's two forks: (1) **ONE LANGUAGE EVERYWHERE** — the live bidding stage (`AuctionStage`/`WhisperPanel`/both floors) converts to the same hard-edge standard as the rest of the journey, retiring the soft-premium `--auc-*` visual family for the draft journey (layout/class-names/test-hooks are retained — treatments change, not DOM); (2) the `FranchiseSetup` wizard is held OUT of this sweep, deferred to a separate franchise-wide pass. Per the doc's own §6 sequencing, the reskin lane dispatches AFTER Wave 2 ("THE BOARD") lands, since they touch the same files; Wave 2 builds its new UI to the skin standard from birth.
1w. **COCKPIT WAVE 2 "THE BOARD" LANDED (2026-07-08, merge `c53478e8`, lane `5312c37d` + audit-rework `8cf4e4b4`):** the B3 remainder of the Assistant-GM S5 stage — a shared `RankReorderList` component extracted from `RosterDesigner`'s proven drag+arrow mechanics (byte-identical old surface, 20/20 tests unchanged); `Team.boardRankOverrides` persisted as a new sibling field to `rosterDesign` (NO DB version bump — `leagueBuilderStorage`'s `DB_VERSION` is unpinned by any test, verified by grep); a setup "rank your board" tab (global + per-position 5-deep, GM-sortable, pool-scoped) in Draft Setup's Zone-3 club editor; `assembleBoard` gained an optional `rankOverrides` blend porting `best22Target`'s own `gmPreferenceWeight` constant (2.5, read-only, that file untouched) — sort order only, the displayed worth number never changes (one-ceiling preserved); the live Tier-3 whisper board is now the full sortable global/per-position view with write-back to the same store. This closes the ratified `ASST_GM_DRAFT_INTELLIGENCE_SPEC_2026-07-04.md`'s B3 + Corrections 5/7. **Audit-driven rework** (`8cf4e4b4`, initial APPROVE-WITH-NOTES → delta APPROVE): the auto-advance line originally fired on any latest result, wrongly announcing a promotion when a first-pass player merely recycled back into the pool under reserve pricing; fixed to gate on `latestResultDisposition === 'SOLD'` only, plus a new "On the block now" copy variant when the promoted target is the current lot. Gates: tsc clean; build `✓ 11.07s`; focused suites green throughout (RankReorderList 10/10, RosterDesigner 20/20, WhisperPanel 35/35, LeagueBuilderAuctionDraft 20/20 + computeBoardAutoAdvanceLine 16/16, LeagueBuilderFarmAuctionDraft 2/2 untouched, rosterIntelligencePayload 38/38, best22Target 18/18 untouched, LeagueBuilderDraftSetup.RankYourBoardZone 7/7 + the full file solo 71/71). No through-the-UI integration test was added for the live board's write-back (flagged by the builder; covered instead by WhisperPanel component tests + the pure-function tests + the unchanged regression suite); live browser verification was not performed this lane (dev-server port occupied).
1x. **RESKIN SWEEP LANDED (2026-07-08, 4 commits `aa85dff9`/`da2472c9`/`7107add4`/`d8fc9561`, merge `3266207c`):** one premium-retro hard-edge skin across the entire draft journey per the ratified `DRAFT_SKIN_STANDARD_2026-07-08.md`. Canon-first: 6 new `ballpark-kit.css` tokens + the ruled gold-CTA spec unified onto `.ballpark-press-gold`; `EndOfDraftStaffing`/`ArchetypePicker` repointed off stale pre-flip bare hex (snapshot regenerated, diff-verified hex→token only). Chrome sweep: `LeagueBuilderDraftSetup`/`RosterDesigner`/`LeagueBuilderAuctionDraft` literal debt tokenized, incl. the undocumented `#3B7DD8` HANDOFF banner → action-green + brass border (the unrelated POS-badge blue on the live board and the Draft Setup arsenal-toggle/Save-button/Available-Players-accent blues were explicitly left untouched, routed to JK's eye). **THE STAGE CONVERSION** (JK's one-language ruling, executed): `auction-theme.css` + `WhisperPanel.tsx` convert from soft-premium to hard-edge — zero border-radius, 2-4px borders (row/section dividers stay 1px), hard offset shadows, decorative gradients flattened (functional data-viz gradients and the team-color swatch deliberately kept), the BID button now matches the ruled gold-CTA spec exactly; verified live via a screenshot of `/__preview/auction-stage`. Cleanup: the orphaned duplicate Ballpark component kit deleted after a zero-import verification (the live `BallparkKit.tsx` barrel untouched). Audit: APPROVE-WITH-NOTES (treatments-only mechanically proven — no DOM restructuring, no copy changes; notes = the pre-existing Draft Setup blue accents + token-hygiene debt, routed to JK's eye). **Consolidated JK browser checklist written:** `spec-docs/JK_BROWSER_CHECKLIST_2026-07-08.md`, folding every accumulated JK-eye item from Wave 1 + Wave 2 + the reskin into one pass per the Browser-verification-backlog ruling.
1y. **NOW/ FOLDER CREATED (2026-07-08, commit `08363276`):** a single real-time entry-point folder — `NOW/README.md` indexes and live-links every canonical doc (`CURRENT_STATE.md`, `CONTINUITY_CHECKPOINT.md`, this doc, `V1_CANON_2026-07-07.md`, `JK_BROWSER_CHECKLIST_2026-07-08.md`, `MODE1_PUNCHLIST_2026-07-08.md`, `SESSION_RULES.md`, `PATHWAY_TO_V1_2026-07-07.md`) plus the binding design standards (`DRAFT_COCKPIT_DESIGN_2026-07-08.md`, `DRAFT_SKIN_STANDARD_2026-07-08.md`, `UI_TRUTH_MAP.md`) and a pointer to the Historical Legends side-project's own repo. A sibling folder exists in the legends backlog repo. **Standing practice from this commit forward: a scribe verifies the `NOW/README.md` index (links resolve, read-order still current) on every booking pass.**
1z. **BOARDFIX1 MERGED (2026-07-08, commit `dfbd31f4`, lane `ba23dc97`):** design-first ranking surfaces were reading the wrong pool copy — fixed to read the EXTRACTED pool (a locked→extracted predicate fix, repro-proven by reverting it and watching accepted tests fail); the START THE DRAFT gate was independently re-verified as NOT loosened (a display bug, not a gate bug, explained the "stuck" feel some testers reported); rank-badge typed edits gain type-to-move plus a send-to-top action on every ranking surface (setup global/per-position, live board). Audit: APPROVE-WITH-NOTES (live-floor rank controls still unstyled pending the next CSS pass — closed by TEXTLAW-SWEEP below; LOCK POOL button discoverability flagged for JK's eye).
2a. **BOARDFIX2 MERGED (2026-07-08, commit `fb3c9fd9`, lane `ffa7fe46`) — JK's third-walkthrough findings:** (a) a new always-visible readiness panel (`data-testid="draft-readiness-panel"`) on the draft-setup start screen enumerates every unmet LOCK POOL / START THE DRAFT condition in plain language (gates re-verified byte-identical against the pre-existing logic, not loosened); (b) rank-badge typed edits now land at the LITERAL displayed position on every board surface — setup global, per-position, and the live draft-room board (the prior blend treated an explicit typed rank as a soft nudge rather than a placement; regression proven by reverting the fix and watching 3 tests fail); (c) board reorders are now instant on-screen with a debounced single-save persistence path, flushing on unmount/tab-hide (spy-proven: 5 rapid moves collapse to 1 save call). Audit: APPROVE-WITH-NOTES (note 1: a sub-500ms cross-club rank edit could drop the outgoing club's pending save — setup screen only, fixed in the very next lane, TEXTLAW-SWEEP Item C below; note 2: a benign duplicate-save console warning on a mid-flush unmount).
2b. **TEXTLAW-SWEEP MERGED (2026-07-08, commit `39660e86`, lane commits `72a3a9b2` Item A / `712126b2` Item B / `93317821` Item C — JK ruling: tutorial text lives behind Help):** executes the §7 ratified Text Law classification (design doc, commits `eb9de7c6`/`2475b329`) verbatim across the draft journey. Item A: tutorial/instructional strings on `LeagueBuilderDraftSetup`, `ArchetypePicker` (parent-wired), `AuctionStage`, and `EndOfDraftStaffing` now gate behind a top-right Help toggle — `EndOfDraftStaffing` gained its own Help button where none existed before (verified: a `HelpCircle` import + new `showHelp` state; the old always-on instruction banner now renders only when Help is open). Two SPLIT cases were carved out per the ratified rule (a dynamic number stays always-visible; the surrounding static lecture gates): the Cap-Fit readout and a stray-notice banner. A REVERSE FIX ran the other direction: the auction phase-label pill, previously hideable, is now always visible (matching the ratified classification that phase state is always-valuable, not tutorial). Item B: `whisper-board-*` rank controls (the ones BOARDFIX1/2 left unstyled) are now styled hard-edge using existing ballpark CSS variables only — no new tokens. Item C: the BOARDFIX2 audit note-1 race (a sub-500ms cross-club rank edit dropping the outgoing club's pending save) is fixed repro-first — the auditor re-proved the bug by reverting the fix and watching it reproduce, then re-verified the fix closes it. Audit: APPROVE-WITH-NOTES (N1: `LeagueBuilderDraftSetup` solo suite carries a pre-existing intermittent async flake on the "locked pool falls short of the required floor" test, unrelated to this sweep — re-run solo if red; N3: the scout price-range bar shows no eyebrow label when Help is closed — per the ratified classification this is JK's-eye-gates, not a defect).
- **S5/S6 reconciliation note (2026-07-08, scribe pass):** BOARDFIX1, BOARDFIX2, and TEXTLAW-SWEEP are post-wave-closure polish/bugfix passes riding on top of the completed Wave 2 board and reskin (items 1w/1x above) — they fix real board bugs JK's continued walkthrough surfaced (design-first pool source, rank-typed-edit placement, a self-explaining readiness panel, debounced persistence) and execute the ratified Text Law (tutorial copy behind Help). None of them reopen Wave 1/Wave 2/reskin scope or change S5's BUILT-AND-WIRED status. JK's consolidated browser checklist (`JK_BROWSER_CHECKLIST_2026-07-08.md`, refreshed this pass) remains the standing gate before P9.
2c. **COCKPIT WIRING AUDIT + LIVE-CALL DESIGN AMENDMENT LANDED (2026-07-08→09, PR #26, merge `4d33085c`, docs-only):** JK's walkthrough caught the Asst GM verdict strip frozen mid-lot (bid moved past YOUR NUMBER, strip still said PUSH). A full spec-to-wiring sweep (5 dimension auditors + a dedicated verdict-plumbing tracer, then independent adversarial verification of every claim) found the root cause: the verdict FORMULA never consumed the live bid at all — two separate code paths (the strip word vs. the live BID/PASS guidance) did their own independent live comparisons, so the panel could contradict itself. Result: **10 CONFIRMED gaps / 5 downgraded / 6 REFUTED / 27 verified WIRED-OK**. Landed as `spec-docs/COCKPIT_WIRING_AUDIT_2026-07-08.md` plus two BINDING design amendments to `DRAFT_COCKPIT_DESIGN_2026-07-08.md`: **§2.6 THE LIVE CALL** (strip/headline/fine-print become one bid-aware ladder computed in the shared engine — lead/push/stretch/out, single source, can never contradict itself again) and **§2.7 reason priority + payload hygiene** (the Tier-1 reason chip becomes RULED-priority not alphabetical; dead payload fields dropped). One gap (luxury tax display-only, never draining budget) was flagged as a JK product-economics fork, not auto-built — see TAXTEETH below.
2d. **CALLFIX LANDED (2026-07-08→09, PR #27, merge `88c34d30`):** builds the §2.6/§2.7 amendments plus 4 more audited cockpit gaps. THE LIVE CALL ladder is live on both floors (fog-safe on farm, the F9 one-ceiling rule test-locked into the ladder's own thresholds); the reason chip is priority-ranked (all 12 codes ordered, test-locked); lot-log names are tappable (closes the 4th ratified popover surface, farm fog-gated); auto-advance's "Next up" line now reads the same live rank-edit overlay the board itself renders (was up to ~500ms stale after a live re-rank); payload hygiene removed 2 dead fields, deleted a dead per-lot tax compute (proven equivalent to the correct marginal-tax path), surfaced the replacement-value estimate in the scarcity chip's tap-through, and single-sourced the stage market banner with the whisper's own per-seat read. **Adversarial audit: APPROVE-WITH-NOTES** (auditor independently re-ran every gate and reproduced the pre-fix bug from a rebuilt pre-fix worktree; notes ask for an eyeball pass on two hard-to-script live-timing states during JK's walk — the fast rank-edit-then-sale case and the new "you're on top, sit tight" state). Gates: tsc clean, build exit 0, 8 focused suites 168/168 (auditor re-run). Contract: `spec-docs/contracts/CONTRACT_CALLFIX_2026-07-08.md`.
2e. **AUCTION FLOOR REFIT RULING LANDED (2026-07-09, PR #28, merge `50da7ad4`, docs-only):** JK's floor-usability directive ("whisper panel embedded in its own window that needs to be scrolled … plenty of space underneath"; "unclear which team is up to bid … doesn't effectively use teams' colors"; "only what is usable and actionable by the GMs should be on the screen, everything else is noise") is now a ratified captain design doc, `spec-docs/AUCTION_FLOOR_REFIT_2026-07-09.md` — table, not a dashboard. Four laws: one scroll context (both whisper height cages deleted — no nested scrollbars); say it once (the duplicated seat-strip band collapses into the whisper's own tiers); color means exactly one thing (a full-width ON THE CLOCK banner in the acting team's own colors, brass-on-ink fallback if a club's colors are unpopulated); left column = the public table + my actions, right column = my private advisor. The roster-fill board moves into the left column's reclaimed dead space. Build lane dispatches AFTER TAXTEETH merges (same floor-page files) — see FLOORREFIT (in-flight) below.
2f. **TAXTEETH LANDED (2026-07-08→09, PR #29, merge `c0a24363`):** JK ruling — the luxury tax gets teeth. Settlement now drains real budget: salary **+** the marginal tax of the player just won; the live bid ceiling reserves that same tax (a bid you can't afford to settle can no longer be placed); CPU/shill bidders inherit the same ceiling automatically (no separate CPU math to keep in sync); the whisper's TRUE COST line now equals the actual budget drain, proven end-to-end by test. **Adversarial audit: APPROVE-WITH-NOTES** — the highest-risk piece (this partially reverses a JK-ruled fix from 6 days earlier that had deliberately stripped tax OUT of the bid ceiling because the old shape froze bidding late-draft) was independently re-derived: the new mechanism charges only the ONE-TIME marginal tax of the current player (paid once, then gone), not the old snowballing whole-roster hypothetical, and the auditor drove a real late-draft taxed-team scenario to full roster completion to prove no freeze-out reappears. Stale mid-draft saves self-heal on load (poison-state test included); under-threshold teams are byte-identical to pre-TAXTEETH. **Foundations check (JK's own question — does the tax respect each archetype's exact boost/neutral/nerf cap shifts): PARTIAL at merge time**, reconciled in a parallel lane — the tax computation chain was proven live against archetype cap shifts, but the sweep also surfaced that the legacy Teams-page editor could silently clobber a team's archetype tax identity on ANY save, even a name/color edit (this became the TEAMIDGUARD lane, landed same wave, below) and that the tax engine itself was reading a COARSE 2-modifier approximation of a team's archetype shifts rather than the exact percentages (this became the TAXPRECISION lane — see in-flight below; captain ruled EXACT wins, coarse table is a fallback only). **Two forced end-of-draft completion paths (pool-exhaustion backfill, settle-short-clubs) charge cost UNTAXED — ruled unexploitable for v1 (reserve-priced leftovers only, no competitive lane) and ticketed, not blocking.** Gates: tsc clean, build exit 0, 204/204 focused tests (auditor re-run). Contract: `spec-docs/contracts/CONTRACT_TAXTEETH_2026-07-08.md`.
2g. **TEAMIDGUARD LANDED (2026-07-09, PR #30, merge `677a7607`):** closes the Teams-page clobber TAXTEETH's audit surfaced. Saving a team on the legacy Teams page — even a name/color-only edit — no longer rebuilds an archetype team's luxury-tax identity out from under it. An archetype-assigned team's identity section is now READ-ONLY on the Teams page (shows the exact stored cap shifts plus a "set by archetype" note); non-archetype teams keep the legacy editor, now behind a dirty-guard, and the precise `rawShift` percentages now survive a load→save round trip instead of being silently coarsened. **Adversarial audit: APPROVE-WITH-NOTES** — the repro was independently re-proven (5 tests fail on reverted source, 32/32 green after the fix), the merge onto current main was verified conflict-free with zero file overlap against the tax lane, and the farm identity editor was confirmed to write a wholly separate advisory-only field (cannot clobber MLB tax identity — ruled a legitimate scope cut, its own rebuild ticketed as a follow-up). **Audit note-1 (pre-existing, not this diff — becomes the TAXPRECISION lane's charter):** the auction tax engine reads the COARSE copy of an archetype's cap shifts (a 2-modifier cap — a 3-boost archetype's third boost is silently dropped) while the snake-draft path and the on-screen display both already use the exact `rawShift` percentages. Captain ruled: exact wins everywhere; TAXPRECISION dispatched separately to close the gap in the auction engine only. Gates: tsc clean, build exit 0, 32/32 + 209/209 leagueBuilder sweep (auditor re-run). Contract: `spec-docs/contracts/CONTRACT_TEAMIDGUARD_2026-07-09.md`.
2h. **SETUP → DRAFT INTEGRITY SWEEP RECORDED (2026-07-09):** a JK-ordered follow-up sweep — 5 dimension sweepers (money / basis-staleness / teams-lifecycle / player-edits / save-shapes) + adversarial verification of every claim, 39 claims total → **8 CONFIRMED / 10 downgraded / 4 refuted / 17 SAFE**. Headline: nothing confirmed lets a locked pool silently mis-price or mis-legalize a normal lock→start draft; the real gaps are pool-first mode missing the same staleness net design-first already has (2 items), and two Duplicate-League/team-lifecycle bugs (a stranded fix that needs re-landing, plus a farm-session-blind mutation guard). Full record: `spec-docs/SETUP_DRAFT_INTEGRITY_AUDIT_2026-07-09.md` (raw evidence: `spec-docs/data/integrity-sweep-2026-07-09-raw.json`). **In-flight work opened by this wave** (all worktrees, none merged as of this pass): **TAXPRECISION** (auction tax reads the archetype's exact `rawShift`, not the coarse table — build+gates complete, awaiting audit) and **FLOORREFIT** (the §2e ruling above, built — Moves 1/4/5/6 + Move 2 uncage, gates green at 129/129 up from a 99/99 baseline, awaiting audit) are both build-complete pending an independent adversarial audit and a PR; **STALEPARITY** (closes the integrity sweep's SB-2/SB-4/MONEY-1 pool-first staleness gap) has its contract landed but no build yet. **Queued behind those:** COPYFIX (SEATS-02/SEATS-06/F5/SEATS-04 — the Duplicate-League + farm-guard fixes, queued behind STALEPARITY, same storage-layer file surface) and GAUNTLET (re-proves draft-completion-under-real-tax now that TAXTEETH is live, queued behind TAXPRECISION, same engine-test surface — not to be confused with the Mode-1 punch-list's own "gauntlet" validation grid, a different exercise from 2026-07-08).
5. **Plan of record for execution:** `spec-docs/PATHWAY_TO_V1_2026-07-07.md` (incl. PART 8 runbook: owners, gates, agent stand-down). Draft-economy thread governed by `DRAFT_ECONOMY_RESET_2026-07-05.md` (now committed; was stranded untracked). Score/Skip + score-only verified present [git: ScheduleContent.tsx:94-111]; trunk simulate code is hardcoded OFF [git: FranchiseHome.tsx:181] — dead code for the cut-over DELETE list.

---

## §1. CANONICAL SPEC CLUSTER (read these; everything else is history)

- **`V1_PLAN_2026-06-30.md`** — the operative critical path.
- **`V1_HANDOFF_2026-06-30_DRAFT_AND_LIVING_SEASON.md`** — the vision (§3.1 = the setup RE-DESIGN; the 24-archetype lock).
- **`SCOUTING_INTELLIGENCE_SPEC.md`** — the scout / Assistant-GM / auction-intelligence vision (v1 blocker). Supersedes `_V2` (draft) + `DRAFT_GUIDE_INTELLIGENCE_SPEC`.
- **`TEAM_ARCHETYPES_24.md`** — the 24 locked identities.
- **`IN_SEASON_CAP_DEADCAP_ANALYSIS_2026-06-30.md`** — draft economy (Option-A wrong-fit penalty; dead-cap on intrinsic).
- **`ASSEMBLY_PLAN_2026-07-01.md`** — the branch-merge runbook (git-confirmed).
- **`AUDIT_PREDRAFT_TO_SEASON_2026-07-01.md`** + the **FABLE-C1…C5 contracts** in `PROMPT_CONTRACTS.md` — today's decomposition of the draft-depth work.
- Living season: **`FRANCHISE_V1_LIVING_SEASON_SPEC.md`**, **`MODE_2_V1_FINAL.md`**.

---

## §2. THE GROUND TRUTH — branch/tree reality (this is the core drift)

**Nothing draft/hub is on trunk yet. "Built" usually means "built on a branch, not on trunk."** Assembly is the gate for everything UI.

| Tree / branch | Ahead of trunk | Holds | State |
|---|---|---|---|
| **trunk** `experiment/manager-wpa-window` (`/kbl-tracker`) | — | engines, soul layer, season/stats/WAR, **freeze backend**, season-finale + L-SIM | the base |
| `claude/v1-draft-ui` (`/kbl-draftlane`) | **+27** | all draft/auction/setup/scout UI + draft engines | **UNMERGED** |
| `claude/lineups-fenway-hub` (`/kbl-lineups-fenway`) | **+64** | the polished franchise-lens hub + finale/awards ceremony UI | **UNMERGED** |
| `claude/v1-soul-gaps` | 0 | (already in trunk) | drop |
| `claude/v1-playoff-driver` | +2 | headless playoff engine | park (playoffs deferred) |
| `codex/draft-setup-ui`, `codex/auction-draft-ux-rehaul`, `codex/mode1-v1-b`, `codex/draft-pipeline-fix` | contained/0 | ancestors/subsumed | drop (per ASSEMBLY_PLAN) |

Merge facts (git-confirmed 2026-07-01): draft-ui → trunk = **1 conflict** (`PROMPT_CONTRACTS.md`, base-aware union); hub → trunk = **0 conflicts**; draft-ui ↔ hub overlap = 3 files (union `main.tsx`; keep draft-ui's `AuctionStage.tsx` + `auction-theme.css`). `main` is 1108 behind / 0 ahead of trunk (clean FF later).

---

## §3. A-to-Z STATUS (13 stages)

Legend: **DONE** / **PARTIAL** / **NOT-BUILT**. Outstanding units tagged **[BE]** (backend) / **[UI]**.

### MODE-1 — setup → draft → freeze

**S1. League Build** (League Builder: leagues/teams/players/pool/import) — *on draft lane*
- BE **DONE** (CRUD + SML/MLB import + roster/tier cap). UI **DONE** (hub + all CRUD screens).
- Conference editor **DONE for v1** (M1N): League Builder create/edit can create/rename conferences and assign every team exactly once, persisting through existing `conferences` + hidden one-division-per-conference `divisions`. **No division editor**.

**S2. Setup RE-DESIGN** (per-(league,team) shadow store + identity-bundle team-edit page + durable seats) — *the 2026-06-30 headline foundation*
- BE **NOT-BUILT** (no per-league override store; identity still on global team; seat spine partial/dark). UI **NOT-BUILT** (no identity-bundle editor; old scout-hire/staff-hire screens still live).
- Outstanding: [BE] per-(league,team) shadow-override store (identity ONLY, never roster/capIdentity) + register in backup/sync/L-SIM/save-slot; [BE] shadow-resolution read path wired to consumers; [BE] durable seat-name writer (the deferred "ticket #11"); [UI] the identity-bundle editor (GM/Asst-GM/Scout/Manager+style/Beat-reporter + MLB/Farm archetype dropdowns + draft board); [UI] relocate staffing into the team page + delete the old scout-hire/staff-hire screens; [UI] convert draft-setup entry from the `/__preview` mock hub to a real page that READS the pre-config.

**S3. Team Archetypes (24) + picker**
- BE **DONE** on trunk (24 locked, cap-shift math live). UI **PARTIAL** — the picker is wired on the draft lane but shows only **15** (its catalog is stale; the 24 never merged to the draft tree).
- Outstanding: [BE] port the 24-archetype module (trunk `efc7cfb6`) onto the draft lane → picker auto-shows 24; [UI] fix "15"→"24" copy; [BE] **reconcile the two archetype cap-vocabularies** (HISTORICAL_ARCHETYPES vs auction capIdentity/CAP_MODIFICATION_FRACTIONS — the V1_PLAN "7-pre" prerequisite); [UI] decide numeric trade-off readout vs plain chips (beware the value-inversion trap — don't bar-length by raw rating points).

**S4. The SCOUT** (farm prospect evaluation; 3/5/7 archetype-derived confidence bands)
- BE **PARTIAL** — farm archetype-derived 3/5/7 per-area bands are built, skipped ScoutHire no longer kills farm reads, generated pitcher arm slots now feed the Farm IV arm-slot layer, and the amended s8.4 overall band follows the prospect's primary rated area instead of the dead constant-5 mean. UI **PARTIAL** — farm auction renders banded reads and the ruled lot-card trait count, but the broader scout naming/farm-archetype placement work still belongs on the team/setup page.
- Outstanding: [BE] enforce/verify "wider farm bands than MLB" as a gauntlet invariant if still required beyond the 3/5/7 table; [UI] move scout naming + farm-archetype pick onto the team page; [UI] add the cap-space risk-discipline headline.

**S5. The ASSISTANT GM intelligence** (the v1 HEADLINE — largely UNBUILT) — *= FABLE-C1/C2/C3*
- BE **PARTIAL→mostly NOT-BUILT** — reusable primitives (buildBestRoster, poolFeasibility, evaluateCpuValuation, auctionMaxBid, selectNextNominee) exist, but the actual intelligence is not built. UI **PARTIAL** — a `DraftGuideCard` placeholder (heuristic thresholds, ±spread "price range", naive position-count fit; bargain/trap flag never populated).
- Outstanding: [BE] the strategy-first optimizer (per-position priorities → best build path) + the Conservative/Optimal/Aggressive posture dial (**FABLE-C1**); [BE] the Second-Price market model (clearing ≈ runner-up + increment; own_need × scarcity; low/median/high band that widens for shills/early/thin) + type EstimatedMarket/CompetingTeamProfile/ShillProfile (**FABLE-C2**); [BE] the surplus/bargain signal; [BE] **THE KILLER FEATURE** — deterministic bid-vs-pass board re-projection; [BE] the accurate completion-floor (strip projectedTax); [BE] bid-log infra (Lot.bidLog + AuctionResult.{bidderSet,underbidder,numBidders}); [BE] nomination-timing overspend-vs-wait; [BE] `auctionTuningSim` calibration test (~85-90% band coverage — the pre-ship gate, absent today); [UI] the ONE live board + posture dial; [UI] the per-position-priority input (ROBUST/SIMPLIFIED); [UI] the bid-vs-pass split view; [UI] replace the placeholder DraftGuideCard internals.
- **Blocked on:** the player-archetype taxonomy ("Move 2", undesigned) for the ROBUST dropdowns; and the per-league layer (S2) to hold the inputs.
- **RECONCILIATION (2026-07-08, scribe pass):** the "largely UNBUILT" framing above is stale on its own — §0 item 1 (this doc) already records the asst-GM **B-series (B1/B2/B5, bid-vs-pass board wired)** landed on trunk 2026-07-02→04. Corrected status: **B1 + B2 + B5 are BUILT AND WIRED** (roster-construction intelligence, second-price market model, bid-vs-pass board); **B3 (live board + auto-advance honoring rankOverrides) and B4 (posture dial) remain NOT-BUILT** — no contract, no commit (PROMPT_CONTRACTS ends at CODEX-B2B-RANK-DRAG-UI) — matching the Outstanding bullet above, which stands unchanged. `spec-docs/DRAFT_COCKPIT_DESIGN_2026-07-08.md` (Fable's 3-tier THE CALL/THE READ/THE BOARD redesign, §0 item 1r) is **RATIFIED (commit `ecce2d9c`)** and is now the PLAN OF RECORD for finishing this stage.
- **WAVE 1 COMPLETE (2026-07-08, scribe pass, §0 items 1s-1u):** all four cockpit sub-waves are merged — WT-D popovers (`b4936b8d`), W1a+W1b Tier-1/Tier-2 MLB reads incl. the TRUE COST tax line (`a66d00ed`), and W1d the MLB-need-driven farm bridge (`28a4ec9e`), each pre-merge adversarially audited (APPROVE-WITH-NOTES throughout; W1d's audit REJECTED once on a fog-leak before landing — see §0 item 1u). B3 (live board) remains the one open item, now scoped as Wave 2 "THE BOARD" (shared rank component, a setup RANK YOUR BOARD zone, a live Tier-3 sortable board + auto-advance) — reported in flight at this scribe pass but with no contract file or worktree found to confirm it independently yet. B4 (posture dial) stays PARKED per the ratified fork, to be felt after Wave 2. RB-3's tax-wiring half is now closed by W1a+W1b (`auctionMarginalTax` live in the strip); a latent cap-table divergence (`auctionMarginalTax` global tables vs `projectedTax` pool-specific caps) is ticketed, not blocking. The reskin sweep (`DRAFT_SKIN_STANDARD_2026-07-08.md`, §0 item 1v) is sequenced to dispatch AFTER Wave 2 lands, same files.
- **THE COCKPIT WAVE IS NOW COMPLETE (2026-07-08, scribe pass, §0 items 1w-1x):** Wave 2 "THE BOARD" landed (merge `c53478e8` + rework `8cf4e4b4`) closing B3 + Corrections 5/7 of the ratified `ASST_GM_DRAFT_INTELLIGENCE_SPEC_2026-07-04.md` — the shared rank component, the setup RANK YOUR BOARD zone, and a live sortable Tier-3 board with a SOLD-gated auto-advance line, all pre-merge adversarially audited (APPROVE-WITH-NOTES, delta APPROVE on the rework). The reskin sweep then landed (4 commits, merge `3266207c`) executing JK's one-language ruling — the live bidding stage converts from soft-premium to the same hard-edge treatment as the rest of the journey — plus a chrome sweep and orphan-kit cleanup (APPROVE-WITH-NOTES). **B4 (posture dial) stays PARKED** pending JK's in-browser feel of Wave 2, per the ratified fork; **the BALANCE light returns only after a future handedness-signal spec** (not yet authored); **P9 (the wrong-fit penalty) is next**, scoped to the economy lane, not the cockpit. The wave's accumulated JK-eye items are consolidated into one checklist: `spec-docs/JK_BROWSER_CHECKLIST_2026-07-08.md` — this is now the gate before further cockpit work.

**S6. The DRAFT EVENT** (live auction floor + farm + shills + mock + public/private)
- BE **PARTIAL** — engine-driven nomination + one-chance + shill engine + crash-safe per-pick persistence BUILT (on trunk); shill split-classifier BUILT. Gaps: dissolve-to-pool for a winning shill NOT wired (**FS-3 latent landmine**: shill count >0 can block the freeze — safe only because default=0); mock-draft toggle NOT built; second-price/bid-log/accurate-floor unbuilt. UI **PARTIAL** — the premium `AuctionStage` (MLB) is on the draft lane, **unmerged**; farm auction is a separate legacy layout (not on AuctionStage); no mock toggle; the Draft-Setup hub that captures GM/archetype/shills is a `/__preview` mock that persists nothing.
- Outstanding: [BE] dissolve-to-pool bridge (RB-10b) + FS-3 fix; [BE] mock-draft toggle (gate both durable writes); [BE] bid-log infra; [UI] merge AuctionStage to trunk; [UI] put the farm auction on AuctionStage; [UI] mock toggle control + badge; [UI] real persisting Draft-Setup page (replaces the preview mock).

**S7. DRAFT ECONOMY** (tiers → cap, luxury tax, wrong-fit penalty, freeze inputs)
- BE **PARTIAL** — tiers/per-team cap, archetype-shifted luxury tax, dead-cap engine (0.75 on intrinsic), and the 4-number freeze inputs all BUILT. **The Option-A wrong-fit penalty (visible draft-budget debit shown before the bid) is NOT built** — the off-fit cost is still the invisible projectedTax reserve. UI **PARTIAL** — tier selector + "most you can bid" exist; no visible "true cost = bid + penalty" line.
- Outstanding: [BE] the graduated wrong-fit penalty, actually debited + stored separately at sale; [UI] the visible "true cost" line before the bid; [BE] lock capIdentity for the auction's duration (anti-dodge); [BE] §16 tune the cap magnitudes + penalty curve.

**S8. FREEZE → season handoff** (4-number freeze) — **CORRECTION: this is on TRUNK, not just the lane**
- BE **DONE** on trunk (merged `e75836b0`; draftFreeze/draftMorale/draftFanMorale wired into franchiseInitializer; morale baselines + settled salaries + draft-baseline TV rows written; 14 Mode-2 consumers read them). UI **PARTIAL** — trigger exists; no post-freeze summary; the setup→auction UI that FEEDS the freeze is unmerged (on the lane).
- Outstanding: [UI] post-freeze summary (starting morale/fan-morale/salaries — silent today); [BE] auction-only guard/assert; [BE] confirm morale-store 4-registry coverage. (Mock toggle listed in S6.)

### MODE-2 — living season → end of regular season

**S9. Hub + season launch + schedule**
- BE **DONE** on trunk (season creation, manual/CSV schedule with no-simulate policy, standings recompute, next-game pull, offseason gate correctly flag-off). UI **DONE** on trunk (functional franchise hub) — the **polished** franchise-lens hub is unmerged on `lineups-fenway`.
- Outstanding: [UI] decide the canonical hub (merge the polished one vs keep trunk's) + canonicalize the duplicate lineups editor to one.

**S10. Season play + stats/standings/WAR** — the mature backbone
- BE **DONE** / UI **DONE** on trunk (GameTracker → processCompletedGame → season/career aggregation → bWAR/fWAR/rWAR/pWAR → standings → season-end finalize). Test-covered.
- Outstanding: [BE] minor hygiene (delete orphan `aggregateGameToCareer`; verify the dual-copy milestoneAggregator stays in sync); [BE] prove a full simulated regular season end-to-end (score-only games through the same WAR/awards path).

**S11. Soul layer + flags** (fame, player+fan morale, narrative/reporter, rivalry, checkpoint development, honors)
- BE **DONE, built-dark** on trunk behind **11 default-OFF Phase-2 flags** (MORALE, FAME, FLASHPOINT, CHECKPOINT, TRAITS, L10, L11, L12, L13, L14, STADIUM_RECORDS) — flippable only via test hooks today. UI **PARTIAL** — read-paths exist but show fallbacks (stores empty while flags OFF); missing surfaces listed below. **L14 rebrand has no caller** (unwired). morale→in-game-WAR is INTENTIONALLY deferred (morale drives development only — do NOT "fix").
- Outstanding: [BE] wire L14 rebrand trigger; [BE] a **production activation mechanism** for the 11 flags (test-only today); [BE] the checkpoint-development ADOPTION write path; [UI] morale ledger/history view, fan-morale dashboard + dampener readout, rivalry board, checkpoint confirm console, manager hot-seat/firing, L14 rebrand flow; [UI] wire the beat-reporter newsboard to render live season takes. (§16 tuning covers all soul constants.)

**S12. Finale + L-SIM hardening + ASSEMBLY**
- BE **DONE** on trunk (season-finale chain: freeze TV artifact → WAR awards (8 categories + MoY) → honors emission; fame reach-floor ratchet + snub morale **decoupled from the LLM nod** — fire regardless; **26 L-SIM soul invariants + a 39-case falsification harness** driving the REAL production chain). All L12-gated (the flag to flip). UI **PARTIAL** — awards-ceremony UI is spec-only on trunk; the real ceremony/newsboard lives on the unmerged hub branch.
- Outstanding: **[assembly]** commit ASSEMBLY_PLAN (untracked); merge draft-ui → trunk (1 conflict) then hub → trunk (0), branch-only + JK-gated; run L-SIM (60g leg LAST) for byte-identical proof. [BE] **the freeze→REAL-frozen-franchise L-SIM bridge** (the single riskiest line — unbuilt; depends on assembly + setup-spine). [BE] §16 tuning (~100 feel-numbers, all 0 today — the single largest remaining piece). [BE] saved-game migration check before the flag-flip. [BE] flip the 11 soul flags ON + full-season gate. [UI] flip the live `/franchise` route to the canonical hub. [UI] **JK browser sign-off** (sole acceptance gate). Open JK rulings: emission-snub live-green graduation; booger-glove fame direction; winner-honor prestige magnitudes.

---

## §4. THE CRITICAL PATH (ordered; V1_PLAN PHASE order)

1. **ASSEMBLY** (JK-gated, branch-only): commit ASSEMBLY_PLAN → merge draft-ui → merge hub → gate green. *Prerequisite for every UI item.*
2. **Archetype vocab reconciliation (7-pre)** + port the 24 to the draft tree.
3. **DRAFT DEPTH (the headline):** the setup re-design foundation (S2 per-league layer + identity bundle) → FABLE-C1 (roster-construction intelligence) → C2 (second-price market) → C3 (pool/advisor) → C4 (setup UI + 24 picker) → the auction gaps (dissolve/mock/bid-log/wrong-fit penalty/accurate floor) → scout archetype-derived bands.
4. **Setup-spine seams** (seat-write, freeze-confirm, draft-recap, persistence, post-freeze summary).
5. **L-SIM hardening:** the freeze→real-frozen-franchise bridge (riskiest — de-risk with a thin spike right after the draft-ui merge).
6. **PHASE 4 (strict order):** saved-game migration check → flip 11 soul flags ON → **§16 tuning** (~100 numbers) → flip live `/franchise` route → **JK browser sign-off**. (Flag-flip MUST precede tuning; tuning MUST precede sign-off.)

**Biggest remaining lifts:** (a) the Assistant-GM intelligence (S5, mostly greenfield); (b) §16 tuning over a live season; (c) the L-SIM freeze-bridge. **Most-mature (leave alone):** S8 freeze, S10 season play, S11/S12 soul+finale backend.

---

## §5. SUPERSEDED DOCS (add a banner pointing here)

Mark these stale (their branch maps + 15-archetype era + pre-re-design status are wrong):
`ROADMAP_TO_V1.md` (Jun-24) · `V1_BUILD_QUEUE.md` (Jun-22, self-flagged stale) · `FRANCHISE_SETUP_TO_SEASON_ROADMAP.md` (Jun-25) · `V1_ACTIVATION_READINESS_MAP.md` (Jun-26) · `V1_STATUS_AND_ASSEMBLY_PLAN.md` (Jun-27, already demoted to companion) · plus the Jun-20 `MODE1_V1_VERIFICATION` / `MODE1_LEAGUE_BUILD_TO_DRAFT_VISION` / `MODE2_V1_COMPLETENESS` and `SCOUTING_INTELLIGENCE_SPEC_V2` (draft) / `AUCTION_DRAFT_SPEC.md` (v1, pre-engine-nomination).

*(Banners not yet applied — pending JK confirm of this doc.)*
