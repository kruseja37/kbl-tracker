{
  "summary": "Trace exact data wiring for the parallel team-lens franchise hub",
  "agentCount": 3,
  "logs": [],
  "result": {
    "routeData": "I have the complete wiring map. Here is the concrete recipe.\n\n---\n\n# Wiring map: \"load teams + colors + records on a fresh preview route\"\n\n## Provider/router context (all already mounted at the app root — your preview route inherits them automatically)\n`src/main.tsx:12-16` wraps everything in `<BrowserRouter>` → `<AppProvider>` → `<App />`. So any `<Route>` you add inside `src/App.tsx`'s `<Routes>` already sits inside React Router **and** `AppProvider`. You do NOT need to add any provider yourself.\n- React Router hooks (`useParams`, `useNavigate`) resolve.\n- `useAppContext()` (`src/context/AppContext.tsx:103`) resolves — but note its `AppState` only holds `selectedTeamId` / `currentSeasonId` / `preferences`; it does **not** hold the active franchise. Franchise identity comes from the route param (see below), not context.\n\n## 1. Which franchise — route param, exactly like the live hub\n\nThe live hub is routed at `src/App.tsx:298`:\n```tsx\n<Route path=\"/franchise/:franchiseId\" element={<FranchiseHome />} />\n```\nAdd a parallel preview route the same way (live hub untouched):\n```tsx\nconst FranchiseLensHub = lazy(() =>\n  import(\"./src_figma/app/pages/FranchiseLensHub\").then((m) => ({ default: m.FranchiseLensHub }))\n);\n// inside <Routes>:\n<Route path=\"/__preview/franchise-lens/:franchiseId\" element={<FranchiseLensHub />} />\n```\nInside the component, read the id exactly as `FranchiseHome.tsx:274` does:\n```tsx\nimport { useParams } from \"react-router\";\nconst { franchiseId } = useParams<{ franchiseId: string }>();\n```\nThere is no \"current/active franchise\" from context. The `franchiseId` always comes from the URL. (A storage-level `getActiveFranchise()` exists at `src/utils/franchiseManager.ts:265` returning `Promise<FranchiseId | null>` if you ever want a no-param fallback, but the live hub does not use it — it is purely route-driven.)\n\nTo load the franchise object itself (config + metadata), mirror `useFranchiseData.ts:316,324`:\n```tsx\nimport { getFranchiseConfig, loadFranchise } from \"../../../utils/franchiseManager\";\nconst config = await getFranchiseConfig(franchiseId);  // Promise<StoredFranchiseConfig | null>\nconst meta   = await loadFranchise(franchiseId);       // Promise<FranchiseMetadata | null>\n```\n\n## 2. Teams + colors + records — the important gotcha\n\n**`useFranchiseData` does NOT give you team objects with colors.** Its `UseFranchiseDataReturn` (`useFranchiseData.ts:79-114`) only exposes `standings` (name strings + W/L), a `teamNameMap` (`Record<teamId,name>`), and a `stadiumMap`. There is no `colors`, `abbreviation`, or `Team[]` in that return. So for a color-driven \"team lens\" you need **two sources joined on `teamId`**: the raw `Team[]` (identity + colors) and the standings (records).\n\n### A. Teams with id / name / abbreviation / colors.primary / colors.secondary\nUse the **franchise-scoped** team loader (same one the hook and hub call — `useFranchiseData.ts:346,427`, `FranchiseHome.tsx:46`):\n```tsx\nimport { getAllFranchiseTeams, getFranchiseTeam } from \"../../../utils/franchisePlayerStorage\";\nconst teams = await getAllFranchiseTeams(franchiseId);          // Promise<Team[]>\n// single: getFranchiseTeam(franchiseId, teamId) -> Promise<Team | null>\n```\n`Team` type — `src/utils/leagueBuilderStorage.ts:120-140`:\n```ts\nexport interface Team {\n  id: string;\n  name: string;\n  abbreviation: string;\n  location: string;\n  nickname: string;\n  colors: { primary: string; secondary: string; accent?: string };\n  logoUrl?: string;\n  stadium: string;\n  leagueIds: string[];\n  // ...\n}\n```\n**Colors live on `team.colors.primary` / `team.colors.secondary` — this is the canonical, per-team, persisted source.** It is populated when teams are imported/created in League Builder (the import path at `leagueBuilderStorage.ts:2110-2112` maps `team.primaryColor`/`team.secondaryColor` → `colors.primary`/`colors.secondary`) and copied into the franchise-scoped store at franchise creation. Read colors off the `Team` object — **do not** rely on `getTeamColors(teamId)` from `@/config/teamColors` (`teamColors.ts:75`): it is a hardcoded map with only ~2 real entries and otherwise returns a single green `DEFAULT_TEAM_COLORS` (`teamColors.ts:64-73`). FranchiseHome imports it only for incidental field/scorebug tinting, not as the team-identity color source.\n\n> Fallback note: if `getAllFranchiseTeams(franchiseId)` returns `[]` (damaged/legacy save), the hook falls back to global `getAllTeams()` from `leagueBuilderStorage.ts:935` (`Promise<Team[]>`, same `Team` shape). Mirror that fallback if you want parity.\n\n### B. W-L record + division/standing\nRecords are keyed by `teamId`, computed from completed games. Source — `src/utils/seasonStorage.ts:957`:\n```tsx\nimport { calculateStandings } from \"../../../utils/seasonStorage\";\nimport { getFranchiseSeasonId } from \"../../../utils/franchisePersistenceContract\";\nconst seasonId  = getFranchiseSeasonId(franchiseId, currentSeason); // currentSeason: number, default 1\nconst standings = await calculateStandings(seasonId);               // Promise<TeamStanding[]>\n```\n`TeamStanding` type — `seasonStorage.ts:927-941`:\n```ts\nexport interface TeamStanding {\n  teamId: string; teamName: string;\n  wins: number; losses: number; winPct: number;\n  runsScored: number; runsAllowed: number; runDiff: number;\n  streak: { type: 'W' | 'L'; count: number };\n  lastTenWins: number;\n  homeRecord: { wins: number; losses: number };\n  awayRecord: { wins: number; losses: number };\n  gamesBack: number;\n}\n```\nJoin: build `Map<teamId, TeamStanding>` and look each `team.id` up. **If a team has no entry, no games were played — seed `0-0` / `gamesBack \"-\"`** (the hook does exactly this at `useFranchiseData.ts:493-512`).\n\n**Division / conference placement** is not on `TeamStanding` — it comes from the league template's `divisions[].teamIds` / `conferences[]`. The hook reconstructs this for franchises via `buildFranchiseLeagueTemplate(config, teams)` (`useFranchiseData.ts:241-292`, fed by `getFranchiseConfig` + `getAllFranchiseTeams`), then groups standings into `Eastern`/`Western` → division → sorted entries and **recomputes per-division games-back** (`useFranchiseData.ts:515-548`). If you want the same division buckets and intra-division GB, reuse that template + grouping logic rather than reading division off the standing.\n\n### Shortcut if you only need pre-grouped standings (not colors)\nYou can call the hook directly inside your preview component for the grouped standings + leaders, then layer colors on top from `getAllFranchiseTeams`:\n```tsx\nimport { useFranchiseData } from \"@/hooks/useFranchiseData\";\nconst fd = useFranchiseData(franchiseId, currentSeason);\n// fd.standings: LeagueStandings (Eastern/Western → division → StandingEntry[])\n// fd.teamNameMap, fd.stadiumMap, fd.isLoading, fd.hasRealData\n```\nBut `fd.standings` entries (`StandingEntry`, `useFranchiseData.ts:26-32`) carry only `team` (name string), `wins`, `losses`, `gamesBack`, `runDiff` — **no teamId, no colors**. To color them you must re-join to `Team[]` by name or build your own teamId-keyed path. For a color-first \"team lens,\" the cleaner path is to skip the grouped `standings` shape and join `getAllFranchiseTeams` ⨝ `calculateStandings` directly on `teamId`.\n\n## 3. Async / loading states\nEvery loader above is `async` (IndexedDB). On a fresh route nothing is preloaded, so:\n- Drive each load in `useEffect` with a `let cancelled = false` guard and clear it in cleanup — the exact pattern at `useFranchiseData.ts:313-337, 342-362, 420-463`.\n- `getFranchiseConfig` / `loadFranchise` / `getAllFranchiseTeams` / `calculateStandings` can each return empty/null on a brand-new or damaged save — render an empty/`0-0` state, don't assume data (`useFranchiseData.ts:484, 504-511`).\n- If you reuse `useFranchiseData`, gate render on `fd.isLoading` (`= seasonData.isLoading || seasonStats.isLoading`, `useFranchiseData.ts:627`) and branch on `fd.hasRealData` (`useFranchiseData.ts:372-374`) for the \"no games yet\" view. `fd.error` (`useFranchiseData.ts:628`) carries any underlying error.\n- `currentSeason` is a `number` you own as local state (the hub seeds it from the route via `getInitialRouteSeasonNumber(franchiseId)` at `FranchiseHome.tsx:294`); default `1` is fine for a preview.\n\n---\n\n### Copy-paste import block for the new component\n```tsx\nimport { useParams } from \"react-router\";\nimport { getFranchiseConfig, loadFranchise } from \"../../../utils/franchiseManager\";\nimport { getAllFranchiseTeams, getFranchiseTeam } from \"../../../utils/franchisePlayerStorage\";\nimport { getAllTeams, type Team } from \"../../../utils/leagueBuilderStorage\";\nimport { calculateStandings, type TeamStanding } from \"../../../utils/seasonStorage\";\nimport { getFranchiseSeasonId } from \"../../../utils/franchisePersistenceContract\";\n// optional, if you reuse the grouped standings/leaders:\nimport { useFranchiseData, type UseFranchiseDataReturn } from \"@/hooks/useFranchiseData\";\n```\n(From a page in `src/src_figma/app/pages/`, the `../../../utils/...` relative paths and the `@/` alias both resolve — same paths `FranchiseHome.tsx` uses.)\n\n### One-line recipe\nRoute `/__preview/franchise-lens/:franchiseId` → `useParams` for `franchiseId` → in a cancel-guarded `useEffect`, `await getAllFranchiseTeams(franchiseId)` for `Team[]` (id/name/abbreviation/**colors.primary**/**colors.secondary**) and `await calculateStandings(getFranchiseSeasonId(franchiseId, season))` for `TeamStanding[]` (wins/losses/gamesBack/runDiff) → **join on `teamId`**, defaulting missing teams to `0-0` → for division/standing buckets, rebuild the template via `getFranchiseConfig` + `buildFranchiseLeagueTemplate` (or just reuse `useFranchiseData(franchiseId, season).standings`). No new provider needed; everything inherits from the root `AppProvider`/`BrowserRouter`.\n\nKey file:line references: route pattern `src/App.tsx:298`; param read `FranchiseHome.tsx:274`; franchise object load `useFranchiseData.ts:316,324`; team loader `franchisePlayerStorage.ts:240,248`; `Team` type `leagueBuilderStorage.ts:120-140`; color population `leagueBuilderStorage.ts:2110-2112`; standings `seasonStorage.ts:957`; `TeamStanding` type `seasonStorage.ts:927-941`; 0-0 fallback `useFranchiseData.ts:493-512`; provider mount `main.tsx:12-16`.",
    "rosterDesig": "I now have the full wiring map verified against source. Here is the complete recipe.\n\n---\n\n# Wiring Map: (franchise, teamId) → roster + per-player designations\n\nAll paths relative to `/Users/johnkruse/Projects/kbl-tracker--auction-ux/`. Worktree component studied: `src/src_figma/app/components/TeamHubContent.tsx` (6738 lines).\n\n## TL;DR — there is no single hook\n\nTeamHubContent does NOT use a \"team roster hook.\" It loads **all** franchise players once via a raw async storage call into local state, then `.filter()`s by `teamId` in a `useMemo`. Designations come from a **separate** IndexedDB store, loaded by its own async effect into separate state. WAR/salary stats come from a **third** source (`useSeasonStats`). You join all three by `playerId` in memos. Everything is **async (IndexedDB)** with manual `useState` + `useEffect`; there is no off-the-shelf loading hook that returns the joined shape.\n\n---\n\n## 1. Team's players + name / position / WAR / salary\n\n### Imports\n```ts\nimport { getAllFranchisePlayers } from \"../../../utils/franchisePlayerStorage\";   // src/utils/franchisePlayerStorage.ts:204\nimport { getFranchiseTeam } from \"../../../utils/franchisePlayerStorage\";\nimport { useSeasonStats, type BattingLeaderEntry, type PitchingLeaderEntry } from \"../../../hooks/useSeasonStats\"; // src/hooks/useSeasonStats.ts\nimport { getSeasonIdForScope } from \"../../../utils/franchisePersistenceContract\";\nimport type { Player } from \"../../../utils/leagueBuilderStorage\"; // re-exported via franchisePlayerStorage; Player iface at src/utils/leagueBuilderStorage.ts:248\n```\n\n### Calls (all async, IndexedDB)\n- **Roster source:** `getAllFranchisePlayers(franchiseId): Promise<Player[]>` — returns ALL players in the franchise, NOT team-scoped. (`franchisePlayerStorage.ts:204`)\n  - Loaded in the effect at **TeamHubContent.tsx:1540–1582** → `setFranchiseAllPlayers(allPlayers)` (1564).\n- **Team filter (this is the \"team lens\"):** two pure predicates, filter `franchiseAllPlayers` by `selectedTeamId`:\n  - `isActiveFranchisePlayerForTeam(player, teamId, leagueId)` — MLB/null roster status (**TeamHubContent.tsx:618**)\n  - `isFarmFranchisePlayerForTeam(player, teamId, leagueId)` — FARM (**:626**)\n  - Both walk `player.leagueAssignments[]` matching `assignment.teamId === teamId`.\n- **WAR + season-aggregated stats:** `useSeasonStats(seasonId)` → `getBattingLeaders('totalWAR', 500)` / `getPitchingLeaders('pWAR', 500)`, indexed into `Map<playerId, Entry>` at **TeamHubContent.tsx:2427 (battingByPlayer)** and **:2439 (pitchingByPlayer)**.\n  - `seasonId = getSeasonIdForScope(franchiseId, seasonNumber)` (**:1317–1318**).\n  - WAR field: batters → `BattingLeaderEntry.totalWAR`; pitchers → `PitchingLeaderEntry.pWAR` (`useSeasonStats.ts:74, 83`).\n- **Salary:** `Number(player.salary)` (raw `Player.salary: number`, `leagueBuilderStorage.ts:286`), gated through `getVisibleSafeFranchisePlayerSalary` for hidden farm players (**:859**).\n\n### Where the roster table is built / rendered\n- **Built:** `rosterData` memo at **TeamHubContent.tsx:2469–2512**. It filters `franchiseAllPlayers` (2471–2475) then maps each `Player` → `RosterTableItem` via `convertFranchisePlayerToRosterItem(...)` (**defined :835–894**), passing in the matched `battingByPlayer.get(player.id)` / `pitchingByPlayer.get(player.id)` / `moraleSnapshot` / `projectedDesignationRows`.\n- **Sorted:** `getSortedRoster()` at **:2566–2589**.\n- **Rendered (table):** `<table aria-label=\"Franchise roster scan table\">` at **:3302**, rows mapped at **:3330** (`getSortedRoster().map(...)`):\n  - name → **:3339** (`player.name`)\n  - position → **:3342** (`player.position`)\n  - salary → **:3347** (`player.contract`)\n  - WAR (inside stat summary string) → **:3355** (`player.statSummary`, e.g. `\"2.4 WAR · 18 HR · 55 RBI\"` or `\"1.8 pWAR · 3.20 ERA\"`, built at **:788 rosterStatSummary**)\n  - designations → **:3358** (`player.designationSummary`)\n\n### Player object shape consumed (`RosterTableItem`, interface at TeamHubContent.tsx:200)\nDisplay name, position, WAR, salary live on the converted row, NOT the raw `Player`:\n```ts\n{\n  playerId?: string;            // = Player.id\n  name: string;                 // getFranchisePlayerName(player) = `${firstName} ${lastName}` (:443)\n  position: string;             // = player.primaryPosition (:872)\n  contract: string;             // formatted salary string, e.g. \"$1.2M\" (:878, formatSalary)\n  salarySortValue: number;      // raw salary for sorting (:879)\n  statSummary: string;          // contains the WAR text (:883)\n  statSortValue: number;        // = totalWAR (batters) or pWAR (pitchers) — the numeric WAR (:807/:800)\n  designationSummary: string;   // see §2 (:885)\n  grade, morale, moraleState, rosterStatus, teamContext, fitness, rookieStatus, hiddenSafe, originalIndex\n}\n```\nRaw `Player` fields if you want the numbers directly: `player.id`, `player.firstName`/`player.lastName`, `player.primaryPosition` (`leagueBuilderStorage.ts:264`), `player.salary` (`:286`), `player.overallGrade` (`:277`). WAR is NOT on `Player` — it comes only from `useSeasonStats` joined by `player.id`.\n\n---\n\n## 2. Per-player designations (TEAM_MVP / ACE / FAN_FAVORITE / ALBATROSS)\n\n### Imports\n```ts\nimport { getFranchiseDesignationRows } from \"../../../utils/franchiseDesignationStorage\";   // src/utils/franchiseDesignationStorage.ts:245\nimport {\n  getLiveDesignationBadge,\n  getProjectedDesignationBadge,\n  type FranchisePlayerDesignationRecord,\n} from \"../../../utils/franchiseDesignations\";   // src/utils/franchiseDesignations.ts:49 / :208 / :214\n```\n\n### Call (async, IndexedDB — separate store from players)\n```ts\ngetFranchiseDesignationRows({ franchiseId, seasonId, statsScopeId: seasonId })\n  : Promise<FranchisePlayerDesignationRecord[]>\n```\n- Signature/return: `franchiseDesignationStorage.ts:245–265`. Scope input is `{ franchiseId, seasonId, statsScopeId }` (`:31`). Returns ALL teams' rows for the season — filter by `row.teamId` yourself.\n- Loaded in TeamHubContent at **:1729–1740** inside `loadValueTruthReports()` → `setProjectedDesignationRows(designationRows)` (state declared at **:1383**).\n\n### Record shape (`FranchisePlayerDesignationRecord`, franchiseDesignations.ts:49)\n```ts\n{\n  franchiseId: string; seasonId: string; statsScopeId: string; seasonNumber: number;\n  teamId: string;\n  playerId: string;\n  playerName: string;\n  type: 'TEAM_MVP' | 'ACE' | 'FAN_FAVORITE' | 'ALBATROSS';   // FranchiseDesignationType (:3)\n  status: 'projected' | 'locked' | 'active';                 // FranchiseDesignationStatus (:10)\n  // + sourceInputs, calculationVersion, calculatedAt, lockedAt, carryover\n}\n```\nNote the status taxonomy: `'active'` = live/final holder; `'projected'` = mid-season projection. (`'locked'` exists in the type but TeamHubContent's UI keys off `'active'` vs `'projected'`.)\n\n### How TeamHubContent renders the badges\nTwo distinct renders:\n\n**(a) Per-player string in the roster table** (the `~811–825` you cited) — `rosterDesignationSummary(...)` at **:811–833**, called from `convertFranchisePlayerToRosterItem` at **:885**. It filters `projectedDesignationRows` by `record.playerId === playerId`, splits by `record.status === 'active'` vs `'projected'`, maps each to a label via `getLiveDesignationBadge(record.type) ?? getProjectedDesignationBadge(record.type)` (**:820**), and produces a string like `\"MVP, Ace live; Proj. Fan Favorite projected\"`. Rendered as text at **:3358**.\n\n**(b) Colored badge chips in the team designation strip** — `teamDesignationStripSlots` memo at **:2094–2166** (filters rows to `row.teamId === selectedTeamId` and the 4 types at **:2097–2098**, picks one holder per type). Each slot's `badge` is `getLiveDesignationBadge(type)` (active) or `getProjectedDesignationBadge(type)` (projected) at **:2116–2118**. Rendered as actual styled chips via `renderTeamHubBadge(slot.badge)` at **:2925** (helper `renderTeamHubBadge` at **:290**, uses `badge.colorHex`, `badge.borderStyle`, `badge.backgroundHex`).\n\n### Badge object shapes (returned by the getters)\n- `getProjectedDesignationBadge(type)` → `FranchiseProjectedDesignationBadge` (`franchiseDesignations.ts:67`): `{ type, label: 'Proj. MVP'|'Proj. Ace'|..., prefix:'Proj.', borderStyle:'dotted', status:'projected', colorHex, backgroundHex }`. Always non-null (`:208`).\n- `getLiveDesignationBadge(type)` → `FranchiseLiveDesignationBadge | null` (`:77`): `{ type, label:'MVP'|'Ace'|'Fan Favorite'|'Albatross', borderStyle:'solid', status:'active', colorHex, backgroundHex }`. **Can return null** — always fall back to the projected badge (`:214–218`; tables at `:105 PROJECTED_BADGES`, `:144 LIVE_DESIGNATION_BADGES`).\n\n---\n\n## 3. Sync vs async / loading hook\n\n**All async (IndexedDB).** No single loading hook returns the joined shape. The three sources each have their own async path + `isLoading`/state:\n\n| Source | Loader | Loading signal |\n|---|---|---|\n| Roster players | `getAllFranchisePlayers` in `useEffect` (TeamHubContent.tsx:1540) | local `useState` (`franchiseAllPlayers`); no dedicated flag — empty array until resolved |\n| Designations | `getFranchiseDesignationRows` in `useEffect` (TeamHubContent.tsx:1704) | `valueTruthLoading` state (TeamHubContent-local), set around :1725/1750 |\n| WAR/salary stats | `useSeasonStats(seasonId)` hook | `seasonStats.isLoading` (`useSeasonStats.ts:97, 341`) — guarded at battingByPlayer/:2429 |\n| (Stock/offseason fallback) | `useOffseasonData()` | `isLoading` / `hasRealData` (`useOffseasonData.ts:70, 90`) — only used when no franchise selected |\n\nThe join is done in `useMemo`s keyed on `playerId` (`battingByPlayer`/`pitchingByPlayer`/`moraleSnapshotByPlayerId`/`rosterFarmRecordByPlayerId`) and woven together in the `rosterData` memo (:2469). You must replicate that join — there's no prebuilt `{name, position, war, salary, designations[]}` provider.\n\n---\n\n## Recipe: `(franchiseId, teamId) → { name, position, war, salary, designations[] }[]`\n\n```\nseasonId        = getSeasonIdForScope(franchiseId, seasonNumber)          // franchisePersistenceContract\n\nallPlayers      = await getAllFranchisePlayers(franchiseId)               // Player[]  (ASYNC)\nteamPlayers     = allPlayers.filter(p =>\n                    isActiveFranchisePlayerForTeam(p, teamId, leagueId) ||\n                    isFarmFranchisePlayerForTeam(p, teamId, leagueId))    // team lens\n\nstats           = useSeasonStats(seasonId)                                // hook (ASYNC; guard stats.isLoading)\nbattingById     = Map(stats.getBattingLeaders('totalWAR',500), b => b.playerId)\npitchingById    = Map(stats.getPitchingLeaders('pWAR',500),   p => p.playerId)\n\ndesigRows       = await getFranchiseDesignationRows({franchiseId, seasonId, statsScopeId: seasonId})  // ASYNC\n                  // FranchisePlayerDesignationRecord[]; filter row.teamId === teamId for this team\n\nresult = teamPlayers.map(p => ({\n  name:        `${p.firstName} ${p.lastName}`.trim(),        // getFranchisePlayerName\n  position:    p.primaryPosition,\n  war:         isPitcher(p) ? (pitchingById.get(p.id)?.pWAR ?? 0)\n                            : (battingById.get(p.id)?.totalWAR ?? 0),\n  salary:      Number(p.salary),                              // or getVisibleSafeFranchisePlayerSalary(p) if hidden-farm\n  designations: desigRows\n                  .filter(r => r.playerId === p.id /* && r.teamId === teamId */)\n                  .map(r => ({\n                     type:   r.type,                          // 'TEAM_MVP'|'ACE'|'FAN_FAVORITE'|'ALBATROSS'\n                     status: r.status,                        // 'active'|'projected'|'locked'\n                     badge:  r.status === 'active'\n                               ? (getLiveDesignationBadge(r.type) ?? getProjectedDesignationBadge(r.type))\n                               : getProjectedDesignationBadge(r.type),\n                  })),\n}))\n```\n\nPitcher test in this file: `isFranchisePitcher(player)` (TeamHubContent.tsx:1126), mirrored by `FRANCHISE_PITCHER_POSITIONS` set (:228). The `??0` WAR fallbacks reproduce the \"No stats\" / `Number.NEGATIVE_INFINITY` handling at `rosterStatSummary` (:788).",
    "morale": "I now have the complete wiring map. Both player and fan morale flow through the same `listFranchiseMoraleSnapshots` array in TeamHubContent, filtered by `targetType`. Here is the recipe.\n\n---\n\n# Morale read wiring map — `/Users/johnkruse/Projects/kbl-tracker--auction-ux/src`\n\nEverything for a hub flows through ONE durable store: `franchiseMoraleState.ts` (IndexedDB `kbl-franchise-morale` v1, store `moraleSnapshots`). TeamHubContent loads the whole season's snapshots once via `listFranchiseMoraleSnapshots`, then filters by `targetType` for player vs. fan. No separate fan store. All reads are async.\n\n## Imports (copy-paste)\n\n```ts\n// Store reads (async, IndexedDB)\nimport {\n  listFranchiseMoraleSnapshots,   // bulk: all snapshots for a season (player + team-fan)\n  getFranchiseMoraleSnapshot,     // single: one target by (scope, targetType, targetId)\n  type FranchiseMoraleSnapshot,\n  type FranchiseMoraleHistoryEntry,\n  type FranchiseMoraleScope,\n  type FranchiseMoraleTargetType,\n} from '../../utils/franchiseMoraleState';\n\n// Player morale -> {value,state,risk,trend,history} view model (pure, sync)\nimport {\n  buildFranchisePlayerMoraleSpecViewModel,\n  getPlayerMoraleSpecState,        // (0-99) -> ECSTATIC..DEMORALIZED\n  getPlayerMoraleRiskLevel,        // (0-99) -> SAFE/WATCH/DANGER/CRITICAL\n  type FranchisePlayerMoraleSpecViewModel,\n} from '../../utils/franchisePlayerMoraleSpecAdapter';\n\n// Fan (team) morale -> view model (pure, sync)\nimport {\n  buildFranchiseFanMoraleSpecViewModel,\n  getFanMoraleSpecState,           // (0-99) -> EUPHORIC..HOSTILE\n  getFanMoraleRiskLevel,\n} from '../../utils/franchiseFanMoraleSpecAdapter';\n```\n(Adjust the `../../` depth to your file; from `src_figma/app/components/` the depth above is correct.)\n\n---\n\n## 1. Player morale: current value (0-99) + state label + risk, by playerId\n\n**Scope identity required** (TeamHub uses `seasonId` for both `seasonId` AND `statsScopeId`):\n`{ franchiseId, seasonId, statsScopeId: seasonId, seasonNumber }`.\n\n**Single-target store read** — `getFranchiseMoraleSnapshot(scope, 'player', playerId)` at `franchiseMoraleState.ts:228`. Returns `FranchiseMoraleSnapshot | null`. Current value is `snapshot.currentValue`.\n\n**Snapshot type** — `franchiseMoraleState.ts:35-45`:\n```ts\ninterface FranchiseMoraleSnapshot {\n  id; contractVersion; franchiseId; seasonId; statsScopeId; seasonNumber;\n  targetType: 'team-fan' | 'player';\n  teamId?: string; playerId?: string;\n  baselineValue: number;   // always 50 (neutral)\n  currentValue: number;    // 0-99, clamped\n  lastModified: string;    // ISO\n  history: FranchiseMoraleHistoryEntry[];\n}\n```\n\n**Value -> labels** (pure, sync) — `franchisePlayerMoraleSpecAdapter.ts`:\n- `getPlayerMoraleSpecState(value)` `:96` -> `>=85 ECSTATIC | >=65 MOTIVATED | >=45 CONTENT | >=25 FRUSTRATED | else DEMORALIZED`\n- `getPlayerMoraleRiskLevel(value)` `:105` -> `<10 CRITICAL | <25 DANGER | <45 WATCH | else SAFE`\n\n**How TeamHubContent actually loads it** — it does NOT call `getFranchiseMoraleSnapshot` per player. It bulk-loads ALL season snapshots into a `moraleSnapshots` array, then picks the player's by filter:\n- bulk load `TeamHubContent.tsx:1903-1908` (and `:1935-1941`): `listFranchiseMoraleSnapshots(franchiseId, seasonId, seasonId, seasonNumber)` -> `setMoraleSnapshots(...)`\n- per-player pick `TeamHubContent.tsx:3548-3550`: `moraleSnapshots.find(s => s.targetType === 'player' && s.playerId === selectedProfile.playerId) ?? null` -> passed as `moraleSnapshot`\n- view model `TeamHubContent.tsx:3894-3898` (inside `FranchisePlayerMoraleHistoryPanel`, def `:3875`): `buildFranchisePlayerMoraleSpecViewModel({ snapshot, fallbackPlayerId, fallbackPlayerName })`\n- render: `view.currentValue` `:3931`, `view.state` `:3932`, `view.trend` `:3933`, `view.riskLevel` `:3934`, `view.previousValue` `:3935`.\n\nThe view model (`franchisePlayerMoraleSpecAdapter.ts:143-214`) gives you directly: `currentValue` (clamped 0-99), `state`, `riskLevel`, `trend` (RISING/STABLE/FALLING from latest history delta), `previousValue`, `lastEvent`, and `recentHistory` (top 6, newest first). If snapshot is null or not a player snapshot, it returns a neutral-50 baseline view safely.\n\n**Recipe** `playerId -> { moraleValue, state }`:\n```ts\nconst snap = await getFranchiseMoraleSnapshot(scope, 'player', playerId); // or filter the bulk array\nconst value = snap?.currentValue ?? 50;            // neutral default\nconst state = getPlayerMoraleSpecState(value);     // ECSTATIC..DEMORALIZED\nconst risk  = getPlayerMoraleRiskLevel(value);     // SAFE..CRITICAL\n// or one call: buildFranchisePlayerMoraleSpecViewModel({ snapshot: snap }) -> {currentValue, state, riskLevel, trend, ...}\n```\n\n---\n\n## 2. The history log\n\nThere is no separate \"list history\" function — history is embedded in the snapshot as `snapshot.history`. The view model sorts/slices it.\n\n**Entry type** — `FranchiseMoraleHistoryEntry`, `franchiseMoraleState.ts:23-33`:\n```ts\ninterface FranchiseMoraleHistoryEntry {\n  id: string;\n  sourceEventId: string;\n  sourceKind: 'draft-seed' | 'random-event-confirmation' | 'manual-override'\n            | 'matrix-auto' | 'relationship-reporter' | 'rebrand-reset';\n  previousValue: number;\n  currentValue: number;\n  delta: number;\n  reason: string;            // human-readable\n  actorDisplayName: string;  // 'User' | 'System' | 'Master Morale Matrix' | ...\n  timestamp: string;         // ISO 8601\n}\n```\n\n**Get the list (newest first)** via the view model — `franchisePlayerMoraleSpecAdapter.ts:187-191`: `view.recentHistory` (sorted desc by `Date.parse(timestamp)`, capped at 6). For the unbounded list, read `snapshot.history` directly and sort it yourself.\n\n**Render** — `TeamHubContent.tsx:3965-3980`: iterates `view.recentHistory`, shows `previousValue -> currentValue (+/-delta)` `:3971`, `reason` `:3975`, `sourceKind` `:3976`, and the date.\n\n**Human \"week\"/date label** — there is NO week derivation; the timestamp is rendered raw. `formatProfileHistoryDate(value)` at `TeamHubContent.tsx:3717-3722` is the only formatter:\n```ts\nfunction formatProfileHistoryDate(value?: string): string {\n  if (!value) return 'Date unavailable';\n  const parsed = new Date(value);\n  if (Number.isNaN(parsed.getTime())) return value;\n  return parsed.toLocaleString();   // local datetime string\n}\n```\nUsed at `:3973` (`formatProfileHistoryDate(entry.timestamp)`). If you want a \"Week N\" label you must compute it yourself; the morale entry carries only the wall-clock ISO `timestamp`, not a season day/week index.\n\n**Recipe** `playerId -> [history entries]`: `(await getFranchiseMoraleSnapshot(scope,'player',playerId))?.history ?? []` (sort desc by `timestamp`), or `buildFranchisePlayerMoraleSpecViewModel({snapshot}).recentHistory` for the top-6 newest.\n\n---\n\n## 3. Fan morale (team-level) — the \"club pulse\" number\n\nSame store, `targetType: 'team-fan'`, keyed by teamId.\n\n**Single read** — `getFranchiseMoraleSnapshot(scope, 'team-fan', teamId)` (`franchiseMoraleState.ts:228`). Value = `snapshot.currentValue` (0-99). Same `FranchiseMoraleSnapshot` shape as §1, with `teamId` set instead of `playerId`.\n\n**Value -> labels** — `franchiseFanMoraleSpecAdapter.ts`:\n- `getFanMoraleSpecState(value)` `:102` -> `>=90 EUPHORIC | >=75 EXCITED | >=55 CONTENT | >=40 RESTLESS | >=25 FRUSTRATED | >=10 APATHETIC | else HOSTILE`\n- `getFanMoraleRiskLevel(value)` `:113` -> `<10 CRITICAL | <25 DANGER | <40 WATCH | else SAFE`\n\n**How TeamHub gets it** — same bulk array, filtered for team-fan: `TeamHubContent.tsx:5757-5765` — `snapshots.find(s => s.targetType === 'team-fan' && s.teamId === selectedTeamId)`, then `buildFranchiseFanMoraleSpecViewModel({ snapshot, fallbackTeamId, fallbackTeamName })`.\n\n**Recipe** `teamId -> fanMorale value`:\n```ts\nconst fanSnap = await getFranchiseMoraleSnapshot(scope, 'team-fan', teamId);\nconst fanMorale = fanSnap?.currentValue ?? 50;          // 50 neutral if dormant\nconst fanState  = getFanMoraleSpecState(fanMorale);     // EUPHORIC..HOSTILE\n```\n\n> Do NOT use `useFanMorale` (`src_figma/app/hooks/useFanMorale.ts`) or the legacy `fanMoraleEngine`/`fanMoraleIntegration` for the hub pulse. That hook is an explicitly STUBBED, in-memory-only path (header comment `:7-13`: \"not imported/used anywhere\", \"currently STUBBED OUT\"), unrelated to the durable `kbl-franchise-morale` store. The canonical team pulse is the `team-fan` snapshot above.\n\n---\n\n## 4. Sync vs. async; existing React wrapper\n\n- **Store reads are ASYNC** (IndexedDB). `getFranchiseMoraleSnapshot` `:228` and `listFranchiseMoraleSnapshots` `:241` both `await initFranchiseMoraleDatabase()` (shared cached `dbInstance`, DB `kbl-franchise-morale` v1).\n- **The two view-model builders and all state/risk helpers are PURE and SYNC** — no IO. Do the async read once, then map synchronously.\n- **No dedicated reusable hook.** There is NO `useFranchiseMorale` hook. TeamHubContent does its own loading inline: a `useEffect` at `TeamHubContent.tsx:1911-1962` (and refresh callback `:1893-1909`) calls `listFranchiseMoraleSnapshots` into local `useState` `moraleSnapshots` + `moraleLoading`/`moraleError`. For a new hub, replicate that pattern (load-once into state, then build view models) or just `await getFranchiseMoraleSnapshot` for the single target you need. The only `useFanMorale` hook that exists is the dead stub from §3.\n\n---\n\n## Live-with-real-data vs. dormant in a normal franchise season\n\n- **Player & fan snapshots only exist if a `draft-seed`, a CONFIRMED random event, a manual override, or a `matrix-auto`/`relationship`/`rebrand` write has fired.** Until then `getFranchiseMoraleSnapshot` returns `null` and both adapters fall back to neutral **50** (state CONTENT). So in a fresh/normal season the \"club pulse\" and player morale will commonly read **50 / no history**.\n- **Matrix-auto writes (the in-sim automatic morale engine) are gated DARK** by `isFranchisePhase2MoraleEnabled()` — `applyFranchiseMoraleMatrixConsequence` returns `status:'dark-noop'` and writes nothing when the Phase-2 flag is off (`franchiseMoraleState.ts:495-507`). In a normal season with the flag off, the ONLY things that populate morale are **draft-seed baselines** and **user-confirmed random events + manual overrides**.\n- **Therefore what's genuinely LIVE today:** draft-seed starting values, confirmed-random-event deltas, and manual adjustments — these persist and show real history. **Dormant/empty by default:** any automatic per-game morale movement (Phase-2 matrix), and the entire `useFanMorale`/legacy `fanMoraleEngine` path. A \"club pulse\" widget will read a real number only after a fan-morale-affecting event is confirmed; otherwise it is a true neutral-50 placeholder, not stale data."
  }
}