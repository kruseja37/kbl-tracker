# TRAIT MEASUREMENT SPEC (consolidated — FINDING-150 follow-up)
**Created:** 2026-06-18 (JK ruling 2026-06-18: "I consolidate, you ratify"). **Method:** workflow `wf_368f24d0-78d` — 6 cluster readers reconciling the 4 authoritative sources per trait + assembly. ~499K tokens.
**Purpose:** the authoritative per-trait MEASUREMENT (reality signal + personality modifier) for the buildable trait set, citing every source, with each trait marked **fully-specced** (build to it) or **needs-ruling** (JK decides before build). Mandate: NO inferred/back-filled measurements — gaps surfaced, not invented.
**Status:** ALL rulings RATIFIED by JK 2026-06-18. **§0.6 is the AUTHORITATIVE per-trait proxy table — read it, not §A–§D.**
**47 earnable v1 traits** (Noodle Arm + Stimulated dropped to dormant — no clean proxy; Sign Stealer cut; see §0.6/§0.0).

> **GOVERNING RULE (JK 2026-06-18):** NO trait earns in v1 from personality ALONE. Every earnable trait has a
> documented performance/data/ratings PROXY; personality is influence (a tilt) on top. A trait with no proxy is
> dormant/out — never personality-only. (Reason: only a real proxy puts a trait's acquisition probability P on the
> same comparison currency as every other trait, so it can be ranked/displaced fairly.) §A–§D below are HISTORICAL
> source-citation appendix — where any row still says "personality-primary (no data proxy)", **§0.6 overrides it.**

> The 24 correctly-dormant + 1 cut traits are NOT measurement-spec'd here (they gate on still-missing inputs — see TRAIT_DETECTION_SCOPE_AUDIT.md).

---

## §0. RATIFIED DECISIONS + BUILD PLAN (JK 2026-06-18)

### 0.1 ACQUISITION MODEL (architectural — governs every trait)
- **P (acquisition probability, 0–1) is the single comparison currency** for BOTH the 2-trait-cap displacement (displace the lowest-P held) AND lose-low (drop a held trait when its P falls ≤ lose threshold). A performance-trait's P and a personality/ratings-trait's P are compared as P even though the *inputs* differ — a deliberate modeling choice.
- **Re-evaluate-to-drop:** every HELD trait re-emits a candidate each cycle and recomputes P from its best-available signal in priority **data > ratings > personality**. Personality earns a trait when evidence is thin; real contradicting evidence (or shifted ratings) can still REVOKE it → no trait is permanently sticky. *(Builder change: emit candidates for held traits too, not just gain-candidates; acquisition lose path uses the recomputed P.)*
- Personality stays a **tilt** layered on the data/ratings signal (the existing `buildProposalBase` multipliers), and becomes the **primary** driver only where no data/ratings signal exists.

### 0.2 PER-TRAIT MEASUREMENT RULINGS (resolving the 15)
- **Strikeout family — K Collector / K Neglector / Whiffer / Tough Out:** BUILD NOW on auto-persisted strikeout-rate-vs-peers (K∈{K,Kc,Ꝁ,D3K,WP_K,PB_K}). K Collector/Whiffer = high-K; K Neglector/Tough Out = low-K (inverse). Count only refines (Phase 2), never gates.
- **Pitcher count-family — BB Prone / Composed / Gets Ahead / Falls Behind:** data-proxy = **walks-allowed rate** (BB+IBB per batter faced) + personality tilt. BB Prone / Falls Behind = high walks; **Composed / Gets Ahead = low walks (inverse)** — this reclassifies Composed as buildable (no count needed) and keeps the pair symmetric.
- **Big Hack / Little Hack:** PERFORMANCE proxy — **Big Hack = high HR-rate + low AVG; Little Hack = low HR-rate + high AVG** (replaces the static POW/CON ratings ratio — performance-earned + droppable) + personality tilt.
- **Slow Poke / Sprinter:** **Slow Poke = GIDP (`DP`) rate** (grounds into DPs = slow); **Sprinter = FC (`FC`) rate** (beats the relay → only a force elsewhere = fast). Both auto-persisted `AtBatResult`s — no new capture. (Optional later enhancement: persist the BT/`beat_throw` quick-button — today it is hook-state-only with no log row, `GAMETRACKER_INTERACTION_INVENTORY.md:168` — as a complementary direct-speed signal.)
- **Noodle Arm:** **CUT from v1 → dormant** (JK ruling 2026-06-18 latest). No clean reality signal: ARM-rating-alone is single-rating (banned by §0.0); the OF-assist / runner-advance proxies are conflated (a low rate ≠ weak arm). Re-add only if throw-velocity or reliable per-throw fielder attribution is ever captured. *(Supersedes the earlier "ARM rating < 11 + Droopy/Timid + low Ambition" proposal.)*
- **Ace Exterminator:** **wire the opposing-pitcher-grade join** (join `atBat.pitcherId` → that pitcher's grade via `smb4GradeEmulator`/`ovrCalculator`; measure batter success vs A−-or-better). In v1.
- **First Pitch Slayer / Prayer:** measured by **first-pitch hits/outs** — the outcome (favorable for Slayer, unfavorable for Prayer) on logged first-pitch PAs (`pitchesInAtBat==1`). If the user hasn't logged enough first-pitch data to clear the min-sample threshold, they DON'T trigger — exactly like every other enrichment-gated trait. **NOT personality-earned** (per the §0.0 governing rule); personality is the tilt on top. No misleading "reached base on pitch 1" proxy. *(JK ruling 2026-06-18 latest — supersedes the earlier "personality-earned when no pitch-count data" framing.)*
- **Stimulated:** **DROPPED from the v1 earnable set → dormant.** Its only signal is a mojo auto-derivation that is unbuilt; with no proxy it would be personality-only, which §0.0 forbids. Re-add when the mojo auto-derivation engine exists. *(JK ruling 2026-06-18 latest.)*
- **IMAGE_DRIVER additions:** K Neglector → **low Charisma + Timid/Droopy** (adds a CHARISMA factor to the combiner — new); Big Hack → Egotistical; Little Hack → Tough; First Pitch Slayer → Competitive/Egotistical; First Pitch Prayer → Timid/Droopy.

### 0.3 ENABLING PIECES (foundational — build before the families)
- **E1 — Thread player ratings + grades into the builder input** (`SeasonTraitCandidateInput`): per-player ARM rating (Noodle Arm) + a grade lookup keyed by playerId joinable on `pitcherId` (Ace Exterminator). New input field; the franchise player records carry ratings.
- **E2 — Charisma factor in `buildProposalBase`** (K Neglector low-charisma; reusable). The combiner has ambition/resilience/morale but not charisma today.
- **E3 — Re-evaluate-to-drop (§0.1):** builder emits held-trait candidates each cycle; acquisition lose-low recomputes P. Architectural — do this with E1/E2.

### 0.4 PROPOSED BUILD SEQUENCE (each = build → independent audit → host gate → commit, build-DARK)
1. **R-E (enabling): E1 + E2 + E3** — the input-threading + charisma factor + re-evaluate-to-drop model. Foundation for the rest. **✅ R-E COMPLETE (2026-06-18):** E2 (charisma + positive-Resilience) + the 3 live-bug fixes = R-E-a `9eeb69d5`; E3 (re-evaluate-to-drop = displacement-currency unification on recomputed P) = R-E-b. **E1 (ratings/grade thread) DEFERRED to R3** — the Noodle-Arm cut removed its near-term consumer; the only remaining E1 user is Ace Exterminator's opposing-grade join (R3), to be aligned with the app-wide grade-freshness ticket.
2. **R1 (clean outcome proxies):** strikeout family (4) + Big/Little Hack (HR/AVG) + Slow Poke/Sprinter (DP/FC) + Easy Target + Mind Gamer + the other audit "fully-specced" outcome/event proxies (Base Rounder, Pick Officer, Easy Jumps, Distractor, Utility, Two Way×3, Crossed Up, Bunter).
3. **R2 (data-proxy + personality):** pitcher count-family (walks-allowed) + First-pitch pair (personality + opt-in) + the handedness platoon splits (6, verify the L9a-3 join is fed).
4. **R3 (ratings-gated):** Ace Exterminator (opposing-pitcher-grade join) — depends on E1. *(Noodle Arm was here — CUT per §0.6 / JK 2026-06-18 latest: no clean reality signal.)*

### 0.5 OPEN MICRO-ITEM (non-blocking)
- The 8 personality-modifier TENSIONS in §A: 2 are by-design (seed-vs-axis, Stealer/Bad Jumps), 6 are IMAGE_DRIVER omissions now ruled in §0.2 (Big/Little Hack, First Pitch pair, K Neglector) + Composed (gets DISCIPLINED+Resilience tilt). All resolved by §0.2.

### 0.6 AUTHORITATIVE EARNABLE-TRAIT PROXY TABLE (v1) — THE SINGLE SOURCE

> This table is canonical (every cell cross-checked against §VI.3 + §0.2 + DECISIONS_LOG, spec-over-code;
> reconciliation workflow wf_c4bac237-5d7). Every earnable v1 trait has a real PROXY (§0.0).
> **Personality = Layer 1 (universal) + Layer 2 (image axis):** `↑Amb` = positive trait → high-Ambition tilt ·
> `↓Res` = negative trait → low-Resilience tilt · `neutral` = universal tilt only (no image axis). Named
> personalities are the Layer-2 image drivers (§VI.3:116-122). ("Ambitious"/"high-Resilience" are hidden
> *modifiers*, not personalities — they are the Layer-1/Composure tilts, not Layer-2 drivers.) Phase = §0.4 order.
> "OPT-IN" = enrichment-gated (dormant until logged + min-sample). **†** = the code's image sets are narrower than
> this row today (tilt silently dead) → see §0.7.

| Trait | Role | Reality proxy (measurable signal) | Personality (Layer 1 + Layer 2) | Phase |
|---|---|---|---|---|
| Clutch | universal | high-leverage favorable WPA rate | ↑Amb + Tough/Competitive | BUILT |
| Choker | universal | high-leverage unfavorable WPA rate | ↓Res + Timid/Droopy | BUILT |
| RBI Hero | position | RBI rate in RISP PAs | ↑Amb + Tough/Competitive | BUILT |
| RBI Zero | position | non-RBI rate in RISP PAs | ↓Res + Timid/Droopy | BUILT |
| Rally Starter | position | reach-base rate when losing + bases empty | ↑Amb + Tough/Competitive | BUILT |
| Rally Stopper | pitcher | favorable-outcome rate with ≥2 runners on | ↑Amb + Tough/Competitive | BUILT |
| Surrounded | pitcher | unfavorable-outcome rate with ≥2 runners on | ↓Res + Timid/Droopy | BUILT |
| Meltdown | pitcher | ≥4-consecutive-baserunner episodes per game/inning unit | ↓Res + Timid/Droopy | BUILT |
| Pinch Perfect | position | favorable-outcome rate as substitute | ↑Amb + Tough/Competitive + bench roster tilt | BUILT |
| Stealer | position | steal-success rate (high) | ↑Amb + Competitive/Tough | BUILT |
| Bad Jumps | position | steal-success rate (low) | ↓Res + Relaxed/Droopy | BUILT |
| Cannon Arm † | position | OF-assist rate | ↑Amb + Competitive | BUILT |
| Butter Fingers | position | error rate on diving/athletic chances | ↓Res + Timid/Droopy (doubly sensitive) | BUILT |
| Wild Thrower | position | throw-error rate | ↓Res + Timid/Droopy | BUILT |
| Durable † | universal | injury rate per games (low) | ↑Amb (no image axis); catcher-most-detectable | BUILT (magnitudes placeholder) |
| Injury Prone † | universal | injury rate per games (high) | ↓Res (no image axis) | BUILT (magnitudes placeholder) |
| K Collector | pitcher | strikeout rate vs peers (high) | ↑Amb + Competitive/Egotistical | R1 |
| K Neglector † | pitcher | strikeout rate vs peers (low — inverse) | ↓Res + **low Charisma** + Timid/Droopy | R1 |
| Whiffer | position | strikeout rate vs peers (high) | ↓Res + Egotistical | R1 |
| Tough Out | position | strikeout rate vs peers (low — avoidance) | ↑Amb + Tough | R1 |
| Big Hack † | position | HR-rate (high) + AVG (low) | ↑Amb + Egotistical | R1 |
| Little Hack † | position | HR-rate (low) + AVG (high) | ↑Amb + Tough | R1 |
| Slow Poke | position | GIDP (`DP`) rate | ↓Res + Relaxed/Droopy | R1 |
| Sprinter | position | FC (`FC`) rate | ↑Amb + Competitive/Tough | R1 |
| Easy Target | position | strikeout rate as batter (high) | neutral (universal) | R1 |
| Mind Gamer | position | walk rate as batter (walk half; pitch-grind opt-in) | neutral (universal) | R1 |
| Distractor | position | walk-allowed rate while owner on 1B/2B (walk half) | neutral (universal) | R1 |
| Base Rounder | position | extra-base advancement rate | ↑Amb + Competitive/Tough | R1 |
| Pick Officer | pitcher | opposing steal-success rate (low) | neutral (universal) | R1 |
| Easy Jumps | pitcher | opposing steal-success rate (high) | neutral (universal) | R1 |
| Utility | position | fielding perf at secondary (non-primary) position | neutral (universal) + bench roster tilt | R1 |
| Two Way (C/IF/OF) | pitcher | **elite hitting for a pitcher** — high percentile of wOBA/PA vs the PITCHER peer pool (valve-gated → super-rare); C/IF/OF = random fielding position assigned on grant; pitcher-only, NO batting-trait gateway (§0.8) | ↑Amb + Egotistical | R1 |
| Crossed Up | pitcher | passed-ball events (attributed to pitcher) | neutral (universal) | R1 · OPT-IN (PB logging) |
| Bunter | position | bunt success rate (SAC; bunt-for-hit partial) | ↑Amb + Tough | R1 · OPT-IN |
| BB Prone † | pitcher | walks-allowed rate (high; BB+IBB per BF) | ↓Res (no image axis) | R2 |
| Composed † | pitcher | walks-allowed rate (low — inverse of BB Prone) | ↑Amb + high-Resilience | R2 |
| Gets Ahead † | pitcher | walks-allowed rate (low) | ↑Amb + Competitive + high-Resilience | R2 |
| Falls Behind † | pitcher | walks-allowed rate (high) | ↓Res + Timid | R2 |
| First Pitch Slayer † | position | first-pitch hits/outs — favorable outcome on logged 1st-pitch PAs | ↑Amb + Competitive/Egotistical | R2 · OPT-IN (first-pitch enrichment) |
| First Pitch Prayer † | position | first-pitch hits/outs — unfavorable outcome | ↓Res + Timid/Droopy | R2 · OPT-IN (first-pitch enrichment) |
| CON vs LHP | position | contact rate vs LHP cohort | neutral (universal) | R2 · OPT-IN (handedness) |
| CON vs RHP | position | contact rate vs RHP cohort | neutral (universal) | R2 · OPT-IN (handedness) |
| POW vs LHP | position | power (ISO/SLG) vs LHP cohort | neutral (universal) | R2 · OPT-IN (handedness) |
| POW vs RHP | position | power (ISO/SLG) vs RHP cohort | neutral (universal) | R2 · OPT-IN (handedness) |
| Specialist | pitcher | K/control vs same-handed batter cohort | neutral (universal) | R2 · OPT-IN (handedness) |
| Reverse Splits | pitcher | K vs opposite-handed batter cohort | neutral (universal) | R2 · OPT-IN (handedness) |
| Ace Exterminator | position | batter success vs A−-or-better pitchers (opposing-pitcher-grade join on `pitcherId`) | ↑Amb + Competitive/Egotistical | R3 (needs E1 grade join) |

*(**DORMANT-v1 / not earnable:** **Noodle Arm** — CUT (no clean reality signal: ARM-rating-alone is single-rating §0.0; OF-assist/runner-advance proxies are conflated — a low rate ≠ weak arm; re-add only if throw-velocity or per-throw fielder attribution is captured). **Stimulated** — mojo auto-derivation unbuilt. **Sign Stealer** — CUT. The 24 correctly-dormant traits gate on still-missing inputs — see TRAIT_DETECTION_SCOPE_AUDIT.md.)*

### 0.6b DORMANT-TRAIT ENABLEMENT MATRIX (JK-ruled 2026-06-25 — the "dormant-trait wave"; full rulings + scoring in `DECISIONS_LOG.md` 2026-06-25)

The **25** traits below were previously dormant (blank rows — see `TRAIT_DETECTION_SCOPE_AUDIT.md` 2026-06-25: their **capture field + difficulty already exist**; only the **signal builder + min-sample valve** remain). Difficulty is auto-derived by `assignTier`. ALL build-dark, **opt-in** (fire only on user-tagged data), **min-sample-gated** (dormant until enough tagged events → never fires on noise, zero forced clicking). Build template = elite pitches (**T-9**). Sequence/queue = `V1_BUILD_QUEUE.md` Lane 4D-W2.

**NET-OUTCOME SCORING (groups A + B — per pitch-bucket, averaged over the user-tagged ABs, min-sample-gated, percentile vs role peers; the selective tagging of good AND bad outcomes IS the signal):**
- **HITTER** (Fastball/Off-Speed/High/Low/Inside/Outside): **HR +++** · big hit ++ · single/RBI + · BB mild + · out 0 · **K −**
- **PITCHER** (Elite-`<pitch>`): **K +** · out mild + · **BB −** · hit-allowed −− · **HR-allowed −−−** (heaviest; a HR off the pitch is the opposite of elite)

| Grp | Trait(s) | Role | Proxy (signal) | Input field |
|---|---|---|---|---|
| **A** | Elite 2F/4F/CB/CF/CH/FK/SB/SL (8) | pitcher | net-quality on that pitch (K+ / BB− / hit−− / **HR−−−**), pct vs pitcher peers | `enrichment.pitchType` |
| **A** | Fastball Hitter | hitter | net-outcome on **{4F,2F,CF}**-tagged pitches, pct vs hitter peers | `enrichment.pitchType` |
| **A** | Off-Speed Hitter | hitter | net-outcome on **{FK,CB,SL,CH,SB}**-tagged pitches | `enrichment.pitchType` |
| **B** | High / Low / Inside / Outside Pitch (4) | hitter | net-outcome on pitches tagged in that zone (big hits/Ks/BBs/RBIs incl. SF + RBI-GO) | `enrichment.pitchLocation` |
| **C** | Magic Hands | fielder | web-gem (diving/leaping/sliding) plays · **GATE: fielding rating < 80** | `enrichment.fieldingPlayType` + ratings |
| **C** | Dive Wizard | fielder | web-gem plays · **GATE: arm rating > 80** (CAN co-hold Magic Hands; firing-roll decides slots) | `enrichment.fieldingPlayType` + ratings |
| **C** | Bad Ball Hitter | hitter | hits-on-chase ÷ (hits + **outs**-on-chase) [outs-denominator = anti-game] | `enrichment.chased` + outcome |
| **D** | Wild Thrower | position | throwing/fielding errors, rate vs peers | `enrichment.errors[]` |
| **D** | Noodle Arm (RE-ADDED) | position | **MENTAL** errors, league-leader earns (reverses the §0.6 cut; now mental-error, NOT arm) | `enrichment.errors[] type:'mental'` |
| **E** | Volatile / Consistent (2) | universal | MANY mojo changes → Volatile · FEW → Consistent | mojo-change events (`mojoEngine` / `BetweenPlayEvent` `mojo_change`) |
| **F** | Base Jogger | position | **REVERSE Base Rounder** (deep hits→single / fewer bases than expected) | `runnerOutcomes` (reuse `addBaseRounderSignals`) |
| **F** | Metal Head | **pitcher** | the **pitcher-VICTIM** of killed-pitcher (fitness↓) + nut-shot (mojo↓), league-leader → **PROTECTIVE** | enrichment KP/nut-shot buttons (pitcher-keyed) |
| **F** | Wild Thing | pitcher | wild pitches advancing runners + D3K_WP, rate vs pitcher peers | `betweenPlayEvents` WP + `WP_K` |
| **F** | Workhorse | pitcher | high IP/game (**ELITE** tier), role-split SP/RP vs peers | IP = `outsRecorded`/3 + appearances |

**CUT (no re-add):** **Sign Stealer**, **Stimulated**.
**Build forks (settle as the wave builds):** C's **rating-gate** (arm/fielding thresholds) is a NEW trait-engine mechanism (engine is performance-only today). Metal Head's exact pitcher-keyed enrichment field — verify at build (JK: it's an enrichment-panel button tied to the pitcher). Mojo-change source — confirm `mojoEngine`/team-hub exposes a per-player change count.

### 0.7 CODE DELTAS THE BUILD MUST APPLY (`src/engines/traitAcquisition.ts` — code is narrower than §0.6)

The acquisition engine's `POSITIVE_IMAGE_TRAITS` / `NEGATIVE_IMAGE_TRAITS` / `IMAGE_DRIVER_SETS` are narrower than
§0.6, so these traits' personality tilts **silently resolve to neutral and do not fire today.** Each R-phase fixes
the code to match §0.6 — never the reverse. (Reconciled + grep-verified, wf_c4bac237-5d7.)

> **APPLIED in R-E-a (2026-06-18):** the 3 LIVE latent bugs (Cannon Arm / Durable / Injury Prone) + the E2
> **Charisma factor** + the **positive-Resilience path** are DONE (verified, suite 7,568/7,566 pass / 2 char fail).
> The remaining rows (K Neglector / Big Hack / Little Hack / BB Prone / Composed / Gets Ahead / Falls Behind /
> First Pitch Slayer / First Pitch Prayer image-set memberships) land with their R1/R2 trait tickets.

| Trait (†) | Fix in `traitAcquisition.ts` | Severity |
|---|---|---|
| Cannon Arm | + `POSITIVE_IMAGE_TRAITS`; `IMAGE_DRIVER_SETS['Cannon Arm']=['COMPETITIVE']` | **LIVE latent bug** — built, tilt dead |
| Durable | + `POSITIVE_IMAGE_TRAITS` (no driver — universal Ambition only) | **LIVE latent bug** |
| Injury Prone | + `NEGATIVE_IMAGE_TRAITS` (no driver — universal low-Resilience) | **LIVE latent bug** |
| K Neglector | + `NEGATIVE_IMAGE_TRAITS` + `IMAGE_DRIVER_SETS['K Neglector']=['TIMID','DROOPY']` + the E2 Charisma factor (low-Charisma raises) | R1 (needs E2) |
| Big Hack | + `POSITIVE_IMAGE_TRAITS` + `IMAGE_DRIVER_SETS['Big Hack']=['EGOTISTICAL']` | R1 |
| Little Hack | + `POSITIVE_IMAGE_TRAITS` + `IMAGE_DRIVER_SETS['Little Hack']=['TOUGH']` | R1 |
| BB Prone | + `NEGATIVE_IMAGE_TRAITS` (no driver — universal low-Resilience; DISCIPLINED dropped per JK) | R2 |
| Composed | + `POSITIVE_IMAGE_TRAITS` + the high-Resilience positive path (see architectural) | R2 |
| Gets Ahead | + `POSITIVE_IMAGE_TRAITS` + `IMAGE_DRIVER_SETS['Gets Ahead']=['COMPETITIVE']` + high-Resilience path | R2 |
| Falls Behind | + `NEGATIVE_IMAGE_TRAITS` + `IMAGE_DRIVER_SETS['Falls Behind']=['TIMID']` | R2 |
| First Pitch Slayer | + `POSITIVE_IMAGE_TRAITS` + `IMAGE_DRIVER_SETS['First Pitch Slayer']=['COMPETITIVE','EGOTISTICAL']` | R2 |
| First Pitch Prayer | + `NEGATIVE_IMAGE_TRAITS` + `IMAGE_DRIVER_SETS['First Pitch Prayer']=['TIMID','DROOPY']` | R2 |

**Architectural (E2 + the high-Resilience path) in `buildProposalBase`:** (1) add the **Charisma factor** (mirrors
`resilienceTilt`: for a charisma-sensitive trait, low Charisma raises P) — first consumer K Neglector. (2) add a
**positive high-Resilience path** (today `resilienceTilt` only *lowers* negatives; there is no path for high
Resilience to *raise* a positive trait) — first consumers Composed/Gets Ahead. *(Note: §VI.3:116 implies the same
high-Resilience lean for the whole Composure-positive cluster — Clutch/RBI Hero/Rally Starter/Pinch Perfect/Rally
Stopper; v1 scopes the path to the §0.6-named traits and leaves the cluster extension as a tuning question.)*

**NOT a code delta (verified):** Two Way — "Ambitious" is the Layer-1 modifier tilt (already fires via
`POSITIVE_IMAGE_TRAITS` membership) and `IMAGE_DRIVER_SETS['Two Way (*)']=['EGOTISTICAL']` supplies Layer 2, so the
code already does "↑Amb + Egotistical." K Collector / Ace Exterminator — code already correct (in positive set +
driver). Stale code entries (Stimulated, Magic Hands, Base Jogger, Consistent, Volatile) are out of v1 scope — flag only.

### 0.8 ACQUISITION GATES — what IS vs ISN'T a requirement (JK-confirmed 2026-06-18)

- **Personality is a TILT, never a gate.** P = reality proxy (percentile) × bounded personality/morale tilts
  (~0.8–1.2, sim-tunable; default ×1.0 when they don't apply). A "wrong" personality NEVER disqualifies a player —
  strong performance still earns the trait; a matching personality just raises the odds. P is the single comparison
  currency for gain-high / lose-low / 2-trait-cap displacement.
- **The actual gates (none are personality):** (1) **ROLE** — pitcher-only / position-only / universal
  (`isTraitEligibleForRole`); (2) **MIN-SAMPLE valve** (§VI.1) — the signal must clear a minimum sample or the trait
  stays dormant; (3) the **2-trait cap + no-opposite-pair** (can't hold a trait and its opposite); enrichment-gated
  **OPT-IN** traits additionally need their optional signal logged.
- **Two Way is pitcher-only with NO trait→trait gateway (JK 2026-06-18):** a two-way player can only ever hold
  **PITCHER** traits — earning Two Way does NOT open the position-player (batting) trait pool. The on-grant random
  IF/OF/C fielding position is defensive-roster only. **There are now NO trait→trait eligibility dependencies in the model.**

### 0.9 R1 PROXY DERIVATIONS — ruled (JK 2026-06-18); build R1 to these

All rates are **per plate appearance (PA)** = the player's `atBatEvents` count (as batter or pitcher) unless noted.
Percentile = vs the role-bucketed peer pool (the scorer, basis 'none'); the min-sample valve always applies.

- **Strikeout family** (K Collector / K Neglector / Whiffer / Tough Out / Easy Target): strikeout = the FULL K-family
  `{K, Kc, Ꝁ, D3K, WP_K, PB_K}` (NOT just K/Kc); rate = K/PA; the "low" traits (K Neglector, Tough Out) use `1 − K/PA`.
  *(R1-a.)*
- **Slow Poke** = `DP`/PA · **Sprinter** = `FC`/PA · **Mind Gamer** = `(BB+IBB)`/PA. *(R1-a.)*
- **Pick Officer / Easy Jumps** = opposing steal-success rate vs the pitcher (`stolenBase.isSuccessful` joined via
  `runnerAttribution.pitcherId`); Pick Officer = `1 − rate`. *(R1-a.)*
- **Big Hack / Little Hack — OPTION B (percentile-merge), JK-ruled:** percentile each player's HR-rate (HR/PA) AND
  AVG (hits/AB) vs the peer pool, then **Big Hack score = (HR-rate %ile + (1 − AVG %ile)) / 2**;
  **Little Hack = ((1 − HR-rate %ile) + AVG %ile) / 2**. Both inputs on the 0–1 percentile scale → fair blend (no
  unit mismatch). The merged score is the signalValue (needs a within-builder percentile pre-pass over HR-rate + AVG).
  *(R1-b.)*
- **Base Rounder** = a success is a runner advancing **beyond the forced minimum** (1st→3rd on a single, scoring from
  2nd on a single, etc.) over the runner's advancement opportunities, from `atBat.runnerOutcomes` (from→to). The forced
  minimum is the standard baseball forced-advance model (port `isRunnerForced`/`getMinimumAdvancement` from
  `atBatLogic.ts` into the engine — do NOT import the UI-layer file; a non-forced runner's minimum is to stay put).
  **JK rulings 2026-06-18 (R1-b):** (1) DENOMINATOR = every recorded advancement is a chance, **including being thrown
  out trying for the extra base** (`toBase:'out'` counts as a non-success opportunity; a held runner `toBase:'end'`
  is NOT a chance). (2) SCOPE = **include the batter-runner's own stretches** (`fromBase:'batter'` — single→double
  etc.; the batter's forced minimum = the base the result entitles them to), not only true baserunners. Credited to
  `runnerOutcomes[].runnerId`. *(R1-b.)*
- **Distractor** (the pitcher fails more with this runner on) = success is the batter **reaching base (hit OR walk OR
  HBP)** while the Distractor-owner is the runner on **1B or 2B**; denominator = PAs where the owner is on 1B/2B;
  credited to the **runner** (owner), not the batter. *(R1-b.)*
- **Two Way** = ONE earn-signal: elite hitting for a pitcher = the pitcher's **wOBA** (`calculateWOBA`,
  `bwarCalculator.ts`) percentile vs the **pitcher** peer pool (valve-gated → super-rare). **R1-b3 BUILD (JK ruling
  2026-06-18 — "earn-signal now, defer C/IF/OF"):** the builder computes each PITCHER-role player's batting wOBA from
  their `batterId` at-bats and emits ONE candidate under a single representative canonical variant **`Two Way (C)`**
  (the v1 label — all three variants share the EGOTISTICAL driver + POSITIVE membership, so the choice is cosmetic for
  the earn-signal). sampleSize = batting PA; basis `'none'`. Pitcher-only, NO batting-trait gateway (§0.8). No
  acquisition change (the triplet is already POSITIVE + EGOTISTICAL).
  **PRE-ACT-TRAITS-1 BUILD (2026-06-18 — the C/IF/OF family, realized in the BUILDER, no grant-path/scorer/acquisition
  surgery):** the builder assigns each two-way pitcher's variant by a **deterministic FNV-1a hash of `playerId` mod 3 →
  `Two Way (C)/(IF)/(OF)`** (stable forever; pseudo-random across pitchers; no `Math.random`), and emits ONE candidate
  under that variant. The peer pool **canonicalizes all 3 variants to a single `Two Way` family key**
  (`poolTraitKey`), so a pitcher's wOBA is percentiled vs ALL two-way pitchers (shared pool), and the per-pitcher seed
  keeps re-evaluate-to-drop stable (same variant each cycle → a held variant always matches its own candidate). Position
  assigned at BUILD via the seed (outcome-identical to "at grant": a stable pseudo-random C/IF/OF per pitcher) —
  sidesteps the anticipated L9b-3c grant-path change. All 3 variants enter `BUILDABLE_TRAITS`; no scorer/acquisition/
  grant change. *(R1-b3 = earn-signal under `Two Way (C)`; PRE-ACT-TRAITS-1 = the full C/IF/OF family.)*
- **Crossed Up** = passed-ball events (attributed to the pitcher via `wildPitchOrPassedBall.pitcherId`) per
  **batters-faced** (the pitcher's PA count). **OPT-IN** (dormant until PBs are logged + min-sample). *(R1-b2.)*
- **Bunter** (JK ruling 2026-06-18 — **VOLUME/frequency, NOT a success rate**): signalValue = successful sacrifice
  bunts (`result==='SAC'`) **per PA** (position batters), percentiled vs the position pool. Failures don't drag it
  (numerator = SAC successes only); computable from the standard `SAC` result, so **no longer enrichment-gated**.
  *(R1-b2. NOTE — sparse-signal caveat: when few/no SAC are logged league-wide, the rate-signal family's
  `getPercentile`-on-mostly-zeros inflates everyone's percentile; this is a known property of ALL the rate signals
  [Slow Poke/Sprinter/Bunter/etc.], acute for the sparsest — a §16 sim-tune / pooling-convention FINDING, not an
  R1-b2 blocker; build-DARK contains it.)*
- **Utility** = fielding perf at a **non-primary** position — requires threading a `primaryPositionByPlayer` map into
  `SeasonTraitCandidateInput` (mechanical input addition). *(R1-b.)*

### 0.10 R2 PROXY DERIVATIONS — ruled (JK 2026-06-18); build R2 to these

JK ruled "do all of R2 now" (the handedness splits build DARK + dormant until the handedness join is wired). All rates
per the player's relevant PA bucket; percentile vs the role-bucketed peer pool; min-sample valve always applies.

- **Pitcher count-family** (BB Prone / Composed / Gets Ahead / Falls Behind): data proxy = **walks-allowed rate** =
  `(BB+IBB) / batters-faced` (the pitcher's PA count). **BB Prone / Falls Behind = the rate (high walks)**;
  **Composed / Gets Ahead = `1 − rate` (low walks — inverse)**. All four share the SAME walks-allowed signal in their
  high/low pair; personality TILT differentiates them (§0.7). Buildable now (standard results, no count needed). *(R2.)*
- **First Pitch Slayer / Prayer** (position; OPT-IN on `enrichment.pitchesInAtBat===1`): on logged first-pitch PAs,
  **JK ruling 2026-06-18 = HIT vs OUT.** Denominator = first-pitch PAs that ended in a **hit OR an out** (a first-pitch
  HBP / reached-on-error is excluded — neither a hit nor an out). **Slayer = hits / (hits+outs)** (favorable);
  **Prayer = outs / (hits+outs) = 1 − Slayer** (unfavorable). Hits = `{1B,2B,3B,HR,ITPHR,GRD}`; outs = `isOut(result)`
  (a first-pitch K is impossible — K needs ≥3 pitches). NOT personality-earned; personality is the tilt (§0.7). *(R2.)*
- **Handedness platoon splits** (6; OPT-IN; **DORMANT until the handedness join is fed** — thread
  `pitcherHandByPlayer` + `batterHandByPlayer` maps into `SeasonTraitCandidateInput`, mirror Utility's
  `primaryPositionByPlayer`; the hook that populates them is a deferred wiring step. `opposingHand` is currently
  hardcoded `'R'` in the reconstructor — the splits read the threaded maps, NOT that field). **JK rulings 2026-06-18:**
  - **CON vs LHP / CON vs RHP** (position) = **avoid-strikeout rate `1 − K/PA`** in PAs where the OPPOSING PITCHER is
    L / R (`pitcherHandByPlayer.get(pitcherId)`). Higher = better contact vs that hand.
  - **POW vs LHP / POW vs RHP** (position) = **ISO** = `(TB − H) / AB` in PAs vs that-handed pitchers (TB: 1B=1,2B/GRD=2,
    3B=3,HR/ITPHR=4; H = hits; AB = PA − BB/IBB/HBP/SF/SAC). Higher = more power vs that hand.
  - **Specialist** (pitcher) = **`1 − BAA` vs SAME-handed batters**; **Reverse Splits** (pitcher) = **`1 − BAA` vs
    OPPOSITE-handed batters**. BAA (batting-average-against) = `hits-allowed / AB` by that cohort of batters; same/opposite
    = `batterHandByPlayer.get(batterId)` vs `pitcherHandByPlayer.get(pitcherId)`. **JK chose BAA over K-rate so Specialist
    isn't conflated with K Collector.** Low BAA = good → invert (`1 − BAA`) so a tough pitcher ranks HIGH. **Switch
    hitters (`batterHand==='S'`) are EXCLUDED from the same/opposite cohorts** (no fixed hand). *(R2.)*

### 0.11 R3 PROXY DERIVATION — ruled (JK 2026-06-18); build R3 to this

- **Ace Exterminator** (position; OPT-IN; **DORMANT until the E1 grade join is fed**) = the batter's **reached-base
  rate vs A−-or-better opposing pitchers**. **JK ruling 2026-06-18 = REACHED BASE** (hit OR walk OR HBP — the same set
  as Distractor: `{1B,2B,3B,HR,ITPHR,GRD,BB,IBB,HBP}`). For each PA, the opposing pitcher's grade =
  `pitcherGradeByPlayer.get(atBat.pitcherId)`; the PA counts iff that grade is **A− or better**
  (`SMB4_GRADE_TO_INDEX[grade] >= SMB4_GRADE_TO_INDEX['A-']`, from `smb4GradeEmulator.ts`). Denominator = PAs vs A−+
  pitchers; numerator = those where the batter reached base. **E1 plumbing:** thread an OPTIONAL
  `pitcherGradeByPlayer?: ReadonlyMap<string, Smb4Grade>` into `SeasonTraitCandidateInput` (mirror the handedness maps).
  **WIRED (W1, JK ruling 2026-06-18 "wire all 4 now"):** the grant hook `franchiseTraitGrantCompute.ts` populates this
  map on-demand from each pitcher-role player's CURRENT ratings via the canonical pure `scoreSmb4Player` (the same
  function the app-wide grade-freshness ticket uses → no divergence; on-demand from current ratings is itself "fresh");
  the grade-freshness ticket may later add caching/unification. Still flag-gated (`isFranchisePhase2TraitsEnabled` OFF)
  → build-dark. No acquisition change (Ace Exterminator is already POSITIVE + COMPETITIVE/EGOTISTICAL). *(R3 = the LAST
  earnable v1 trait → 47/47; W1 wires it + the handedness/Utility maps live-dark.)*

---

---

> **§A–§D are HISTORICAL — source-citation appendix only.** They predate the §0 rulings and some rows still
> carry pre-ruling "personality-primary (no data proxy)" phrasing. **§0.6 is authoritative and overrides any
> conflict.** Kept for the per-trait source citations (smb4_traits_reference / traitInteractionMatrix / etc.).

## A. PERSONALITY-MODIFIER TENSIONS (chemistry-type vs coded IMAGE_DRIVER)
- Stealer — EXPLICIT MISMATCH: source 1 chemistry CRAFTY vs source 4 IMAGE_DRIVER_SETS ['COMPETITIVE','TOUGH'] (traitAcquisition.ts:146). Row reconciles as 'two different uses' (static seed vs acquisition axis), but the chemistry word and the image driver disagree on the surface.
- Bad Jumps — DIVERGENCE: source 1 chemistry CRAFTY (static seed) vs source 4 IMAGE_DRIVER_SETS ['RELAXED','DROOPY'] (traitAcquisition.ts:149). Same seed-vs-axis split as Stealer.
- Big Hack — DISCREPANCY/OMITTED: source 1 SCHOLARLY chemistry but source 4 IMAGE_DRIVER_SETS has NO Big Hack entry (traitAcquisition.ts:133-161); falls back to universal Ambition tilt only.
- Little Hack — DISCREPANCY/OMITTED: source 1 SCHOLARLY chemistry but source 4 omits Little Hack from IMAGE_DRIVER_SETS; universal-tilt fallback.
- First Pitch Slayer — DISCREPANCY/OMITTED: source 1 COMPETITIVE chemistry (+§VI.3:118 big-game axis) but source 4 has no IMAGE_DRIVER_SETS entry; universal Ambition-only fallback.
- First Pitch Prayer — DISCREPANCY/OMITTED: source 1 COMPETITIVE chemistry but source 4 omits it from BOTH POSITIVE and NEGATIVE image sets and has no driver entry; universal resilience-tilt fallback.
- Composed — OMITTED: source 1 DISCIPLINED chemistry but source 4 lists no IMAGE_DRIVER. Row states 'no disagreement, but SOURCE 4 omits it' (also flagged measurement-dependent / NOT personality-primary per audit:164).
- K Neglector — OMITTED: source 1 COMPETITIVE chemistry but source 4 has no IMAGE_DRIVER entry; treated as neutral/mechanical (strikeout-frequency), so omission is plausibly by-design but worth noting against the COMPETITIVE chemistry word.

---

## B. NEEDS-RULING (15) — JK decides before build

### K Collector
- **Q:** Audit (TRAIT_DETECTION_SCOPE_AUDIT.md:125) classifies K Collector as 'wrongly-dormant' (Tier 1, strikeout-outcome proxy available). Should it be added to BUILDABLE_TRAITS in traitCandidateBuilder.ts:37-54? Is the auto-persisted strikeout-rate-vs-peers signal sufficient to build now without ball-strike count precision (Q1-deferred)?
- **Captain rec (not a ruling):** RECOMMENDATION (not a ruling): BUILD NOW. The strikeout-outcome proxy fires on every auto-persisted AB record, IMAGE_DRIVER alignment is clean (COMPETITIVE chemistry == IMAGE_DRIVER ['COMPETITIVE','EGOTISTICAL']), and the count predicate only refines, not gates. Treat 2-strike-count precision as a Phase-2 enhancement, not a v1 blocker.

### K Neglector
- **Q:** Same as K Collector: should K Neglector move to BUILDABLE_TRAITS? Its signal (low-strikeout / permissive rate in 2-strike scenarios) is measurable from existing auto-persisted ABs. Does the personality-primary mechanism apply, or is it measurement-sufficient to build now without count precision?
- **Captain rec (not a ruling):** RECOMMENDATION: BUILD NOW as the negative mirror of K Collector, lose-low. Signal is auto-persisted and symmetric. Note the IMAGE_DRIVER omission (source 4 lists no entry) — accept it as a mechanical/strikeout-frequency trait with universal tilt; do not invent a driver set.

### Whiffer
- **Q:** Audit:128 flags Whiffer as 'wrongly-dormant' (Tier 1, strikeout-outcome proxy). Should it be added to BUILDABLE_TRAITS? Is the high-strikeout-rate-vs-peers signal (auto-persisted) sufficient to build now, or is the personality-primary tilt the required gate?
- **Captain rec (not a ruling):** RECOMMENDATION: BUILD NOW, lose-low. Strikeout outcome auto-persists independent of count; IMAGE_DRIVER ['EGOTISTICAL'] (traitAcquisition.ts:160) is defined and aligns with §VI.3:119 free-swinger. Personality is a TILT, not the gate.

### Tough Out
- **Q:** Audit:127 classifies Tough Out as 'wrongly-dormant' (Tier 1, strikeout-avoidance proxy). Should it move to BUILDABLE_TRAITS? The matrix note ('computable standoff vs K Collector at 0-2') implies a dual-signal model — is the measured strikeout-avoidance rate sufficient to build now, or is personality-primary the required gate?
- **Captain rec (not a ruling):** RECOMMENDATION: BUILD NOW as gain-high (low strikeout rate vs peers = high-contact). Signal is the auto-persisted inverse of Whiffer; IMAGE_DRIVER ['TOUGH'] is defined. The K-Collector standoff is a within-AB display nuance, not an acquisition blocker.

### Composed
- **Q:** TRAIT_DETECTION_SCOPE_AUDIT.md:164 explicitly EXCLUDES Composed from the Q12 personality-primary ruling ('NOT personality-primary -> needs count, Q1-deferred'), unlike its mirror BB Prone. Should Composed be (a) built on count-capture as Q1-scope input, (b) deferred to Phase 2, or (c) granted personality-primary tilt despite the audit flag to match the Gets Ahead / Falls Behind treatment?
- **Captain rec (not a ruling):** RECOMMENDATION: option (c) reconcile UP to personality-primary for v1 parity. BB Prone (its negative mirror) is already 'fully-specced' on the personality-primary mechanism with the identical count-absent constraint; leaving Composed measurement-dependent creates an asymmetric pair. Build it on DISCIPLINED + high-Resilience tilt now, and add count precision in Phase 2. If JK prefers strict audit-fidelity, fall back to (b) defer.

### Big Hack
- **Q:** Does Big Hack merit an explicit IMAGE_DRIVER_SETS entry (source 4, traitAcquisition.ts:133-161), given source 1 SCHOLARLY chemistry but a current omission that forces universal-Ambition-only acquisition? And does §VI.3:122 'personality-primary' mean the acquisition engine should suppress the count predicate entirely (morale/personality/resilience only), or synthesize a count-proxy from the morale envelope?
- **Captain rec (not a ruling):** RECOMMENDATION: (1) ADD an IMAGE_DRIVER entry — ['EGOTISTICAL'] (free-swinger / big-cut, matching the §VI.3 free-swinger family and the +POW/-CON profile) rather than copying Tough Out's ['TOUGH']. (2) Personality-primary should SUPPRESS the count predicate for v1 (no morale-synthesized count proxy — that adds unfalsifiable model risk). Revisit when ball-strike count is captured (Q1).

### Little Hack
- **Q:** Does Little Hack merit an explicit IMAGE_DRIVER_SETS entry? Source 4 omits it; should it share ['TOUGH'] with Tough Out or get a distinct SCHOLARLY/contact-discipline set? And should personality-primary suppress the count predicate entirely or synthesize a contact-rate proxy from morale/mojo for v1?
- **Captain rec (not a ruling):** RECOMMENDATION: (1) ADD ['DISCIPLINED'-flavored -> map to 'TOUGH'] as the closest canonical driver, since Little Hack is the protect-the-plate / contact-over-power sibling (mirror of Big Hack). (2) SUPPRESS the count predicate for v1 personality-primary; no morale-synthesized proxy. Keep Big/Little Hack symmetric in whatever JK chooses.

### First Pitch Slayer
- **Q:** Should First Pitch Slayer get an explicit IMAGE_DRIVER_SETS entry (source 1 COMPETITIVE + §VI.3:118 big-game axis suggest ['COMPETITIVE','EGOTISTICAL'], but source 4 omits it)? And does the opt-in pitchesInAtBat==1 condition (Tier 5) leave it dormant until the user logs first-pitch enrichment, or should an AB-outcome fallback proxy ('reached base on AB #1') serve, with personality-primary as a secondary earner when count signal is thin?
- **Captain rec (not a ruling):** RECOMMENDATION: (1) ADD IMAGE_DRIVER ['COMPETITIVE','EGOTISTICAL'] — consistent with the chemistry word and the spotlight axis. (2) Keep the precise signal opt-in (Tier 5) but ALSO allow personality-primary as the v1 secondary earner so the trait is not fully dormant under Franchise-lite. Do NOT add a 'reached base on AB#1' proxy — first-pitch contact is not the same as reaching base, so that proxy would be misleading.

### First Pitch Prayer
- **Q:** Should First Pitch Prayer get an explicit IMAGE_DRIVER_SETS entry? It is absent from BOTH POSITIVE and NEGATIVE image sets and has no driver entry; source 1 COMPETITIVE + negative effect suggest ['TIMID','DROOPY'] per §VI.3:116. Does opt-in pitchesInAtBat==1 (Tier 5) block it entirely in v1, or should personality-primary earn it when count enrichment is thin?
- **Captain rec (not a ruling):** RECOMMENDATION: (1) ADD IMAGE_DRIVER ['TIMID','DROOPY'] as the negative mirror of First Pitch Slayer (Composure-axis -negative). (2) Allow personality-primary (low-Resilience / TIMID tilt) as the v1 earner so the negative trait is not unreachable when first-pitch enrichment is unlogged. Keep the Slayer/Prayer pair symmetric.

### Slow Poke
- **Q:** The named beat_throw/beat_runner play types never persist (dead signal). Is the infield-single AB-outcome proxy (derived from base-advancement / infield-single rate) a sufficiently discriminating signal for Slow Poke, or should the trait remain cut from the v1 earnable set? (Audit FLAGGED-UNCERTAIN, lines 61-62.)
- **Captain rec (not a ruling):** RECOMMENDATION: ACCEPT the infield-single proxy but flag it LOW-CONFIDENCE. For a negative speed trait, lose-low on a below-peer infield-single rate is directionally correct and the proxy auto-persists. Gate acquisition on a higher sample-size threshold than the strong-signal traits to offset proxy noise. If JK wants purity, fall back to leaving it dormant until a real speed/beat-throw field is captured.

### Sprinter
- **Q:** Named beat_throw/beat_runner signals are dead. Is the infield-single AB-outcome proxy (extra bases on ground-ball/infield plays) sufficiently discriminating for Sprinter (gain-high), or should it remain cut from v1 earnable? (Audit FLAGGED-UNCERTAIN, lines 62-63.)
- **Captain rec (not a ruling):** RECOMMENDATION: ACCEPT the infield-single proxy, LOW-CONFIDENCE, as the positive mirror of Slow Poke (gain-high on above-peer infield-single rate). Same higher sample-size threshold to absorb proxy noise. Keep the Sprinter/Slow Poke decision symmetric — accept both or defer both.

### Noodle Arm
- **Q:** Is the bWAR-borrowed expected-extra-bases proxy acceptable for Noodle Arm measurement, or should it be cut from the earnable v1 set? Audit §Reverse-findings marks it 'suspect' (currently IN BUILDABLE_TRAITS but flagged as possibly firing on a weak/borrowed proxy). Decision: (a) accept proxy + stay built, (b) cut from earnable v1, or (c) defer pending throw-velocity auto-capture.
- **Captain rec (not a ruling):** RECOMMENDATION: option (c) DEFER pending a real throw-velocity / non-max-throw signal. The trait fires only on NON-max-power throws, which the borrowed bWAR extra-bases proxy cannot isolate (it conflates range, positioning, and arm). Keeping it 'built' on a non-discriminating proxy risks false grants. If JK wants coverage now, option (a) is acceptable ONLY with an explicit low-confidence label; do not silently leave it as-is.

### Durable
- **Q:** (1) Is the 0.75 fitness-decay-rate factor calibrated or still a placeholder pending playtest? (2) What is the live measurement source — a hidden durability-modifier multiplier (inverse-exposed), auto-injury events with accumulator thresholds, or an absence-frequency proxy? (3) Should Durable stay UNIVERSAL or scope to position (catcher-most-detectable per §VI.2:109)? The injury accumulator is currently unbuilt.
- **Captain rec (not a ruling):** RECOMMENDATION: (1) Treat 0.75 as an UNCALIBRATED placeholder — flag it, do not ship as a real number. (2) Prefer the auto-injury-event accumulator (season-scaled thresholds) over hidden-modifier exposure, because it is falsifiable and persists; games-played has no discriminating power per the audit. (3) Keep role UNIVERSAL but weight detectability by position (catcher-most-detectable) inside the percentile pool rather than changing the role field. Build the accumulator before declaring Durable earnable.

### Injury Prone
- **Q:** (1) Is the 1.25 decay-rate factor calibrated? (2) What is the live signal for increased injury chance / faster fitness decay — auto-injury accumulator, hidden-modifier exposure, or games-played inverse? (3) How does low-Resilience x Injury Prone combine in acquisition (both pessimistic anchors) — additive or multiplicative?
- **Captain rec (not a ruling):** RECOMMENDATION: Mirror Durable. (1) 1.25 is an uncalibrated placeholder. (2) Use the same auto-injury accumulator (lose-high), shared engine with Durable. (3) Combine low-Resilience x Injury Prone ADDITIVELY in the tilt, not multiplicatively — multiplicative double-counting of two pessimistic anchors would over-grant the negative trait. Build the shared accumulator first; resolve Durable and Injury Prone together.

### Ace Exterminator
- **Q:** Is the opponent-pitcher-grade roster join wired in the franchise game path (audit:63 / :156 says 'unverified', verdict 'wrongly-dormant ... needs roster join')? If the join is absent, how should the A-or-better opponent-grade condition be proxied (opponent-team average grade, ERA-based threshold, or leave dormant)?
- **Captain rec (not a ruling):** RECOMMENDATION: First VERIFY the opponent-pitcher-grade roster join in the franchise path before any proxy work — this is a wiring fact, not a design choice, and should be confirmed with a code trace. If the join exists, build directly (IMAGE_DRIVER ['COMPETITIVE','EGOTISTICAL'] already defined and aligned). If it is genuinely absent, prefer an ERA/opponent-grade threshold proxy over a team-average proxy (individual matchup is the trait's whole point), but label it interim until the per-pitcher grade join lands.

---

## C. FULLY-SPECCED (36) — build to these

| Trait | Role | Chem | Reality signal (measurement) | Personality modifier | Threshold | Persist | Built |
|---|---|---|---|---|---|---|---|
| BB Prone | pitcher | DISCIPLINED | personality-primary (SUPERSEDED — §0.6 has the ruled data proxy) / §VI.3:122 'Personality is PRIMARY where the measured signal is thin (Stimulated, Gets Ahead/Falls Behind, Big/Little Hack)'. Ball-strike count field ABSENT (TRAIT_SIGNAL_CERTIFICATION.md:52 'Ball-strike count (ABSENT)'); BB Prone blocked by 'count field missing'. Per §VI.3:122 and Q12 ruling, BB Prone earns from personality tilt even when count precision unavailable. | DISCIPLINED chemistry (smb4_traits_reference.md:89). BB Prone not in IMAGE_DRIVER_SETS (traitAcquisition.ts lines 98-161); per §VI.3:120 'No personality image (universal tilt only)' — it is neutral/mechanical. HOWEVER, TRAIT_SIGNAL_CERTIFICATION.md:52 & §VI.3:122 gate it on personality-PRIMARY mechanism for v1 (count unavailable). Reconciliation: BB Prone is built on personality + Resilience-low tilt, pending count capture. | lose-low: pitcher with low-Resilience / high-Volatility personality = higher loss probability for BB Prone (negative trait) | no — count field does not persist (gameState.balls/strikes reset every AB, advanceCount has zero callers; TRAIT_SIGNAL_CERTIFICATION.md:52 'ABSENT') | — |
| Crossed Up | pitcher | SCHOLARLY (source 1:150) | [SUPERSEDED — §0.6: proxy = passed-ball events, OPT-IN] (historical: source 3:VI.3 — Crossed Up not listed in any personality-axis group, implying universal tilt only per source 3:122); conditional on passed_ball events logged per source 3:156, 'Tier 5 conditional' — fired only when user logs PBs (source 3:47: 'conditional on users logging PBs') | universal tilt only: source 3:122 'Crossed Up not listed [in image axes]'; source 4:133-161 omits Crossed Up from IMAGE_DRIVER_SETS; §VI.0 (source 3:99) applies universal Ambition↑-positive / low-Resilience↑-negative rule | gain-high: when passed_ball events exceed peer rate + personality ambition/resilience tilt ≥ gainThreshold (0.75); lose-low: ≤ loseThreshold (0.35) | opt-in (passed_ball events; source 3:156 'only if the user logs PBs'; source 3:47 'sparse / opt-in (signal exists but is manual)') | — |
| Easy Jumps | pitcher | CRAFTY | stolen_base and caught_stealing events allowed by pitcher, peer-relative opposing steal-success rate (higher rate = Easy Jumps signal). Delta SPD +7 at L2 (source 2:340, A8 mirroring Stealer), but INVERTED polarity — pitcher's Easy Jumps BUFFS opposing stealers (loses high). | No IMAGE_DRIVER_SETS entry (source 4: traitAcquisition.ts:133-161 omits it). Source 3 §VI.3:120 lists Easy Jumps as 'no personality image (universal tilt only)' — mechanical trait. Personality is NOT a PRIMARY driver. | lose-low (higher opposing steal-success rate vs peers = acquisition, negative trait) | yes — SB-allowed/IP rate auto-computed from events (source 3: TRAIT_DETECTION_SCOPE_AUDIT.md:130, 'wrongly-dormant...Tier 1') | — |
| Falls Behind | pitcher | SCHOLARLY | personality-primary (SUPERSEDED — §0.6 has the ruled data proxy) / §VI.3:122 'Personality is PRIMARY where the measured signal is thin'. Ball-strike count ABSENT (TRAIT_SIGNAL_CERTIFICATION.md:52). Per Q12 / JK 2026-06-16 ruling (§VI.3 line 122), Falls Behind earns from personality tilt even when count precision unavailable. | SCHOLARLY chemistry (smb4_traits_reference.md:149). Falls Behind not in IMAGE_DRIVER_SETS (traitAcquisition.ts); treated as neutral/mechanical pending count. Per §VI.3:122, Falls Behind is personality-PRIMARY in v1 (count unavailable) — acquires via personality-tilt (Resilience-low / TIMID-leaning). | lose-low: pitcher with low-Resilience / TIMID personality = higher loss probability for Falls Behind (negative trait) | no — count field does not persist (TRAIT_SIGNAL_CERTIFICATION.md:52 'ABSENT') | — |
| Gets Ahead | pitcher | SCHOLARLY | personality-primary (SUPERSEDED — §0.6 has the ruled data proxy) / §VI.3:122 'Personality is PRIMARY where the measured signal is thin (Stimulated, Gets Ahead/Falls Behind, Big/Little Hack)'. Ball-strike count field ABSENT (TRAIT_SIGNAL_CERTIFICATION.md:52); Gets Ahead earns from personality tilt even when count precision unavailable, per Q12 / JK 2026-06-16 ruling (TRAIT_SIGNAL_CERTIFICATION.md:§VI.3 line 122). | SCHOLARLY chemistry (smb4_traits_reference.md:135). Gets Ahead not in IMAGE_DRIVER_SETS (traitAcquisition.ts lines 98-161); per §VI.3:120 'No personality image (universal tilt only)'. HOWEVER §VI.3:122 mandates personality-PRIMARY mechanism for Gets Ahead v1 (count unavailable). Reconciliation: built on personality-tilt (Resilience-high / TOUGH-leaning), pending count capture. | gain-high: pitcher with high-Ambition / COMPETITIVE personality = higher acquisition probability | no — count field does not persist (TRAIT_SIGNAL_CERTIFICATION.md:52 'ABSENT') | — |
| Meltdown | pitcher | SPIRITED | pitcher-unit signal: count of instances where a meltdown (≥4 consecutive baserunners, no outs) occurred / distinct game/inning/half units worked by that pitcher. Denominator is distinct game/pitcher/inning/half units (traitCandidateBuilder.ts:398-402); note approximation: consecutive-reach field not persisted, derived from outsRecorded + reach events (TRAIT_DETECTION_SCOPE_AUDIT.md:114, accuracy caveat). | SPIRITED chemistry (source 1:117) + IMAGE_DRIVER_SETS maps Meltdown to ['TIMID', 'DROOPY'] (traitAcquisition.ts:145). §VI.3 Composure: −negative (TRAIT_SIGNAL_CERTIFICATION.md:116). Personality TILT; resilienceTilt applied. | gain-high: above peer percentile for meltdown frequency; lose-low: below percentile | yes* — outsRecorded + reach events auto-persist; consecutive-reach field itself NOT persisted, reconstructed from events (TRAIT_DETECTION_SCOPE_AUDIT.md:114) | Y |
| Pick Officer | pitcher | CRAFTY | stolen_base and caught_stealing events vs pitcher, steal success rate (opposing runners' perspective). Delta SPD -7 at L2 mirrors Stealer L2 +7 (source 2:608, A8 'magnitude unpublished; -7 SPD mirrors Stealer'). Computed as peer-relative opposing steal-success rate (lower rate = better Pick Officer performance). | No IMAGE_DRIVER_SETS entry for Pick Officer (source 4: traitAcquisition.ts:133-161 omits it). Source 1 chemistry CRAFTY; source 3 §VI.3:120 lists Pick Officer as 'no personality image (universal tilt only)' — mechanical/platoon trait. Personality is NOT a PRIMARY driver. Acquisition tilts only on Ambition/Resilience universal axes, not a dedicated image-axis modifier. | gain-high (lower opposing steal-success rate vs peers = acquisition) | yes — SB/CS between-play events auto-written (source 3: TRAIT_DETECTION_SCOPE_AUDIT.md:129, 'wrongly-dormant...Tier 1' but signal persists) | — |
| Rally Stopper | pitcher | SPIRITED | pitcher outcome under ≥2 runners: pitcherFavorable = (out recorded AND no runs scored) among activations; success rate = favorable outcomes / total ≥2-runner at-bats (traitCandidateBuilder.ts:384) | SPIRITED chemistry (source 1:102) + IMAGE_DRIVER_SETS maps Rally Stopper to ['TOUGH', 'COMPETITIVE'] (traitAcquisition.ts:139). §VI.3 Composure: +positive (TRAIT_SIGNAL_CERTIFICATION.md:116). Personality TILT over outcome/runners signal. | gain-high: above peer percentile for favorable outcomes with runners on; lose-low: below percentile | yes — runners + outcome (outsRecorded, runsScored) auto-populated (TRAIT_SIGNAL_CERTIFICATION.md:33) | Y |
| Reverse Splits | pitcher | CRAFTY (source 1:46) | pitcher strikeout rate vs opposite-handed batter cohort, percentile-ranked vs peer baseline; source 2:691 notes Reverse Splits is 'the only counter to switch hitters in the game' | neutral tilt; source 4:133-161 omits Reverse Splits from IMAGE_DRIVER_SETS | gain-high: strikeout/control percentile ≥ gainThreshold (0.75); lose-low: ≤ loseThreshold (0.35) | opt-in (handedness join per source 3:44, Tier 2) | — |
| Specialist | pitcher | CRAFTY (source 1:45) | pitcher strikeout rate (or opponent batting average / slugging) vs same-handed batter cohort, percentile-ranked vs peer baseline; §VI.0 (source 3:99) governs the percentile acquisition formula | neutral tilt per source 3:120 ('No personality image ... platoon/mechanical-skill traits'); source 4:133-161 omits Specialist from IMAGE_DRIVER_SETS | gain-high: strikeout/control percentile ≥ gainThreshold (0.75); lose-low: ≤ loseThreshold (0.35) | opt-in (handedness join per source 3:44, Tier 2: verify franchise path feeds handedness) | — |
| Surrounded | pitcher | SPIRITED | pitcher outcome under ≥2 runners: pitcherUnfavorable = (reached base OR any run scored) among activations; failure rate = unfavorable outcomes / total ≥2-runner at-bats (traitCandidateBuilder.ts:385) | SPIRITED chemistry (source 1:118) + IMAGE_DRIVER_SETS maps Surrounded to ['TIMID', 'DROOPY'] (traitAcquisition.ts:144). §VI.3 Composure: −negative (TRAIT_SIGNAL_CERTIFICATION.md:116). Personality TILT; resilienceTilt applied. | gain-high: above peer percentile for unfavorable outcomes with runners on (failure measure); lose-low: below percentile | yes — runners + outcome auto-populated | Y |
| Two Way (C) | pitcher | SPIRITED | [SUPERSEDED — §0.6: Two Way is earned by ELITE HITTING for a pitcher (high percentile of wOBA/PA vs the pitcher peer pool), NOT fielding. Historical fielding text retained below for citations only.] fielding performance at catcher (FLD, error rate, or defensive run value) when pitcher appears there. FieldingEvent.position='catcher' paired with pitcher role. Adaptive-Standards peer-relative percentile within pitcher-catching performance pool. Gate: [SUPERSEDED — JK 2026-06-18: Two Way is pitcher-only, a two-way player holds ONLY pitcher traits, NO batting-trait gateway; on grant assign a random IF/OF/C fielding position (defensive roster only). See §0.6/§0.8.] | EGOTISTICAL (traitAcquisition.ts:154) — upper-end ambition tilt; per §VI.3:118 'Big-game/spotlight: ... Two Way ← Ambitious/Egotistical' | gain-high (better catcher defense for a pitcher = elite two-way trait, fires at high percentile) | yes — FieldingEvent.position persists; pitcher-role join available (TRAIT_DETECTION_SCOPE_AUDIT.md Tier 4: 'Two Way (C)/(IF)/(OF) — FieldingEvent.position at non-primary spots persists') | — |
| Two Way (IF) | pitcher | SPIRITED | [SUPERSEDED — §0.6: earned by ELITE HITTING for a pitcher (wOBA/PA vs pitcher pool), NOT fielding. Historical text below.] fielding performance at infield positions (FLD, error rate, defensive run value) when pitcher appears there. FieldingEvent.position in {2B, 3B, SS} paired with pitcher role. Adaptive-Standards peer-relative percentile within pitcher-infield performance pool. Gate: [SUPERSEDED — JK 2026-06-18: pitcher-only, NO batting gateway; assign random IF/OF/C position on grant (defensive only); see §0.8] | EGOTISTICAL (traitAcquisition.ts:155) — upper-end ambition tilt; per §VI.3:118 'Two Way ← Ambitious/Egotistical' | gain-high (better infield defense for a pitcher = elite two-way trait, fires at high percentile) | yes — FieldingEvent.position persists; pitcher-role join available (TRAIT_DETECTION_SCOPE_AUDIT.md Tier 4) | — |
| Two Way (OF) | pitcher | SPIRITED | [SUPERSEDED — §0.6: earned by ELITE HITTING for a pitcher (wOBA/PA vs pitcher pool), NOT fielding. Historical text below.] fielding performance at outfield (FLD, error rate, defensive run value, or assist rate) when pitcher appears there. FieldingEvent.position in {LF, CF, RF} paired with pitcher role. Adaptive-Standards peer-relative percentile within pitcher-outfield performance pool. Gate: [SUPERSEDED — JK 2026-06-18: pitcher-only, NO batting gateway; assign random IF/OF/C position on grant (defensive only); see §0.8] | EGOTISTICAL (traitAcquisition.ts:156) — upper-end ambition tilt; per §VI.3:118 'Two Way ← Ambitious/Egotistical' | gain-high (better outfield defense for a pitcher = elite two-way trait, fires at high percentile) | yes — FieldingEvent.position persists; pitcher-role join available (TRAIT_DETECTION_SCOPE_AUDIT.md Tier 4) | — |
| Bad Jumps | position | CRAFTY | stolen_base and caught_stealing events, rate vs peers (low steal success = signal for trait). Delta SPD -7 at L2 (source 2:166-170, A10 workbook-equivalent, source 3:60 mirroring Stealer L2 +7) | RELAXED/DROOPY per §VI.3 Hustle/aggression axis (source 3:117: 'Bad Jumps...← Relaxed/Droopy, low Ambition'). Source 4 IMAGE_DRIVER_SETS maps Bad Jumps to ['RELAXED', 'DROOPY'] (traitAcquisition.ts:149). CHEMISTRY TYPE SOURCE 1 (CRAFTY) is the static seed; personality is RELAXED/DROOPY. | lose-low (lower steal success rate = acquisition) | yes — SB/CS events auto-written (source 3: TRAIT_DETECTION_SCOPE_AUDIT.md:116, 'correctly-built') | Y |
| Base Rounder | position | DISCIPLINED | extra-base advancement from runnerOutcomes persisted on every runner-scoring path. Delta SPD +2.5 at L2 (source 2:184, A10 workbook-equivalent). Source 3 §VI.4:126 'OF extra-base-credit (borrow bWAR expected-extra-bases; credit the fielder)' — suggests a peer-relative extra-base rate (source 3:36 'runner-advancement') | COMPETITIVE/TOUGH per §VI.3 Hustle/aggression axis (source 3:117: 'Base Rounder ← Competitive/Tough/Ambitious'). Source 4 IMAGE_DRIVER_SETS maps Base Rounder to ['COMPETITIVE', 'TOUGH'] (traitAcquisition.ts:148). | gain-high (higher extra-base advancement rate vs peers = acquisition) | yes — runnerOutcomes auto-written on every runner-advance path (source 3: TRAIT_DETECTION_SCOPE_AUDIT.md:124, 'wrongly-dormant...Tier 1' — currently not in BUILDABLE_TRAITS but should be) | — |
| Bunter | position | SCHOLARLY (source 1:131) | bunt success rate (SAC or bunt-for-hit outcome), percentile-ranked vs peer baseline; source 2:217 notes 'In-game effect is bunt-placement quality (x1/x2/x4); deltas are workbook L2 rating-equivalents'; source 3:57-59 'Bunter ... exitType=bunt auto-stamps for SAC' and notes 'partial (SAC)' signal — bunt-for-hit not captured | Approach/discipline image axis: source 3:119 'Bunter ← Disciplined (grinder/selfless)'; source 4:159 'Bunter: [TOUGH]' in IMAGE_DRIVER_SETS; source 4:275-277 applies imageAxisTilt when isImageDriver=true | gain-high: bunt success percentile ≥ gainThreshold (0.75); lose-low: ≤ loseThreshold (0.35) | partial (source 3:59 'exitType=bunt auto-stamps for SAC only; bunt-for-hit not captured'; source 3:154 verdict 'Tier 5 conditional') | — |
| Butter Fingers | position | DISCIPLINED | error-likelihood on fielding events with difficulty='DIVING' or athletic-move context; Adaptive-Standards peer-relative percentile vs defensive opportunity pool (TRAIT_SIGNAL_CERTIFICATION.md §VI.4: DefensivePlacementRisk.errorLikelihood scale) | TIMID/DROOPY (traitAcquisition.ts:142); imageValence='negative' — Resilience-sensitive pessimistic anchor (§VI.3:116 Composure axis: 'Butter Fingers is doubly sensitive: universal low-Resilience + Timid/Volatile flavor'). Reconciles: chemistry=DISCIPLINED is structural; personality image=TIMID/DROOPY is acquisition modifier (§VI.3:116-122, traitAcquisition.ts) | lose-high (higher error rate = worse, trait flips at low percentile per negative image) | opt-in — FieldingEvent.success=false writes, but fielder attribution + difficulty='DIVING' tag are opt-in per audit (TRAIT_DETECTION_SCOPE_AUDIT.md §D/Tier 1, TRAIT_SIGNAL_CERTIFICATION.md §C) | Y |
| CON vs LHP | position | SPIRITED (source 1:107) | batter contact rate vs LHP pitcher cohort, percentile-ranked vs peer-group baseline, rolled into acquisition via realityPercentile (§VI.0, source 3:99; source 2:274-279 perTier values guide-explicit) | neutral tilt per source 3:120 ('No personality image ... platoon/mechanical-skill traits'); source 4:133-161 IMAGE_DRIVER_SETS omits CON vs LHP entirely, confirming no personality driver | gain-high: contact-rate percentile ≥ gainThreshold (0.75 per tuning); lose-low: ≤ loseThreshold (0.35) | opt-in (handedness join only persists when handednessById supplied at franchise game init per source 3:44) | — |
| CON vs RHP | position | SPIRITED (source 1:108) | batter contact rate vs RHP pitcher cohort, percentile-ranked vs peer baseline; source 2:289 notes righties more common so RHP split preferable | neutral tilt; source 4:133-161 omits CON vs RHP from IMAGE_DRIVER_SETS | gain-high: contact-rate percentile ≥ gainThreshold (0.75); lose-low: ≤ loseThreshold (0.35) | opt-in (handedness join per source 3:44, Tier 2 action: verify franchise path feeds roster handedness) | — |
| Cannon Arm | position | COMPETITIVE | outfield-assist rate (auto-derived from FieldingEvent log, fieldingEventExtractor.ts:563-568) as proxy for max-power throw frequency × success; Adaptive-Standards peer-relative percentile (TRAIT_SIGNAL_CERTIFICATION.md §B Cannon Arm promoted from C; §VI.4 'OF extra-base-credit' for Cannon/Noodle Arm borrow bWAR expected-extra-bases; credit the fielder) | COMPETITIVE (primary chemistry; no secondary from IMAGE_DRIVER_SETS — absent entry means 'neutral tilt', using universal Ambition axis only). Per §VI.3:120 'No personality image' for mechanical-skill traits | gain-high (higher assist rate on outfield throws = better performance, trait fires at high percentile) | yes — FieldingEvent persists on every fielding action; outfield-assist proxy auto-extracted (TRAIT_DETECTION_SCOPE_AUDIT.md Tier 1) | Y |
| Distractor | position | CRAFTY | derived from walks (walks allowed with runner on 1B/2B with 3B open) and rWAR (source 3: TRAIT_DETECTION_SCOPE_AUDIT.md:134 'walks/rWAR (+grind opt-in)' — walk half persists, pitcher-grind half is opt-in). Delta ACC -20 at L2 (source 2:312). Peer-relative walk rate while Distractor owner is on 1B/2B. | No IMAGE_DRIVER_SETS entry (source 4: traitAcquisition.ts:133-161 omits it). Source 3 §VI.3:120 lists Distractor as 'no personality image' — mechanical/situational trait. Personality is NOT a PRIMARY driver. | gain-high (higher walk rate allowed when runner on 1B/2B vs peers = acquisition of batter's Distractor trait) | yes (walk half) — walks persisted on every pitcher at-bat path; pitcher-grind (opt-in enrichment) absent but not blocking. Source 3: TRAIT_DETECTION_SCOPE_AUDIT.md:134, 'wrongly-dormant...Tier 1 (walk half)' | — |
| Easy Target | position | CRAFTY | AB-outcome proxy: strikeout rate (owner as batter; outcome persists independent of enrichment) per TRAIT_DETECTION_SCOPE_AUDIT.md §III Tier 1 line 35 'AB-outcome proxies and event proxies that fire on data written on every record path'. Measured as strikeouts / AB, earning when below peer percentile (low success = high Easy Target acquisition risk). | Reconciled: source 1 (CRAFTY chemistry, no personality association per §VI.3:120 'No personality image (universal tilt only): the platoon/mechanical-skill traits') + source 4 (IMAGE_DRIVER_SETS omits Easy Target; not in traitAcquisition.ts:98-131) → NO PERSONALITY IMAGE. Easy Target listed in TRAIT_DETECTION_SCOPE_AUDIT.md line 123 as Tier 1 (buildable now), but source 4 carries no driver entry, consistent with §VI.3:120 'mechanical-skill trait' classification. Uses only universal Ambition↑-positive / low-Resilience↑-negative per §VI.0, which for a negative trait inverts to: low-Ambition↑ / high-Resilience↑ RAISE acquisition risk. | lose-low (negative trait); batter acquires Easy Target when outcome-quality is poor (strikeout rate percentile ↑); ownership of trait raises enemy pitcher's likelihood of success → lose-high hysteresis (acquire at loseThreshold, persist at lowerThreshold per §VI.0:100 gain-high/lose-low rule). | yes (strikeout outcome auto-persists on every at-bat record path) | — |
| Mind Gamer | position | CRAFTY | Walk rate (owner as batter; +grind opt-in) per TRAIT_DETECTION_SCOPE_AUDIT.md §III Tier 1 line 33 'Mind Gamer + Distractor (walk-rate half persists; pitch-grinding half is opt-in — build the walk half)'. Primary signal: walks / plate appearances (auto-persists). Secondary/opt-in: pitch-grinding/grinding plate appearance length (pitchesInAtBat mode, enrichment). | Reconciled: source 1 (CRAFTY chemistry, no explicit personality axis per §VI.3:120) + source 4 (IMAGE_DRIVER_SETS omits Mind Gamer; not in traitAcquisition.ts:133-161) → NO PERSONALITY IMAGE ENTRY. CRAFTY chemistry has no assigned personality axis in §VI.3; Mind Gamer is mechanical/outcome-based. Falls back to universal Ambition↑-positive per §VI.0. Note: §VI.3:122 'Personality is PRIMARY where signal thin' does NOT apply here — Mind Gamer is outcome-primary (walk rate is measurable, not thin). | gain-high; positive image valence per §VI.3 'Approach/discipline' axis (mental toughness in AB battles); high walk rate → high acquisition likelihood | yes (partial: walk outcome auto-persists; pitch-grinding opt-in) | — |
| POW vs LHP | position | SPIRITED (source 1:109) | batter power (SLG-proxy or ISO) vs LHP pitcher cohort, percentile-ranked vs peer baseline; source 2:641 cross-check: Torrens '25 power ... often bats with 45' at Tier 3 (+20) | neutral tilt; source 4:133-161 omits POW vs LHP from IMAGE_DRIVER_SETS | gain-high: power percentile ≥ gainThreshold (0.75); lose-low: ≤ loseThreshold (0.35) | opt-in (handedness join per source 3:44, Tier 2) | — |
| POW vs RHP | position | SPIRITED (source 1:110) | batter power (SLG/ISO) vs RHP cohort, percentile-ranked vs peer baseline | neutral tilt; source 4:133-161 omits POW vs RHP from IMAGE_DRIVER_SETS | gain-high: power percentile ≥ gainThreshold (0.75); lose-low: ≤ loseThreshold (0.35) | opt-in (handedness join per source 3:44, Tier 2) | — |
| Pinch Perfect | position | DISCIPLINED | substitution context at-bat outcome: reachedBase(result) OR favorableToBattingTeam(atBat) (both reach and WPA-favorable count as success per traitCandidateBuilder.ts:376, dual-valve design); success rate = (reaches + favorable PAs) / total substitution ABs (traitCandidateBuilder.ts:372-378) | DISCIPLINED chemistry (source 1:71) + IMAGE_DRIVER_SETS maps Pinch Perfect to ['TOUGH', 'COMPETITIVE'] (traitAcquisition.ts:137). §VI.4:122 rostering note: Bench classification raises acquisition likelihood (trait is natural to bench/utility carriers). Personality TILT; ambitionTilt applied (positive trait, high Ambition favors gain). | gain-high: above peer percentile for favorable outcomes on first plate appearance as substitute; lose-low: below percentile | yes — batterContext.enteredAs populated from live sub log (useGameState.ts per TRAIT_SIGNAL_CERTIFICATION.md:37) | Y |
| RBI Hero | position | SPIRITED | RISP plate appearance outcome: rbiCount > 0 (at-bat result: did the PA produce an RBI?); success rate = RBIs / RISP plate appearances (traitCandidateBuilder.ts:368) | SPIRITED chemistry (source 1:106) + IMAGE_DRIVER_SETS maps RBI Hero to ['TOUGH', 'COMPETITIVE'] (traitAcquisition.ts:135). §VI.3 Composure: +positive (TRAIT_SIGNAL_CERTIFICATION.md:116). Personality is TILT layer over strong measured signal (runners + outcome persist cleanly). | gain-high: player above peer percentile for RBI rate in RISP situations; lose-low: below percentile | yes — runners + rbiCount + runsScored auto-populated on every at-bat (TRAIT_SIGNAL_CERTIFICATION.md:32) | Y |
| RBI Zero | position | SPIRITED | RISP plate appearance outcome: rbiCount == 0 (did the PA NOT produce an RBI?); failure rate = non-RBI PAs / total RISP PAs (traitCandidateBuilder.ts:369) | SPIRITED chemistry (source 1:120) + IMAGE_DRIVER_SETS maps RBI Zero to ['TIMID', 'DROOPY'] (traitAcquisition.ts:141). §VI.3 Composure: −negative (TRAIT_SIGNAL_CERTIFICATION.md:116). Personality TILT; resilienceTilt applied (low Resilience favors gain). | gain-high: above peer percentile for non-RBI rate in RISP (failure measure); lose-low: below percentile | yes — runners + rbiCount auto-populated | Y |
| Rally Starter | position | SPIRITED | PA outcome when (losing + bases empty): reachedBase(result) i.e. batter reached base (1B/2B/3B/HR/E/BB/etc.); success rate = reached-base outcomes / total at-bats in losing-bases-empty context (traitCandidateBuilder.ts:370) | SPIRITED chemistry (source 1:105) + IMAGE_DRIVER_SETS maps Rally Starter to ['TOUGH', 'COMPETITIVE'] (traitAcquisition.ts:136). §VI.3 Composure: +positive (TRAIT_SIGNAL_CERTIFICATION.md:116). Personality TILT over clean outcome signal. | gain-high: above peer percentile for reaching base when down with empty bases; lose-low: below percentile | yes — teamLosing (score comparison) + basesEmpty (runners count) + outcome auto-derived from game state (TRAIT_SIGNAL_CERTIFICATION.md:34) | Y |
| Stealer | position | CRAFTY | stolen_base events with runner identity, rate-vs-peers (source 3: TRAIT_SIGNAL_CERTIFICATION.md:36, VI.4:127) | COMPETITIVE/TOUGH (source 4: traitAcquisition.ts:146, IMAGE_DRIVER_SETS). SOURCE 1 chemistry type CRAFTY; source 4 IMAGE_DRIVER_SETS maps Stealer to ['COMPETITIVE', 'TOUGH']. MISMATCH: chemistry type (CRAFTY) disagrees with IMAGE_DRIVER_SETS (COMPETITIVE). Per §VI.3 Hustle/aggression axis: 'Stealer...← Competitive/Tough/Ambitious' (source 3:117). IMAGE_DRIVER_SETS is authoritative for personality tilt (source 3:122 'personality is PRIMARY'). Note: source 1 chemistry is the static seed classification; source 4 is the personality acquisition axis — no contradiction, two different uses. | gain-high (higher steal success rate = acquisition) | yes — stolen_base + runner identity auto-written on every SB path (source 3: TRAIT_DETECTION_SCOPE_AUDIT.md:115, 'correctly-built') | Y |
| Utility | position | SCHOLARLY | fielding performance (FLD rating, run-value, or error rate) scaled to secondary-position context (non-primary position at which player has appeared). Derived from FieldingEvent.position field when ≠ player's primary position (§VI.4: 'Utility / Two Way [use] auto-populated FieldingEvent log at non-primary positions'). Adaptive-Standards peer-relative percentile within secondary-position fielder pool | SCHOLARLY chemistry only; no IMAGE_DRIVER_SETS entry (entry absent) — per §VI.3:120 'No personality image' for mechanical-skill traits. Universal Ambition tilt only | gain-high (better fielding at secondary position = better performance, trait fires at high percentile) | yes — FieldingEvent.position persists on every fielding play; secondary-position context derives via roster-primary-position join (TRAIT_DETECTION_SCOPE_AUDIT.md Tier 1) | — |
| Wild Thrower | position | CRAFTY | error-likelihood on fielding throws scaled by inning/situation (or flat rate); Adaptive-Standards peer-relative percentile vs fielding-error pool. EV model: scale errorLikelihood in DefensivePlacementRisk (§VI.4, traitInteractionMatrix.ts:835) | TIMID/DROOPY (traitAcquisition.ts:143); imageValence='negative' — Resilience-sensitive (universal low-Resilience tilt, per §VI.3 Composure axis) | lose-high (higher errant-throw rate = worse, trait flips at high percentile as a negative trait) | yes — FieldingEvent.result='error' with error type auto-tagged (TRAIT_DETECTION_SCOPE_AUDIT.md Tier 1: 'Wild Thrower (FieldingEvent error)') | Y |
| Choker | universal | SPIRITED | high-leverage PA outcome (unfavorable-to-batting-team = battingDelta < 0 OR unfavorableToPitchingTeam = fieldingDelta < 0); failure % among activations (traitCandidateBuilder.ts:367, 383) | SPIRITED chemistry (source 1:116) + IMAGE_DRIVER_SETS maps Choker to ['TIMID', 'DROOPY'] (traitAcquisition.ts:140). §VI.3 Composure: −negative, linked to Timid/Droopy/Volatile and low Resilience (TRAIT_SIGNAL_CERTIFICATION.md:116). Personality is a TILT; buildProposalBase applies resilienceTilt factor (traitAcquisition.ts:272-274). | gain-high: above peer percentile for unfavorable outcomes under pressure; lose-low: below percentile | yes — leverageIndex + wpa auto-populated | Y |
| Clutch | universal | SPIRITED | high-leverage PA outcome (favorable-to-batting-team = battingDelta > 0 OR favorableToPitchingTeam = fieldingDelta > 0, measured independently for batter/pitcher); success % among activations at pressured plate appearances (TRAIT_SIGNAL_CERTIFICATION.md:31; traitCandidateBuilder.ts:366, 382) | SPIRITED chemistry (source 1:103) + IMAGE_DRIVER_SETS maps Clutch to ['TOUGH', 'COMPETITIVE'] (traitAcquisition.ts:134). Strong alignment. §VI.3 Composure axis places Clutch as +positive, linked to Tough/Competitive/Composed and high Resilience (TRAIT_SIGNAL_CERTIFICATION.md:116). Personality is a TILT where measured signal (leverage/WPA) is strong (§VI.3:122). | gain-high: players above the peer percentile for favorable outcomes under high pressure; lose-low: below percentile | yes — leverageIndex + wpa auto-populated on every at-bat path (useGameState.ts per TRAIT_SIGNAL_CERTIFICATION.md:31) | Y |
| Stimulated | universal | CRAFTY | Personality-primary (SUPERSEDED — §0.6 has the ruled data proxy) per TRAIT_SIGNAL_CERTIFICATION.md §VI.3:122 'Personality is PRIMARY where the measured signal is thin (Stimulated, Gets Ahead/Falls Behind, Big/Little Hack)'. The mojo auto-derivation engine (Phase 2, §VI.4:127) is unbuilt; Stimulated earns from personality + morale composition even when measured late-game fitness signal is thin. Inning context (lastNInnings trigger) provides structural condition but not a measurable performance signal. | Reconciled: source 1 (CRAFTY chemistry, but Stimulated is categorized as Pitching trait per smb4_traits_reference.md:173) + source 4 (IMAGE_DRIVER_SETS includes Stimulated: ['TOUGH', 'EGOTISTICAL'] at traitAcquisition.ts:157) ↔ §VI.3:118 'Big-game/spotlight: Stimulated ← Ambitious/Tough/Egotistical (PED logic)' → RECONCILED. Source 4 maps Stimulated to ['TOUGH', 'EGOTISTICAL']; §VI.3 mentions 'Ambitious' but source 4 uses TOUGH instead. Ambition and Tough are distinct in the personality model; source 4 is canonical. Image-axis driver set is explicit and consistent. | gain-high; positive image valence per §VI.3:118 'Big-game/spotlight: ... Stimulated ← Ambitious/Tough/Egotistical (PED logic)'; Ambition↑ + Toughness↑ + Morale↑ raise acquisition threshold | no (personality-primary per §VI.3:122; mojo auto-derivation Phase 2 unbuilt; inning context is structural, not a measured signal per TRAIT_DETECTION_SCOPE_AUDIT.md line 82 'Mojo/fitness auto-derivation (TYPED_UNWRITTEN — manual-only today)') | — |

---

## D. NEEDS-RULING TABLE (same columns + effect/predicate for reference)

| Trait | Role | In-game effect | Matrix predicate | Proposed signal | Personality | Persist | Built |
|---|---|---|---|---|---|---|---|
| Composed | pitcher | +ACC in 3-ball counts: +12 (L1), +25 (L2), +50 (L3) / smb4_traits_reference.md:73 / positive mirror of BB Prone / traitInteractionMatrix.ts:264-269 | countIn [3-0, 3-1, 3-2] / traitInteractionMatrix.ts:266 {kind:'countIn', counts:[{balls:3,strikes:0},{balls:3,strikes:1},{balls:3,strikes:2}]} | personality-PRIMARY mechanism (SUPERSEDED — §0.6 has the ruled data proxy); §VI.3:122 'Personality is PRIMARY where the measured signal is thin'. Ball-strike count ABSENT (TRAIT_SIGNAL_CERTIFICATION.md:52); Composed requires count capture (Q1-deferred) OR personality-primary tilt (Q12). TRAIT_DETECTION_SCOPE_AUDIT.md:164 'NOT personality-primary → needs count (Q1-deferred)' — differentiates Composed from the Gets Ahead/Falls Behind pair, marking it as NOT covered by Q12. | DISCIPLINED chemistry (smb4_traits_reference.md:73). Composed not in IMAGE_DRIVER_SETS (traitAcquisition.ts); treated as neutral/mechanical, pending count signal. SOURCE 1 (DISCIPLINED) vs SOURCE 4 (none listed) — no disagreement, but SOURCE 4 omits it. Per TRAIT_DETECTION_SCOPE_AUDIT.md:164 'NOT personality-primary', Composed is MEASUREMENT-DEPENDENT, not personality-gated. | no — count field does not persist (TRAIT_SIGNAL_CERTIFICATION.md:52 'ABSENT') | — |
| K Collector | pitcher | +VEL/+JNK with 2-strike count: +8/+8 (L1), +15/+15 (L2), +30/+30 (L3) / spec §4.3 example matches (smb4_traits_reference.md:22, traitInteractionMatrix.ts:504-509) | twoStrikes (strikeout predicate) / spec encodes {kind:'twoStrikes'} / traitInteractionMatrix.ts:506 | strikeout-outcome rate during 2-strike counts vs peers; AB-outcome proxy (count itself absent); derivable from AUTO-persisted strikeout results and peer-relative batting performance (traitCandidateBuilder.ts:125-140 BATTING_TRAITS; TRAIT_DETECTION_SCOPE_AUDIT.md:34-39 Tier-1 K-outcome proxies, line 125) | COMPETITIVE chemistry (source 1: smb4_traits_reference.md:22) ALIGNS with IMAGE_DRIVER_SETS['K Collector']=['COMPETITIVE','EGOTISTICAL'] (traitAcquisition.ts:153); +imageAxisTilt when personality matches either driver (traitAcquisition.ts:275-277); no disagreement | yes — strikeout outcome auto-persists on every AB (useGameState.ts §recordAtBat paths per TRAIT_SIGNAL_CERTIFICATION.md:33-34; TRAIT_DETECTION_SCOPE_AUDIT.md:125 'yes') | — |
| K Neglector | pitcher | -VEL/-JNK with 2-strike count: -30/-30 (L1), -15/-15 (L2), -8/-8 (L3) / smb4_traits_reference.md:29 / guide spelling 'K Neglecter' resolved to 'K Neglector' per traitInteractionMatrix.ts:512 A16 | twoStrikes / traitInteractionMatrix.ts:516 {kind:'twoStrikes'} | strikeout-avoidance rate (low-strikeout outcome during 2-strike scenarios) vs peers; AB-outcome proxy; derivable from auto-persisted pitch/result logs (TRAIT_DETECTION_SCOPE_AUDIT.md:126 'strikeout-avoidance outcome', 'yes') | COMPETITIVE chemistry (smb4_traits_reference.md:29). K Neglector not listed in IMAGE_DRIVER_SETS (traitAcquisition.ts lines 98-161); treated as neutral/universal tilt (no image-axis amplification). Per §VI.3:120 'no personality image (platoon/mechanical-skill traits)' — K Neglector is strikeout-frequency mechanical, not personality-driven. | yes — strikeout avoidance proxied from auto-persisted AB outcomes (TRAIT_DETECTION_SCOPE_AUDIT.md:126 'yes') | — |
| Ace Exterminator | position | +10/+3 / +20/+5 / +40/+10 POW/CON vs A- or better pitchers (Tier 1/2/3) (source 1:130, guide-explicit per-tier values) | opponentTier: minGrade='A-' (source 2:145-153, batter fires only vs opponent pitcher grade A- or better) | batter batting average / slugging vs A- and better pitcher cohort, percentile-ranked vs peer baseline; source 2:152 notes 'Persists vs fatigue: tier check uses the pitcher's ROSTER grade, not fatigue-adjusted current form' | Big-game/spotlight image axis: source 3:118 'Ace Exterminator ← Competitive/Egotistical'; source 4:152 'Ace Exterminator: [COMPETITIVE, EGOTISTICAL]' in IMAGE_DRIVER_SETS → imageAxisTilt applied per source 4:275-277 when isImageDriver returns true | unverified (source 3:63 flags 'Ace Exterminator needs an opponent-pitcher-grade roster join that's unverified'; source 3:156 verdict: 'wrongly-dormant ... needs roster join') | — |
| Big Hack | position | +POW/-CON in 2-0, 3-0, 3-1 counts: L1 +10/-20, L2 +20/-10, L3 +40/-5 (smb4_traits_reference.md:133) | countIn [2-0, 3-0, 3-1] — OR-combined; mixed-sign trait where POW bonus grows AND CON malus shrinks with level (traitInteractionMatrix.ts:205-208) | Personality-primary (SUPERSEDED — §0.6 has the ruled data proxy) — cite TRAIT_SIGNAL_CERTIFICATION.md §VI.3:122 'Personality is PRIMARY where the measured signal is thin (Stimulated, Gets Ahead/Falls Behind, Big/Little Hack)' | Reconciled: source 1 (SCHOLARLY chemistry) + source 4 (IMAGE_DRIVER_SETS omits Big Hack — NOT listed in traitAcquisition.ts:133-161) → DISCREPANCY: source 4 lacks Big Hack mapping; falls back to universal Ambition↑-positive / low-Resilience↑-negative per §VI.0. SCHOLARLY chemistry associates with grinder/discipline logic (§VI.3:119); no explicit image-axis driver conflict. Lean needs-ruling on IMAGE_DRIVER_SETS omission. | no (personality-primary per §VI.3:122 — trait is earned from personality even when measured count signal is thin) | — |
| First Pitch Prayer | position | -POW/-CON on 0-0 count: L1 -20/-30, L2 -10/-15, L3 -5/-8 (smb4_traits_reference.md:32; traitInteractionMatrix.ts:448) | count (balls=0, strikes=0) — exact-count predicate; negative trait with inverted scaling (L1 worst, L3 mildest per A1 ruling: traitInteractionMatrix.ts:448-452) | pitchesInAtBat==1 (opt-in via EnrichmentPanel) as conditional proxy per TRAIT_DETECTION_SCOPE_AUDIT.md §V Tier 5 line 57. Builds only when user logs enrichment; dormant under Franchise-lite (§VI.1:103). | Reconciled: source 1 (COMPETITIVE chemistry, 0-0 weakness frame) + source 4 (IMAGE_DRIVER_SETS omits First Pitch Prayer; not listed in traitAcquisition.ts:98-131 NEGATIVE_IMAGE_TRAITS and not in the driver map) → DISCREPANCY. First Pitch Prayer absent from both POSITIVE and NEGATIVE sets + no driver entry. Negative trait frame (lowered POW/CON) suggests Timid/Droopy affinity per §VI.3 Composure axis (traitAcquisition.ts:140-145), but source 4 carries no explicit mapping. Falls back to universal resilience-tilt only. Lean needs-ruling. | opt-in (fires only when pitchesInAtBat==1 is logged) | — |
| First Pitch Slayer | position | +POW/+CON on 0-0 count: L1 +5/+8, L2 +10/+15, L3 +20/+30 (smb4_traits_reference.md:20; traitInteractionMatrix.ts:458, note A13 guide in-game values differ from workbook pricing) | count (balls=0, strikes=0) — exact-count predicate (traitInteractionMatrix.ts:459) | pitchesInAtBat==1 (opt-in via EnrichmentPanel) as conditional proxy per TRAIT_DETECTION_SCOPE_AUDIT.md §V (Tier 5 line 57): 'First Pitch Slayer / First Pitch Prayer — pitchesInAtBat==1 (opt-in via EnrichmentPanel)'. Builds only when user logs enrichment; falls dormant under Franchise-lite (§VI.1:103). | Reconciled: source 1 (COMPETITIVE chemistry, aggressive 0-0 mindset per §VI.3 'big-game/spotlight' axis) + source 4 (IMAGE_DRIVER_SETS omits First Pitch Slayer; not in traitAcquisition.ts:133-161) → DISCREPANCY. First Pitch Slayer absent from IMAGE_DRIVER_SETS; no explicit personality driver. Should align with Competitive/Egotistical per the chemistry type (smb4_traits_reference.md:12-24), but source 4 carries no entry. Falls back to universal Ambition↑. Lean needs-ruling. | opt-in (fires only when pitchesInAtBat==1 is logged) | — |
| Little Hack | position | +CON/-POW in 0-1, 0-2, 1-2 counts: L1 +10/-20, L2 +20/-10, L3 +40/-5 (smb4_traits_reference.md:134) | countIn [0-1, 0-2, 1-2] — OR-combined; guide counts (0-1/0-2/1-2) override spec §4.3's '2-strike family' grouping; 0-1 is not a 2-strike count (traitInteractionMatrix.ts:526, note A11) | Personality-primary (SUPERSEDED — §0.6 has the ruled data proxy) — cite TRAIT_SIGNAL_CERTIFICATION.md §VI.3:122 'Personality is PRIMARY where the measured signal is thin (Stimulated, Gets Ahead/Falls Behind, Big/Little Hack)'; count precision (Q1) deferred per TRAIT_DETECTION_SCOPE_AUDIT.md §VI.1 line 81, §VI.5:97 | Reconciled: source 1 (SCHOLARLY chemistry) + source 4 (IMAGE_DRIVER_SETS omits Little Hack — NOT listed in traitAcquisition.ts:133-161) → DISCREPANCY: source 4 lacks Little Hack mapping; falls back to universal. SCHOLARLY discipline logic (grinder/contact emphasis per §VI.3:119) aligns with contact-bonus frame; no explicit image-axis conflict. Lean needs-ruling on omission. | no (personality-primary per §VI.3:122 — earned from personality even when count signal is thin) | — |
| Noodle Arm | position | Decreased throw speed when throwing with less than maximum power (x4/x2/x1 per tier, inverted scaling) — per-tier L2 rating-delta ARM: -25 (traitInteractionMatrix.ts:581) | fieldingChance (traitInteractionMatrix.ts:582) — fires only on NON-max-power throws, inverse of Cannon Arm (traitInteractionMatrix.ts:585 note: 'Mirror of Cannon Arm but fires on NON-max-power throws') | arm-velocity deficiency on non-max throws (off-the-shelf proxy via expected-extra-bases penalty, bWAR-borrowed, credited to fielder; or runner-advance-rate vs outfielders if throw-velocity metadata unavailable). Requires validation: TRAIT_DETECTION_SCOPE_AUDIT.md §Reverse-findings 'Noodle Arm IS in BUILDABLE_TRAITS, but §VI calls it a bespoke no-proxy mechanic; it's likely firing on a weak/borrowed bWAR proxy. May belong in cut or correctly-dormant. Re-examine.' → marked as 'suspect' and needing JK micro-ruling | COMPETITIVE (primary chemistry; no secondary IMAGE_DRIVER_SETS entry). Per §VI.3:120 'No personality image' for mechanical-skill traits | typed-unwritten / proxy-dependent — no direct throw-velocity field; candidate derives via bWAR advance proxy (TRAIT_DETECTION_SCOPE_AUDIT.md: 'weak/borrowed bWAR proxy'). Audit flags as 'correctly-dormant candidate if no-proxy conclusion holds' | Y |
| Slow Poke | position | Decreased speed running out of batter's box (x4/x2/x1 at Tier 1/2/3, inverted negative scaling) — source 1: smb4_traits_reference.md:31 | runningOutOfBox, potency='standardInverted' (source 2: traitInteractionMatrix.ts:705-709, line 706) | infield-single AB-outcome proxy (stolen-base success rate alternatively, but named beat_throw/beat_runner signals are DEAD per audit). Delta SPD -5 at L2 workbook equivalent. Source 3:62 flags 'beat_throw/beat_runner play types are never written (dead signal). Buildable only via an infield-single AB-outcome proxy instead' — source 3 TRAIT_DETECTION_SCOPE_AUDIT.md:155. Source 3:131 also lists 'Slow Poke...SB success-rate...Tier 1 (sparse)'. | RELAXED/DROOPY per §VI.3 Hustle/aggression axis (source 3:117: 'Slow Poke...← Relaxed/Droopy, low Ambition'). Source 4 IMAGE_DRIVER_SETS maps Slow Poke to ['RELAXED', 'DROOPY'] (traitAcquisition.ts:150). | sparse/opt-in — source 3:62 notes 'named signal dead' (beat_throw); the infield-single proxy is an AB-outcome persists but is a weak proxy. Source 3 TRAIT_DETECTION_SCOPE_AUDIT.md:131 marks Slow Poke as 'wrongly-dormant' pending proxy confirmation. | Y |
| Sprinter | position | Increased speed running out of batter's box (x1/x2/x4 at Tier 1/2/3, positive ascending). Source 1: smb4_traits_reference.md:21. Guide: 'Increased running speed while running out of the batter's box' — source 2:727 | runningOutOfBox (source 2: traitInteractionMatrix.ts:722-728, line 725) | Named signals beat_throw/beat_runner are DEAD (never written per source 3:62). Source 3 §FLAGGED-UNCERTAIN (line 62-63) notes 'Buildable only via an infield-single AB-outcome proxy instead'. Delta SPD +5 at L2 (source 2:724, A10 workbook-equivalent). Peer-relative infield-single rate (extra bases from speed plays). | COMPETITIVE/TOUGH per §VI.3 Hustle/aggression axis (source 3:117: 'Sprinter ← Competitive/Tough/Ambitious'). Source 4 IMAGE_DRIVER_SETS maps Sprinter to ['COMPETITIVE', 'TOUGH'] (traitAcquisition.ts:147). | no (named signals dead; proxy sparse) — source 3 TRAIT_DETECTION_SCOPE_AUDIT.md:155, 'beat_throw (DEAD)...needs infield-single proxy' — currently marked 'wrongly-dormant' pending proxy confirmation | Y |
| Tough Out | position | +CON with 2-strike count: +12 (L1), +25 (L2), +50 (L3) / smb4_traits_reference.md:23 / traitInteractionMatrix.ts:761 'Remains active for the rest of the at bat once the count reaches two strikes' | twoStrikes (batter predicate, reach-based: activates and persists once count reaches 2-0) / traitInteractionMatrix.ts:763 {kind:'twoStrikes'} + note: 'persists through fouls' | strikeout-avoidance rate (low-strikeout outcome rate during 2-strike scenarios) vs peers; AB-outcome proxy; derivable from auto-persisted non-strikeout AB results at 2-strike counts (TRAIT_DETECTION_SCOPE_AUDIT.md:127 'strikeout-avoidance outcome', 'yes') | COMPETITIVE chemistry (smb4_traits_reference.md:23). IMAGE_DRIVER_SETS['Tough Out']=['TOUGH'] (traitAcquisition.ts:158); +imageAxisTilt when personality is TOUGH. Per §VI.3:119 'Tough Out ← Disciplined (grinder/selfless)', maps to TOUGH/COMPETITIVE flavor (source 1 chemistry matches source 4 driver). Computable standoff vs K Collector at 0-2 (traitInteractionMatrix.ts:509). | yes — AB outcomes (strikeout/non-strikeout) auto-persist (TRAIT_DETECTION_SCOPE_AUDIT.md:127 'yes') | — |
| Whiffer | position | -CON with 2-strike count: -50 (L1), -25 (L2), -12 (L3) / smb4_traits_reference.md:30 / THE numeric anchor for negative-potency ruling (traitInteractionMatrix.ts:821 'THE numeric anchor for the A1 negative-potency ruling') | twoStrikes (batter predicate) / traitInteractionMatrix.ts:818 {kind:'twoStrikes'} | strikeout rate during 2-strike counts vs peers (batter perspective); AB-outcome proxy (strikeout outcome auto-persists independent of count precision); derivable from auto-persisted strikeout results and peer-relative opportunity (TRAIT_DETECTION_SCOPE_AUDIT.md:128 'strikeout outcome', 'yes') | COMPETITIVE chemistry (smb4_traits_reference.md:30). IMAGE_DRIVER_SETS['Whiffer']=['EGOTISTICAL'] (traitAcquisition.ts:160); +imageAxisTilt when personality is EGOTISTICAL. Per §VI.3:119 'Whiffer ← Egotistical/Volatile (free-swinger)'. Resolves to EGOTISTICAL-driven + COMPETITIVE chemistry (source 1) — acquisition leans on personality-image axis. | yes — strikeout outcome auto-persists (TRAIT_DETECTION_SCOPE_AUDIT.md:128 'yes') | — |
| Durable | universal | Reduced chance of injury during play and slower fitness decay from being overplayed — fitnessDecayRate factor 0.75 (L2 placeholder; positive trait, standard scaling x1/x2/x4 ascending) (traitInteractionMatrix.ts:331) | always (traitInteractionMatrix.ts:332) — unconditional; applies to all gameplay | fitness-decay rate OR injury count per games-played OR absence-frequency; measured against peer pool (Adaptive-Standards) scaled by position (catchers have fastest decay, §4.4). Per §VI.4: 'Durable, Injury Prone … expose the hidden durability modifier OR auto-injury events' — currently a PLACEHOLDER where the factor 0.75 is derived from x1/x2/x4 only, not calibrated by playtest (traitInteractionMatrix.ts:335 note A9: 'Guide gives direction + x1/x2/x4 only. Scaling: stronger reduction at higher level. Factor 0.75 is a PLACEHOLDER') | COMPETITIVE chemistry only; no IMAGE_DRIVER_SETS entry — per §VI.3:120 'No personality image' for mechanical-skill traits. Universal Ambition tilt. §VI.2:109 'Durable most-detectable for catchers' (roster-role context, not personality) | typed-unwritten / placeholder — no injury accumulator or auto-fitness-decay field live; threshold/measurement pending §VI.4 'injury accumulator (folds into the fitness/dev engine, season-scaled thresholds)' unbuilt (TRAIT_SIGNAL_CERTIFICATION.md §D §VI.5: 'Durable / Injury Prone: expose the hidden durability modifier OR auto-injury events') | Y |
| Injury Prone | universal | Increased chance of injury during play and faster fitness decay from being overplayed — fitnessDecayRate factor 1.25 (L2 placeholder; negative trait, standardInverted scaling x4/x2/x1 descending, worst at L1) (traitInteractionMatrix.ts:486) | always (traitInteractionMatrix.ts:487) — unconditional; applies to all gameplay | fitness-decay rate OR injury count per games-played OR absence-frequency; measured against peer pool (Adaptive-Standards). Per §VI.4: same as Durable — 'expose the hidden durability modifier OR auto-injury events'. Inverted: stacking the chemistry suppresses it (traitInteractionMatrix.ts:490 note A9: 'Inverted: stacking the chemistry suppresses it (forcing you to stack Competitive players). Guide line (x4)/(x2)/(x1).' Factor 1.25 is PLACEHOLDER) | COMPETITIVE chemistry only; no IMAGE_DRIVER_SETS entry — per §VI.3:120 'No personality image' for mechanical-skill traits. Resilience-sensitive pessimistic anchor (universal low-Resilience tilt per §VI.0 formula: negative traits scale with low resilience) | typed-unwritten / placeholder — same as Durable; no injury accumulator or auto-fitness-decay field live; measurement unbuilt (TRAIT_SIGNAL_CERTIFICATION.md §D: 'Durability / injury (hidden modifiers; no injury accumulator — games counter has no discriminating power)') | Y |
