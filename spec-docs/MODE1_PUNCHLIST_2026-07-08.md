# MODE-1 PUNCH LIST → "JK CAN FULLY TEST THE DRAFT A-Z"
**Status doc, 2026-07-08 (JK Friday-walk findings + audit synthesis). Parent: V1_CANON §2-§3, ORPHAN_WIRING_MATRIX. Update in place as items land.**

## §1 JK FRIDAY FINDINGS (browser walk, 2026-07-03/04 — ruled defects, no further ruling needed)
| ID | Defect | Class | Fix direction |
|---|---|---|---|
| F1 | **Deterministic nomination order across drafts** — same players surface in the same order every draft (e.g. Buttons Bunterson always first) if the pool is unchanged. | Illogic (wired-but-wrong) | Nomination seed must be per-draft-instance: derive from the auction SESSION id/launch nonce, persisted in the session (resume keeps order within a draft; every new draft differs). Applies to MLB and farm. Surface: selectNextNominee seed inputs. |
| F2 | **Farm draft shows no grade-number band per scouting specialty** — beyond the intentional rating-fog differences, the farm lots lack the banded grade read the spec requires. | Missing UI + wrong source | Bands must render on farm lots AND derive from the FARM ARCHETYPE (3/5/7-band confidence per SCOUTING_INTELLIGENCE_SPEC §7) — the shipped source (hired-scout descriptor specialties) is the known S4 spec violation. **Table RATIFIED 2026-07-08 → spec-docs/FARM_ARCHETYPE_SCOUT_CONFIDENCE_2026-07-08.md (24 archetypes × 8 areas + captain rulings §8). Lane M1D staged, queued behind M1b's merge (shared engine file).** |
| F3 | **Scout hiring shows a generic scout group** — the hire pool is not dependent on the chosen archetypes or prospect pool. | Wrong source | Scout-hire options must derive from the team's farm archetype (the archetype→per-area-confidence table, unbuilt); the legacy scout-pool/hire-draft mechanism is the deprecated path (Q11 refactor, never executed). **RULED (Fable, 2026-07-08): execute the §11 deprecation now — each team's scout AUTO-derives from its farm archetype (specialties = 3-band areas, weaknesses = 7-band areas); ScoutHire becomes a no-choice "meet your scout" reveal; persistence record shape unchanged. Rides lane M1D.** |
| F4 | **Prospect quality inflated** — generated farm prospects do not match the spec's grade distribution curve. | Distribution defect | Measure: histogram generated prospects against PROSPECT_GENERATION_SPEC's curve (generator must be the inverse of the scoreSmb4Player grade oracle); fix the generator/pool-shaping to the curve; add the histogram to the gauntlet as a permanent invariant. |
| F5 | **UI style mismash across the arc** (known; design pass ordered after correctness — Staff-Your-Clubs/ballpark kit target). | Cosmetic | Reskin wave rides the kit (built 2026-07-07); after the punch list. **LANDED 2026-07-08 — the RESKIN sweep (merge `3266207c`, §9 below) unified one premium-retro hard-edge skin across the entire draft journey A-to-Z, incl. the live auction stage per JK's one-language ruling; awaiting JK's browser verdict via `JK_BROWSER_CHECKLIST_2026-07-08.md`.** |

## §1b ILLOGIC-PASS FINDINGS F6–F11 (Mode-1 GM-lens review, 2026-07-08 — 3 lenses: economy/flow/roster, 617k tokens, 10 raw → 6 deduped; none overlap F1-F5/P1-P12)
| ID | The illogic (plain) | Severity | Evidence anchor | Disposition |
|---|---|---|---|---|
| F6 | "Confirm Staff → Continue to Franchise Setup" drops the league you just drafted — lands on a blank unfiltered league picker; every other hop threads leagueId. | breaks-immersion, every draft | EndOfDraftStaffing.tsx:161 navigates without leagueId; FranchiseSetup.tsx never reads one (Step1SelectLeague ~422-469 expects a handoff the sender never makes) | Fold into **P5 lane** (same file surface): thread `?leagueId=`, auto-select on mount, completion badge on the drafted league |
| F7 | Farm lot "Scout value $" comes from an unrelated static letter-grade salary table (self-documented build-dark) — routinely disagrees with the real Opening ask beside it; identical for all same-band prospects. | breaks-immersion, every farm lot | LeagueBuilderFarmAuctionDraft.tsx:112-134, 673-694 → GRADE_SALARY_BOUNDS (ratingsAdjustmentEngine.ts:149-162) vs calculateIvBaseSalary (farmAuctionPool.ts:108-114) | **Folded into lane M1D** (same function it restructures): scout value = archetype-band-width fog around the true-IV opening ask |
| F8 | Once a position's legal minimum is met, the CPU values a 5th body there same as filling an empty spot → simulated rooms reproducibly hoard (5 SS / 4 C teams) while sibling positions sit at bare minimum. | systemic economy defect (sim-confirmed, 36 rosters) | ownNeedMultiplier never <1.0 post-floor (auctionMarketModel.ts:398-406); needBucketForCandidate buckets post-floor as position-blind filler; live via cpuShillBidding.ts:23,322 | ~~New economy lane **M1E**~~ **PARKED (2026-07-08 overnight): the M1E-FINAL v1-scale gate FAILED on the repaired engine** (M1/M2/M4 fail in BOTH states; the sim-lane baseline itself is unhealthy; the liveEngine gate-lane is unjudgeable — the harness throws on auction-uncompletable). The GAUNTLET leg 1 subsequently proved real 8-team drafts complete cleanly → **the harness's pool construction is the artifact, not the product**. F8/F11 stay parked on `lane/m1e-diminish` (commits incl. gate report `6f6d0bbd`) pending a properly-based harness. **NOT a v1 walkthrough blocker.** F16 (over-pessimistic archetype advisory) is the same tuning family. |
| F9 | Whisper panel contradicts itself on one lot: verdict/MAX BID use the liquidity-reserved ceiling, room-read/budget-light use the unreserved ceiling → "Let him go" + green "inside expectations" simultaneously, common whenever slots remain open. | confusing | WhisperPanel.tsx:344,383-386,432-436,518-526; rosterIntelligencePayload.ts:277-325,575-610 | **RULED (Fable 2026-07-08): ONE ceiling — the liquidity-adjusted maxBid drives verdict, room-relation AND budget light; capValue may render only as a separately-labeled "total capacity" line.** ~~Small lane M1F~~ **LANDED (2026-07-08, M1P MERGED `500e66bf` — audit CLEAN APPROVE):** one ceiling drives all three reads (`68570a39`); a counter-fixture proved the OLD leak (the contradiction reproduces on pre-fix code); engine diff-verified untouched. |
| F10 | A league can begin (and farm-draft) having skipped Scout Hire entirely — only symptom is silent "N/A" scout ranges everywhere. Also: code-verified premise correction to P11 (scout-hire does NOT actually gate the MLB auction today). | confusing → structural | beginAuction blockers (LeagueBuilderAuctionDraft.tsx:1225-1258) check no scout state; loadOptionalFarmScouts → null → "N/A" | **Structurally resolved by lane M1D**: bands/scout identity derive from the farm ARCHETYPE at draft time, not from ScoutHire persistence — a skipped reveal screen can no longer kill scouting. M1D contract hardened accordingly; P11 annotated |
| F11 | The roster advisor only warns "too thin," never "over-stacked" — five shortstops + one emergency catcher draws zero findings. | confusing | analyzeRoster keys only off thinPositions (rosterAnalyzerEngine.ts:840,898,923) | Fold into **M1E** (the advisor names post-hoc what F8's pricing fix prevents) |

**Also parked alongside F8/F11:** `spec-docs/DRAFT_POOL_UNIVERSE_SPEC_2026-07-08.md` — multi-league curated draft-pool sources, captain-ratified spec draft, PARKED (JK not yet committed to build; 3 open JK forks inside; do not build until JK commits + rules the forks).

## §1c JK ITEMS 2026-07-08 EVENING (diagnosis in flight)
| ID | Item | Status |
|---|---|---|
| F12 | **Farm prospect generation currency check** — verify the farm generator is the NEWEST path using all IV-engine inputs (litmus: pitcher arm slots) AND the farm lot UI shows exactly: name, age, primary/secondary position, trait COUNT — no other specifics. | **TRACED (2026-07-08).** Generator IS the current spec-v2 analyzer-anchored path (old gradeEngine generator orphaned as ordered; all 3 farm entry points on the live one). ONE IV-input gap: **armSlot is type-pinned `null`** (prospectScoutingDraftEngine.ts:195, :1458) — NO generator assigns arm slots though ivEngine prices them (:378,:603) and database players carry them → farm pitchers can never be Sub-slot; IV parity break vs pool pitchers. (Arm-slot DATA landed 2026-06-10/11; what landed ~last week was card DISPLAY, e778eb8a.) UI: **auction lot card missing trait COUNT** (otherwise conformant, zero over-disclosures); **startup board OVER-discloses 5 fields** (potential grade, chemistry, personality, archetype family, salary — deliberately in VisibleSafeProspectReport → JK ruling needed before stripping). Fixes: **lane M1H** (after M1D — shared engine file): seeded arm-slot draw anchored to database pitcher distribution + DTO type widen + spec section + lot-card trait count. **F12c RULED (JK, 2026-07-08): startup board KEEPS its five extra fields (the visible-safe set stands); the strict 4-field display spec binds the farm AUCTION lot card only.** |
| F13 | **Farm draft CPU takeover of a user team** (JK Friday): with a shill configured in the MLB draft and no shill wiring in the farm draft, the engine treated a USER team as a CPU/shill bidder — it silently bid and won players. Suspected: farm role assignment doesn't read Draft Setup ownership (or single-human assumption). | **ROOT CAUSE CONFIRMED (2026-07-08):** farm page writes a DEFAULT shill count (floor(N/3)) into session config, but farm builds no synthetic shill seats — so the legacy "last N nomination-order teams become CPU" fallback captures REAL clubs, ownership never consulted (useFarmAuctionDraft.ts:99-119,:484). With 2 human clubs in a 6-team room ≈60% chance per draft; persisted sessions re-trigger on every resume. MLB was already hardened (useAuctionDraft.ts:647-649) but has a latent legacy-RESUME hole (cpuTeamRoles.ts:31-34,62-77 exclude only AI clubs). **RULED (Fable): farm has NO shills in v1** — CPU behavior only for `controlledBy==="ai"` clubs. **Fix lane M1G dispatched** (zero the count, kill the fallback, heal persisted sessions, human-exclusion belt-and-braces in cpuTeamRoles closing the MLB hole too). |
| F14 | **Live auction completion terminal-path bug — CONFIRMED on unmodified code (2026-07-08).** `surfaceNextPlayer` marks `AUCTION_COMPLETE` directly when no nominee remains (src/engines/auctionStateMachine.ts:421 entry; direct-complete branches :430 and :444), bypassing the exhaustion cleanup — `backfillFromPassedLots` (:695) + the Lever A affordable-pricing amendment run only on the `advanceLot` terminal (:650-654). Real drafts can terminate "complete" with SHORT rosters, silently. Live-driver matrix at v1 scale: 13-16/18 and 21-22/24 legal rosters across BOTH k=0 and k=0.65 — likely the polished cousin of JK's stuck-draft manual finding. Discovered by the M1E gate chain (sim gate fail → A/B attribution → live-driver truth run). | **LANDED (2026-07-08, M1J MERGED `08236fdf` — audit APPROVE-WITH-NOTES):** exhaustion cleanup runs on EVERY terminal path (`d8c64fa3`); genuinely-uncompletable pools surface an explicit `terminalShortfall` status instead of a silent short. Audit rigor: the falsification check was self-run by the auditor against genuine base code; an organic full-draft driver showed ZERO incomplete rosters across all legs. **Chain consequence:** F8/F11 re-judges on this fixed base — lane M1E-FINAL dispatched. Residual → F14b. |
| F14b | **Uncompletable-pool UI drop (residual of F14; strictly better than the old silent-short).** The engine computes `terminalShortfall` for genuinely-uncompletable pools (auctionStateMachine.ts:140,:671) but the UI hook throws and drops it (`useAuctionDraft.ts:150 transitionOrThrow`) — the user gets a soft-lock with an error toast instead of a graceful "draft cannot complete" screen. | **TICKETED (2026-07-08):** small UI lane — fold into a later Mode-1 UI pass. Not urgent: pool sizing makes it near-impossible to trigger at v1 scale. |
| F15 | **Stale pool-legality check after regenerate** (gauntlet leg 1): regenerating the pool leaves the shared-pool legality panel showing a FALSE ✗ ("seats 0 of 8, fails at CP") until a manual RE-CHECK — a user could misjudge pool legality either direction. | **TICKETED (2026-07-08, gauntlet):** small fix — auto re-check (or invalidate-with-banner) on pool regenerate. |
| F16 | **Archetype market advisory wildly pessimistic** (gauntlet leg 1): "Archetype market outlook" claimed 0/24 archetypes buildable and flagged closers tightest — the real auction then completed easily with money to spare for every club. | **TICKETED (2026-07-08, gauntlet):** tuning-class — same family as the parked F8/M1E economy modeling (the advisory's market model diverges from real outcomes); revisit together. **Leg-2 note:** the same pessimism pattern re-observed in the design-first walk (not re-verified in depth; nothing suggested improvement). |
| F17 | **Farm-gaps sidebar never updates during the draft** (gauntlet leg 1): the per-position tracker stayed "0 of 10, all OPEN" all draft while slots actually filled; post-draft Roster tab proved the underlying data correct — cosmetic staleness in the live sidebar only. | **TICKETED (2026-07-08, gauntlet):** small fix. |
| F18 | **Farm slots not position-enforced** (gauntlet leg 1): unlike the MLB "Eight", farm position gaps (C GAP, 1B GAP…) are advisory-only — passively claiming at reserve produced a legal 10/10 all-RP/CP farm class with zero position players. | ~~**JK DESIGN FORK (morning check-in):** enforce farm position minimums, or ratify advisory-only farm composition for v1.~~ **RULED-ADVISORY (2026-07-08 morning, verbal):** JK — "teams should be allowed to draft whoever they want to meet their needs in the farm draft." Farm position gaps stay advisory-only; no build. The gaps display stays as-is. Only remaining farm-gaps work: F17 (sidebar staleness, cosmetic). See `V1_CANON_2026-07-07.md` §6. |
| F19 | **Initial pool generated ILLEGAL** (gauntlet leg 1): first-try pool was 176/202 (tail too fat); the "Regenerate production-shaped pool" action recovered to 220/220 legal with zero hand-adds. | **TICKETED (2026-07-08, gauntlet):** pool-gen first-try-legal — the generator should produce what regenerate produces, first time. (Leg-2 contrast: the design-first extractor WAS legal first-try — the defect is pool-first-path-specific.) |
| F20 | **Silent re-extraction on pool lock** (gauntlet leg 2, design-first): LOCK POOL silently re-runs extraction/resizing (317→246) instead of locking what was shown — the design-target fit jumped 12/22 → 22/22 only AFTER lock; a user who checked fit pre-lock could misread it (either direction). | **LANDED (2026-07-08, M1R MERGED `46ebc2aa` — audit APPROVE-WITH-NOTES):** `fd8bd61c` — a display-vs-final divergence detector blocks LOCK until re-extraction, plus a hard set-equality guard in `lockLeaguePool` that throws on any drift, so no silent swap is possible and fit verdicts always reflect the displayed pool. Audit-corrected framing: this is a STRICTER route than the "finalize the pool before displaying it" description first proposed — it satisfies the what-you-see-is-what-locks ruling by blocking-and-forcing-re-extraction rather than upstream-finalizing, a superset of the old (looser) lock gate. CUT2-2 floor semantics untouched; no new recompute cost. Residual (small, new): **F22** — post-import roster changes can make LOCK refuse with no in-UI recovery hint until reload. |
| F21 | **Completion deadlock on pool-exhausted shortfall** (gauntlet leg 3, live repro on GauntletD8): the completion gate (`finalizeTerminalAuction`) runs its backfill cascade, still finds a shortfall, and refuses to set `AUCTION_COMPLETE` — but the one recovery action ("settle empty seats from Market Shills", `settleShortClubs`) only works when state IS `AUCTION_COMPLETE`. Circular trap, no UI escape; NEXT LOT = byte-identical no-op. | **LANDED (2026-07-08 overnight, M1Q MERGED `3f98eb06` — audit CLEAN APPROVE):** automatic shill reclamation in the terminal cascade (`27cac1f4`) — theft structurally impossible; post-completion settle byte-identical. Audit notes: the leg-3 repro carried a naive-auto-bidding caveat (walker's aggressive bidding contributed to the uneven pool drain — noted, doesn't change the deadlock's reality); the "Can't afford her and still fill the roster" spin-loop is a minor UX/automation gotcha, noted for automation. |
| F22 | **Pool-first opaque lock-refusal edge** (found auditing F20's landing): the once-per-league auto-import guard (`autoImportedRef`) never re-fires — if a roster changes AFTER that auto-import, the roster-union pool (`leagueBuilderPoolRegistration.ts:100-110`) drifts from the displayed assignments and LOCK refuses via F20's new guard, with no in-UI recovery hint until a page reload remounts and re-imports (`LeagueBuilderDraftSetup.tsx:2006`). Correct-but-opaque; low severity. | **TICKETED (2026-07-08).** Fix direction: surface a re-import/re-sync affordance or an explanatory hint when the lock guard trips, instead of a silent refusal requiring reload. |

**Gauntlet leg-1 tool-limitation note (not a defect):** the press-and-hold scout-band reveal's CONTENT is unverified — scripted browser events can't trigger React's onPointerDown; plumbing (locked-by-default, trait-count-only, no names) verified correct. Needs a real pointer in JK's walkthrough.

**F1 landing note (2026-07-08, MERGED — audit APPROVE):** `lane/m1a-nomseed` `b92b9f43` — nomination order derives from session id + persisted launch nonce; crash/resume bit-stable via persisted `config.nominationOrderSeed`; real Draft Room entries hit the fixed path; sim/harness determinism untouched. Small known gap: no MLB-side resume-order integration test (farm has one).
**F2/F3/F7/F10 landing note (2026-07-08, M1D MERGED — audit APPROVE-WITH-NOTES):** per-area farm scout bands from the ratified 24x8 archetype table (row-by-row fidelity verified) render on BOTH farm surfaces; scout value = IV-pipeline opening ask with band fog (true ask always within range; tight strictly narrower than wide); ScoutHire = deterministic no-choice auto-specialization (persistence shape unchanged); scouting no longer requires a ScoutHire visit. AMENDMENT from audit: the overall grade band was constant-5 under the mean rule (dead knob) — ruling amended to PRIMARY-AREA (prospect's highest-rated tool), rides lane M1H with the arm-slot generation + lot-card trait count.
**F12/s8.4 landing note (2026-07-08, M1H MERGED `3dfa1d38` — audit APPROVE-WITH-NOTES):** generated pitcher prospects now draw a deterministic arm slot from the real `playerDatabase.ts` pitcher-pool distribution (High 65/36.31%, Mid 65/36.31%, Low 44/24.58%, Sub 5/2.79%, N=179); generated hitters remain `null`; Farm IV receives the slot and prices its arm-slot layer. Farm auction UP NOW lot cards now show only the ruled four public fields: name, age, position(s), and trait COUNT (no trait names — no-leak verified by the audit); startup board visible-safe extras remain untouched per F12c. s8.4 amendment landed: overall scout band and scout-value fog follow the prospect's primary applicable scouting area by highest true rating, fixed tie order power/contact/speed/fielding/arm/velocity/junk/accuracy; no-archetype fallback stays band 5. Audit note: only the Sub slot carries a pricing premium — pre-existing IV model, not an M1H change.
**F13 landing note (2026-07-08, MERGED 72d518ce — audit APPROVE-WITH-NOTES, no code defects):** farm CPU set = AI clubs only (legacy last-N fallback REMOVED — human hijack structurally impossible); persisted sessions heal cpuShillCount→0 with a real store write before first auto-advance; MLB legacy-resume hole closed (human exclusion in cpuTeamRoles, synthetic shills preserved); farm CPU-count UI removed. Coverage notes (non-blocking): no direct CPU-wins assertion; no all-AI-league completion test.
**F4 landing note (2026-07-08, MERGED — audit APPROVE-WITH-NOTES):** `lane/m1b-curve` `bdb8c2d4`+`34419899` — opt-in farm prospect histogram invariant + seeded pool-level largest-remainder grade quotas replacing independent draws. Before: B- +2.0pp / C +2.2pp, total abs 8.8pp at N=500; after: exact, 0.0pp (also exact at the spec's 40k validation). Audit notes for JK: (1) same-size pools now share an identical grade-count SKELETON by design (between-league top-end variety traded for spec exactness); (2) the fix pins the TRUE curve — any remaining *felt* inflation lives in the display band (F2/F7 → lane M1D); (3) harness measures N=500 vs production default N=300 (harmless; quotas exact at any N).
**F6/P5 landing note (2026-07-08, M1I MERGED `8eb24037` — audit APPROVE-WITH-NOTES + M1I-B fix `ad4e5b46`):** leagueId threads through the staffing→franchise handoff (`2850f3cc` — auto-select on mount, no more blank league picker) and hired reporter/manager identities reach the hub (`f99128ed`); the draft-complete badge now reads the AUCTION SESSION (M1I-B) instead of the stale source it briefly shipped with. Residual notes (non-blocking): URL param not cleared → a low-likelihood re-snap; per-league badge scan is perf-only; no empty-staffing coverage regression (M1I-B restored it). Process note: the first M1I dispatch died silently mid-build and was re-dispatched (known long-dispatch kill pattern).
**F14 landing note (2026-07-08, M1J MERGED `08236fdf` — audit APPROVE-WITH-NOTES):** completion guarantee on every auction terminal path (`d8c64fa3`) — see the F14 row above for detail; F14b (uncompletable-pool UI drop) ticketed as the residual.
**P3 landing note (2026-07-08, M1K MERGED `e21b23cf` — audit APPROVE-WITH-NOTES):** farm auction folds onto AuctionStage (`fc6d6742`) — the mid-journey UI register break is dead. Audit-proven: the MLB tier is INERT under the fold (no behavior change to the MLB path); privacy verified at the DOM level — covered values are ABSENT from the DOM, not merely hidden. Notes (non-blocking): missing `.catch` on the load-init chain; bid increment is now fixed at 1000 — flagged **VERIFY-DRAFT-SETUP-OWNERSHIP** follow-up (confirm Draft Setup owns/feeds the increment rather than the stage hardcoding it).
**P6 landing note (2026-07-08, M1L MERGED `11a80835` — audit APPROVE-WITH-NOTES):** post-freeze summary screen (`d310db95`) — read-only display of what froze (salaries, morale baselines), honest about gaps rather than inventing data. Notes (non-blocking): roster fallback is DERIVED when the store lacks it; add an empty-store branch test.
**P8 landing note (2026-07-08, M1N MERGED `7714b82a` — audit APPROVE-WITH-NOTES):** League Builder's existing league create/edit modal now has a conference assignment editor with single-conference, balanced split, add/remove, rename, and per-team assignment controls. Untouched leagues still persist the old empty `conferences: []` / `divisions: []` shape; edited leagues persist conference names plus team membership through existing `LeagueTemplate.conferences` and one hidden `Division` per conference. Validation blocks unknown/orphan/duplicate team assignments. Consumer status: FranchiseSetup already reads the stored league shape; `useFranchiseData` now reuses valid source league conference/division membership for franchise standings; FranchiseLens standings still render one league-wide group and are documented/tested as ignoring conferences today; schedule import/manual schedule remains conference-agnostic; playoff seeding has an existing out-of-v1 reader for template divisions/conferences.
**P7 landing note (2026-07-08, M1M MERGED `d35f6a62` — audit APPROVE-WITH-NOTES, incl. M1M-B follow-up):** RULES-V1-PRUNE executed — every visible rules/season-config knob is now wired to real behavior or removed (`8ceaaf3e`); the M1M-B follow-up (`d2795095`) removed the unconsumed `defaultRulesPreset` dropdown and marked the playoff step deferred (playoffs are post-v1). No decorative knobs remain to poison a validation pass.
**P11 landing note (2026-07-08, M1O MERGED `67b39e1b` — audit APPROVE-WITH-NOTES):** scout reveal moves adjacent to the farm phase (`391a0487`) — the draft-arc order is now coherent with the canonical scout-is-farm-only split; hub-based back-navigation; audit note: `proceedLabel` fallback behavior flagged (non-blocking).
**F9/P4 landing note (2026-07-08, M1P MERGED `500e66bf` — audit CLEAN APPROVE):** ONE liquidity-adjusted ceiling now drives the whisper verdict, room-read AND budget light (`68570a39`) — a counter-fixture proved the old contradiction reproduces on pre-fix code; the farm draft gains a live Assistant-GM whisper (`ac1ed30f`) that discriminates via budget-driven maxBid and shows honest "unknown" stubs where farm data genuinely lacks a signal. Engine surfaces diff-verified untouched (UI/advice layer only). Built Claude-side (codex flakiness reroute), audited on the same gate.
**F20 landing note (2026-07-08, M1R MERGED `46ebc2aa` — audit APPROVE-WITH-NOTES):** `fd8bd61c` adds a display-vs-final divergence detector that blocks LOCK until re-extraction, plus a hard set-equality guard in `lockLeaguePool` that throws on any drift between the displayed pool and the locked one — no silent swap possible, fit verdicts always reflect what's on screen. Audit corrected the landing description: the actual mechanism is block-and-force-re-extraction, not upstream-finalize-then-display; it is a strict superset of the old lock gate (never looser), CUT2-2 floor semantics are untouched, and there is no new recompute cost. New small residual surfaced by the audit: **F22** (pool-first lock refuses opaquely after a post-import roster change, no in-UI recovery hint until reload) — ticketed §1c.

## §2 THE REST OF THE PUNCH LIST (from today's audits; all ticketed)
| ID | Item | Why it gates the A-Z test |
|---|---|---|
| P1 | CPU identity auto-assign ("auto-fill remaining", deterministic, never overwrites user picks) | 30-club rooms require 60 hand-picks before LOCK — untestable at scale without it |
| P2 | Design-first mode re-validation post-port (pool-first is browser-proven; design-first is not) | ~~Half the pool-system (Mode A) unverified~~ **LANDED-BY-GAUNTLET (2026-07-08, leg 2 PASS):** design-first proven lockable+draftable in a real browser — legal pool FIRST roll (zero regenerates, zero manual adds), design targets 22/22 post-lock, 30 lots drafted normally incl. shill purchases, 0 console errors. See `MODE1_GAUNTLET_REPORT_2026-07-08.md` LEG 2. Residual: F20 **LANDED** (M1R `46ebc2aa`, §1c) — its own residual F22 (opaque lock-refusal on stale roster-union) ticketed §1c. |
| P3 | Farm auction → AuctionStage fold (stage already implements farm tier + fog) | Kills the mid-journey register break + gives farm the same floor UX |
| P4 | Farm-side Assistant-GM whisper (DJ-28: zero Asst-GM presence on farm) | ~~The farm draft is advice-dead today~~ **LANDED (2026-07-08, M1P MERGED `500e66bf` — audit CLEAN APPROVE):** farm whisper live (`ac1ed30f`) — discriminates via budget-driven maxBid; honest "unknown" stubs where farm data genuinely lacks a signal (no invented reads); engine diff-verified untouched |
| P5 | Staff carry-through (hired reporter/manager identity → hub; generic fallback today) | Staffing choices must visibly matter |
| P6 | Post-freeze summary screen (freeze results are silent today) | JK must SEE what froze (salaries, morale baselines) to validate it |
| P7 | RULES-V1-PRUNE (ruled 2026-07-07): every rules/season knob wired or removed | Decorative knobs poison a validation pass |
| P8 | Conference editor (ruled IN v1 2026-07-01; conferences:[] hardcoded) | League-build completeness — **LANDED M1N** |
| P9 | Wrong-fit penalty Option A (visible pre-bid debit; design §3.3 + recovered §13.3 copy) | The economy's archetype tooth — last unbuilt S7 law |
| P10 | FS-3 shill-dissolve validation (believed fixed by C3's nonCompletingTeamIds; prove in gauntlet with shills>0) | ~~Shills-on configs must not block launch~~ **LANDED-BY-GAUNTLET (2026-07-08, leg 3):** shill dissolution PROVEN CLEAN on a real full shills draft — IndexedDB-verified: exactly 8 real clubs hold rosters (22 each), zero shill roster holders, no stranded players; farm shill-absence + human-seat-never-CPU confirmed in the same leg. See `MODE1_GAUNTLET_REPORT_2026-07-08.md` LEG 3 (the leg's F21 deadlock discovery fixed same night, M1Q `3f98eb06`) |
| P11 | Scout-hire placement in the journey: it currently gates the MLB auction though the scout is farm-only (canonical split) | Journey logic coherence; restructure = move scout-hire adjacent to the farm phase. **NOTE (2026-07-08): F10's code-verified read contradicts this row's premise — beginAuction checks NO scout-hire state today (LeagueBuilderAuctionDraft.tsx:1225-1258); the step sits in the nav chain but doesn't actually gate. With the F3 auto-specialization ruling, P11 reduces to screen PLACEMENT only (move the reveal adjacent to the farm phase).** |
| P12 | JK feel-pass on reserve prices (Lever A, merged; dial default 0.65, k=0 = old economy) | The standing human gate |

## §3 THE MODE-1 GAUNTLET (the validation harness — runs after F1-F4 + P1/P2 land; re-runs after every subsequent P-item)
Scripted end-to-end browser journeys (league build → import → draft setup → lock → scout hire → MLB auction → farm auction → staffing → freeze → rules → launch → lens lands), across the grid:
- {pool-first, design-first} × {6-team SML-scale, 8-team v1-scale} × {shills 0, shills 1-2} × {reserve k=0, k=0.65} — **grid re-based per JK ruling 2026-07-08: v1 draft scale = 8 teams (+1-2 shills); 30-team configs are post-v1**
- Invariants per run: zero 404s · zero console errors · pool locks with zero hand-adds · **nomination order differs between two consecutive drafts of the same pool (F1 regression)** · farm bands render + derive from archetype (F2/F3) · prospect histogram within curve tolerance (F4) · every roster completes legally (no auto-fill stranding; budgets alive per reset-doc targets) · session mid-draft crash/resume preserves state · staffing identities reach the hub (P5) · freeze summary shown (P6) · launch lands on the lens with a playable schedule flow.
- Plus one RUN-IT-BACK iterate-loop leg (edit-wall, re-draft same league).
- Output: MODE1_GAUNTLET_REPORT (pass/fail per invariant per config), committed per run.

## §4 THE ILLOGIC PASS (new audit lens, ruled by JK 2026-07-08)
Distinct from wiring audits: a behavioral-coherence review of the ENTIRE A-Z (draft arc and, separately, living season) asking per screen/step: "would a knowledgeable baseball fan/GM find this behavior nonsensical?" — e.g. identical nomination orders, generic scout pools, inflated prospect curves, prices that ignore context, advice that ignores the roster. Executor: fresh-eyes agents role-playing a GM, walking the real app + inspecting the math behind each surface, reporting illogic findings F-numbered into this doc. Run once on Mode 1 (pre-gauntlet) and once on Mode 2 (post-wiring wave).

## §5 DEFINITION OF DONE for "JK can fully test the draft A-Z"
F1-F4 fixed + P1-P8 landed + gauntlet green across the grid + illogic pass clean (or all findings ticketed) → THEN JK's feel-pass (P12) + design reskin wave (F5). P9/P11 may land in parallel with JK testing if he prefers earlier hands-on.

## §6 CONCURRENCY WITH MODE 2 (per the ruled plan)
Mode-1 lanes above touch draft-side files; Mode-2 wiring wave (flag-activation mechanism → morale trigger wiring → **matchup-engine build lane (M-L, ratified 2026-07-08 per MATCHUP_ENGINE_RECONCILIATION_2026-07-08.md; slots with/immediately after the morale wave, hard dep: morale ledger write-back)** → L10 completion → in-season Asst-GM surface → TV-award rewire → ~~rivalry-edge HISTORY change~~ *(CANCELLED by the 2026-07-08 record-overtake reversal, canon §6 — RIVALRY typing correct-as-built)* → truth-map re-walk) touches season-side files — disjoint surfaces, concurrent lanes, same per-lane gates (Codex builds / adversarial audit / captain browser gate / SOT line per landing).

## §7 MORNING WALKTHROUGH WT-WAVE (2026-07-08, JK live walkthrough — 4 findings triaged, 3 lanes landed, 1 in flight)
JK's Mode-1 walkthrough this morning was largely successful. A tracer sweep triaged four findings into named lanes (WT-A/B/C/D), distinct from the F#/P# numbering above (a separate naming pass for this wave). Contracts: `spec-docs/contracts/CONTRACT_WTA_AUCTION_UX_2026-07-08.md`, `CONTRACT_WTB_PERSONALITY_2026-07-08.md`, `CONTRACT_WTC_RULES_SETUP_2026-07-08.md`.

| ID | Item | Status |
|---|---|---|
| WT-A | Auction floor scout-report control converted hold-to-reveal → click/tap toggle; honest `UNSOLD` (first-pass-recycled under reserve pricing) vs `GONE` (terminal) pass overlays. | **LANDED (merge `c7d4688e`, lane `b051af24`) — audit APPROVE-WITH-NOTES** (timing/predicate/privacy/scope clean; notes are non-blocking test-hardening follow-ups: an engine-driven no-bid round-trip test, a farm-privacy fixture trait-count guard). |
| WT-B | JK-ruled weighted personality draw (Droopy 0.10 / Timid 0.10 / other five 0.16 each) at both live assignment sites + canonical-7 taxonomy fixes (missing "Competitive", 11→7 union narrowing, dark `DraftFlow` hardcode removed). | **LANDED (merge `a4de48c7`, lane `bcc47014`) — audit APPROVE-WITH-NOTES** (weighted-draw math re-derived clean; golden-hash re-pin verified non-masking; DraftFlow's pre-existing `Math.random` display-name gen stays dark, separate issue). **Follow-up `22f3fe5b`:** a full merged-tree suite run caught a second stale personality golden (`prospectChemistryRebalance.test.ts`) WT-B's focused gates missed; re-pinned same session (only-personality diff proven programmatically); a new SESSION_RULES lesson requires a cross-directory golden-fixture grep on future seeded-draw changes. |
| WT-C | Dead `/league-builder/rules` page deleted entirely; FranchiseSetup Season step gains free-entry custom games-per-team (8-200) + innings-per-game (3-9); extra-innings rule restyled radio-dots → box-buttons. | **LANDED (merge `a2a66956`, lane `892f53ae`; cleanup `b532e3a7`) — audit APPROVE-WITH-NOTES** (no-live-schedule-generator claim verified; clamps correct; GameTracker untouched). |
| WT-D | Clickable won-player/overflow/farm-lot profile popovers on both auction floors (extends the WT-A click-toggle pattern). | **LANDED (merge `b4936b8d`, lane `f4466cd2`) — audit APPROVE-WITH-NOTES** (privacy attack refuted: prospect reveal state is a required literal `'hidden'`; band branch structurally cannot render true ratings/trait names; notes = revealFull defense-in-depth + card-vs-popover chip asymmetry, both queued for W1d, §8 below). |

**Also this wave:** `spec-docs/DRAFT_COCKPIT_DESIGN_2026-07-08.md` (Fable, commit `6cb97e0b`) — a 3-tier Assistant-GM redesign (THE CALL / THE READ / THE BOARD), **RATIFIED same session (commit `ecce2d9c`)**; all 3 forks resolved (BALANCE-light removal YES, posture-dial timing PARK until Wave 2 is felt, farm chem chip build-dark YES), plus a new §2.5 farm-bridge directive (farm Asst GM reasons about MLB-roster coverage/flexibility) and an anti-generic design principle added at ratification. JK rulings from this session recorded in `V1_CANON_2026-07-07.md` §6 (dated 2026-07-08): scout-report click-to-reveal supersedes long-press for the `AuctionStage` floor only (the separate S6/RB-11 `LeagueBuilderDraft.tsx` long-press ruling is untouched); passed-player recycling stays, with a comeback-on/off A/B measurement queued behind the economy harness re-basing; rules page deletion + Season-step customization ruled; personality fair-random kept but tilted away from Droopy/Timid; cockpit design ratified with forks resolved.

## §8 COCKPIT WAVE 1 + PLAYER UNIVERSE LANDINGS (2026-07-08, same day, post-ratification)

Five merges landed on `main` after the WT-WAVE above and its cockpit ratification, all pre-merge adversarially audited:

| Lane | Merge | What landed | Audit verdict |
|---|---|---|---|
| WT-D | `b4936b8d` (lane `f4466cd2`) | Clickable won-player/overflow/farm-lot profile popovers, both floors (§7 row above) | APPROVE-WITH-NOTES |
| UNIVERSE | `0537bcf9` (lane `788cce31` + rework `242d0ffa`) | Draft-available player universe: per-league source checkboxes, own-league un-checkable, all leagues listed with counts, `sourceLeagueIds` persisted; default corrected to unfiltered (byte-identical) after audit Finding 1 | APPROVE (delta re-verified) |
| UNIVERSE-FIX1 | `aaf5e0ff` (lane `507721ed`) | Fixed 3 automatic candidate paths that leaked past the universe filter (RosterDesigner candidate feed, feasibility/draftability/reroll consumers, design-first identity-critical-ids); manual pins/hand-adds unrestricted with "LEFT THE POOL" surfacing | APPROVE-WITH-NOTES |
| Cockpit W1a+W1b | `a66d00ed` (lane `7df3e08d` + grade-chip rework `427d2e2c`) | MLB Tier-1 verdict strip (VERDICT, YOUR NUMBER, TRUE COST after tax via `auctionMarginalTax` — closes RB-3's tax-wiring half) + Tier-2 promoted reads (bid-vs-pass, nomination-odds chip, grade-sanity chip on `GRADE_SALARY_BOUNDS`), lights→icons, BALANCE stub deleted, ≤60-word budget | APPROVE-WITH-NOTES |
| Cockpit W1d | `28a4ec9e` (lane `e32b42d6` + rework `838cbd2a`) | Farm bridge: `depthAwareNeedNudge` (covered 0.92/adequate 1.00/thin 1.12; bullpen-short separately hits the existing 1.35 ceiling), honest farm whisper, real fogged board, team-conditioned bridge headline; farm chem stays dark behind `FARM_CHEM_FIT_ENABLED=false` | APPROVE-WITH-NOTES after a REJECT→rework fog-leak cycle (see below) |

**W1d audit story:** the first pre-merge audit REJECTED the lane — the farm board ranked and displayed the scout band's raw midpoint, which is algebraically identical to the true opening ask, defeating the fog. This produced a new general design rule (`77d4feb5`): on a fogged surface, no derived/displayed quantity may be an exact deterministic function of a true-value anchor. The rework introduced a seeded `displayedEstimate` with a regression lock that fails on either a midpoint OR a raw-ask revert; the delta audit approved with one non-blocking note (a near-zero-jitter seed can still land a single row's number close to its true reserve by chance — a jitter floor is an optional model-wide follow-up).

**Design docs landed alongside:** `1935cbb1` (§2.5 farm-bridge ground-truth gate cleared, scoping `depthAwareNeedNudge` as the one sanctioned new multiplier); `a8d7f794` + `f4ea3ffa` (`DRAFT_SKIN_STANDARD_2026-07-08.md` authored and its 2 forks resolved — ONE LANGUAGE everywhere on the live bidding stage, FranchiseSetup wizard deferred to a separate pass; gold-CTA spec = the `EndOfDraftStaffing` treatment; reskin scope = the entire draft journey A-to-Z, sequenced to dispatch AFTER Wave 2 lands per the doc's own §6).

**NEW TICKETS (2026-07-08, from this landing wave):**
- **Cap-table divergence** — `auctionMarginalTax` (the live cockpit tax line) reads the global `LUXURY_CAP_TABLES`, while `projectedTax` reads a league's pool-specific caps. No behavioral difference today since no league customizes caps; fires only if that ever changes. Ticketed, not blocking.
- **Scout-fog jitter floor (optional, JK-gated)** — a near-zero-jitter seed can occasionally land a farm board row's `displayedEstimate` very close to its true reserve by chance. A model-wide jitter floor is a possible follow-up; JK's call whether it's worth building, not required for v1.
- **Farm board GM-sortability** — explicitly OUT of Wave 2 scope; Wave 2 ("THE BOARD") targets the MLB side's live sortable board + auto-advance. A farm-side equivalent, if wanted, is a later fast-follow.

## §9 COCKPIT WAVE 2 "THE BOARD" + RESKIN LANDINGS (2026-07-08, same day, wave closure)

Two more merges landed on `main` after §8, closing out the entire cockpit wave, both pre-merge adversarially audited:

| Lane | Merge | What landed | Audit verdict |
|---|---|---|---|
| Cockpit Wave 2 | `c53478e8` (lane `5312c37d` + rework `8cf4e4b4`) | Shared `RankReorderList` extracted from `RosterDesigner` (byte-identical old surface); `Team.boardRankOverrides` persisted sibling to `rosterDesign` (no DB bump); setup "rank your board" tab in the Zone-3 club editor (global + per-position 5-deep, GM-sortable, pool-scoped); `assembleBoard` rankOverrides blend via `best22Target`'s `gmPreferenceWeight` (2.5, read-only port, sort-order-only); live Tier-3 sortable board with write-back; auto-advance line fires on SOLD only (rework closed the reserve-pass-out misfire) with an "On the block now" variant for the current lot | APPROVE-WITH-NOTES + delta APPROVE (rework) |
| RESKIN | `3266207c` (4 commits `aa85dff9`/`da2472c9`/`7107add4`/`d8fc9561`) | 6 new canon tokens + ruled gold-CTA spec unified; `EndOfDraftStaffing`/`ArchetypePicker` repointed off stale pre-flip hex; chrome sweep (Draft Setup, RosterDesigner, pre-session auction chrome, HANDOFF banner blue→green/brass); THE STAGE CONVERSION per JK's one-language ruling (`auction-theme.css` + `WhisperPanel` → hard-edge: radii→0, hairlines 2-4px, hard offset shadows, decorative gradients flattened; data-viz gradients + team swatches kept); orphaned duplicate Ballpark component kit deleted | APPROVE-WITH-NOTES |

This closes the ratified `ASST_GM_DRAFT_INTELLIGENCE_SPEC_2026-07-04.md`'s B3 + Corrections 5/7 (Wave 2) and JK's `DRAFT_SKIN_STANDARD_2026-07-08.md` one-language ruling (the reskin). **THE COCKPIT WAVE IS NOW COMPLETE**: Wave 1 (W1a/b/c/d) + Wave 2 (the board) + the reskin are all merged. Remaining from the ratified design: Wave 3's posture dial stays PARKED pending JK's in-browser feel of Wave 2; the wrong-fit penalty (P9, §2 above) is next, scoped to the economy lane; the BALANCE light returns only after a future handedness-signal spec.

**NEW TICKETS (2026-07-08, from this landing wave):**
- **Draft Setup full tokenization, incl. the blue accents** — the reskin's chrome sweep deliberately left three blue (`#3B7DD8`) spots untouched pending JK's eye: the pitch-arsenal toggle buttons and the Save button on the player edit panel (both in the player-edit modal), and the Available Players column's accent trim. JK rules per-spot: tokenize to the canon palette, or keep blue as a deliberate signal color (it's currently also used for the live board's position badges, unrelated to this ticket). See the new checklist doc for the JK-facing version of this question.
- **Dedicated bright-gold token** — the reskin's ruled gold-CTA spec currently reuses the warn-border token's hue for its bright-gold hover state (`#D4B863`) rather than a purpose-named token. Semantically loose; a dedicated `--ballpark-gold-bright` (or similar) token is a low-priority hygiene follow-up, not blocking.
- **Remove dead `positionBadges` helper** — flagged during the reskin sweep as dead code (superseded by the current position-badge rendering path); a small deletion follow-up, not scoped into this lane to avoid touching an unrelated surface mid-sweep.
- **Auto-advance engine-natural-top-pick branch (optional future)** — Wave 2's `computeBoardAutoAdvanceLine` implements only the "a player the GM had ranked" half of the spec's disjunction; the second half (surfacing a promotion when the GM never set an explicit rank at all, using the engine's own previous top target) was scoped out as needing new cross-turn state plumbing beyond this lane's "no new engine math" mandate. Optional future work if JK wants the fuller behavior.

**In flight, not independently confirmed by this pass:** a Wave 2 "THE BOARD" lane (shared rank component, a setup RANK YOUR BOARD zone, the live Tier-3 sortable board + auto-advance) was reported dispatched. This scribe pass found no contract file in `spec-docs/contracts/` and no `git worktree` entry for it — book it as a status report, not a landing, until a contract or commit appears. The reskin lane is queued to follow Wave 2 (same files, per `DRAFT_SKIN_STANDARD_2026-07-08.md` §6). A full `NODE_ENV= npx vitest run` was observed actively running against the merged tree at booking time; result not yet available.

**Scribe correction (2026-07-08, later booking pass):** the "in flight, not independently confirmed" paragraph directly above is superseded by the §9 table above it — Cockpit Wave 2 (`c53478e8`) and the RESKIN (`3266207c`) both git-verified LANDED. Preserved here rather than deleted per the never-delete-history rule.

## §10 BOARDFIX1 + BOARDFIX2 + TEXTLAW-SWEEP LANDINGS (2026-07-08, same day, post-wave polish)

Three more merges landed on `main` after §9's wave closure, all pre-merge adversarially audited via the standard builder≠auditor triangle (Codex builds off a contract, Opus audits read-only, the captain merges):

| Lane | Merge | What landed | Audit verdict |
|---|---|---|---|
| BOARDFIX1 | `dfbd31f4` (lane `ba23dc97`) | Design-first ranking surfaces were reading the wrong pool copy — fixed to read the EXTRACTED pool (locked→extracted predicate fix, repro-proven by reverting it and watching accepted tests fail); the START THE DRAFT gate independently re-verified as not loosened (a display bug explained the "stuck" feel); rank-badge typed edits gain type-to-move + send-to-top on every ranking surface. Contract: `CONTRACT_BOARDFIX1_2026-07-08.md` | APPROVE-WITH-NOTES (live-floor rank controls unstyled until the next CSS pass, closed by TEXTLAW-SWEEP below; LOCK POOL discoverability routed to JK's eye) |
| BOARDFIX2 | `fb3c9fd9` (lane `ffa7fe46`) | JK's third-walkthrough findings: an always-visible readiness panel (`data-testid="draft-readiness-panel"`) names every unmet LOCK POOL / START THE DRAFT condition in plain language (gates verified byte-identical, not loosened); rank-badge typed edits now land at the LITERAL displayed position on every board surface, not a soft nudge (repro proven by revert: 3 tests fail pre-fix); board reorders are instant on-screen with a debounced single-save persistence, flushing on unmount/tab-hide (spy-proven: 5 moves = 1 save). Contract: `CONTRACT_BOARDFIX2_2026-07-08.md` | APPROVE-WITH-NOTES (note 1: a sub-500ms cross-club rank-edit race could drop the outgoing club's pending save, setup screen only, fixed in the very next lane; note 2: a benign duplicate-save console warning on mid-flush unmount) |
| TEXTLAW-SWEEP | `39660e86` (lane commits `72a3a9b2` Item A / `712126b2` Item B / `93317821` Item C) | Executes JK's §7 ratified Text Law classification (design doc `eb9de7c6`/`2475b329`): tutorial copy on DraftSetup / ArchetypePicker (parent-wired) / AuctionStage / EndOfDraftStaffing (which gained its own top-right Help button) now gates behind Help; two SPLITs (Cap-Fit, a stray notice) keep their number visible while the surrounding lecture gates; a REVERSE FIX made the auction phase-label pill always-visible; whisper-board-* rank controls styled hard-edge with existing ballpark tokens only (closes BOARDFIX1's "unstyled" note); the BOARDFIX2 cross-club save race fixed repro-first (auditor re-proved by revert). Contract: `CONTRACT_TEXTLAW_SWEEP_2026-07-08.md` | APPROVE-WITH-NOTES (N1: a pre-existing intermittent async flake on DraftSetup's floor test, unrelated — re-run solo if red; N3: the scout price-range bar has no eyebrow label while Help is closed, per the ratified classification — a JK-eye call, not a defect) |

Alongside these, the `NOW/` single real-time entry-point folder was created (commit `08363276`, docs-only, no code diff) — `NOW/README.md` indexes and live-links every canonical doc plus the binding design standards; a scribe verifies the index every booking pass from here forward.

No new tickets from this wave beyond what's already tracked: the §9 blue-accents/gold-token/dead-helper/auto-advance-branch tickets stand unchanged. JK's walkthrough of these landings is folded into the refreshed `spec-docs/JK_BROWSER_CHECKLIST_2026-07-08.md`.

## §11 COCKPIT WIRING AUDIT → CALLFIX → TAXTEETH → TEAMIDGUARD + AUCTION FLOOR REFIT (2026-07-09, JK walkthrough finds + product rulings)

JK's continued walkthrough of the merged cockpit (§8-§10) surfaced a frozen-verdict bug, then a tax-economics ruling, then a floor-usability directive. Five landings, all shipped as JK-clicked PRs to `main`, all pre-merge adversarially audited via the standard builder≠auditor triangle:

| Lane | Merge | What landed | Audit verdict |
|---|---|---|---|
| COCKPIT WIRING AUDIT | `4d33085c` (PR #26, docs-only) | Root-caused the frozen-verdict find (bid moved past YOUR NUMBER, strip stayed PUSH): the verdict formula never consumed the live bid at all — two independent code paths could disagree. 5 dimension auditors + a verdict-plumbing tracer, every claim adversarially re-verified: 10 confirmed / 5 downgraded / 6 refuted / 27 wired-OK. Landed `spec-docs/COCKPIT_WIRING_AUDIT_2026-07-08.md` + two BINDING amendments to `DRAFT_COCKPIT_DESIGN_2026-07-08.md` (§2.6 THE LIVE CALL, §2.7 reason priority + payload hygiene). One finding (tax display-only) flagged as a JK fork | N/A — docs-only, spec-first for CALLFIX |
| CALLFIX | `88c34d30` (PR #27) | Builds §2.6/§2.7 plus 4 more audited gaps: THE LIVE CALL ladder (both floors, fog-safe farm), priority-ranked reason chip (12 codes, test-locked), tappable lot-log names (4th popover surface), auto-advance reading the live rank overlay (was ~500ms stale), payload hygiene (dead fields dropped, dead tax compute deleted, replacement-value estimate surfaced, market banner single-sourced). Contract: `CONTRACT_CALLFIX_2026-07-08.md` | APPROVE-WITH-NOTES (auditor reproduced the pre-fix bug on a rebuilt pre-fix worktree; gates 8 suites 168/168) |
| AUCTION FLOOR REFIT (ruling) | `50da7ad4` (PR #28, docs-only) | JK's floor-usability directive (whisper caged and scrolling, unclear whose turn, too much noise) ratified into `spec-docs/AUCTION_FLOOR_REFIT_2026-07-09.md`: one scroll context, say-it-once dedup, team-color ON THE CLOCK banner, roster board into reclaimed dead space. Build dispatches after TAXTEETH (same files) | N/A — docs-only ruling; build = FLOORREFIT lane, in flight (`CONTINUITY_CHECKPOINT.md` §3) |
| TAXTEETH | `c0a24363` (PR #29) | JK ruling: "make it real" — the luxury tax gets teeth. Settlement drains salary + the marginal tax of the player won; the live bid ceiling reserves it; CPU/shill bidders inherit the same ceiling; whisper TRUE COST = actual budget drain, proven end-to-end. Contract: `CONTRACT_TAXTEETH_2026-07-08.md` | APPROVE-WITH-NOTES — highest-risk item (partially reverses a fix from 6 days earlier that pulled tax OUT of the ceiling); auditor proved no late-draft freeze-out returns. Two forced-completion paths stay untaxed on purpose, ruled unexploitable, ticketed. Gates: 204/204 |
| TEAMIDGUARD | `677a7607` (PR #30) | Closes TAXTEETH's audit finding: the legacy Teams-page editor could silently clobber a team's archetype tax identity on any save. Archetype teams' identity section is now read-only on Teams (exact stored shifts + "set by archetype" note); non-archetype teams keep the legacy editor behind a dirty-guard; `rawShift` data now survives load→save. Contract: `CONTRACT_TEAMIDGUARD_2026-07-09.md` | APPROVE-WITH-NOTES — repro re-proven (5 fail reverted, 32/32 fixed), zero file overlap with the tax lane. Note-1 opened TAXPRECISION (tax engine read a coarse approximation, not the exact numbers). Gates: 32/32 + 209/209 |

**New tickets from this wave (integrity sweep, 2026-07-09):** a JK-ordered follow-up sweep of the setup→draft surfaces (5 dimension sweepers + adversarial verification, 39 claims → 8 confirmed / 10 downgraded / 4 refuted / 17 safe) found nothing that breaks a normal lock→start draft. Full record: `spec-docs/SETUP_DRAFT_INTEGRITY_AUDIT_2026-07-09.md`. Opened three lanes (`CONTINUITY_CHECKPOINT.md` §3 has worktree/contract pointers): **TAXPRECISION** (tax engine reads the archetype's exact `rawShift`, not the coarse table — built, awaiting audit), **STALEPARITY** (pool-first mode gets the same staleness net design-first already has — contract landed, build starting), and two queued lanes not yet dispatched — **COPYFIX** (a stranded Duplicate-League fix that needs re-landing, plus a dangling-team-id crash, a dropped board-rank preference, and a farm-session-blind mutation guard) and **GAUNTLET** (re-proves draft completion holds under real tax — not the same thing as this doc's own §3 gauntlet grid).

JK's walkthrough of all five landings above is folded into `spec-docs/JK_BROWSER_CHECKLIST_2026-07-08.md` §7.

## §12 TAXPRECISION + FLOORREFIT + STALEPARITY + ARCHLOCK LANDED; GAUNTLET STOP resolved as a harness bug (2026-07-09, later scribe pass)

Four more landings on `main` since §11, all shipped as JK-clicked PRs, all pre-merge adversarially audited via the standard builder≠auditor triangle:

| Lane | Merge | What landed | Audit verdict |
|---|---|---|---|
| TAXPRECISION | `23a0a11a` (PR #31) | Closes TEAMIDGUARD's audit note-1: the auction tax engine delegates to the exact `rawShift` percentages instead of a coarse 2-modifier table — every archetype team had been overtaxed on every category since TAXTEETH went live (worked example: rangy-defenders $78,014 → $0, murderers-row corrected $937,596). Contract: `CONTRACT_TAXPRECISION_2026-07-09.md` | full APPROVE. Gates: tsc clean, build 0, 146/146 |
| FLOORREFIT | `73fe94da` (PR #33) + rework `782c4cb3`/`d3d38322`/`814d7b4d` | Builds the AUCTION FLOOR REFIT ruling: whisper uncaged, team-colored ON THE CLOCK banner, every advisor atom renders exactly once. Captain-ordered rework retired a duplicate "who's up" pill, fixed a real WCAG-contrast bug on the banner's default-orange text, and made human nomination-turn copy reachable. Contract: `CONTRACT_FLOORREFIT_2026-07-09.md` | APPROVE, then delta APPROVE. Gates: 138/138 |
| STALEPARITY | `0fcb2cf7` (PR #34) | Closes the integrity sweep's SB-2/SB-4/MONEY-1 cluster: pool-first setup gets the same basis-snapshot-at-LOCK drift protection design-first already had. Contract: `CONTRACT_STALEPARITY_2026-07-09.md` | APPROVE-WITH-NOTES. Gates: DraftSetup solo 93/93, RankYourBoardZone 7/7 |
| ARCHLOCK | `03f4e5d1` (PR #35) | Corrects 2 of 24 archetype sheet entries (HDH Royals, Bash Brothers) that had drifted from the live code (code proven correct/deliberate, sheet was stale); adds a permanent conformance test guarding future drift. Contract: `CONTRACT_ARCHLOCK_2026-07-09.md` | APPROVE-WITH-NOTES. Gates: conformance 25/25, balance gate 24/24 in band |

**This scribe pass also fixed** the archetype sheet's own stale "maxDev 4.4%" summary line (ARCHLOCK's audit flagged it, didn't fix it) — now cites the live per-tier numbers 7.7%/3.8%/7.9% at `TEAM_ARCHETYPES_24.md`.

**GAUNTLET's STOP condition, resolved (not yet fully gated):** the D1-D7 full-draft-under-real-tax proof (queued behind TAXPRECISION per §11) hit a STOP — a pool-first round-robin drive left some teams unable to finish a legal roster. Independent adversarial verification (`CONTRACT_GAUNTLET_REWORK_2026-07-09.md`) delivered a verdict: **HARNESS BUG, not a product/economy problem** — a zero-tax control stranded identically (worse, even), the real competitive-bidding path completes the same pool cleanly with real tax charged, and the engine's settlement math is exact across 314 checks. Five required harness fixes are dispatched and actively in progress in the worktree (`/private/tmp/kbl-gauntlet`), not yet committed/gated. **Naming collision reminder (unchanged from §11):** this GAUNTLET is NOT this doc's own §3 gauntlet validation grid.

**Two threads open, not yet landed — full detail in `CONTINUITY_CHECKPOINT.md` top banner + §3:** (1) **SETUP TAX-BLINDNESS**, confirmed this pass — every Draft Setup affordability signal (Cap Fit, THE CLUB CHECK, THE MONEY banner) is salary-only, blind to the now-real luxury tax; no lane dispatched yet. (2) **WHISPER TAX-AWARENESS SWEEP**, captain-reported in flight (5-dimension workflow checking whether the Assistant-GM intelligence and CPU bidders reason about real marginal tax) — this pass could not locate its worktree or contract; status report only. **COPYFIX** (§11's queued item) is now build-complete with gates green, awaiting audit — worktree `/private/tmp/kbl-copyfix`.

JK's walkthrough of the four new landings above is added to `spec-docs/JK_BROWSER_CHECKLIST_2026-07-08.md` §8.

## §13 THE TAX-COHERENCE PROGRAM COMPLETES — COPYFIX, GAUNTLET, TAXWIRE, FLAKEFIX, POOLFLOOR, TAXENGINE, SETUPTAX (2026-07-09, later scribe pass)

Seven more landings on `main` since §12, all shipped as JK-clicked PRs, all pre-merge adversarially audited via the standard builder≠auditor triangle. **This closes the tax-coherence program: every affordability signal in the draft — setup screens, the live whisper, CPU bidding, and full-draft completion — is now honest about the real luxury tax.**

| Lane | Merge | What landed | Audit verdict |
|---|---|---|---|
| COPYFIX | `837732e1` (PR #37) + rework `35ffeea3` | Closes the league-lifecycle integrity cluster: delete-a-team now prunes it from every league's membership; Duplicate League tolerates a pre-existing ghost team id (skip + warn, never silently bricks); the stranded post-draft pool-first copy fix (`84a0a162`) is finally re-landed on main; board-rank preferences survive a league copy; a live FARM auction now locks editing the same way MLB already did. Contracts: `CONTRACT_COPYFIX_2026-07-09.md`, `CONTRACT_COPYFIX_REWORK_2026-07-09.md` | **REJECT → rework → delta APPROVE** — the first build's ghost-id fix was a load-time "heal" that persisted the filtered membership back to storage; the auditor proved this would gut a valid league mid-cloud-sync and echo the damage to every other device. Reworked to read-time tolerance with zero load-path writes; the destructive probe is now a permanent regression test. Gates: 83/83 contract suites, 279/279 leagueBuilder sweep |
| GAUNTLET | `bbf15b97` (PR #38) | The permanent six-draft completion proof under real organic tax — both pool modes including the first-ever full design-first draft, all legal, tax genuinely draining ($2.1M-$12.4M per draft). History: the first attempt STOPPED on stranded drafts; independent verification proved it was a HARNESS BUG (a zero-tax control stranded identically), not a product defect; reworked to faithful CPU drives. Contracts: `CONTRACT_GAUNTLET_2026-07-09.md`, `CONTRACT_GAUNTLET_REWORK_2026-07-09.md` | **Delta APPROVE-WITH-NOTES** — this suite runs shill-less; whether production-default settings (2 shills) complete the same two tight seatings was left UNKNOWN and became the GAUNTLET-2 leg that found POOLFLOOR's real gap (row below). Gates: gauntlet suite green (52s), dependency suites green |
| TAXWIRE | `78c51d7d` (PR #39) | The whisper's Bid-vs-Pass "$ left if you win" figure now nets the same marginal tax settlement actually charges — one-line fix, every downstream "affordable" flag inherits it. Audit refereed a builder-vs-auditor disagreement over a suspected buried tax reason — REFUTED, no such code path exists to bury. Contract: `CONTRACT_TAXWIRE_2026-07-09.md` | **APPROVE-WITH-NOTES.** Gates: 159/159 + 78/78, repro re-broken and matched byte-exact |
| FLAKEFIX | `4e6cfd33` (PR #40, zero product changes) | Ends the `LeagueBuilderDraftSetup.test.tsx` flake (3 diagnosis cycles this week): the 93-test mega-file splits into 5 per-zone suites; two root-caused timing bugs cured (a readiness-panel placeholder race, missing per-test timeouts); a lock→drift→unlock→relock cycle test added. Contract: `CONTRACT_FLAKEFIX_2026-07-09.md` | **APPROVE-WITH-NOTES** — auditor re-derived fidelity via real AST extraction (all 93 originals survive, 74 byte-identical / 19 = the documented cures, zero assertion loosening). Gates: 94/94 × 2 + an 18-file/391-test batch-load run, zero blinks |
| POOLFLOOR | `a14437f1` (PR #41) | Fixes the REAL production-default stranding GAUNTLET-2 found: at production defaults the pool carried exactly one closer per team with zero slack, so ordinary hoarding stranded another team at 18/22. Every hard-legal-minimum position now gets a runtime-derived supply floor with hoarding slack (`teams × min + max(2, ceil(teams/3))`); shortfalls the universe can't satisfy are named plainly in the readiness panel. Contract: `CONTRACT_POOLFLOOR_2026-07-09.md` | **Full APPROVE** — auditor reproduced the byte-exact pre-fix stranding, verified the top-up path is purely additive, confirmed both stranding seatings now complete. Gates: 79/79 pool suites, both gauntlet suites green |
| TAXENGINE | `d6d7f069` (PR #42) + merge-resolution `1773b4af` | The whisper's Fill Reserve / Room readouts and the CPU liquidity-posture chip now fold in the tax of the team's actual cheapest legal completion, recomputed per lot, exactly $0 under-cap (byte-identity locked). Bounded semantics deliberately avoid the old snowballing full-roster hypothetical tax bill that once froze late-draft bidding. Contract: `CONTRACT_TAXENGINE_2026-07-09.md` | **APPROVE → REJECT-one-item → delta APPROVE** — first audit flagged the builder had also rewritten the CPU posture-classification rule (steers live CPU bidding aggression) to make its own fixture pass; **captain REJECTED that as an out-of-scope rule change**, rework restored it byte-identical with two tripwire tests, re-proved the fixture using the tax fix alone. Gates: 208/208 incl. the six-draft gauntlet |
| SETUPTAX | `6fa97d81` (PR #43) | The tax-coherence program's final piece: THE CLUB CHECK row, the identity strip, THE MONEY panel's new TAX WATCH line, and the archetype market outlook all surface the tax-aware picture the engines already computed — zero new tax math, pure display wiring. Contract: `CONTRACT_SETUPTAX_2026-07-09.md` | **APPROVE-WITH-NOTES → REJECT-one-item → delta APPROVE** — the first build's tax-overshoot predicate reduced to plain "not solvent," so a purely salary-blown target still rendered "OWES $0 TAX" — causally dishonest. **Captain ruling: tighten the predicate, don't soften the copy** — TAX now fires only when salary alone is within budget and tax is what pushes it over. Gates: 138/138 |

**Program-level tickets carried to v1.1 (full self-contained text with file:line and evidence pointers lives in `CONTINUITY_CHECKPOINT.md` §4a — do not chase these from session memory, read that doc):** untaxed forced-completion paths (ruled unexploitable for v1, ticketed since TAXTEETH); a TAXENGINE Fill-Reserve/Room live-render integration test (today's coverage is pure-function + a payload-fixture render test, not an end-to-end live-auction render proof); the farm-side sibling of TEAMIDGUARD's identity-clobber fix (`farmCapIdentity`, downgraded to cosmetic — no dollars ride on it today); a hand-edit displacement round-trip test and a pool-first/design-first hand-edit parity check; extending COPYFIX's ghost-id filter to `LeagueBuilderDraftSetup.tsx`'s own team-count reads; the dead `_suppressSync` flag in `syncEngine.ts`; and the five "latent, ticketed for v1.1" rows from the setup→draft integrity sweep (SB-12, F1, F2, F3, F7 — see `SETUP_DRAFT_INTEGRITY_AUDIT_2026-07-09.md` §3).

**Captain rulings this program, recorded in `V1_CANON_2026-07-07.md` §6 (dated 2026-07-09):** tax-cause honesty (a TAX headline requires tax to be the genuine marginal cause, not just present); CPU behavior rules never change as a fixture convenience (the TAXENGINE posture-rule revert); pool supply floors carry hoarding slack derived from the legal-roster minimums, not a hardcoded list; one-writer / read-time-tolerance for lifecycle heals — no persisting a load-path heal, ever, because of the cloud-sync echo hazard (the COPYFIX rework).

**JK's answer recorded this session (not yet a formal audit finding — captured here for v1.1 scoping):** manual-add + re-extract displacement IS supported today in design-first pool building (`src/engines/poolFromDemand.ts`, spec `FABLE_POOL_SIZING_DESIGN_2026-07-03.md` §3.2-4.3) — a hand-add is a protected "must-keep" that displaces the worst-fit unprotected automatic pick, and the pool may exceed its target only when protected players alone exceed it. Pool-first mode's equivalent (`src/utils/leagueBuilderPoolBuilder.ts`'s simpler `handAdds`/`handRemoves` diff-tracking, ~lines 108-136) has not been verified to carry the same protected-displacement guarantee — parity assessment queued for the v1.1 batch, not yet a confirmed gap.

**Status after this program: the tax-coherence thread is COMPLETE.** The only remaining gate is JK's browser walkthrough — see the new checklist section in `JK_BROWSER_CHECKLIST_2026-07-08.md` §9.
