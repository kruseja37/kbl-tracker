# MODE-1 PUNCH LIST → "JK CAN FULLY TEST THE DRAFT A-Z"
**Status doc, 2026-07-08 (JK Friday-walk findings + audit synthesis). Parent: V1_CANON §2-§3, ORPHAN_WIRING_MATRIX. Update in place as items land.**

## §1 JK FRIDAY FINDINGS (browser walk, 2026-07-03/04 — ruled defects, no further ruling needed)
| ID | Defect | Class | Fix direction |
|---|---|---|---|
| F1 | **Deterministic nomination order across drafts** — same players surface in the same order every draft (e.g. Buttons Bunterson always first) if the pool is unchanged. | Illogic (wired-but-wrong) | Nomination seed must be per-draft-instance: derive from the auction SESSION id/launch nonce, persisted in the session (resume keeps order within a draft; every new draft differs). Applies to MLB and farm. Surface: selectNextNominee seed inputs. |
| F2 | **Farm draft shows no grade-number band per scouting specialty** — beyond the intentional rating-fog differences, the farm lots lack the banded grade read the spec requires. | Missing UI + wrong source | Bands must render on farm lots AND derive from the FARM ARCHETYPE (3/5/7-band confidence per SCOUTING_INTELLIGENCE_SPEC §7) — the shipped source (hired-scout descriptor specialties) is the known S4 spec violation. **Table RATIFIED 2026-07-08 → spec-docs/FARM_ARCHETYPE_SCOUT_CONFIDENCE_2026-07-08.md (24 archetypes × 8 areas + captain rulings §8). Lane M1D staged, queued behind M1b's merge (shared engine file).** |
| F3 | **Scout hiring shows a generic scout group** — the hire pool is not dependent on the chosen archetypes or prospect pool. | Wrong source | Scout-hire options must derive from the team's farm archetype (the archetype→per-area-confidence table, unbuilt); the legacy scout-pool/hire-draft mechanism is the deprecated path (Q11 refactor, never executed). **RULED (Fable, 2026-07-08): execute the §11 deprecation now — each team's scout AUTO-derives from its farm archetype (specialties = 3-band areas, weaknesses = 7-band areas); ScoutHire becomes a no-choice "meet your scout" reveal; persistence record shape unchanged. Rides lane M1D.** |
| F4 | **Prospect quality inflated** — generated farm prospects do not match the spec's grade distribution curve. | Distribution defect | Measure: histogram generated prospects against PROSPECT_GENERATION_SPEC's curve (generator must be the inverse of the scoreSmb4Player grade oracle); fix the generator/pool-shaping to the curve; add the histogram to the gauntlet as a permanent invariant. |
| F5 | **UI style mismash across the arc** (known; design pass ordered after correctness — Staff-Your-Clubs/ballpark kit target). | Cosmetic | Reskin wave rides the kit (built 2026-07-07); after the punch list. |

## §1b ILLOGIC-PASS FINDINGS F6–F11 (Mode-1 GM-lens review, 2026-07-08 — 3 lenses: economy/flow/roster, 617k tokens, 10 raw → 6 deduped; none overlap F1-F5/P1-P12)
| ID | The illogic (plain) | Severity | Evidence anchor | Disposition |
|---|---|---|---|---|
| F6 | "Confirm Staff → Continue to Franchise Setup" drops the league you just drafted — lands on a blank unfiltered league picker; every other hop threads leagueId. | breaks-immersion, every draft | EndOfDraftStaffing.tsx:161 navigates without leagueId; FranchiseSetup.tsx never reads one (Step1SelectLeague ~422-469 expects a handoff the sender never makes) | Fold into **P5 lane** (same file surface): thread `?leagueId=`, auto-select on mount, completion badge on the drafted league |
| F7 | Farm lot "Scout value $" comes from an unrelated static letter-grade salary table (self-documented build-dark) — routinely disagrees with the real Opening ask beside it; identical for all same-band prospects. | breaks-immersion, every farm lot | LeagueBuilderFarmAuctionDraft.tsx:112-134, 673-694 → GRADE_SALARY_BOUNDS (ratingsAdjustmentEngine.ts:149-162) vs calculateIvBaseSalary (farmAuctionPool.ts:108-114) | **Folded into lane M1D** (same function it restructures): scout value = archetype-band-width fog around the true-IV opening ask |
| F8 | Once a position's legal minimum is met, the CPU values a 5th body there same as filling an empty spot → simulated rooms reproducibly hoard (5 SS / 4 C teams) while sibling positions sit at bare minimum. | systemic economy defect (sim-confirmed, 36 rosters) | ownNeedMultiplier never <1.0 post-floor (auctionMarketModel.ts:398-406); needBucketForCandidate buckets post-floor as position-blind filler; live via cpuShillBidding.ts:23,322 | ~~New economy lane **M1E**~~ **PARKED (2026-07-08 overnight): the M1E-FINAL v1-scale gate FAILED on the repaired engine** (M1/M2/M4 fail in BOTH states; the sim-lane baseline itself is unhealthy; the liveEngine gate-lane is unjudgeable — the harness throws on auction-uncompletable). The GAUNTLET leg 1 subsequently proved real 8-team drafts complete cleanly → **the harness's pool construction is the artifact, not the product**. F8/F11 stay parked on `lane/m1e-diminish` (commits incl. gate report `6f6d0bbd`) pending a properly-based harness. **NOT a v1 walkthrough blocker.** F16 (over-pessimistic archetype advisory) is the same tuning family. |
| F9 | Whisper panel contradicts itself on one lot: verdict/MAX BID use the liquidity-reserved ceiling, room-read/budget-light use the unreserved ceiling → "Let him go" + green "inside expectations" simultaneously, common whenever slots remain open. | confusing | WhisperPanel.tsx:344,383-386,432-436,518-526; rosterIntelligencePayload.ts:277-325,575-610 | **RULED (Fable 2026-07-08): ONE ceiling — the liquidity-adjusted maxBid drives verdict, room-relation AND budget light; capValue may render only as a separately-labeled "total capacity" line.** ~~Small lane M1F~~ **LANDED (2026-07-08, M1P MERGED `500e66bf` — audit CLEAN APPROVE):** one ceiling drives all three reads (`68570a39`); a counter-fixture proved the OLD leak (the contradiction reproduces on pre-fix code); engine diff-verified untouched. |
| F10 | A league can begin (and farm-draft) having skipped Scout Hire entirely — only symptom is silent "N/A" scout ranges everywhere. Also: code-verified premise correction to P11 (scout-hire does NOT actually gate the MLB auction today). | confusing → structural | beginAuction blockers (LeagueBuilderAuctionDraft.tsx:1225-1258) check no scout state; loadOptionalFarmScouts → null → "N/A" | **Structurally resolved by lane M1D**: bands/scout identity derive from the farm ARCHETYPE at draft time, not from ScoutHire persistence — a skipped reveal screen can no longer kill scouting. M1D contract hardened accordingly; P11 annotated |
| F11 | The roster advisor only warns "too thin," never "over-stacked" — five shortstops + one emergency catcher draws zero findings. | confusing | analyzeRoster keys only off thinPositions (rosterAnalyzerEngine.ts:840,898,923) | Fold into **M1E** (the advisor names post-hoc what F8's pricing fix prevents) |

## §1c JK ITEMS 2026-07-08 EVENING (diagnosis in flight)
| ID | Item | Status |
|---|---|---|
| F12 | **Farm prospect generation currency check** — verify the farm generator is the NEWEST path using all IV-engine inputs (litmus: pitcher arm slots) AND the farm lot UI shows exactly: name, age, primary/secondary position, trait COUNT — no other specifics. | **TRACED (2026-07-08).** Generator IS the current spec-v2 analyzer-anchored path (old gradeEngine generator orphaned as ordered; all 3 farm entry points on the live one). ONE IV-input gap: **armSlot is type-pinned `null`** (prospectScoutingDraftEngine.ts:195, :1458) — NO generator assigns arm slots though ivEngine prices them (:378,:603) and database players carry them → farm pitchers can never be Sub-slot; IV parity break vs pool pitchers. (Arm-slot DATA landed 2026-06-10/11; what landed ~last week was card DISPLAY, e778eb8a.) UI: **auction lot card missing trait COUNT** (otherwise conformant, zero over-disclosures); **startup board OVER-discloses 5 fields** (potential grade, chemistry, personality, archetype family, salary — deliberately in VisibleSafeProspectReport → JK ruling needed before stripping). Fixes: **lane M1H** (after M1D — shared engine file): seeded arm-slot draw anchored to database pitcher distribution + DTO type widen + spec section + lot-card trait count. **F12c RULED (JK, 2026-07-08): startup board KEEPS its five extra fields (the visible-safe set stands); the strict 4-field display spec binds the farm AUCTION lot card only.** |
| F13 | **Farm draft CPU takeover of a user team** (JK Friday): with a shill configured in the MLB draft and no shill wiring in the farm draft, the engine treated a USER team as a CPU/shill bidder — it silently bid and won players. Suspected: farm role assignment doesn't read Draft Setup ownership (or single-human assumption). | **ROOT CAUSE CONFIRMED (2026-07-08):** farm page writes a DEFAULT shill count (floor(N/3)) into session config, but farm builds no synthetic shill seats — so the legacy "last N nomination-order teams become CPU" fallback captures REAL clubs, ownership never consulted (useFarmAuctionDraft.ts:99-119,:484). With 2 human clubs in a 6-team room ≈60% chance per draft; persisted sessions re-trigger on every resume. MLB was already hardened (useAuctionDraft.ts:647-649) but has a latent legacy-RESUME hole (cpuTeamRoles.ts:31-34,62-77 exclude only AI clubs). **RULED (Fable): farm has NO shills in v1** — CPU behavior only for `controlledBy==="ai"` clubs. **Fix lane M1G dispatched** (zero the count, kill the fallback, heal persisted sessions, human-exclusion belt-and-braces in cpuTeamRoles closing the MLB hole too). |
| F14 | **Live auction completion terminal-path bug — CONFIRMED on unmodified code (2026-07-08).** `surfaceNextPlayer` marks `AUCTION_COMPLETE` directly when no nominee remains (src/engines/auctionStateMachine.ts:421 entry; direct-complete branches :430 and :444), bypassing the exhaustion cleanup — `backfillFromPassedLots` (:695) + the Lever A affordable-pricing amendment run only on the `advanceLot` terminal (:650-654). Real drafts can terminate "complete" with SHORT rosters, silently. Live-driver matrix at v1 scale: 13-16/18 and 21-22/24 legal rosters across BOTH k=0 and k=0.65 — likely the polished cousin of JK's stuck-draft manual finding. Discovered by the M1E gate chain (sim gate fail → A/B attribution → live-driver truth run). | **LANDED (2026-07-08, M1J MERGED `08236fdf` — audit APPROVE-WITH-NOTES):** exhaustion cleanup runs on EVERY terminal path (`d8c64fa3`); genuinely-uncompletable pools surface an explicit `terminalShortfall` status instead of a silent short. Audit rigor: the falsification check was self-run by the auditor against genuine base code; an organic full-draft driver showed ZERO incomplete rosters across all legs. **Chain consequence:** F8/F11 re-judges on this fixed base — lane M1E-FINAL dispatched. Residual → F14b. |
| F14b | **Uncompletable-pool UI drop (residual of F14; strictly better than the old silent-short).** The engine computes `terminalShortfall` for genuinely-uncompletable pools (auctionStateMachine.ts:140,:671) but the UI hook throws and drops it (`useAuctionDraft.ts:150 transitionOrThrow`) — the user gets a soft-lock with an error toast instead of a graceful "draft cannot complete" screen. | **TICKETED (2026-07-08):** small UI lane — fold into a later Mode-1 UI pass. Not urgent: pool sizing makes it near-impossible to trigger at v1 scale. |
| F15 | **Stale pool-legality check after regenerate** (gauntlet leg 1): regenerating the pool leaves the shared-pool legality panel showing a FALSE ✗ ("seats 0 of 8, fails at CP") until a manual RE-CHECK — a user could misjudge pool legality either direction. | **TICKETED (2026-07-08, gauntlet):** small fix — auto re-check (or invalidate-with-banner) on pool regenerate. |
| F16 | **Archetype market advisory wildly pessimistic** (gauntlet leg 1): "Archetype market outlook" claimed 0/24 archetypes buildable and flagged closers tightest — the real auction then completed easily with money to spare for every club. | **TICKETED (2026-07-08, gauntlet):** tuning-class — same family as the parked F8/M1E economy modeling (the advisory's market model diverges from real outcomes); revisit together. **Leg-2 note:** the same pessimism pattern re-observed in the design-first walk (not re-verified in depth; nothing suggested improvement). |
| F17 | **Farm-gaps sidebar never updates during the draft** (gauntlet leg 1): the per-position tracker stayed "0 of 10, all OPEN" all draft while slots actually filled; post-draft Roster tab proved the underlying data correct — cosmetic staleness in the live sidebar only. | **TICKETED (2026-07-08, gauntlet):** small fix. |
| F18 | **Farm slots not position-enforced** (gauntlet leg 1): unlike the MLB "Eight", farm position gaps (C GAP, 1B GAP…) are advisory-only — passively claiming at reserve produced a legal 10/10 all-RP/CP farm class with zero position players. | **JK DESIGN FORK (morning check-in):** enforce farm position minimums, or ratify advisory-only farm composition for v1. |
| F19 | **Initial pool generated ILLEGAL** (gauntlet leg 1): first-try pool was 176/202 (tail too fat); the "Regenerate production-shaped pool" action recovered to 220/220 legal with zero hand-adds. | **TICKETED (2026-07-08, gauntlet):** pool-gen first-try-legal — the generator should produce what regenerate produces, first time. (Leg-2 contrast: the design-first extractor WAS legal first-try — the defect is pool-first-path-specific.) |
| F20 | **Silent re-extraction on pool lock** (gauntlet leg 2, design-first): LOCK POOL silently re-runs extraction/resizing (317→246) instead of locking what was shown — the design-target fit jumped 12/22 → 22/22 only AFTER lock; a user who checked fit pre-lock could misread it (either direction). | **TICKETED (2026-07-08, gauntlet leg 2):** small UX ticket — either lock-what-you-see, or surface the re-extraction explicitly (pre-lock preview of the post-lock pool + fit). |
| F21 | **Completion deadlock on pool-exhausted shortfall** (gauntlet leg 3, live repro on GauntletD8): the completion gate (`finalizeTerminalAuction`) runs its backfill cascade, still finds a shortfall, and refuses to set `AUCTION_COMPLETE` — but the one recovery action ("settle empty seats from Market Shills", `settleShortClubs`) only works when state IS `AUCTION_COMPLETE`. Circular trap, no UI escape; NEXT LOT = byte-identical no-op. | **LANDED (2026-07-08 overnight, M1Q MERGED `3f98eb06` — audit CLEAN APPROVE):** automatic shill reclamation in the terminal cascade (`27cac1f4`) — theft structurally impossible; post-completion settle byte-identical. Audit notes: the leg-3 repro carried a naive-auto-bidding caveat (walker's aggressive bidding contributed to the uneven pool drain — noted, doesn't change the deadlock's reality); the "Can't afford her and still fill the roster" spin-loop is a minor UX/automation gotcha, noted for automation. |

**Gauntlet leg-1 tool-limitation note (not a defect):** the press-and-hold scout-band reveal's CONTENT is unverified — scripted browser events can't trigger React's onPointerDown; plumbing (locked-by-default, trait-count-only, no names) verified correct. Needs a real pointer in JK's walkthrough.

**F1 landing note (2026-07-08, MERGED — audit APPROVE):** `lane/m1a-nomseed` `b92b9f43` — nomination order derives from session id + persisted launch nonce; crash/resume bit-stable via persisted `config.nominationOrderSeed`; real Draft Room entries hit the fixed path; sim/harness determinism untouched. Small known gap: no MLB-side resume-order integration test (farm has one).
**F2/F3/F7/F10 landing note (2026-07-08, M1D MERGED — audit APPROVE-WITH-NOTES):** per-area farm scout bands from the ratified 24x8 archetype table (row-by-row fidelity verified) render on BOTH farm surfaces; scout value = IV-pipeline opening ask with band fog (true ask always within range; tight strictly narrower than wide); ScoutHire = deterministic no-choice auto-specialization (persistence shape unchanged); scouting no longer requires a ScoutHire visit. AMENDMENT from audit: the overall grade band was constant-5 under the mean rule (dead knob) — ruling amended to PRIMARY-AREA (prospect's highest-rated tool), rides lane M1H with the arm-slot generation + lot-card trait count.
**F12/s8.4 landing note (2026-07-08, M1H MERGED `3dfa1d38` — audit APPROVE-WITH-NOTES):** generated pitcher prospects now draw a deterministic arm slot from the real `playerDatabase.ts` pitcher-pool distribution (High 65/36.31%, Mid 65/36.31%, Low 44/24.58%, Sub 5/2.79%, N=179); generated hitters remain `null`; Farm IV receives the slot and prices its arm-slot layer. Farm auction UP NOW lot cards now show only the ruled four public fields: name, age, position(s), and trait COUNT (no trait names — no-leak verified by the audit); startup board visible-safe extras remain untouched per F12c. s8.4 amendment landed: overall scout band and scout-value fog follow the prospect's primary applicable scouting area by highest true rating, fixed tie order power/contact/speed/fielding/arm/velocity/junk/accuracy; no-archetype fallback stays band 5. Audit note: only the Sub slot carries a pricing premium — pre-existing IV model, not an M1H change.
**F13 landing note (2026-07-08, MERGED 72d518ce — audit APPROVE-WITH-NOTES, no code defects):** farm CPU set = AI clubs only (legacy last-N fallback REMOVED — human hijack structurally impossible); persisted sessions heal cpuShillCount→0 with a real store write before first auto-advance; MLB legacy-resume hole closed (human exclusion in cpuTeamRoles, synthetic shills preserved); farm CPU-count UI removed. Coverage notes (non-blocking): no direct CPU-wins assertion; no all-AI-league completion test.
**F4 landing note (2026-07-08, MERGED — audit APPROVE-WITH-NOTES):** `lane/m1b-curve` `bdb8c2d4`+`34419899` — opt-in farm prospect histogram invariant + seeded pool-level largest-remainder grade quotas replacing independent draws. Before: B- +2.0pp / C +2.2pp, total abs 8.8pp at N=500; after: exact, 0.0pp (also exact at the spec's 40k validation). Audit notes for JK: (1) same-size pools now share an identical grade-count SKELETON by design (between-league top-end variety traded for spec exactness); (2) the fix pins the TRUE curve — any remaining *felt* inflation lives in the display band (F2/F7 → lane M1D); (3) harness measures N=500 vs production default N=300 (harmless; quotas exact at any N).
**F6/P5 landing note (2026-07-08, M1I MERGED `8eb24037` — audit APPROVE-WITH-NOTES + M1I-B fix `ad4e5b46`):** leagueId threads through the staffing→franchise handoff (`2850f3cc` — auto-select on mount, no more blank league picker) and hired reporter/manager identities reach the hub (`f99128ed`); the draft-complete badge now reads the AUCTION SESSION (M1I-B) instead of the stale source it briefly shipped with. Residual notes (non-blocking): URL param not cleared → a low-likelihood re-snap; per-league badge scan is perf-only; no empty-staffing coverage regression (M1I-B restored it). Process note: the first M1I dispatch died silently mid-build and was re-dispatched (known long-dispatch kill pattern).
**F14 landing note (2026-07-08, M1J MERGED `08236fdf` — audit APPROVE-WITH-NOTES):** completion guarantee on every auction terminal path (`d8c64fa3`) — see the F14 row above for detail; F14b (uncompletable-pool UI drop) ticketed as the residual.
**P3 landing note (2026-07-08, M1K MERGED `e21b23cf` — audit APPROVE-WITH-NOTES):** farm auction folds onto AuctionStage (`fc6d6742`) — the mid-journey UI register break is dead. Audit-proven: the MLB tier is INERT under the fold (no behavior change to the MLB path); privacy verified at the DOM level — covered values are ABSENT from the DOM, not merely hidden. Notes (non-blocking): missing `.catch` on the load-init chain; bid increment is now fixed at 1000 — flagged **VERIFY-DRAFT-SETUP-OWNERSHIP** follow-up (confirm Draft Setup owns/feeds the increment rather than the stage hardcoding it).
**P6 landing note (2026-07-08, M1L MERGED `11a80835` — audit APPROVE-WITH-NOTES):** post-freeze summary screen (`d310db95`) — read-only display of what froze (salaries, morale baselines), honest about gaps rather than inventing data. Notes (non-blocking): roster fallback is DERIVED when the store lacks it; add an empty-store branch test.
**P8 landing note (2026-07-08, M1N MERGED `7714b82a` — audit APPROVE-WITH-NOTES):** League Builder's existing league create/edit modal now has a conference assignment editor with single-conference, balanced split, add/remove, rename, and per-team assignment controls. Untouched leagues still persist the old empty `conferences: []` / `divisions: []` shape; edited leagues persist conference names plus team membership through existing `LeagueTemplate.conferences` and one hidden `Division` per conference. Validation blocks unknown/orphan/duplicate team assignments. Consumer status: FranchiseSetup already reads the stored league shape; `useFranchiseData` now reuses valid source league conference/division membership for franchise standings; FranchiseLens standings still render one league-wide group and are documented/tested as ignoring conferences today; schedule import/manual schedule remains conference-agnostic; playoff seeding has an existing out-of-v1 reader for template divisions/conferences.
**P7 landing note (2026-07-08, M1M MERGED `d35f6a62` — audit APPROVE-WITH-NOTES, incl. M1M-B follow-up):** RULES-V1-PRUNE executed — every visible rules/season-config knob is now wired to real behavior or removed (`8ceaaf3e`); the M1M-B follow-up (`d2795095`) removed the unconsumed `defaultRulesPreset` dropdown and marked the playoff step deferred (playoffs are post-v1). No decorative knobs remain to poison a validation pass.
**P11 landing note (2026-07-08, M1O MERGED `67b39e1b` — audit APPROVE-WITH-NOTES):** scout reveal moves adjacent to the farm phase (`391a0487`) — the draft-arc order is now coherent with the canonical scout-is-farm-only split; hub-based back-navigation; audit note: `proceedLabel` fallback behavior flagged (non-blocking).
**F9/P4 landing note (2026-07-08, M1P MERGED `500e66bf` — audit CLEAN APPROVE):** ONE liquidity-adjusted ceiling now drives the whisper verdict, room-read AND budget light (`68570a39`) — a counter-fixture proved the old contradiction reproduces on pre-fix code; the farm draft gains a live Assistant-GM whisper (`ac1ed30f`) that discriminates via budget-driven maxBid and shows honest "unknown" stubs where farm data genuinely lacks a signal. Engine surfaces diff-verified untouched (UI/advice layer only). Built Claude-side (codex flakiness reroute), audited on the same gate.

## §2 THE REST OF THE PUNCH LIST (from today's audits; all ticketed)
| ID | Item | Why it gates the A-Z test |
|---|---|---|
| P1 | CPU identity auto-assign ("auto-fill remaining", deterministic, never overwrites user picks) | 30-club rooms require 60 hand-picks before LOCK — untestable at scale without it |
| P2 | Design-first mode re-validation post-port (pool-first is browser-proven; design-first is not) | ~~Half the pool-system (Mode A) unverified~~ **LANDED-BY-GAUNTLET (2026-07-08, leg 2 PASS):** design-first proven lockable+draftable in a real browser — legal pool FIRST roll (zero regenerates, zero manual adds), design targets 22/22 post-lock, 30 lots drafted normally incl. shill purchases, 0 console errors. See `MODE1_GAUNTLET_REPORT_2026-07-08.md` LEG 2. Residual: F20 (silent re-extraction on lock, §1c) |
| P3 | Farm auction → AuctionStage fold (stage already implements farm tier + fog) | Kills the mid-journey register break + gives farm the same floor UX |
| P4 | Farm-side Assistant-GM whisper (DJ-28: zero Asst-GM presence on farm) | ~~The farm draft is advice-dead today~~ **LANDED (2026-07-08, M1P MERGED `500e66bf` — audit CLEAN APPROVE):** farm whisper live (`ac1ed30f`) — discriminates via budget-driven maxBid; honest "unknown" stubs where farm data genuinely lacks a signal (no invented reads); engine diff-verified untouched |
| P5 | Staff carry-through (hired reporter/manager identity → hub; generic fallback today) | Staffing choices must visibly matter |
| P6 | Post-freeze summary screen (freeze results are silent today) | JK must SEE what froze (salaries, morale baselines) to validate it |
| P7 | RULES-V1-PRUNE (ruled 2026-07-07): every rules/season knob wired or removed | Decorative knobs poison a validation pass |
| P8 | Conference editor (ruled IN v1 2026-07-01; conferences:[] hardcoded) | League-build completeness — **LANDED M1N** |
| P9 | Wrong-fit penalty Option A (visible pre-bid debit; design §3.3 + recovered §13.3 copy) | The economy's archetype tooth — last unbuilt S7 law |
| P10 | FS-3 shill-dissolve validation (believed fixed by C3's nonCompletingTeamIds; prove in gauntlet with shills>0) | ~~Shills-on configs must not block launch~~ **LANDED-BY-GAUNTLET (2026-07-08, leg 3):** shill dissolution PROVEN CLEAN on a real full shills draft — IndexedDB-verified: exactly 8 real clubs hold rosters (22 each), zero shill roster holders, no stranded players; farm shill-absence + human-seat-never-CPU confirmed in the same leg. See `MODE1_GAUNTLET_REPORT_2026-07-08.md` LEG 3 (the leg's F21 deadlock discovery fixed same night, M1Q `3f98eb06`) |
| P11 | Scout-hire placement in the journey: it currently gates the MLB auction though the scout is farm-only (canonical split) | Journey logic coherence; restructure = move scout-hire adjacent to the farm phase. **NOTE (2026-07-08): F10's code-verified read contradicts this row's premise — beginAuction checks NO scout-hire state today (LeagueBuilderAuctionDraft.tsx:1225-1258); the step sits in the nav chain but doesn't actually gate. With the F3 auto-specialization ruling, P11 reduces to screen PLACEMENT only (move the reveal adjacent to the farm phase).** |
| P12 | JK feel-pass on reserve prices (Lever A, merged; dial default 0.65, k=0 = old economy) | The standing human gate |

## §3 THE MODE-1 GAUNTLET (the validation harness — runs after F1-F4 + P1/P2 land; re-runs after every subsequent P-item)
Scripted end-to-end browser journeys (league build → import → draft setup → lock → scout hire → MLB auction → farm auction → staffing → freeze → rules → launch → lens lands), across the grid:
- {pool-first, design-first} × {6-team SML-scale, 8-team v1-scale} × {shills 0, shills 1-2} × {reserve k=0, k=0.65} — **grid re-based per JK ruling 2026-07-08: v1 draft scale = 8 teams (+1-2 shills); 30-team configs are post-v1**
- Invariants per run: zero 404s · zero console errors · pool locks with zero hand-adds · **nomination order differs between two consecutive drafts of the same pool (F1 regression)** · farm bands render + derive from archetype (F2/F3) · prospect histogram within curve tolerance (F4) · every roster completes legally (no auto-fill stranding; budgets alive per reset-doc targets) · session mid-draft crash/resume preserves state · staffing identities reach the hub (P5) · freeze summary shown (P6) · launch lands on the lens with a playable schedule flow.
- Plus one RUN-IT-BACK iterate-loop leg (edit-wall, re-draft same league).
- Output: MODE1_GAUNTLET_REPORT (pass/fail per invariant per config), committed per run.

## §4 THE ILLOGIC PASS (new audit lens, ruled by JK 2026-07-08)
Distinct from wiring audits: a behavioral-coherence review of the ENTIRE A-Z (draft arc and, separately, living season) asking per screen/step: "would a knowledgeable baseball fan/GM find this behavior nonsensical?" — e.g. identical nomination orders, generic scout pools, inflated prospect curves, prices that ignore context, advice that ignores the roster. Executor: fresh-eyes agents role-playing a GM, walking the real app + inspecting the math behind each surface, reporting illogic findings F-numbered into this doc. Run once on Mode 1 (pre-gauntlet) and once on Mode 2 (post-wiring wave).

## §5 DEFINITION OF DONE for "JK can fully test the draft A-Z"
F1-F4 fixed + P1-P8 landed + gauntlet green across the grid + illogic pass clean (or all findings ticketed) → THEN JK's feel-pass (P12) + design reskin wave (F5). P9/P11 may land in parallel with JK testing if he prefers earlier hands-on.

## §6 CONCURRENCY WITH MODE 2 (per the ruled plan)
Mode-1 lanes above touch draft-side files; Mode-2 wiring wave (flag-activation mechanism → morale trigger wiring → **matchup-engine build lane (M-L, ratified 2026-07-08 per MATCHUP_ENGINE_RECONCILIATION_2026-07-08.md; slots with/immediately after the morale wave, hard dep: morale ledger write-back)** → L10 completion → in-season Asst-GM surface → TV-award rewire → ~~rivalry-edge HISTORY change~~ *(CANCELLED by the 2026-07-08 record-overtake reversal, canon §6 — RIVALRY typing correct-as-built)* → truth-map re-walk) touches season-side files — disjoint surfaces, concurrent lanes, same per-lane gates (Codex builds / adversarial audit / captain browser gate / SOT line per landing).
