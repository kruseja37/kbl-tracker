# NOW — Legends Library (historical players): engine likely FINISHED awaiting exam+freeze; research OPEN

**Separate repo: `/Users/johnkruse/Projects/kbl-historical-player-archetype-backlog` (branch
`codex/historical-player-archetype-backlog`). Its own canon docs + contracts govern; this brief
is a pointer.**

## VERIFIED (that repo's git, 2026-07-11 night)
- HEAD `5e9bfef7`: **SEASON-GRADE built and committed** — canon §22.6 per-season within-position
  defensive grading (the Wilson-straddle fix). Its own commit message: Wilson career FLD restored
  to elite (96/P98.78), Ozzie stable 97, Clark Cannon survives Giants window only (career P77.3
  = an accepted honest miss per the §22.5 evidence-wins ruling), truth rebase `754e0b6b3a13d90f`,
  runGold 14/14 isolated + 165/165 suites + tsc clean — **"awaiting exam+freeze."**
- The pending step is the CAPTAIN'S EXAMINER pass + freeze (the builder was explicitly told to
  STOP before exam/freeze). Contract: `spec-docs/contracts/CONTRACT_SEASONGRADE_2026-07-11.md`
  in that repo (acceptance list inside: Wilson/Yount/Ozzie/Ankiel/Clark states, ten largest
  movers, gate tables, Edge Census delta).
- Canon line to read first in that repo: §22.x docs (GLOVE-TRUTH `6abca92b` → rulings `d7d0f3a7`
  → SEASON-GRADE).

## CARRIED from Fable memory — re-verify
- Product frame: 240 real players → archetype shelves; CAREER CHAPTERS ruled (McGwire two-version
  canon); the converter pipeline exists UNWIRED in the main app (historicalPlayerConverter +
  Lahman adapter); analyzer alive at /builder. "Engine possibly finished" = the grading/canon
  engine (this repo); the RESEARCH (player-by-player curation) and the MAIN-APP WIRING are the
  unfinished halves.
- Next moves on crossing: (1) run the examiner + freeze in the backlog repo per the SEASON-GRADE
  contract; (2) then the research/curation continues against frozen truth; (3) main-app wiring
  is a separate, later lane.

## CORRECTION (2026-07-12 early AM — that repo moved past this brief the same night)
Verified at its HEAD `9debc6d2`: **the exam+freeze already happened** — `cd4867a8` promoted
`goldfinal-2026-07-11` as the FROZEN reference edition per JK's explicit freeze ruling
(supersedes gold2), and manual **§7.4** now defines a per-batch examiner protocol (any fresh
independent AI session can examine; builder never takes its own exam; promotion only on JK's
freeze ruling). The live phase is BATCH RESEARCH/ASSEMBLY per manual §7 (remaining ~200 players;
known open lane: batch-1 ACC HALT). That repo's own CURRENT_STATE/SESSION_LOG are authoritative —
read them first; treat everything in the section above as historical context.
