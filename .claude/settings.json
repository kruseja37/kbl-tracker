{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "p=$(jq -r '.tool_input.file_path // empty'); if echo \"$p\" | grep -qE '\\.claude/skills|spec-docs/skills'; then bash \"$CLAUDE_PROJECT_DIR/scripts/sync-codex-skills.sh\" >/dev/null 2>&1; fi"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "c=$(jq -r '.tool_input.command // empty'); if echo \"$c\" | grep -qE '\\.claude/skills|spec-docs/skills'; then bash \"$CLAUDE_PROJECT_DIR/scripts/sync-codex-skills.sh\" >/dev/null 2>&1; fi"
          }
        ]
      }
    ]
  }
}
