next_ticket: **ASSEMBLY / MERGE to trunk** (LANE A, Tuesday — JK-driven/JK-confirm-gated). AUTH-4 ultra-think Captain loop (Codex builds where needed, Opus audits/gates/commits branch-only). #18 winner-honors is DONE+SHIPPED — do NOT rebuild it.

⚠ THIS IS THE **LANE A** BATON. The draft lane `claude/v1-draft-ui` (worktree `kbl-draftlane`) was Lane B's, but Lane B wound down (its Codex finisher completed); THIS Lane-A session then landed one more draft-lane feature on it (the scout/staff wire below) as sole worker. Before any NEW draft-lane build, confirm no other worker is on that branch.

═══ STATUS (what's DONE) ═══
- **#18 WINNER-HONORS SHIPPED `c7c0982d`** (trunk `experiment/manager-wpa-window`, branch-only, build-dark/L12). All 8 player WAR award winners honored, §16-tunable per-award prestige, additive stacking. Gate fully green incl. L-SIM season findings 0 / sameSeedByteIdentical true / clean re-bake. Contract `e001c2c4`.
- **DRAFT is FINISHED + PLAYABLE on the draft lane** (`claude/v1-draft-ui`, worktree `kbl-draftlane`, **HEAD `adc757e8`**). Two layers, both branch-only:
  - `ff024385` (Lane B) — E2E draft → auction → farm → freeze → **launches into franchise mode-2 with rosters loaded** (gate = `draftPipeline.integration.test.ts`; `STREAMB_FINISH_REPORT.md` = YES; fixed a real chain break where FranchiseSetup cleared ownership on league-select).
  - `adc757e8` (this Lane-A session, contract `DRAFT-STAFF-GUIDE-WIRE` = lane commit `2466d4c8`) — **scout-hire screen BEFORE the auction + the in-auction scout guide (price band / 20-80 grade / affordability) + a manager+beat-reporter staff-hire ceremony after the farm auction**, all persisted (scoutProfiles / managerIdentityStorage / reporterStorage). Additive only (bid/award/freeze untouched). Captain-audited; build 0; `draftPipeline.integration` **4/4** incl. a NEW scout/staff persistence test. Reporter persistence existed → full ceremony, nothing deferred.
  - JK can run the full draft from the `kbl-draftlane` worktree NOW (no merge). **Pending: JK browser sign-off on the new Draft Room + scout/staff routing.** ⚠ Run the worktree on **port 5173** (plain `vite`, NO strictPort → silently uses 5174 if 5173 busy = a SEPARATE IndexedDB; data only carries to main if drafted on 5173; all branches are trackerDb v26 so same-origin data reads cleanly post-merge).

═══ THE TUESDAY HEADLINE: ASSEMBLY/MERGE (nothing is on trunk yet) ═══
Unmerged into `experiment/manager-wpa-window` (commits ahead): `claude/lineups-fenway-hub` (64 — the big user-visible franchise-lens hub), `codex/auction-draft-ux-rehaul` (46 — RETIRE, superseded by lineups-fenway), `claude/v1-draft-ui` (~22, **HEAD `adc757e8`** — the FINISHED+PLAYABLE draft lane incl. scout-hire/guide + staff-hire; the merge picks up this HEAD = all the draft work + JK's tonight-draft code), `codex/draft-setup-ui` (5), `claude/v1-playoff-driver` (2 — PLAYOFF-DRIVER-2), `codex/draft-pipeline-fix` (1 — optimizer engines). **ALL merges are JK-confirm-gated (JK paused the draft branches "until his return"); NEVER push unattended.**
- Canonical assembly order = `spec-docs/V1_STATUS_AND_ASSEMBLY_PLAN.md` §ASSEMBLY ORDER: low-risk indep (done) → **lineups-fenway hub** (reconcile duplicate lineups; keep on preview route) → **draft lanes** (`draft-pipeline-fix` then `draft-setup-ui` — keep its `teamArchetypeCatalog.ts`; ⚠ verify optimizer-engine isn't already on the live line = no double-merge) → **then `claude/v1-draft-ui`** (the finished draft) → retire `auction-draft-ux-rehaul` → playoff-driver → freeze→§16 tuning→flag-flip→sign-off.
- Technique = [[kbl-safe-lane-merge-technique]]: read-only merge-tree pre-assess → isolated worktree + APFS node_modules clone → base-aware union for append-logs (PROMPT_CONTRACTS/ledger) → build+FULL suite+L-SIM gate → ff-only AFTER JK confirm. SERIALIZE heavy vitest/L-SIM gates with Lane B.

═══ DEFERRED LIVING-SEASON (all NON-blocking for playing the draft; do after assembly) ═══
#20 honor-news path for the new non-MVP winners (`FranchiseHonorKind` only has MVP/CY/ALL_STAR/ROY/Reliever; GG/SS/Bench/Booger have none) · #19 TV-award family (Kara/Bust/Comeback) season-end · #34/#35/#37/#38 Tier-6 L-SIM hardening · #23 champion-MVP (DEFERRED — L-SIM postseason has no per-player playoff stats). §16 sim-tuning = JK-driven, post-freeze, the single biggest piece.

═══ DISPATCH / GATE (proven) ═══
Contract → `spec-docs/PROMPT_CONTRACTS.md` (markers) → `sed -n '/CONTRACT: <ID> /,/END CONTRACT: <ID> /p' … | NODE_ENV= ~/.local/bin/codex exec -C <tree> --skip-git-repo-check -s workspace-write -c model_reasoning_effort=high -o /tmp/codex-<id>.out -` via Bash run_in_background + dangerouslyDisableSandbox. `very-high`=400 INVALID. Session must be xhigh/ultra-think. Captain runs the L-SIM (Codex stalls). Gate: build 0 · FULL suite ZERO-NEW-REDS (read failed-FILE list; characterized = `wpaRuntimeBoundary` HARD + `historicalArchetypes` + `poolFeasibility` + order-flakes) · L-SIM season (read SUMMARY JSON: criticalFindings 0, sameSeedByteIdentical true; honors/finalize changes → expected clean re-bake). Commit branch-only BY PATH (leave concurrent-session docs), trailer `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`; stage+commit in ONE call + verify HEAD ([[shared-branch-commit-atomicity]]).

resume_note: JK's priority this weekend = run drafts from the `kbl-draftlane` worktree on **port 5173** (no merge; full flow = setup → hire scout → auction-with-guide → farm → hire manager+reporter → freeze → franchise). His franchise(s) from tonight live in the **:5173 IndexedDB** (per-origin, not per-branch) — Tuesday's merged main, run on :5173, reads the same data (all branches trackerDb v26, no migration risk). Tuesday plan: (1) assembly/merge to trunk (JK-confirm-gated; the draft-lane HEAD to merge = `adc757e8`), (2) JK browser sign-off on the new Draft Room + scout/staff routing, (3) deferred living-season items + §16 tuning. Live ledger = `spec-docs/AUTONOMOUS_RUN_LOG.md` (newest block). Read `STREAMB_FINISH_REPORT.md` (Lane B) for the original draft verdict.
