next_ticket: **A1.5d hop-6 (player RIVALRY edge, §5.6) — when a player OVERTAKES another on a park record, stamp a persisted player↔player `RelationshipEdgeRow` (old holder ↔ new holder) via `putFranchiseRelationshipEdge`, gated `isFranchisePhase2L13Enabled`.** This is the last A1.5d stadium-records hop. ⚠ It writes to a DUMPED store → expect an L-SIM re-bake. The whole HOME-PARK TEAM-RIVALRY feature (a JK-directed §7 detour, separate from this §5.6 PLAYER edge) is DONE + cross-thread-aligned — see below. Continue the AUTH-4 captain loop on `codex/franchise-v1-next`.

branch/topology: **MAIN** `/Users/johnkruse/Projects/kbl-tracker` on `codex/franchise-v1-next` (HEAD = `b012238c` rivalry-done docs). **trackerDb v26**. caffeinate alive. ⚠ PAUSED — DO NOT TOUCH/MERGE (JK): `codex/draft-pipeline-fix` + `codex/auction-draft-ux-rehaul` (the latter is the franchise-lens UI redesign that will ADOPT our work at swap — never merge it in).

resume_note: **SOLE WORKER — claim the baton FIRST** (`mv HANDOFF_NEEDED HANDOFF_DONE_$(date -u +%Y%m%dT%H%M%SZ)_<note>`; confirm no `codex exec` + no other `claude` on this branch). Proven loop: ground FRESH from source (parallel grounding workflow) → Captain RE-verifies the make-or-break → write+commit contract → dispatch Codex (`run_in_background`+`dangerouslyDisableSandbox`, watchdog; `model_reasoning_effort` ∈ high|xhigh, NEVER very-high) → audit the REAL diff (never the Codex paste) → (ultracode: parallel adversarial review) → Captain runs his OWN build + FULL suite + L-SIM → commit branch-only BY PATH → log. Benign Codex stderr `mode2-pilot/SKILL.md: invalid YAML` is NOT a build error.

- **FULL-suite baseline (ZERO-NEW-REDS): ~8453 passing**; the ONLY fails are CHARACTERIZED — `wpaRuntimeBoundary` (HARD) + `franchiseManualSmokeFixture` (order-flake) + **NEW `AwardsWatchlist.test.tsx > renders projected preview…`** (non-deterministic cross-file isolation order-flake introduced by HPR-B2 — green solo/in-dir/on a full-suite RE-RUN; re-run to confirm, NOT a logic defect). Read the vitest SUMMARY (failed-file LIST), not the RC.
- **L-SIM HEALTHY:** `season.config.ts` full clean 60g, `findings:0/criticalFindings:0/stoppedEarly:false/sameSeedByteIdentical:true`. Current committed baselines reflect HPR-A..C: checkpoints 030/040/050/060 = `lsim-h2-baseline-checkpoint-*.json`; the latest finalDigest is `9450363:00221bb5`. Run the STANDARD 60g leg LAST (it regenerates the anchor). DUMPED stores (`DUMP_DATABASES` in `test-utils/lsim/storeDump.ts`): kbl-tracker / kbl-franchise-morale / kbl-league-builder / kbl-schedule / kbl-event-log / kbl-app-meta / kbl-manager-identity / kbl-franchise-<id>. A flags-ON write to a DUMPED store RE-BAKES → verify the per-field delta is SANE (use the per-field python diff vs HEAD) → commit the regenerated baselines. New own-DBs feeding dumped state MUST be added to `test-utils/lsim/sandbox.ts` `DB_NAMES_TO_DELETE`.

**➡ hop-6 (player rivalry edge, §5.6) — GROUNDED MAKE-OR-BREAK FACTS (Captain pre-grounded; re-verify from source before contracting):**
- **The writer EXISTS:** `putFranchiseRelationshipEdge` (`src/utils/franchiseRelationshipEdgesStorage.ts`). The store is **player↔player** (`RelationshipEdgeRow.player1Id/player2Id`, canonicalized sorted pair; `RelationshipEdgeType` includes `RIVALRY` + an unused `HISTORY` slot, `:15-28`). The flag EXISTS: `isFranchisePhase2L13Enabled()` (`franchisePhase2Flags.ts:101`).
- **⚠⚠ THE EDGE STORE DB_NAME = `kbl-tracker`** (`franchiseRelationshipEdgesStorage.ts:7`) — a DUMPED store. So when an OVERTAKE fires in the L-SIM (flags-on) and an edge is written, the L-SIM season baseline RE-BAKES (NOT byte-identical). Expect it; verify the delta is sane (new RIVALRY edge rows on overtakes) + commit the regenerated baselines. **Likely NO trackerDb bump** (the `franchiseRelationshipEdges` store already exists in trackerDb — confirm the store is registered; the relationship subsystem [formation/intensity/morale/intel/flare + an L13 reporter adapter] is already built; §5.6 says only the event-spark WRITER is new).
- **The consumer feed:** the OVERTAKE `stadiumChanges` (the `FranchiseStadiumRecordChange[]` with `changeKind==='overtake'`, `newLeaderPlayerIds` + `priorLeaderPlayerIds`) is already surfaced EARLY in `processCompletedGame.ts` (held as `stadiumChanges`, consumed by the fame/park-record-morale taps). hop-6 adds a NEW dark writer (mirror the hop-3 sibling shape) gated `isFranchisePhase2L13Enabled`, that for each OVERTAKE stamps an old-holder↔new-holder `RIVALRY` edge.
- **GROUND BEFORE CONTRACTING:** the `putFranchiseRelationshipEdge` signature + how it keys/dedups (idempotency — overtake once per checkpoint, mirror the hop-3 sourceEventId/checkpoint guard); whether to use the `RIVALRY` vs `HISTORY` slot (§5.6 says use one — check the existing edge-type semantics); the "event-spark writer BYPASSING the attribute-only formation engine" (§5.6) — confirm the new writer goes DIRECT to `putFranchiseRelationshipEdge` (not through `franchiseRelationshipFormationCompute`); only OVERTAKE makes an edge (a fresh SET with no prior holder → no old holder → no edge); the bidirectional/canonical-pair convention.

After A1.5d the queue is JK-DECISION-BLOCKED (re-grade store · RA-7 park-adjust — need JK rulings). Other BROWSER/JK-PENDING: A1.4 L12-6 UI · L4b LLM emission · A1.3b fork-2 · hop-4 reporter LLM half · hop-5 Almanac UI polish · §8 per-stadium display · BV backlog.

DISPATCH MECHANICS (proven; watchdog; macOS has no `timeout`):
  cd /Users/johnkruse/Projects/kbl-tracker
  export PATH="$HOME/.nvm/versions/node/v20.20.0/bin:$PATH"
  rm -f /tmp/codex-<ID>.out; : > /tmp/codex-<ID>.stdout
  ( PREV=0; STALL=0; ELAPSED=0
    while [ $ELAPSED -lt 1800 ]; do
      sleep 120; ELAPSED=$((ELAPSED+120))
      pgrep -f "codex exec.*codex-<ID>.out" >/dev/null 2>&1 || exit 0
      CUR=$(wc -c < /tmp/codex-<ID>.stdout 2>/dev/null | tr -d ' '); CUR=${CUR:-0}
      CHANGED=$(git -C /Users/johnkruse/Projects/kbl-tracker diff --name-only -- src 2>/dev/null | wc -l | tr -d ' '); CHANGED=${CHANGED:-0}
      if [ "$CUR" -le "$PREV" ] && [ "$CHANGED" -eq 0 ] && [ ! -s /tmp/codex-<ID>.out ]; then STALL=$((STALL+1)); else STALL=0; fi
      PREV=$CUR
      if [ $STALL -ge 3 ]; then echo "WATCHDOG STALL kill at ${ELAPSED}s" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null; exit 0; fi
    done
    echo "WATCHDOG 30m cap kill" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null ) &
  WDOG=$!
  sed -n '/<!-- ===== CONTRACT: <ID> /,/<!-- ===== END CONTRACT: <ID> /p' /Users/johnkruse/Projects/kbl-tracker/spec-docs/PROMPT_CONTRACTS.md \
    | NODE_ENV= ~/.local/bin/codex exec -C /Users/johnkruse/Projects/kbl-tracker --skip-git-repo-check -s workspace-write -c model_reasoning_effort=high -o /tmp/codex-<ID>.out - >> /tmp/codex-<ID>.stdout 2>&1
  RC=$?; kill $WDOG 2>/dev/null; echo "codex rc=$RC"

session-done (2026-06-26, attended JK → "move on"): **🎉 the WHOLE HOME-PARK TEAM-RIVALRY feature SHIPPED (4 commits) + cross-thread-aligned + the next ticket handed to fresh context.** HPR-A `64dd9ab1` (rival brain — own DB, L-SIM byte-identical) · HPR-B1 `5370747e` (standings rival-red) · HPR-B2 `76cb4b29` (races rival-red, checkpoint-060 re-baked digest-only) · HPR-C `bd60f945` (the grudge — rival-game fan ±2 + captain ±1, checkpoints 030-060 re-baked = digest + intended morale→development +2 overlays). Earlier this session: A1.5d hop-5 Almanac `84175be3`. All branch-only / Codex-built / Captain-audited-real-diff / FULL ZERO-NEW-REDS / L-SIM verified. **Cross-thread:** the UI/UX-rework thread (franchise-lens redesign on `codex/auction-draft-ux-rehaul`) confirmed ZERO file overlap + will CONSUME our `useFranchiseData` lensTeamId/rivalTeamId + `getHomeParkRival` seam at swap (it rebases onto us) — keep those stable [[rivalry-ui-seam-consumed-by-redesign]]. Rivalry is JK-browser-pending (enable `isFranchisePhase2StadiumRecordsEnabled` + play games). Handed off at this clean complete seam after a long session.

READ ON RESUME (in order): `spec-docs/AUTONOMOUS_RUN_LOG.md` (newest = the HOME-PARK RIVALRY block) · `spec-docs/CURRENT_STATE.md` LIVE HEADER · `spec-docs/AUTONOMOUS_RUN_PROTOCOL.md` · `spec-docs/SESSION_RULES.md` · `spec-docs/STADIUM_ANALYTICS_SPEC_V2.md` §5.6 (rivalry edge) + §10 (wiring map: `putFranchiseRelationshipEdge`).
