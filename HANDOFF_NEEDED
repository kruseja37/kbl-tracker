next_ticket: **UNATTENDED AUTH-4 — the BUTTON-UP BUILDOUT** (JK attended mid-run, ruled the scope, then went away; keep rolling, make EVERY call by building to spec + the rulings, take a DOCUMENTED conservative §16 default where spec is silent, log it, and CONTINUE; only pause = SET-ASIDE-AND-CONTINUE on a genuine safety wall). JK PAUSED the **draft + draft-UX side branches** and asked to button up EVERYTHING ELSE build-dark while he's away. **Work the BUTTON-UP QUEUE below, in order, all on `codex/franchise-v1-next` (MAIN).** None are contracted yet — GROUND EACH FRESH FROM SOURCE before contracting.

branch/topology: **MAIN** `/Users/johnkruse/Projects/kbl-tracker` on `codex/franchise-v1-next` (HEAD = `0c2b4a04`, the Lane-C merge — the single integration tree: L/D-stack + Mode-1 auction + the WHOLE trait wave + the ratings-finish work A2.5/RA-5/RA-9/V8 now merged in). Lane C (`codex/ratings-finish-c`) is FULLY MERGED (0 un-merged) — do NOT build there anymore. ⚠ **PAUSED — DO NOT TOUCH/MERGE (JK ruling):** `codex/draft-pipeline-fix` (12 commits, draft-pool-setup redesign + pipeline fixes, worktree `/Users/johnkruse/Projects/kbl-draftfix`, "done pending JK sign-off") + `codex/auction-draft-ux-rehaul` (6 commits, auction UX prototype, worktree `/Users/johnkruse/Projects/kbl-tracker--auction-ux`, "greenlight-gated"). These are JK's active work, paused until he's back. ONE Codex build per worktree; ONE committer per branch.

resume_note: UNATTENDED AUTH-4 captain loop (Codex builds → Opus audits the REAL diff [builder≠auditor, READ THE DIFF not the Codex paste] → independent build + FULL-suite gate, zero-new-reds vs characterized → commit branch-only BY PATH, NEVER push → log). **SOLE WORKER — claim the baton FIRST:** `mv HANDOFF_NEEDED HANDOFF_DONE_$(date -u +%Y%m%dT%H%M%SZ)`; confirm no `codex exec` proc + no other `claude` on this branch. **Characterized baseline (ZERO-NEW-REDS gate), post-Lane-C-merge:** full suite **~8366 passing**, 2 hard/flake fails = `wpaRuntimeBoundary` (HARD) + `franchiseManualSmokeFixture` (order-flake, solo-passes 4/4); intermittent solo-pass order-flakes `AwardsWatchlist`/`GameTrackerLaunchState`/`franchiseOffseasonGuards.component`. trackerDb **v25** — see the A1.3b exception below. ⚠ **Do NOT flip Phase-2 traits** (`isFranchisePhase2TraitsEnabled` / the checkpoint flag) — the whole wave is build-dark + JK-review-pending. ⚠ A concurrent DOC-worker keeps `spec-docs/FRANCHISE_MODE1_MODE2_PLAYABLE_V1_GAP_ANALYSIS.md` (M) + an untracked `PLAYER_NUMERIC_GRADE_AND_LEADERBOARD_SPEC.md` dirty — NEVER stage them; commit BY PATH only; leave untracked junk (HANDOFF_DONE_*, scripts/, *.csv, instructions/) alone. **caffeinate PID 84474** (verify alive; re-`caffeinate -s -i` if not).

**⚙️ JK SESSION RULINGS (2026-06-26 attended — DON'T re-litigate):**
1. **BUILD-SCOPE = MAXIMUM** — build everything build-dark-able with documented §16 defaults (all reversible).
2. **RE-GRADE DISPLAY = build-dark default** — re-grade evolves the rating each checkpoint; surfaces only at Phase-2 flip (consistent with the wave). JK: no live seasons matter now → safe either way; "hidden-even-after-flip until EOS" is a small later tweak if he asks.
3. **TRADE-DEMAND A1.3b = BUILD FULLY incl. the `TRACKER_DB_VERSION` bump** — JK EXPLICITLY AUTHORIZED the saved-shape bump because NO critical seasons are live. This LIFTS the "no unattended DB bump" rule FOR A1.3b ONLY. Bump migration-safe + update the version-pin tests (esp. `franchiseSeasonLedgerStorage.test.ts` hard-pins TRACKER_DB_VERSION + the full store list — [[trackerdb-version-bump-test-pins]]). Take documented defaults on the 5 A1.3b forks (demander source-of-truth · flashpoint-tax-vs-one-shot-morale-vs-both · albatross↔trade_demander row-key collision · resolution/counter-reset event · intensity source).
4. **RA-11 = BUILD STANDALONE** — to a documented §4A assumption now; reconcile with the paused Mode-1 B14 at the eventual merge.

**➡ BUTTON-UP QUEUE (ground each FRESH from source — `RATINGS_ADJUSTMENT_SPEC` / `RATINGS_MEASUREMENT_WORKSHEET` / `STADIUM_ANALYTICS_SPEC_V2` / `FRANCHISE_V1_*` + DECISIONS_LOG; all build-dark; FULL-suite gate; the trait/ratings engines feed `processCompletedGame` → transitive-mock-break risk → FULL suite, not scoped):**
RATINGS TAIL — (1) **§6A convex curve** (A2.5 remainder: gamma gate + delta edge-compression + equilibrium dev shape; DISTINCT from the age-modifier bar-shift already merged) · (2) **RA-7 per-game park-adjust** in the ratings expected-stats signal path · (3) **RA-10 bench-IF/bench-OF pool split** (greenfield split of the single RESERVE pool) · (4) **RA-11 standalone** (ruling 4) · (5) **re-grade-per-checkpoint** (ruling 2 — build-dark, surfaces at flip).
LIVING-SEASON TAIL — (6) **A1.3a** trade-propensity wiring (contained, NO DB bump: post-roll filter on L10 `trade_demand`, thread loyalty+teamId) → (7) **A1.3b trade-demand FULL incl. the trackerDb bump** (ruling 3) · (8) **A1.4 L12-6** (race/award/All-Star UI + `allStarSelections` career counter) · (9) **A1.5 L4b** (matrix-sourced season takes over the L4a bus) · (10) **A1.5d STADIUM RECORDS** (own db `kbl-franchise-stadium-records`, new `isFranchisePhase2StadiumRecordsEnabled` flag — NOT a trackerDb bump; §4 catalog + the 6 living-season hops + §8 stat-display).
⚠ USER-VISIBLE (build the data layer, FLAG the UI for JK browser sign-off, do NOT mark closed): A1.5b-2 (EnrichmentPanel SVG marker re-derivation — precondition for Power-native) · T-6b (position-mismatch scout/analyzer flag display, consumes the exported `POSITION_MISMATCH_UNREGAINABLE`).
AFTER THE BUILDOUT (NOT now): the ship gate-chain (L-SIM final → RB-16 → D12 → D13 → flag-flip → F-141) runs after the buildout AND the paused draft branches land. ⚠ 2 BV items pending JK (BV-V8, BV-CAP-MISS) + the new user-visible items.

DISPATCH MECHANICS (proven all session; USE THE STALL-DETECTING WATCHDOG):
  cd /Users/johnkruse/Projects/kbl-tracker
  export PATH="$HOME/.nvm/versions/node/v20.20.0/bin:$PATH"
  rm -f /tmp/codex-<ID>.out; : > /tmp/codex-<ID>.stdout
  ( PREV=0; STALL=0; ELAPSED=0
    while [ $ELAPSED -lt 1800 ]; do
      sleep 120; ELAPSED=$((ELAPSED+120))
      pgrep -f "codex exec.*codex-<ID>.out" >/dev/null 2>&1 || exit 0
      CUR=$(wc -c < /tmp/codex-<ID>.stdout 2>/dev/null | tr -d ' '); CUR=${CUR:-0}
      CHANGED=$(git -C <WORKTREE-ABS> diff --name-only -- src 2>/dev/null | wc -l | tr -d ' '); CHANGED=${CHANGED:-0}
      if [ "$CUR" -le "$PREV" ] && [ "$CHANGED" -eq 0 ] && [ ! -s /tmp/codex-<ID>.out ]; then STALL=$((STALL+1)); else STALL=0; fi
      PREV=$CUR
      if [ $STALL -ge 3 ]; then echo "WATCHDOG STALL kill at ${ELAPSED}s" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null; exit 0; fi
    done
    echo "WATCHDOG 30m cap kill" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null ) &
  WDOG=$!
  sed -n '/<!-- ===== CONTRACT: <ID> /,/<!-- ===== END CONTRACT: <ID> /p' /Users/johnkruse/Projects/kbl-tracker/spec-docs/PROMPT_CONTRACTS.md \
    | NODE_ENV= ~/.local/bin/codex exec -C <WORKTREE-ABS> --skip-git-repo-check -s workspace-write -c model_reasoning_effort=xhigh -o /tmp/codex-<ID>.out - >> /tmp/codex-<ID>.stdout 2>&1
  RC=$?; kill $WDOG 2>/dev/null; echo "codex rc=$RC"
  # run_in_background:true + dangerouslyDisableSandbox:true. model_reasoning_effort ∈ high|xhigh — NEVER very-high. Distinct <ID> per ticket. Captain audits the REAL diff + runs his OWN full suite (never the Codex paste).

GROUNDING (proven all session — 4 grounding agents, all accurate): run a grounding agent that grounds the next ticket FROM SOURCE (filesToModify / verified file:line anchors / make-or-break / build steps / gate type / STOP-IFs / OPEN-DECISIONS / safeToAutoBuild); Captain RE-VERIFIES the make-or-break anchors himself, writes+commits the contract, dispatches, audits the REAL diff, gates on his OWN full suite, commits, logs. ⚠ For enrichment/UI-derived signals, prove the persist path UI→store first ([[verify-data-persistence-before-trait-builds]]).

session-done (2026-06-26, attended `/kbl-captain`): **6 commits — the MAIN trait tail T-3/T-6/T-7 COMPLETE + the Lane-C ratings merge.** T-3a `3ecbc6c0` (SP/RP split) · T-3b-1 `0df09fda` (trend engine) · T-3b-2 `e3b7e76d` (trend sweep) ⇒ T-3 done · T-6a `31afa050` (position-mismatch protect) · T-7 `7340ca18` ⇒ MAIN trait tail done · **Lane-C merge `0c2b4a04`** (A2.5/RA-5/RA-9/V8 folded into MAIN, gated-on-staged-merge, ZERO-NEW-REDS). Then a v1-readiness audit (2 doc-counters + a full branch sweep) found the 3 un-merged work streams (Lane C now merged; draft + auction-UX paused per JK), and JK ruled the button-up scope above. Newest ledger block = `AUTONOMOUS_RUN_LOG.md` 2026-06-26 (the Lane-C merge entry + JK SESSION RULINGS + the BUTTON-UP QUEUE).

READ ON RESUME (in order, via Read on exact paths — never trust summaries): `spec-docs/AUTONOMOUS_RUN_LOG.md` (newest block = Lane-C merge + JK RULINGS + BUTTON-UP QUEUE) · `spec-docs/CURRENT_STATE.md` LIVE HEADER (top block) · `spec-docs/AUTONOMOUS_RUN_PROTOCOL.md` (AUTH-4 + the 4 safety walls) · `spec-docs/SESSION_RULES.md` · `spec-docs/V1_BUILD_QUEUE.md` (A-W2 ratings / A-W1 living-season tails) · `spec-docs/RATINGS_ADJUSTMENT_SPEC.md` + `RATINGS_MEASUREMENT_WORKSHEET.md` + `STADIUM_ANALYTICS_SPEC_V2.md` · `spec-docs/DECISIONS_LOG.md` 2026-06-26.
