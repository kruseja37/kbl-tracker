next_ticket: **A1.3b — trade-demand FULL (incl. the JK-AUTHORIZED trackerDb bump)** — the next item in the BUTTON-UP / living-season tail. Continue the UNATTENDED AUTH-4 captain loop: keep rolling, make EVERY call by building to spec + the JK rulings, take a DOCUMENTED conservative default where spec is silent (log it), only pause = SET-ASIDE-AND-CONTINUE on a genuine safety wall. Work the queue below, in order, all on `codex/franchise-v1-next` (MAIN). None contracted yet — GROUND EACH FRESH FROM SOURCE before contracting (the proven loop: grounding agent → Captain RE-verifies the make-or-break anchors himself → write+commit contract → dispatch Codex → audit the REAL diff → Opus runs his OWN full suite → commit branch-only BY PATH → log).

branch/topology: **MAIN** `/Users/johnkruse/Projects/kbl-tracker` on `codex/franchise-v1-next` (HEAD = `a2fa6706`). trackerDb **v25**. ⚠ **PAUSED — DO NOT TOUCH/MERGE (JK ruling):** `codex/draft-pipeline-fix` + `codex/auction-draft-ux-rehaul` (JK's active draft/auction-UX work). The button-up queue is FILE-DISJOINT from them (partition proof in the ledger) → safe. ONE Codex build per worktree; ONE committer per branch.

resume_note: **SOLE WORKER — claim the baton FIRST:** `mv HANDOFF_NEEDED HANDOFF_DONE_$(date -u +%Y%m%dT%H%M%SZ)_<note>`; confirm no `codex exec` proc + no other `claude` on this branch. **Characterized baseline (ZERO-NEW-REDS gate):** full suite ~**8378+ passing**; the only hard/flake fails = `wpaRuntimeBoundary` (HARD, fails solo too) + `franchiseManualSmokeFixture` (order-flake, solo-passes 4/4); intermittent solo-pass order-flakes `AwardsWatchlist`/`franchiseOffseasonGuards.component`/`GameTrackerLaunchState`. **Read the vitest SUMMARY (failed-file list), NOT the RC.** ⚠ **Do NOT flip Phase-2** (`isFranchisePhase2*` / the checkpoint flag) — the whole wave is build-dark + JK-review-pending. ⚠ A concurrent DOC-worker keeps `spec-docs/FRANCHISE_MODE1_MODE2_PLAYABLE_V1_GAP_ANALYSIS.md` (M) dirty — NEVER stage it; commit BY PATH only; leave untracked junk (HANDOFF_DONE_*, scripts/, *.csv, instructions/, PLAYER_NUMERIC_GRADE_AND_LEADERBOARD_SPEC.md) alone. **caffeinate PID 84474** (verify alive; re-`caffeinate -s -i` if not).

**⚙️ JK SESSION RULINGS still in effect (2026-06-26 attended — DON'T re-litigate):**
1. **BUILD-SCOPE = MAXIMUM** — build everything build-dark-able with documented §16 defaults (all reversible).
2. **RE-GRADE DISPLAY = build-dark default** (surfaces at flip). *(But re-grade is SET-ASIDE — see OPEN-DECISIONS; the STORE needs JK first.)*
3. **TRADE-DEMAND A1.3b = BUILD FULLY incl. the `TRACKER_DB_VERSION` bump** — JK EXPLICITLY AUTHORIZED the saved-shape bump (no critical seasons live). This LIFTS the "no unattended DB bump" rule FOR A1.3b ONLY. Bump migration-safe + update the version-pin tests (esp. `franchiseSeasonLedgerStorage.test.ts` hard-pins TRACKER_DB_VERSION + the full store list — [[trackerdb-version-bump-test-pins]]). Take documented defaults on the 5 A1.3b forks (demander source-of-truth · flashpoint-tax-vs-one-shot-morale-vs-both · albatross↔trade_demander row-key collision · resolution/counter-reset event · intensity source).
4. **RA-11 = BUILD STANDALONE** (done as RA-11a; reconcile w/ Mode-1 B14 at merge).

**➡ BUTTON-UP QUEUE remaining (ground each FRESH from source; all build-dark unless noted; FULL-suite gate — the L-stack/ratings/trait engines feed `processCompletedGame` → transitive-mock-break risk → FULL suite, not scoped; commit BY PATH):**
- **A1.3b** (NEXT) — trade-demand FULL: define a persisted 'confirmed demander' source (the flashpoint runs at `processCompletedGame.ts:627` BEFORE L10 `:665`, reads a PRIOR-game state → the trackerDb bump), fill the `franchiseFlashpointDecayCompute.ts:95` seam, add the `TRADE_DEMAND` morale emitter (`processCompletedGame.ts:382-430` pattern). Builds on A1.3a (the propensity gate `93d39584`). 5 forks → documented defaults. **Ruling 3 = the DB bump is authorized.**
- **A1.4 L12-6** — race/award/All-Star UI surfacing + `allStarSelections` career counter. ⚠ **USER-VISIBLE** (build the data layer, FLAG the UI for JK browser sign-off, do NOT mark closed).
- **A1.5 L4b** — matrix-sourced season takes over the L4a bus (deps L3+L4a met).
- **A1.5d STADIUM RECORDS** — own db `kbl-franchise-stadium-records`, new `isFranchisePhase2StadiumRecordsEnabled` flag (NOT a trackerDb bump); §4 catalog + the 6 living-season hops + §8 stat-display.
- **RETURN TO (once JK rules the OPEN-DECISIONS below):** re-grade-per-checkpoint, RA-7 park-adjust. **DEFERRED enhancement:** RA-11b (pitcher non-pitching windowed/recent trend mirror — doubly-dark, graceful fallback).

**🟡 2 OPEN-DECISIONS FOR JK (logged in the ledger; do NOT block on them — they're set-aside, keep building the queue):**
- **RA-7 park-adjust** — the primary/cumulative ratings signal reads park-BLIND season rows; honoring "per-game park-adjust" needs a net-new event re-aggregation that's NOT default-identity, and the spec forbids the 50/50 approx. Forks: (a) full-season event re-tally, (b) per-player weighted-avg park factor on season rows, (c) recent-leg-only. *(The recent leg alone is auto-buildable but near-zero value at trendTiltWeight=0.)*
- **re-grade-per-checkpoint STORE** — compute is ready (`scoreSmb4Player` from evolved ratings); the per-(player,checkpoint) grade STORE is the blocker: a dedicated store = a trackerDb bump (JK authorized only A1.3b); reusing `franchiseTrueValueSnapshots` is entangled (live trueValue writer `processCompletedGame.ts:362` + race-standings reader → clobber/coordination). Forks: (a) authorize a dedicated grade-snapshot store bump, or (b) rule the read-modify-write reuse design.

DISPATCH MECHANICS (proven all session; USE THE STALL-DETECTING WATCHDOG; macOS has no `timeout`):
  cd /Users/johnkruse/Projects/kbl-tracker
  export PATH="$HOME/.nvm/versions/node/v20.20.0/bin:$PATH"
  rm -f /tmp/codex-<ID>.out; : > /tmp/codex-<ID>.stdout
  ( PREV=0; STALL=0; ELAPSED=0
    while [ $ELAPSED -lt 1800 ]; do
      sleep 120; ELAPSED=$((ELAPSED+120))
      pgrep -f "codex exec.*codex-<ID>.out" >/dev/null 2>&1 || exit 0
      CUR=$(wc -c < /tmp/codex-<ID>.stdout 2>/dev/null | tr -d ' '); CUR=${CUR:-0}
      CHANGED=$(git -C /Users/johnkruse/Projects/kbl-tracker diff --name-only -- src 2>/dev/null | wc -l | tr -d ' '); CHANGED=${CHANGED:-0}
      if [ "$CUR" -le "$PREV" ] && [ "$CHANGED" -eq 0 ] && [ ! -s /tmp/codex-<ID>.out ]; then STALL=$((STALL+1)); else STALL=0; fi
      PREV=$CUR
      if [ $STALL -ge 3 ]; then echo "WATCHDOG STALL kill at ${ELAPSED}s" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null; exit 0; fi
    done
    echo "WATCHDOG 30m cap kill" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null ) &
  WDOG=$!
  sed -n '/<!-- ===== CONTRACT: <ID> /,/<!-- ===== END CONTRACT: <ID> /p' /Users/johnkruse/Projects/kbl-tracker/spec-docs/PROMPT_CONTRACTS.md \
    | NODE_ENV= ~/.local/bin/codex exec -C /Users/johnkruse/Projects/kbl-tracker --skip-git-repo-check -s workspace-write -c model_reasoning_effort=xhigh -o /tmp/codex-<ID>.out - >> /tmp/codex-<ID>.stdout 2>&1
  RC=$?; kill $WDOG 2>/dev/null; echo "codex rc=$RC"
  # run_in_background:true + dangerouslyDisableSandbox:true. model_reasoning_effort ∈ high|xhigh — NEVER very-high. Distinct <ID> per ticket. Captain audits the REAL diff (READ IT, not the Codex paste) + runs his OWN full suite. Launch-sanity check (~25s) that codex is ALIVE + no real 400/401/quota error before ending the turn.

session-done (2026-06-26, UNATTENDED AUTH-4 `/kbl-captain`): **3 features shipped + 3 tickets resolved-without-build + 2 OPEN-DECISIONS surfaced.** RA-6A `55f7db4a` (§6A convex curve) · RA-11a `03be3dd4` (pitcher non-pitching §4A) · A1.3a `93d39584` (trade-propensity gate). RA-10 already-built (spec superseded). RA-7 + re-grade SET-ASIDE (OPEN-DECISIONS). Every ticket grounded from source, make-or-break Captain-RE-verified, gated on the Captain's OWN independent full suite, ZERO-NEW-REDS, NO DB bump (v25), all build-dark. Newest ledger block = `AUTONOMOUS_RUN_LOG.md` 2026-06-26 (the SESSION TALLY at the very bottom).

READ ON RESUME (in order, via Read on exact paths — never trust summaries): `spec-docs/AUTONOMOUS_RUN_LOG.md` (newest block = the SESSION TALLY at the bottom + the A1.3a/RA-6A/RA-11a entries + the 2 OPEN-DECISIONS) · `spec-docs/CURRENT_STATE.md` LIVE HEADER (top block) · `spec-docs/AUTONOMOUS_RUN_PROTOCOL.md` (AUTH-4 + the 4 safety walls) · `spec-docs/SESSION_RULES.md` · `spec-docs/V1_BUILD_QUEUE.md` · `spec-docs/RATINGS_ADJUSTMENT_SPEC.md` + `FRANCHISE_V1_*` + `DECISIONS_LOG.md` 2026-06-26 (esp. the A1.3 5-fork notes + the re-grade ruling :127).
