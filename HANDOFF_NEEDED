next_ticket: **A1.5d STADIUM RECORDS — continue hop-by-hop. The DETECT FOUNDATION is DONE (hop-1a `86ffea39` + hop-1b `8ae5e683`); pick up at hop-1c OR hop-2.** Continue the UNATTENDED AUTH-4 captain loop on `codex/franchise-v1-next` (MAIN). Proven loop: ground FRESH from source → Captain RE-verifies the make-or-break → write+commit contract → dispatch Codex (`run_in_background`+`dangerouslyDisableSandbox`, watchdog) → audit the REAL diff → Opus runs his OWN FULL suite (anything touching `processCompletedGame` → transitive-mock-break risk, see below) → commit branch-only BY PATH → log.

branch/topology: **MAIN** `/Users/johnkruse/Projects/kbl-tracker` on `codex/franchise-v1-next` (HEAD = `c4e7eea3`). **trackerDb v26** (unchanged since A1.3b). A1.5d uses its OWN db `kbl-franchise-stadium-records` → NO trackerDb bump. ⚠ **PAUSED — DO NOT TOUCH/MERGE (JK):** `codex/draft-pipeline-fix` + `codex/auction-draft-ux-rehaul`.

resume_note: **SOLE WORKER — claim the baton FIRST:** `mv HANDOFF_NEEDED HANDOFF_DONE_$(date -u +%Y%m%dT%H%M%SZ)_<note>`; confirm no `codex exec` proc + no other `claude` on this branch. **Characterized baseline (ZERO-NEW-REDS gate): FULL suite ~8400 passing**; the ONLY fails = `wpaRuntimeBoundary` (HARD, solo-fails too) + `franchiseManualSmokeFixture` (order-flake, solo-passes 4/4 — confirmed many times this session). **Read the vitest SUMMARY (failed-file LIST), NOT the RC.** ⚠ **TRANSITIVE-MOCK-BREAK is LIVE for A1.5d:** any new module wired into `processCompletedGame` that imports from `gameStorage`/`franchiseManager`/etc. breaks the 3 partial-mock test files (`processCompletedGame.trueValue`, `processCompletedGame.almanac`, `FranchiseHomeLaunch`) at module-LOAD ("No X export on the ../gameStorage mock") — DETERMINISTIC, a FULL run catches it, a scoped run MISSES it. Fix = add the missing export as a `vi.fn()` stub to those mocks (done for hop-1b's getRecentGames/getFranchiseConfig). ⚠ Do NOT flip any Phase-2 flag. ⚠ DOC-worker keeps `spec-docs/FRANCHISE_MODE1_MODE2_PLAYABLE_V1_GAP_ANALYSIS.md` (M) dirty — NEVER stage it; commit BY PATH; leave untracked junk alone. **caffeinate PID 84474** (verify; re-`caffeinate -s -i` if dead). The benign Codex stderr `failed to load skill .../mode2-pilot/SKILL.md: invalid YAML` is NOT a build error.

**✅ DETECT FOUNDATION DONE (the per-game "a player set/took over a park record" detection, build-dark):**
- **hop-1a `86ffea39`** — `upsertFranchiseStadiumRecordsFromFoundationReport` (`franchiseStadiumRecordsStorage.ts:527`) now returns `changes: FranchiseStadiumRecordChange[]` (`{stadiumId, recordType, recordKey, changeKind:'set'|'overtake', priorValue, priorLeaderPlayerIds, newValue, newLeaderPlayerIds}`). A change fires IFF the new record has a strict SOLE player holder ≠ the prior stored sole holder (SET when no prior, OVERTAKE when a different prior sole holder; ties/same-holder/team-only = SILENT). Pure additive — storage byte-unchanged.
- **hop-1b `8ae5e683`** — new default-OFF `isFranchisePhase2StadiumRecordsEnabled` flag + `franchiseStadiumRecordsTap.ts` (`persistDarkStadiumRecordsForCompletedGame`, mockable `stadiumRecordsTapSeam`), wired into the `processCompletedGame` dark-tap chain after the L12 block, build-dark (flag OFF ⇒ no loads). Per game it builds the foundation report from the LIGHT input (`getRecentGames(1000,{franchiseId,seasonId})` + `getFranchiseConfig(franchiseId)?.stadiums`) and calls the changes[]-returning upsert. **changes[] are captured but NOT consumed yet.**

**➡ REMAINING A1.5d HOPS (spec = `STADIUM_ANALYTICS_SPEC_V2.md` §4-§8; the FULL hop map + file anchors are in the newest `AUTONOMOUS_RUN_LOG.md` block):**
- **hop-1c** (recommended next — makes the records meaningful): extend `BuildFranchiseStadiumFoundationInput` with the at-bat/fielding event loads (the de-orphan caller passes only completedGames; the per-game tap can add `atBatEvents`/`fieldingEvents` via `getGameEvents` like `franchiseL10SweepCompute`) AND thread `hrDistance`+`handedness`+per-play/cumulative WPA onto `FranchiseSprayChartRow` → add the §4 FAME-BEARING record types (farthest-HR RHB/LHB, most-HR-here-season, most-HR-allowed, highest/lowest cumulative WPA position/pitcher, largest +/- single-play WPA swing) to `FranchiseStadiumRecordType` + the candidate builder. §4 build-confirm flags the single-play-WPA-swing records need per-play WPA + at-bat context reconstructible at the per-game tap — VERIFY first. (Uses the A1.5b carry converter for HR carry feet; HR distance is user-entered, never inferred.)
- **hop-2 FAME swap (§6 — first changes[] CONSUMER):** polarity-signed `heatDeltas` (SET `+B·s`; OVERTAKE new `+B·s`/old `−B·s`) concat onto `fameResult.playerHeatDeltas` at `processCompletedGame.ts:~1051`→`resolveFameTap`; gate `isFranchisePhase2FameEnabled` + per-bump sentinel. Double-count guard (pride lives ONLY in fame heat). (Needs hop-1c's fame-bearing records to have anything to swap on.)
- **hop-3 FAN morale (§7 — ⚠ EXHAUSTIVE-RECORD touch):** new `PARK_RECORD_SET` `MasterMoraleEventType` (clone `PLAYER_MILESTONE`, FAN delta only, self-delta 0) — the `satisfies Record<MasterMoraleEventType>` completeness in `masterMoraleMatrix.ts` forces the entry; compose→apply for the stadium HOME team; + the home-park-rival 2× (`fanMoraleEngine.ts:250,374,405`).
- **hop-4 REPORTER (⚠ EXHAUSTIVE-RECORD touch + LLM browser-pending):** new `STADIUM_RECORD` `NarrativeEventType` (`narrativeEngine.ts:77` + `hedgingModifier`:592 + highStakes maps) + a PURE `franchiseStadiumRecordNewsAdapter` (build the adapter only, FLAG the live LLM emission for JK — see [[reporter-adapter-pure-emission-llm-split]]).
- **hop-5 ALMANAC** (`'park-record'` AlmanacNarrativeArchiveEntry + season-summary embed, `franchiseSeasonSummaryStorage.ts:701` de-orphan) · **hop-6 RIVALRY edge** (overtake → `putFranchiseRelationshipEdge`, gate `isFranchisePhase2L13Enabled`) · **§8 STADIUM STAT-DISPLAY (⚠ USER-VISIBLE → FLAG for JK browser sign-off):** per-stadium OPS/ERA/AVG/HR/K splits in the Team Hub stadium tab.

**🟡 AFTER A1.5d the queue is JK-DECISION-BLOCKED (do NOT build unattended):** re-grade-per-checkpoint STORE (dedicated-store bump vs `franchiseTrueValueSnapshots` reuse) · RA-7 park-adjust (full event re-tally vs weighted-avg vs recent-leg-only). **BROWSER/JK-PENDING backlog:** A1.4 L12-6 UI surfacing · L4b live LLM emission + wiring + flag · A1.3b fork-2 · A1.5d §8 display + the reporter LLM half · BV backlog (BV-V8, BV-CAP-MISS, A1.5b-2, T-6b).

DISPATCH MECHANICS (proven all session; watchdog; macOS has no `timeout`; `model_reasoning_effort` ∈ high|xhigh — NEVER very-high):
  cd /Users/johnkruse/Projects/kbl-tracker
  export PATH="$HOME/.nvm/versions/node/v20.20.0/bin:$PATH"
  rm -f /tmp/codex-<ID>.out; : > /tmp/codex-<ID>.stdout
  ( PREV=0; STALL=0; ELAPSED=0
    while [ $ELAPSED -lt 1800 ]; do
      sleep 120; ELAPSED=$((ELAPSED+120))
      pgrep -f "codex exec.*codex-<ID>.out" >/dev/null 2>&1 || exit 0
      CUR=$(wc -c < /tmp/codex-<ID>.stdout 2>/dev/null | tr -d ' '); CUR=${CUR:-0}
      CHANGED=$(git -C /Users/johnkruse/Projects/kbl-tracker diff --name-only -- src 2>/dev/null | wc -l | tr -d ' '); CHANGED=${CHANGED:-0}
      if [ "$CUR" -le "$PREV" ] && [ "$CHANGED" -eq 0 ] && [ ! -s /tmp/codex-<ID>.out ]; then STALL=$((STALL+1)); else STALL=0; fi
      PREV=$CUR
      if [ $STALL -ge 3 ]; then echo "WATCHDOG STALL kill at ${ELAPSED}s" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null; exit 0; fi
    done
    echo "WATCHDOG 30m cap kill" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null ) &
  WDOG=$!
  sed -n '/<!-- ===== CONTRACT: <ID> /,/<!-- ===== END CONTRACT: <ID> /p' /Users/johnkruse/Projects/kbl-tracker/spec-docs/PROMPT_CONTRACTS.md \
    | NODE_ENV= ~/.local/bin/codex exec -C /Users/johnkruse/Projects/kbl-tracker --skip-git-repo-check -s workspace-write -c model_reasoning_effort=high -o /tmp/codex-<ID>.out - >> /tmp/codex-<ID>.stdout 2>&1
  RC=$?; kill $WDOG 2>/dev/null; echo "codex rc=$RC"
  # Launch-sanity (~25s): codex ALIVE + no real 400/401/quota. Captain audits the REAL diff + runs his OWN full suite.

session-done (2026-06-26, UNATTENDED AUTH-4 `/kbl-captain` — long session): **5 build-dark slices SHIPPED, all branch-only / Codex-built / Opus-audited-REAL-diff / Opus-ran-INDEPENDENT-FULL-suite / ZERO-NEW-REDS.** **A1.3b `c87ff779`** (trade-demand FULL + the JK-authorized trackerDb v25→v26 bump) · **A1.4/L12-6 data-layer `bcf12d0a`** (allStarSelections career counter) · **A1.5/L4b core `fee76d37`** (matrix-news adapter, pure/dormant) · **A1.5d hop-1a `86ffea39`** (changes[] DETECT) · **A1.5d hop-1b `8ae5e683`** (per-game DETECT tap + flag; transitive-mock-break caught+fixed in-commit). Also: answered JK's reporter-surfacing questions (the news ENGINE is build-dark/decoupled; all DISPLAY + LLM narration is browser-pending for the UI/UX rework — connect at the tier-routing seam). Handed off at the DETECT-foundation milestone (clean seam, nothing in flight) because the remaining hops touch exhaustive Records + fame/reporter-LLM and warrant fresh context.

READ ON RESUME (in order, via Read on exact paths): `spec-docs/AUTONOMOUS_RUN_LOG.md` (newest blocks = hop-1b, hop-1a, then the 3-feature tally) · `spec-docs/CURRENT_STATE.md` LIVE HEADER (top block) · `spec-docs/AUTONOMOUS_RUN_PROTOCOL.md` · `spec-docs/SESSION_RULES.md` · `spec-docs/STADIUM_ANALYTICS_SPEC_V2.md` §4-§8.
