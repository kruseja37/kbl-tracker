next_ticket: **A1.5d hop-6 (player RIVALRY edge, §5.6) — an OVERTAKE stamps a persisted `RelationshipEdgeRow` (old↔new holder) via `putFranchiseRelationshipEdge`, gated `isFranchisePhase2L13Enabled`.** A1.5d DETECT(hop-1) + FAME swap(hop-2 `219bdb3a`) + FAN buzz(hop-3 `6805ab74`) + REPORTER pure adapter(hop-4 `20732a1c`) + ALMANAC(hop-5 `84175be3`) are all DONE + FULLY VERIFIED; the L-SIM season is GREEN + reproducible (digest `9425360:10944d37`). Continue the UNATTENDED AUTH-4 captain loop on `codex/franchise-v1-next` (MAIN). Proven loop: ground FRESH from source (a parallel grounding workflow keeps heavy reads off main context) → Captain RE-verifies the make-or-break → write+commit contract → dispatch Codex (`run_in_background`+`dangerouslyDisableSandbox`, watchdog) → audit the REAL diff → (ultracode: a parallel 4-lens adversarial design-review workflow before accepting; source-trace any MAJOR finding) → Captain runs his OWN FULL suite + L-SIM → commit branch-only BY PATH → log.

branch/topology: **MAIN** `/Users/johnkruse/Projects/kbl-tracker` on `codex/franchise-v1-next` (HEAD = the hop-5-doc commit `c9453809`). **trackerDb v26**. ⚠ **PAUSED — DO NOT TOUCH/MERGE (JK):** `codex/draft-pipeline-fix` + `codex/auction-draft-ux-rehaul`.

resume_note: **SOLE WORKER — claim the baton FIRST** (`mv HANDOFF_NEEDED HANDOFF_DONE_$(date -u +%Y%m%dT%H%M%SZ)_<note>`; confirm no `codex exec` + no other `claude` on this branch). **caffeinate PID 84474** (verify). Benign Codex stderr `mode2-pilot/SKILL.md: invalid YAML` is NOT a build error.
- **FULL-suite baseline (ZERO-NEW-REDS): ~8431 passing**; ONLY fails = `wpaRuntimeBoundary` (HARD) + `franchiseManualSmokeFixture` + `franchiseOffseasonGuards` (order-flakes, re-run SOLO → green). Read the vitest SUMMARY (failed-file LIST), NOT the RC.
- **L-SIM HEALTHY:** `season.config.ts` runs a full clean 60g (digest `9425360:10944d37`, `findings:0`, `criticalFindings:0`, `stoppedEarly:false`, `sameSeedByteIdentical:true`); smoke 24g all-CRITICAL-pass. Always read the SUMMARY JSON, NOT the RC. SMOKE(24g) does NOT reach game 36 — run the SEASON for late coverage. Run the STANDARD 60g leg LAST (it regenerates `test-utils/lsim/results/lsim-h2-baseline-checkpoint-*.json`). A flags-ON change touching a DUMPED store (`DUMP_DATABASES` in `test-utils/lsim/storeDump.ts`: kbl-tracker / kbl-franchise-morale / kbl-league-builder / kbl-schedule / kbl-event-log / kbl-app-meta / kbl-manager-identity / kbl-franchise-<id>) RE-BAKES the anchor → verify the delta is sane → commit the regenerated baseline JSONs. A DORMANT/non-dumped-store change stays byte-identical (no re-bake). New own-DBs feeding dumped state MUST be added to `test-utils/lsim/sandbox.ts` `DB_NAMES_TO_DELETE`.

**DETECT→living-season chain:** hop-1 DETECT ✅ · hop-2 FAME swap ✅ · hop-3 FAN buzz ✅ · hop-4 REPORTER pure adapter ✅ · hop-5 ALMANAC ✅ (`84175be3` — `'park-record'` derived-view materialization in `listAlmanacNarrativeArchive` + an optional `stadiumRecords` season-summary embed; both flag-gated build-dark; L-SIM byte-identical/dormant). Settled facts: the fame bridge moves SELF morale only; PARK_RECORD_SET is the sole home-fan path; the reporter LLM-emission half (hop-4) + the Almanac UI polish (hop-5) are JK browser/LLM-pending.

**⚠ KEY VERIFIED INVARIANT (source-traced 2026-06-26, useful for hop-6+):** for FRANCHISE games, `statsScopeId === seasonId === getFranchiseSeasonId(franchiseId, seasonNumber)` is GUARANTEED (WAR scope `{seasonId, statsScopeId: seasonId}` at `processCompletedGame.ts:275-278`; canonical handoff `franchisePersistenceContract.ts:79-85`; the L-SIM `lsimIds` shows them equal). Records/summaries/Almanac all key off this.

**➡ hop-6 (rivalry edge, §5.6) — GROUNDED MAKE-OR-BREAK FACTS (Captain pre-grounded; re-verify from source before contracting):**
- **The writer EXISTS:** `putFranchiseRelationshipEdge` at `src/utils/franchiseRelationshipEdgesStorage.ts:109`. The flag EXISTS: `isFranchisePhase2L13Enabled()` (`franchisePhase2Flags.ts:101`) + `setFranchisePhase2L13EnabledForTests` (`:105`).
- **⚠⚠ THE EDGE STORE IS IN `kbl-tracker` (a DUMPED store)** — `franchiseRelationshipEdgesStorage.ts:7 DB_NAME='kbl-tracker'`, written via `syncEngine.upsert('kbl-tracker', STORE_NAME, …)` (`:118`). So when an overtake fires in the L-SIM (flags-on) and an edge is written, **the L-SIM season baseline RE-BAKES** (NOT byte-identical like hop-5). EXPECT a re-bake: run the season, verify the delta is sane (new RelationshipEdgeRows on overtakes), commit the regenerated `lsim-h2-baseline-checkpoint-*.json`. This is the heavy part — give it fresh attention.
- **No trackerDb bump expected** (the `franchiseRelationshipEdges` store already exists in `kbl-tracker` — the relationship subsystem is built: formation/intensity/morale/intel/flare computes + an L13 reporter adapter all exist). CONFIRM the store is already registered in `trackerDb.ts` (grep the STORE_NAME) → if yes, NO bump; the spec §5.6 says only the event-spark WRITER is new.
- **The consumer feed:** the overtake `stadiumChanges` (the `FranchiseStadiumRecordChange[]` with `changeKind==='overtake'`, `newLeaderPlayerIds` + `priorLeaderPlayerIds`) is already surfaced EARLY in `processCompletedGame.ts` (the hop-2 restructure; held as `stadiumChanges`, consumed by the fame writer + hop-3's `persistDarkParkRecordMoraleForCompletedGame`). hop-6 adds a NEW dark writer (mirror hop-3's sibling shape) AFTER the existing blocks, gated `isFranchisePhase2L13Enabled`, that for each OVERTAKE change stamps an old↔new-holder edge.
- **GROUND BEFORE CONTRACTING:** (1) the `RelationshipEdgeRow` shape (+ the `RIVALRY` vs unused `HISTORY` slot semantics — §5.6 says use one; check the existing edge-type/slot enum); (2) how `putFranchiseRelationshipEdge` keys/dedups (idempotency — overtake must fire once per checkpoint, mirror the hop-3 `sourceEventId`/checkpoint guard); (3) the "event-spark writer BYPASSING the attribute-only formation engine" (§5.6) — confirm the formation engine `franchiseRelationshipFormationCompute.ts` is attribute-driven and the new writer goes DIRECT to `putFranchiseRelationshipEdge` (not through formation); (4) the bidirectional/edge-direction convention (old↔new holder); (5) only SET-with-prior / OVERTAKE makes an edge (a fresh SET with no prior holder has no old holder → no edge).

Then the §7 home-park-rivalry 2× deferred slice (archive aggregation + `vsRival` flag-flip + 2× dial in `fanMoraleEngine` `:250/374/405`) → §8 per-stadium stat-display (USER-VISIBLE → flag for JK). **AFTER A1.5d the queue is JK-DECISION-BLOCKED** (re-grade store · RA-7 park-adjust). BROWSER/JK-PENDING: A1.4 L12-6 UI · L4b LLM emission · A1.3b fork-2 · A1.5d hop-4 reporter LLM half · hop-5 Almanac UI polish (a "PARK RECORDS" `KIND_FILTERS` chip + `kindLabel`/`kindIcon` case in `AlmanacNarratives.tsx`) + §8 display · BV backlog.

DISPATCH MECHANICS (proven; watchdog; macOS has no `timeout`; `model_reasoning_effort` ∈ high|xhigh — NEVER very-high):
  cd /Users/johnkruse/Projects/kbl-tracker
  export PATH="$HOME/.nvm/versions/node/v20.20.0/bin:$PATH"
  rm -f /tmp/codex-<ID>.out; : > /tmp/codex-<ID>.stdout
  ( PREV=0; STALL=0; ELAPSED=0
    while [ $ELAPSED -lt 1800 ]; do
      sleep 120; ELAPSED=$((ELAPSED+120))
      pgrep -f "codex exec.*codex-<ID>.out" >/dev/null 2>&1 || exit 0
      CUR=$(wc -c < /tmp/codex-<ID>.stdout 2>/dev/null | tr -d ' '); CUR=${CUR:-0}
      CHANGED=$(git -C /Users/johnkruse/Projects/kbl-tracker diff --name-only -- src 2>/dev/null | wc -l | tr -d ' '); CHANGED=${CHANGED:-0}
      if [ "$CUR" -le "$PREV" ] && [ "$CHANGED" -eq 0 ] && [ ! -s /tmp/codex-<ID>.out ]; then STALL=$((STALL+1)); else STALL=0; fi
      PREV=$CUR
      if [ $STALL -ge 3 ]; then echo "WATCHDOG STALL kill at ${ELAPSED}s" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null; exit 0; fi
    done
    echo "WATCHDOG 30m cap kill" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null ) &
  WDOG=$!
  sed -n '/<!-- ===== CONTRACT: <ID> /,/<!-- ===== END CONTRACT: <ID> /p' /Users/johnkruse/Projects/kbl-tracker/spec-docs/PROMPT_CONTRACTS.md \
    | NODE_ENV= ~/.local/bin/codex exec -C /Users/johnkruse/Projects/kbl-tracker --skip-git-repo-check -s workspace-write -c model_reasoning_effort=high -o /tmp/codex-<ID>.out - >> /tmp/codex-<ID>.stdout 2>&1
  RC=$?; kill $WDOG 2>/dev/null; echo "codex rc=$RC"

session-done (2026-06-26, UNATTENDED AUTH-4): **A1.5d hop-5 ALMANAC SHIPPED (`84175be3`, doc `c9453809`).** Two flag-gated build-dark deliverables (the Almanac is a DERIVED VIEW → materialize `'park-record'` entries from the persisted records, NOT a new write store; + an optional `stadiumRecords` season-summary embed, omit-when-off). An ultracode 4-lens adversarial review raised a scope-derivation MAJOR → source-traced to the verified franchise `statsScopeId===seasonId` invariant (can't fire) → HARDENED anyway + pinned with a divergent-statsScopeId regression test. Branch-only / Codex-built / Captain-audited-REAL-diff / FULL ZERO-NEW-REDS (8431) / L-SIM season clean 60g BYTE-IDENTICAL (no re-bake — dormant in sim). Clean seam — nothing in flight. Handing hop-6 (a fresh L13 multi-subsystem hop that WILL re-bake the L-SIM) to fresh context after a long session.

READ ON RESUME (in order): `spec-docs/AUTONOMOUS_RUN_LOG.md` (newest block = hop-5) · `spec-docs/CURRENT_STATE.md` LIVE HEADER · `spec-docs/AUTONOMOUS_RUN_PROTOCOL.md` · `spec-docs/SESSION_RULES.md` · `spec-docs/STADIUM_ANALYTICS_SPEC_V2.md` §5.6 (rivalry edge) + §7 (home-park-rivalry 2×) + §10 (wiring map: `putFranchiseRelationshipEdge`).
