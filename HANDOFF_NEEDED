next_ticket: L12 (races / All-Star / awards-fame) — recon-split FIRST (mirror the L10/L11 recons), then build the sub-pieces. L12-Q10 is RESOLVED (no standalone work).
branch: codex/franchise-v1-next
resume_note: AUTH-4 OVERNIGHT ACTIVE (caffeinate running). AUTH-4 is the standing go — do the session-start reads, RESTATE, then PROCEED without waiting for JK. ⚠ KEEP EXACTLY ONE AUTH-4 WORKER ACTIVE: this session hit a concurrent-worker collision (the cron + a manual "start new session" both ran; another session had built L11-4 in the working tree uncommitted) — handled cleanly (JK ruled TAKE OVER; took over the stopped WIP, independently audited, host-gated, committed) but verify no other session is live before building (git status should be clean except untracked HANDOFF_DONE_* + the reference-docs CSV; check file mtimes / `ps` for a live `codex`/`node` build). This session COMMITTED (build-DARK behind isFranchisePhase2L11Enabled): L11-4 3e718e4f (Almanac tenure join + durable fired-tenure persistence — recordManagerTenureEnd rides the managerId-keyed identity profile, NO DB bump, captured before the successor swap; almanacQueries ManagerTeamTenureAggregate gains hireDate/endDate/endReason via findTenureRecord) · L11-5 f77b3c75 (reporter tap: pure franchiseL11ManagerChangeNewsAdapter → SeasonNewsEvent eventType MANAGER_CHANGE [added to narrativeEngine NarrativeEventType + hedgingModifier 0.90 + highStakesEvents, additive+dormant]). ⇒ L11 (managers) FULLY COMPLETE (1-5). Suite baseline now 7,729/442, 7,726 pass / 3 fail = 2 characterized (wpaRuntimeBoundary + franchiseManualSmokeFixture) + 1 order-flake (EliminationTeamHub, passes solo 16/16); trackerDb v23. READ FIRST: spec-docs/CURRENT_STATE.md (live header) + spec-docs/AUTONOMOUS_RUN_LOG.md (newest entries) + spec-docs/L11_L14_OPEN_QUESTIONS.md (the L12 rulings: Q1 award-cat extension to season-race slots ALL_STAR/BENCH_PLAYER/BOOGER_GLOVE/RELIEVER_OF_YEAR [defer the 2 one-shots]; Q2 race standing = single weighted composite w/ per-race-type weights [fame-led fan-vote, merit-dominant awards]; Q5 All-Star roster shape; Q13 All-Star lock at 60%; Q8 §20.4 status-fame layer = L6 owns it, L12 only consumes the tier) + DECISIONS_LOG (the 43 ruled Qs + the 2026-06-19 L12-Q10 ruling). L12-Q10 (fame double-ladder) RULED 2026-06-19 = DEFER the live getFameTier label-purge to post-D13 activation; L12's ONLY fame-ladder requirement = every race/fame-tier read goes through resolveFameTier (fameModel.ts:191), NEVER the scalar getFameTier (fameEngine.ts:349). Add the live getFameTier label-purge to the post-D13 activation checklist. Build-DARK behind a new isFranchisePhase2L12Enabled flag (clone the L11 block). ROUTING (ratified, proven this run): Codex builds via `codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -` fed a stdin heredoc that POINTS at the contract section in spec-docs/PROMPT_CONTRACTS.md (do NOT duplicate the contract into a temp file; do NOT use a Claude subagent — Codex is the canonical builder), run as a background Bash task (run_in_background + dangerouslyDisableSandbox=true), node v20 on PATH (export PATH="$HOME/.nvm/versions/node/v20.20.0/bin:$HOME/.local/bin:$PATH"); Opus (Captain) audits the diff line-by-line (builder != auditor) + runs the FULL host gate (Codex only runs the targeted test file); host gate = `NODE_ENV= npm run build` (tsc -b + vite) + `NODE_ENV= npx vitest run` (full suite, characterized baseline 7,729/442, 2 fail + the EliminationTeamHub order-flake which passes solo); contracts MUST specify `npm run build` as the builder verification (the L11-3 fix1 lesson) and reasoning effort TWICE (route header + closing). Commit branch-only, NEVER push, co-author trailer. On resume: delete this HANDOFF_NEEDED + write HANDOFF_DONE_<ISO>, then start the L12 recon (a read-only workflow fan-out → spec-docs/L12_SCOPE_MAP.md is the right first step) before contracting any build.
