next_ticket: **FAME-FLOOR-1 — wire the fame WAR-legitimacy "merit floor"** (the next soul-gap on the `claude/v1-soul-gaps` lane). Today fame is WPA-only; `applyWarLegitimacyGravity` (`src/engines/fameModel.ts:161`) is BUILT + correctly UPWARD-ONLY (nudges heat up toward a WAR-justified target: `max(0,(target-current)*strength)`; takes a `FameMeritLevel` enum OR a raw number) but has ZERO callers. The fame compute (`src/utils/franchiseFameCompute.ts`, `persistDarkFameRecordsForCompletedGame`, per-player loop ~:101-139) only sees `playerWpaTotals` + `fameEvents` — NO WAR. WAR/merit IS available in `src/utils/processCompletedGame.ts` via `trueValueResult` (per-player `trueValue`, seasonWAR-derived, ~:365-376) computed BEFORE the fame tap. **BUILD** = thread a per-player WAR/merit measure from `trueValueResult` into the fame compute → derive a `FameMeritLevel` (⚠ GROUND `FameMeritLevel` + `config.warGravity.meritHeatTarget` in fameModel/FAME_TUNING from source FIRST) → call `applyWarLegitimacyGravity` in the heat chain right after `applyHeatUpdate` (`franchiseFameCompute.ts:114`). **⚠ This MOVES fame in the L-SIM → a BEHAVIORAL re-bake** (not a dormant 0-field like FIELD-CREDITS-1) — verify the fame-heat delta is sane + upward-only. **ALSO tighten** the currently-vacuous `soul.fame-war-legitimacy-floor` L-SIM invariant to actually assert the floor fires (today it passes whether or not the gravity is wired = false comfort). Gated `isFranchisePhase2FameEnabled` (already flipped on in the L-SIM). **Direction = UPWARD-ONLY** per the soul-layer-direction rule — do NOT make it bidirectional (the Captain once mis-ratified it bidirectional; JK caught it).

branch/topology: **SOUL-GAP LANE = `claude/v1-soul-gaps`** in worktree **`/Users/johnkruse/Projects/kbl-soulgaps`** (own APFS-cloned `node_modules`), branched off the trunk `experiment/manager-wpa-window` @ `7b5093a3`. The trunk = the engine/mWAR integration line + ALL Captain docs. **2 tickets committed on the lane:** `11371b50` SIM-DBCAP-1 (dump stadium-records + home-park-rivals into the L-SIM snapshot) · `7484e9c8` FIELD-CREDITS-1 (rescuedThrow/basesSaved → season fielding stats, incl. a fix-iter). **⚠ The MAIN tree (`/Users/johnkruse/Projects/kbl-tracker`) has UNCOMMITTED Lineups-tab UI work from another thread** (`LineupsTabContent.tsx`, `franchiseLineupDomain.ts`, `TeamHubContent.tsx`, `FranchiseHome.tsx`) — DO NOT touch/sweep it; that's why the lane is an isolated worktree. Other active threads (own branches, don't disturb): UI redesign `codex/auction-draft-ux-rehaul` · draft-setup `codex/draft-setup-ui` · draft-pipeline/optimizer `codex/draft-pipeline-fix` · rivalry-registration DONE on `claude/cranky-mcnulty-d3f4bb`.

resume_note: **SOLE WORKER on the soul-gap lane** — confirm no `codex exec` running, then resume. **CANONICAL PLAN = `spec-docs/V1_ACTIVATION_READINESS_MAP.md` §0d (the whole-arc widened plan, OPERATIVE) + §0c (the soul-layer spine)** — read those FIRST; they hold the full pathway + every JK ruling (rivalry = ONE combined engine; rebrand = IN for v1; tier = budget-only; etc.). **THE PROVEN PER-TICKET LOOP (the triangle — Codex builds, Captain/Opus audits, builder≠auditor; JK reprimanded a self-report lapse — DO NOT trust a builder's "done"):** (1) GROUND the make-or-break from source FIRST — the grounding caught real hidden complexity on BOTH tickets (a persistence trace + an attribution model); (2) write a CONTRACT to MAIN's `spec-docs/PROMPT_CONTRACTS.md` wrapped in `<!-- ===== CONTRACT: <ID> ===== -->` … `<!-- ===== END CONTRACT: <ID> ===== -->` markers; (3) DISPATCH Codex to the worktree via the watchdog block below (run_in_background + dangerouslyDisableSandbox; `model_reasoning_effort=high`); (4) **AUDIT THE REAL DIFF** — `git -C /Users/johnkruse/Projects/kbl-soulgaps diff`, NEVER the Codex paste; verify ONLY contracted files changed + the make-or-break holds (FIELD-CREDITS-1 needed a fix-iter when the audit caught two production-aggregator THROWS the paste glossed over); a correct Codex STOP-IF is GOOD; max 2 fix-iters; (5) GATE = my OWN `NODE_ENV= npm run build` exit 0 + the affected test file; (6) if it touches the season/soul pipeline, run the **L-SIM re-bake** `NODE_ENV= npx vitest run -c test-utils/lsim/season.config.ts` (~8min) → verify `sameSeedByteIdentical:true` + `criticalFindings:0` + a **python STRUCTURAL diff** of `test-utils/lsim/results/lsim-h2-baseline-checkpoint-*.json` (confirm ONLY the intended delta; existing data byte-identical); (7) **COMMIT BRANCH-ONLY by PATHSPEC** — `git commit -m "..." -- <paths>` (the `-m`+message BEFORE the `--`; this avoids an index-sweep that bit me once); **NEVER push**; **EXCLUDE `spec-docs/SEASON_SIMULATION_REPORT.md`** (the L-SIM run pollutes it with worktree absolute paths). Trailer: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

BASE-GATE (already run — the lane base is SOLID): trunk `7b5093a3` → build exit 0 · L-SIM season byte-identical (`criticalFindings:0`, the mWAR re-bake reproduces) · full suite ~8509 passing, only HARD red = `wpaRuntimeBoundary`. **⚠ 3 archetype/optimizer tests** (`archetypeBalanceSimulator`, `historicalArchetypes`, `poolFeasibility`) show as full-suite "reds" but are HEAVY-SIM **TIMEOUTS** (vitest default 5s) — they PASS with `--testTimeout=120000` (archetype logic 15/15 in band). **FLAG for the draft-pipeline thread**: add a per-test timeout to those 3 on their own files so their suite reads clean. NOT regressions, NOT a lane blocker.

REMAINING SOUL-GAPS after FAME-FLOOR-1 (per §0d, JK-ruled): the **COMBINED rivalry engine** (record-overtake + race/All-Star-envy → relationship edges, ONE engine) · the **REBRAND TRIGGER** (wire a per-game dwell trigger into `processCompletedGame` → `getRebrandOffer`/cascade; rebrand is IN for v1). Then **harness completion** (full-season config + npm script + make the scenario test gate on the summary JSON, not RC + the milestone/Almanac correctness invariant) → the **freeze→season bridge** (drive the L-SIM from a REAL drafted+frozen franchise, not the synthetic seed) → the **headless PLAYOFF driver** (the ONE genuinely-new engine — CRITICAL; reuse the season runner's post-game machinery on a bracket) → **whole-arc assembly** (build league → competitive auction [shills ON] → freeze → full season → playoffs → ceremony) → **§16 tuning sweep** + the tuning report (season-soul + draft knobs). UI rework + JK manual browser tests come AFTER.

DISPATCH MECHANICS (proven this session; macOS has no `timeout` → shell watchdog; run via Bash run_in_background + dangerouslyDisableSandbox):
```
ID=<TICKET-ID>
cd /Users/johnkruse/Projects/kbl-tracker
export PATH="$HOME/.nvm/versions/node/v20.20.0/bin:$PATH"
rm -f /tmp/codex-$ID.out; : > /tmp/codex-$ID.stdout
( PREV=0; STALL=0; ELAPSED=0
  while [ $ELAPSED -lt 1800 ]; do
    sleep 120; ELAPSED=$((ELAPSED+120))
    pgrep -f "codex exec.*codex-$ID.out" >/dev/null 2>&1 || exit 0
    CUR=$(wc -c < /tmp/codex-$ID.stdout 2>/dev/null | tr -d ' '); CUR=${CUR:-0}
    CHANGED=$(git -C /Users/johnkruse/Projects/kbl-soulgaps diff --name-only 2>/dev/null | wc -l | tr -d ' '); CHANGED=${CHANGED:-0}
    if [ "$CUR" -le "$PREV" ] && [ "$CHANGED" -eq 0 ] && [ ! -s /tmp/codex-$ID.out ]; then STALL=$((STALL+1)); else STALL=0; fi
    PREV=$CUR
    if [ $STALL -ge 3 ]; then pkill -9 -f "codex exec.*codex-$ID.out"; exit 0; fi
  done
  pkill -9 -f "codex exec.*codex-$ID.out" ) &
WDOG=$!
sed -n '/<!-- ===== CONTRACT: '"$ID"' /,/<!-- ===== END CONTRACT: '"$ID"' /p' /Users/johnkruse/Projects/kbl-tracker/spec-docs/PROMPT_CONTRACTS.md \
  | NODE_ENV= ~/.local/bin/codex exec -C /Users/johnkruse/Projects/kbl-soulgaps --skip-git-repo-check -s workspace-write -c model_reasoning_effort=high -o /tmp/codex-$ID.out - >> /tmp/codex-$ID.stdout 2>&1
RC=$?; kill $WDOG 2>/dev/null; echo "codex rc=$RC"
```

READ ON RESUME (in order): `spec-docs/V1_ACTIVATION_READINESS_MAP.md` **§0d + §0c** (THE plan + rulings) · this HANDOFF · `git -C /Users/johnkruse/Projects/kbl-soulgaps log --oneline -3` (the 2 banked tickets) · `spec-docs/PROMPT_CONTRACTS.md` (the SIM-DBCAP-1 / FIELD-CREDITS-1 / FIELD-CREDITS-1-FIX contracts = templates for tone/scope/STOP-IF).
