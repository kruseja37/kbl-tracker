next_ticket: L11-4 — Almanac fire/hire-date join + DURABLY persist the fired-tenure (resolves the L11-3 OPEN decision)
branch: codex/franchise-v1-next
resume_note: AUTH-4 OVERNIGHT ACTIVE (caffeinate running). AUTH-4 is the standing go — do the session-start reads, RESTATE, then PROCEED immediately without waiting for JK. This stretch committed (build-DARK behind isFranchisePhase2L11Enabled): L11-1 46c3c761 · L11-2 1821ad21 · L11-3 4c59ecbd · L11-3b 7268f9f1 (+ gitignore ad75afa4, ruling-pass d8bd0670). READ FIRST: spec-docs/CURRENT_STATE.md (live header) + spec-docs/AUTONOMOUS_RUN_LOG.md (the QUEUE + the L11-3/L11-3b OPEN DECISIONS + the routing mechanism) + spec-docs/L11_L14_OPEN_QUESTIONS.md + DECISIONS_LOG (the 43 ruled Qs). L11-4 = the Almanac legacy surfacing: ManagerTeamTenureAggregate (almanacQueries.ts:193) + finalizeManagerTenure (:1203)/getOrCreateManagerTenure (:1066) gain hire/fire dates + an end-reason ('fired'/'resigned'/'relocated'); AND resolve the L11-3 OPEN DECISION — the successor saveManagerAssignment overwrites the fired [mode,instanceId,teamId] key so the setManagerFired tombstone (fired/endDate/firedReason) is TRANSIENT; the fired tenure-end (date+reason, from the firing event) must be persisted DURABLY in L11-4 (likely: capture it in fireManager BEFORE the successor swap — restructure franchiseManagerFiring.ts steps 6/7 — OR write a firing-history record the Almanac reads). Build-DARK. QUEUE after L11-4: L11-5 (reporter tap → SeasonNewsEvent) → fame double-ladder collapse (L12-Q10, a pre-L12 cleanup: retire fameEngine.ts getFameTier forbidden labels; races must read resolveFameTier) → L12 (recon-split first: award-cat extension [ALL_STAR/BENCH_PLAYER/BOOGER_GLOVE/RELIEVER_OF_YEAR per L12-Q1] · All-Star roster · race standing weighted-composite · TV-family KK/Bust/Comeback) → L13 (recon-split: §24 six-edge taxonomy · per-type formation gate · L3 matrix edge rows · reporter inaccuracy flat-10%-hedge · reuse rivalryScores store) → L14 (rebrand cascade, 6-step ordered, fireManager({reason:'rebrand',suppressFanReliefBump:true})) → L-SIM gate. ROUTING (proven this run): Codex builds via `codex exec --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check - ` fed a stdin heredoc that POINTS at the contract section in spec-docs/PROMPT_CONTRACTS.md (do NOT duplicate the contract into a temp file; do NOT use a Claude subagent — Codex is the canonical builder), run as a background Bash task with dangerouslyDisableSandbox=true; Opus (Captain) audits the diff line-by-line (builder != auditor); host gate = `NODE_ENV= npm run build` (tsc -b + vite — NOT just tsc --noEmit) + `NODE_ENV= npx vitest run` (characterized baseline 7,713/441, 2 fail: wpaRuntimeBoundary + franchiseManualSmokeFixture); commit branch-only NEVER push, co-author trailer; CONTRACTS must specify `npm run build` as the builder verification (L11-3 fix1 lesson). VERIFY-AT-ACTIVATION (batch for JK, do NOT block): the gameState.homeTeamId/awayTeamId team-id namespace vs the morale-snapshot + manager-assignment team-ids used by L11-3 fireManager + L11-3b auto-backstop. On resume: delete this HANDOFF_NEEDED (write HANDOFF_DONE_<ISO> if the cron expects it) and continue the loop.
