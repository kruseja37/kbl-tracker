next_ticket: **#18 Winner-honors resolver (LANE A — the living-season / season-finalize track)**. AUTH-4 ultra-think autonomous Captain loop (Codex builds, Opus audits the REAL diff + gates + commits branch-only + logs). The full roadmap is WRITTEN — do NOT re-audit.

⚠ THIS IS THE **LANE A** BATON (living season: playoff driver / winner-honors / L-SIM, Tiers 4/6). **LANE B (draft UI / Stream B) is a SEPARATE concurrent Captain with its OWN coordination — do NOT touch its surface or claim its work.** Per memory [[lane-b-draft-ui-progress]], Lane B has already SHIPPED #1 (archetype→capIdentity `2e53a0a6`), #2/#3, #7-10, and merged the draft preview screens (#27) on `claude/v1-draft-ui` (`kbl-draftlane`). So #1 is DONE — do not rebuild it. Stay on LANE A surface (Mode-2 finalize/soul/L-SIM); SERIALIZE heavy vitest/L-SIM gates with Lane B.

═══ READ FIRST (the roadmap is written) ═══
- `spec-docs/V1_PRE_FREEZE_CHECKLIST.md` — THE 38-ticket queue + "RECOMMENDED NEXT 3". #18 is the next LANE-A item (P2/P3 done/deferred; #1 is Lane B, done).
- `spec-docs/AUTONOMOUS_RUN_LOG.md` — live ledger; newest block = PLAYOFF-DRIVER-2 SHIPPED + the OPEN-DECISIONS.
- `spec-docs/V1_STATUS_AND_ASSEMBLY_PLAN.md` `## JK RULINGS — 2026-06-27` (winner-honors = ALL winners honored, rarity-scaled, magnitude-tuned per §16; the FREEZE GATE).
- Memory: [[lane-b-draft-ui-progress]] (Lane B status — do not touch), [[lsim-postseason-determinism-pattern]] (the L-SIM wiring pattern), [[lsim-season-finalize-and-emission-llm-gate]].

═══ #18 WINNER-HONORS (the LANE A ticket) ═══
Checklist #18 (NOT-BUILT, M; JK ruled 2026-06-27 — ALL award winners honored, scaled by rarity, supersedes the snub-only design). GOAL: extend `franchiseSeasonEndHonors.ts` + `FranchiseHonorTier` to the Gold Glove / Silver Slugger / Booger / Reliever-of-Year / Rookie winner-side (today only MVP/CY/ALL_STAR are honored via `FranchiseHonorKind`), with a TUNABLE PLACEHOLDER magnitude. **GROUND from source first** — re-read `franchiseSeasonEndHonors.ts` (`emitFranchiseSeasonEndHonors`, the honor tiers/kinds) + how the awards feed it (`computeAndPersistFranchiseWarAwards` → the award categories; the L-SIM `runSeasonFinalize` already drives this chain, so #18 has L-SIM coverage). **Rarity metric (ADDENDUM-resolved, build the mechanism):** rarity = per-award PRESTIGE value, ADDITIVE across awards — build the mechanism with a tunable placeholder prestige table; flag the exact prestige numbers for §16 tuning. Soul-layer no-inference: the measurement comes from the spec/ruling verbatim; placeholder + flag where silent; never infer from current behavior. After #18: **#20** (honor-news path for the new non-MVP winners — `FranchiseHonorKind` only has MVP/CY/ALL_STAR). Then LANE-A Tier-6 hardening (#34 soul-carry-post-freeze asserts · #38 the ~9 missing fail-on-wrong soul checks [must land AT/BEFORE freeze] · #35 freeze→season bridge [cross-agent seam] · #37 whole-arc assembly).

═══ STATE (all durable) ═══
Trunk = `experiment/manager-wpa-window` (HEAD `f24b5b61` docs). **PLAYOFF-DRIVER-2 SHIPPED `303c799a`** on lane `claude/v1-playoff-driver` (worktree `/Users/johnkruse/Projects/kbl-playoff`, own node_modules). PLAYOFF-DRIVER **P2 (#22) + #36 clean-end DONE**; **P3 (playoff fame-amp + champion-MVP #23) DEFERRED** — the L-SIM postseason produces NO per-player playoff stats by design (deterministic seeded-score `playGame`, no `processCompletedGame` for playoff games) → champion-MVP is NOT "easy" → JK OPEN-DECISION (revisit: MVP from frozen regular-season stats scoped to the champion roster, OR a postseason stats-fidelity ticket). **The playoff lane is NOT merged to trunk** — merge the whole lane only at its final clean seam (read-only merge-tree pre-assess → resolve PROMPT_CONTRACTS append-union → build+suite+L-SIM gate → push) and the **merge+push needs JK confirm** (do NOT push unattended; [[kbl-safe-lane-merge-technique]]).

═══ DISPATCH (proven; macOS no `timeout`) ═══
#18 is a LANE-A ticket → build it in the MAIN tree `/Users/johnkruse/Projects/kbl-tracker` on trunk `experiment/manager-wpa-window` (NOT the playoff lane). Append the contract to `spec-docs/PROMPT_CONTRACTS.md` wrapped in `<!-- ===== CONTRACT: <ID> ===== -->` markers, then:
`sed -n '/CONTRACT: <ID> /,/END CONTRACT: <ID> /p' /Users/johnkruse/Projects/kbl-tracker/spec-docs/PROMPT_CONTRACTS.md | NODE_ENV= ~/.local/bin/codex exec -C /Users/johnkruse/Projects/kbl-tracker --skip-git-repo-check -s workspace-write -c model_reasoning_effort=high -o /tmp/codex-<id>.out -`
via Bash `run_in_background: true` + `dangerouslyDisableSandbox: true`. `very-high` = 400 (INVALID; use `high`/`xhigh`). Session MUST be xhigh/ultra-think or long bg dispatches get KILLED ([[long-background-dispatch-needs-ultra-think]]). Captain runs the L-SIM (Codex stalls on it). Commit branch-only BY PATH, NEVER push; trailer `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. ⚠ On the shared trunk, a concurrent JK/Lane-B commit can sweep your staged files — stage+commit in ONE Bash call, verify HEAD is your message ([[shared-branch-commit-atomicity]]).

═══ GATE ═══
Build 0 · the affected test green · **FULL suite (read the failed-FILE list, ZERO-NEW-REDS)** — characterized reds: `wpaRuntimeBoundary` (HARD) + `historicalArchetypes` (load flake) + `franchiseManualSmokeFixture` (order-flake, solo-passes); confirm any "new" red passes solo before treating it as real · #18 touches the Mode-2 finalize/honors path → run the **L-SIM season** (Captain runs; read the SUMMARY JSON not the RC; expect `criticalFindings:0`, `sameSeedByteIdentical:true`; the honors change is in the dumped trackerDb so a deterministic checkpoint re-bake is EXPECTED — structural-diff it: rowCounts/findings identical, delta = the new honor rows + digest).
