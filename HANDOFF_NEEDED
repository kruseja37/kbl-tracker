next_ticket: **A1.5d hop-3 (fan morale — the `PARK_RECORD_SET` morale row).** A1.5d hop-2 FAME SWAP is DONE + FULLY VERIFIED (`219bdb3a`) and the pre-existing L-SIM season trait-trip that briefly blocked it is ROOT-CAUSED + FIXED (`514ce366`) — **the L-SIM season is GREEN again (full clean 60g) and the canonical baseline is re-baked + healthy.** Continue the UNATTENDED AUTH-4 captain loop on `codex/franchise-v1-next` (MAIN). Proven loop: ground FRESH from source → Captain RE-verifies the make-or-break → write+commit contract → dispatch Codex (`run_in_background`+`dangerouslyDisableSandbox`, watchdog) → audit the REAL diff → (ultracode: a parallel adversarial-audit subagent earns its keep) → Opus runs his OWN FULL suite + L-SIM (smoke + season) → commit branch-only BY PATH → log.

branch/topology: **MAIN** `/Users/johnkruse/Projects/kbl-tracker` on `codex/franchise-v1-next` (HEAD = the hop-3-doc commit after `514ce366`). **trackerDb v26** (unchanged). A1.5d uses its own db `kbl-franchise-stadium-records` → NO trackerDb bump. ⚠ **PAUSED — DO NOT TOUCH/MERGE (JK):** `codex/draft-pipeline-fix` + `codex/auction-draft-ux-rehaul`.

resume_note: **SOLE WORKER — claim the baton FIRST:** `mv HANDOFF_NEEDED HANDOFF_DONE_$(date -u +%Y%m%dT%H%M%SZ)_<note>`; confirm no `codex exec` proc + no other `claude` on this branch. **caffeinate PID 84474** (verify; re-`caffeinate -s -i` if dead). Benign Codex stderr `mode2-pilot/SKILL.md: invalid YAML` is NOT a build error.
- **Characterized FULL-suite baseline (ZERO-NEW-REDS gate): ~8413 passing**; the ONLY fails = `wpaRuntimeBoundary` (HARD) + `franchiseManualSmokeFixture` + `franchiseOffseasonGuards` (order-flakes — re-run SOLO). Read the vitest SUMMARY (failed-file LIST), NOT the RC.
- **L-SIM is HEALTHY now.** `season.config.ts` runs a **full clean 60g** (findings 0, `sameSeed: true`); `git diff` the `lsim-h2-baseline-checkpoint-*.json` after a run → byte-identical = dormancy/stability proof. Smoke 24g all-CRITICAL-pass. Falsification 44/44. **Always read the SUMMARY JSON (`stoppedEarly`/`criticalFindings`/`sameSeed`), NOT the vitest RC** (the scenario test asserts games>0, not zero-findings). The SMOKE(24g) does NOT reach game 36 — run the SEASON for late-season coverage. ⚠ New own-DBs that feed dumped state MUST be added to `test-utils/lsim/sandbox.ts` `DB_NAMES_TO_DELETE` (see the hop-2 `kbl-franchise-stadium-records` add `19b75104`) or they leak across determinism legs.

**A1.5d hop-2 (DONE + VERIFIED) — design recap:** §6 polarity sign-law fame swap, FOLDED into the single fame heat write (`franchiseFameCompute.persistDarkFameRecordsForCompletedGame` optional `stadiumChanges` param; `applyHonorHeatBump`, no decay; writes dethroned non-playing holders) so heat moves AND the EXISTING bridge moves morale from the combined per-player delta — **bridge untouched, no new sentinel** (the spec's literal §5.2 concat+distinct-sentinel is unimplementable without a bridge change; fold is the documented faithful resolution, adversarial-review-upheld). Detect runs EARLY in `processCompletedGame`; tap returns `changeList[]`; NEW pure `franchiseStadiumRecordFame.ts` (`buildStadiumRecordFameHeatBumps`). Optional doc nicety (deferred, non-blocking): back-annotate `STADIUM_ANALYTICS_SPEC_V2.md` §5.2/§6 with the fold deviation.

**Pre-existing trait-trip (FIXED `514ce366`):** `soul.trait-two-slot-no-offset-hysteresis` was checking the GLOBAL gain threshold (0.75) instead of the grant's PER-TIER threshold (`assignTier`: UNCOMMON 0.70 / MODERATE 0.65 / etc.) → false-flagged valid lower-tier gains, stopping the season at g36. Invariant now mirrors the grant's per-tier fallback (`traitOverlayThresholdsFor` in `test-utils/lsim/invariants/soul.ts`); a new falsification entry pins a genuine sub-tier trip. Test-invariant-only; the trait grant is correct + untouched.

**➡ hop-3 (fan morale, §7) — the seam + the constraints (ground from source first):** add a new `PARK_RECORD_SET` `MasterMoraleEventType` (an **EXHAUSTIVE-Record touch** — `masterMoraleMatrix.ts` `satisfies Record<MasterMoraleEventType>` + the `narrativeEngine.ts` hedging/highStakes exhaustiveness maps; mirror an existing fan-only milestone row like `PLAYER_MILESTONE`). Compose via `composeMoraleConsequence → applyFranchiseMoraleMatrixConsequence` for the stadium **home** team, gated `isFranchisePhase2MoraleEnabled`, `sourceEventId` dedup. **§6 DOUBLE-COUNT GUARD (make-or-break): the `PARK_RECORD_SET` row carries ONLY fan + teammate deltas, self-delta 0** — hop-2's fame heat path already moves the holder's self-pride morale via the bridge (mirror the §13 Albatross guard). Buzz only when the new holder is on the HOME team (a visitor still earns his own fame, no home buzz); no negative to the overtaken team's fans in v1. Consume hop-2's `stadiumChanges` (already surfaced early in `processCompletedGame`). Then hop-4 reporter (exhaustive-Record `STADIUM_RECORD` NarrativeEventType + PURE adapter, LLM emission browser-pending) → hop-5 Almanac → hop-6 rivalry edge → §8 display (USER-VISIBLE → flag for JK). **AFTER A1.5d the queue is JK-DECISION-BLOCKED** (re-grade store · RA-7). BROWSER/JK-PENDING: A1.4 L12-6 UI · L4b LLM emission · A1.3b fork-2 · A1.5d §8 + reporter LLM half · BV backlog.

DISPATCH MECHANICS (proven; watchdog; macOS has no `timeout`; `model_reasoning_effort` ∈ high|xhigh — NEVER very-high):
  cd /Users/johnkruse/Projects/kbl-tracker
  export PATH="$HOME/.nvm/versions/node/v20.20.0/bin:$PATH"
  rm -f /tmp/codex-<ID>.out; : > /tmp/codex-<ID>.stdout
  ( PREV=0; STALL=0; ELAPSED=0
    while [ $ELAPSED -lt 1800 ]; do
      sleep 120; ELAPSED=$((ELAPSED+120))
      pgrep -f "codex exec.*codex-<ID>.out" >/dev/null 2>&1 || exit 0
      CUR=$(wc -c < /tmp/codex-<ID>.stdout 2>/dev/null | tr -d ' '); CUR=${CUR:-0}
      CHANGED=$(git -C /Users/johnkruse/Projects/kbl-tracker diff --name-only -- src 2>/dev/null | wc -l | tr -d ' '); CHANGED=${CHANGED:-0}
      if [ "$CUR" -le "$PREV" ] && [ "$CHANGED" -eq 0 ] && [ ! -s /tmp/codex-<ID>.out ]; then STALL=$((STALL+1)); else STALL=0; fi
      PREV=$CUR
      if [ $STALL -ge 3 ]; then echo "WATCHDOG STALL kill at ${ELAPSED}s" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null; exit 0; fi
    done
    echo "WATCHDOG 30m cap kill" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null ) &
  WDOG=$!
  sed -n '/<!-- ===== CONTRACT: <ID> /,/<!-- ===== END CONTRACT: <ID> /p' /Users/johnkruse/Projects/kbl-tracker/spec-docs/PROMPT_CONTRACTS.md \
    | NODE_ENV= ~/.local/bin/codex exec -C /Users/johnkruse/Projects/kbl-tracker --skip-git-repo-check -s workspace-write -c model_reasoning_effort=high -o /tmp/codex-<ID>.out - >> /tmp/codex-<ID>.stdout 2>&1
  RC=$?; kill $WDOG 2>/dev/null; echo "codex rc=$RC"

session-done (2026-06-26, UNATTENDED AUTH-4 `/kbl-captain` — fresh session, then JK greenlit the bug fix): **1 feature SHIPPED + 2 pre-existing issues FIXED.** **A1.5d hop-2 `219bdb3a`** (records→fame swap centerpiece, fold design, fully verified). **L-SIM harness leak `19b75104`** (stadium DB reset). **Pre-existing trait-trip `514ce366`** (the invariant checked the global instead of the per-tier gain threshold → false-flagged valid lower-tier gains, stopping the season at g36; FIXED in the invariant; season now runs clean 60g + re-baked). The L-SIM season anchor is HEALTHY again. Clean seam — nothing in flight.

READ ON RESUME (in order, via Read on exact paths): `spec-docs/AUTONOMOUS_RUN_LOG.md` (newest 2 blocks = hop-2 shipped + the trait-trip fix) · `spec-docs/CURRENT_STATE.md` LIVE HEADER (top block) · `spec-docs/AUTONOMOUS_RUN_PROTOCOL.md` · `spec-docs/SESSION_RULES.md` · `spec-docs/STADIUM_ANALYTICS_SPEC_V2.md` §6-§9 (hop-3 fan morale + the double-count guard).
