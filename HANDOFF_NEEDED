next_ticket: **A1.5d hop-4 (reporter — `STADIUM_RECORD` narrative news for a park record).** A1.5d hop-2 (fame swap `219bdb3a`) + hop-3 (PARK_RECORD_SET fan buzz `6805ab74`) are DONE + FULLY VERIFIED; the pre-existing trait-trip is fixed (`514ce366`) and the L-SIM season is GREEN + re-baked + healthy. Continue the UNATTENDED AUTH-4 captain loop on `codex/franchise-v1-next` (MAIN). Proven loop: ground FRESH from source (a parallel grounding workflow keeps the heavy reads off main context) → Captain RE-verifies the make-or-break → write+commit contract → dispatch Codex (`run_in_background`+`dangerouslyDisableSandbox`, watchdog) → audit the REAL diff → (ultracode: a parallel adversarial design-review subagent before accepting the build) → Captain runs his OWN FULL suite + L-SIM (smoke+falsification+season) → commit branch-only BY PATH → log.

branch/topology: **MAIN** `/Users/johnkruse/Projects/kbl-tracker` on `codex/franchise-v1-next` (HEAD = the hop-3-doc commit `976b1911` or later). **trackerDb v26** (unchanged). A1.5d uses own db `kbl-franchise-stadium-records` → NO trackerDb bump. ⚠ **PAUSED — DO NOT TOUCH/MERGE (JK):** `codex/draft-pipeline-fix` + `codex/auction-draft-ux-rehaul`.

resume_note: **SOLE WORKER — claim the baton FIRST** (`mv HANDOFF_NEEDED HANDOFF_DONE_$(date -u +%Y%m%dT%H%M%SZ)_<note>`; confirm no `codex exec` + no other `claude` on this branch). **caffeinate PID 84474** (verify; re-`caffeinate -s -i` if dead). Benign Codex stderr `mode2-pilot/SKILL.md: invalid YAML` is NOT a build error.
- **FULL-suite baseline (ZERO-NEW-REDS): ~8419 passing**; ONLY fails = `wpaRuntimeBoundary` (HARD) + `franchiseManualSmokeFixture` + `franchiseOffseasonGuards` (order-flakes, re-run SOLO). Read the vitest SUMMARY (failed-file LIST), NOT the RC.
- **L-SIM is HEALTHY.** `season.config.ts` runs a full clean 60g (findings 0, sameSeed true); smoke 24g all-CRITICAL-pass; falsification 44/44. **Always read the SUMMARY JSON (`stoppedEarly`/`criticalFindings`/`sameSeed`), NOT the RC.** The SMOKE(24g) does NOT reach game 36 — run the SEASON for late-season coverage. ⚠ Any new own-DB feeding dumped state MUST be added to `test-utils/lsim/sandbox.ts` `DB_NAMES_TO_DELETE`. ⚠ A flags-ON change that touches a DUMPED store (kbl-tracker / kbl-franchise-morale) RE-BAKES the season anchor — run the season, verify the delta is sane (no new critical findings, morale-bounds hold), then commit the regenerated `lsim-h2-baseline-checkpoint-*.json`.

**DETECT→living-season chain status:** hop-1 (DETECT, `changes[]`) ✅ · hop-2 (FAME swap, folds polarity heat into the fame write) ✅ · hop-3 (FAN buzz, `PARK_RECORD_SET` self-delta-0 row, home-team-only) ✅. The morale double-count guard is SETTLED: `resolveFameTap` (`masterMoraleMatrix.ts:439`) zeros team-fan, so the fame bridge moves SELF/player morale ONLY — PARK_RECORD_SET is the sole home-fan path. (Any note claiming the fame bridge "moves both player and fan morale" is STALE — it's player-only.)

**➡ hop-4 (reporter, §5.4) — ground from source first:** add a new `STADIUM_RECORD` `NarrativeEventType` + a PURE deterministic news adapter (clone the L12 award adapter / `franchiseRelationshipFlareEmission` pattern) building a `SeasonNewsEvent` from a stadium record change (facts = recordType, stadiumName, newValue, oldValue, overtakenHolderId, batterHand, playContext for swing records). **This IS the narrativeEngine EXHAUSTIVE touch** (`narrativeEngine.ts:77` NarrativeEventType union + the `:592` `hedgingModifier` Record + the `:640` `highStakesEvents` map — all keyed by NarrativeEventType, distinct from hop-3's MasterMoraleEventType). Per [[reporter-adapter-pure-emission-llm-split]]: **BUILD the PURE adapter (build-dark, unit-testable, "the matrix is the math"); FLAG the LLM emission seam** (`generateSeasonNewsTake`→`callClaudeMessages`, the `processCompletedGame` wiring, the flag) for JK browser/LLM sign-off — do NOT build the live emission. Reuse an existing `NarrativeEventType` ONLY if it avoids the exhaustive touch without losing fidelity; else add `STADIUM_RECORD` + every exhaustive map entry. Dedup on (stadiumId, recordType, recordKey, newValue). Then hop-5 Almanac → hop-6 rivalry edge (+ the §7 home-park-RIVALRY 2× deferred slice) → §8 display (USER-VISIBLE → flag for JK). **AFTER A1.5d the queue is JK-DECISION-BLOCKED** (re-grade store · RA-7). BROWSER/JK-PENDING: A1.4 L12-6 UI · L4b LLM emission · A1.3b fork-2 · A1.5d §8 + reporter LLM half · BV backlog.

DISPATCH MECHANICS (proven; watchdog; macOS has no `timeout`; `model_reasoning_effort` ∈ high|xhigh — NEVER very-high):
  cd /Users/johnkruse/Projects/kbl-tracker
  export PATH="$HOME/.nvm/versions/node/v20.20.0/bin:$PATH"
  rm -f /tmp/codex-<ID>.out; : > /tmp/codex-<ID>.stdout
  ( PREV=0; STALL=0; ELAPSED=0
    while [ $ELAPSED -lt 1800 ]; do
      sleep 120; ELAPSED=$((ELAPSED+120))
      pgrep -f "codex exec.*codex-<ID>.out" >/dev/null 2>&1 || exit 0
      CUR=$(wc -c < /tmp/codex-<ID>.stdout 2>/dev/null | tr -d ' '); CUR=${CUR:-0}
      CHANGED=$(git -C /Users/johnkruse/Projects/kbl-tracker diff --name-only -- src 2>/dev/null | wc -l | tr -d ' '); CHANGED=${CHANGED:-0}
      if [ "$CUR" -le "$PREV" ] && [ "$CHANGED" -eq 0 ] && [ ! -s /tmp/codex-<ID>.out ]; then STALL=$((STALL+1)); else STALL=0; fi
      PREV=$CUR
      if [ $STALL -ge 3 ]; then echo "WATCHDOG STALL kill at ${ELAPSED}s" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null; exit 0; fi
    done
    echo "WATCHDOG 30m cap kill" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null ) &
  WDOG=$!
  sed -n '/<!-- ===== CONTRACT: <ID> /,/<!-- ===== END CONTRACT: <ID> /p' /Users/johnkruse/Projects/kbl-tracker/spec-docs/PROMPT_CONTRACTS.md \
    | NODE_ENV= ~/.local/bin/codex exec -C /Users/johnkruse/Projects/kbl-tracker --skip-git-repo-check -s workspace-write -c model_reasoning_effort=high -o /tmp/codex-<ID>.out - >> /tmp/codex-<ID>.stdout 2>&1
  RC=$?; kill $WDOG 2>/dev/null; echo "codex rc=$RC"

session-done (2026-06-26, UNATTENDED AUTH-4 then JK-attended): **2 features SHIPPED + 2 pre-existing fixes + hop-3.** A1.5d hop-2 `219bdb3a` (fame swap) · L-SIM harness leak `19b75104` · pre-existing trait-trip `514ce366` (invariant used global instead of per-tier gain threshold) · A1.5d hop-3 `6805ab74` (PARK_RECORD_SET fan buzz). All branch-only / Codex-built or Captain-fixed / adversarially-reviewed / FULL ZERO-NEW-REDS / L-SIM season clean 60g + re-baked. Clean seam — nothing in flight.

READ ON RESUME (in order): `spec-docs/AUTONOMOUS_RUN_LOG.md` (newest blocks = hop-3 + the trait-fix + hop-2) · `spec-docs/CURRENT_STATE.md` LIVE HEADER · `spec-docs/AUTONOMOUS_RUN_PROTOCOL.md` · `spec-docs/SESSION_RULES.md` · `spec-docs/STADIUM_ANALYTICS_SPEC_V2.md` §5.4 (reporter) + §10 (the narrativeEngine wiring map).
