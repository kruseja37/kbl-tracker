next_ticket: **#1 Archetype→capIdentity converter (LANE B / Stream B — the foundational draft unblocker)** — primary; OR **#18 Winner-honors resolver (LANE A — same season-finalize domain just worked)** if the LANE-B worktree is unclear or you prefer LANE-A continuity. AUTH-4 ultra-think autonomous Captain loop (Codex builds, Opus audits the REAL diff + gates + commits branch-only + logs). The full roadmap is WRITTEN — do NOT re-audit.

═══ READ FIRST (the roadmap is written) ═══
- `spec-docs/V1_PRE_FREEZE_CHECKLIST.md` — THE 38-ticket queue (Tiers 0-6) + "RECOMMENDED NEXT 3" block. P2/P3 (#22/#23/#36) was item 1 — see STATE below; the remaining recommended next-3 are #1 then #18.
- `spec-docs/AUTONOMOUS_RUN_LOG.md` — the live ledger; the newest block = PLAYOFF-DRIVER-2 SHIPPED with the full grounding + the OPEN-DECISIONS.
- `spec-docs/V1_STATUS_AND_ASSEMBLY_PLAN.md` `## JK RULINGS — 2026-06-27` (winner-honors rarity-scaled + magnitude-tuned; the FREEZE GATE).
- For LANE B: `spec-docs/STREAM_B_DRAFT_UIUX_BUILD_PLAN.md` (the draft-process UI/UX plan; #1 is its starting brick). Memory [[stream-b-draft-is-next]] / [[v1-assembly-corrected-picture]] / [[gm-name-and-team-archetype-wiring]].

═══ STATE (all durable) ═══
Trunk = `experiment/manager-wpa-window` (Captain docs + Mode-2 L-stack). **PLAYOFF-DRIVER-2 SHIPPED `303c799a`** on lane `claude/v1-playoff-driver` (worktree `/Users/johnkruse/Projects/kbl-playoff`, own APFS node_modules). PLAYOFF-DRIVER **P2 (#22 L-SIM wiring) + #36 clean-end DONE**; **P3 (playoff fame-amp + champion-MVP #23) DEFERRED** — the L-SIM postseason produces NO per-player playoff stats by design (deterministic seeded-score `playGame`, no `processCompletedGame` for playoff games), so champion-MVP is NOT "easy" → JK OPEN-DECISION (revisit: MVP from frozen regular-season stats scoped to the champion roster, OR a postseason stats-fidelity ticket). **The playoff lane is NOT merged to trunk** — when the whole playoff lane is at a final clean seam, merge like the soul lane (read-only merge-tree pre-assess → resolve any PROMPT_CONTRACTS append-union → build+suite+L-SIM gate → push) — but the **merge+push needs JK confirm** (do NOT push unattended; [[kbl-safe-lane-merge-technique]]).

═══ #1 ARCHETYPE→capIDENTITY CONVERTER (primary — LANE B) ═══
Checklist #1 (NOT-BUILT, M, "the intricate one — silent vocab-mismatch trap"; no JK gate; pure build-dark + unit test; gates the setup spine + archetype picker + Draft Setup hub). GOAL: `selectTeamArchetype`/`archetypeToIdentity` + an ArchetypeStat→ModStat→`CAP_MODIFICATION_FRACTIONS` vocab bridge + unit test; add `mlbArchetypeKey`/`farmArchetypeKey` to `Team`. **GROUND from source FIRST** (the vocab bridge is the make-or-break — the named SMB4 presets live in `CAP_MODIFICATION_FRACTIONS`; the archetype stat vocab must map exactly or it silently mis-applies). Identify the correct LANE-B worktree/branch before contracting — candidates: a draft worktree (`kbl-draftlane`/`claude/v1-draft-ui`, `kbl-draft-ui`/`codex/draft-setup-ui`) or `kbl-lineups-fenway`/`claude/lineups-fenway-hub` per [[stream-b-draft-is-next]]; confirm which is Stream B's live lane (`git worktree list` + the build plan) — STOP-IF the lane is ambiguous, pick the one the build plan names.

═══ #18 WINNER-HONORS (alternative — LANE A, same domain just worked) ═══
Checklist #18 (NOT-BUILT, M; JK ruled 2026-06-27 — ALL award winners honored, scaled by rarity, supersedes snub-only). Extend `franchiseSeasonEndHonors.ts` + `FranchiseHonorTier` to the Gold Glove / Silver Slugger / Booger / Reliever / Rookie winner-side with a TUNABLE PLACEHOLDER magnitude. **OPEN-DECISION (flag, don't block): the exact rarity metric** (ADDENDUM resolved it to per-award PRESTIGE value, additive across awards — build the mechanism, placeholder the prestige numbers, flag for §16 tuning). Soul-layer no-inference: the measurement comes from the spec/ruling verbatim; placeholder + flag where silent. This is the same `runSeasonFinalize` → `emitFranchiseSeasonEndHonors` domain the just-shipped PLAYOFF-DRIVER-2 finalize path uses, so it has L-SIM coverage already.

═══ DISPATCH (proven; macOS no `timeout`) ═══
Append the contract to the LANE's `spec-docs/PROMPT_CONTRACTS.md` wrapped in `<!-- ===== CONTRACT: <ID> ===== -->` markers, then:
`sed -n '/CONTRACT: <ID> /,/END CONTRACT: <ID> /p' <lane>/spec-docs/PROMPT_CONTRACTS.md | NODE_ENV= ~/.local/bin/codex exec -C <lane-abs> --skip-git-repo-check -s workspace-write -c model_reasoning_effort=high -o /tmp/codex-<id>.out -`
via Bash `run_in_background: true` + `dangerouslyDisableSandbox: true`. `very-high` = 400 (INVALID; use `high`/`xhigh`). The session MUST be on xhigh/ultra-think or long bg dispatches get KILLED ([[long-background-dispatch-needs-ultra-think]]). Captain runs the L-SIM (Codex stalls on it). Commit branch-only BY PATH, NEVER push; trailer `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

═══ GATE ═══
Build 0 · the affected test green · **FULL suite (read the failed-FILE list, ZERO-NEW-REDS)** — characterized reds: `wpaRuntimeBoundary` (HARD) + `historicalArchetypes` (load flake) + `franchiseManualSmokeFixture` (order-flake, solo-passes); confirm any "new" red passes solo before treating it as real · for anything touching the Mode-2 finalize/soul path also run the **L-SIM season** (Captain runs; read the SUMMARY JSON not the RC; expect `criticalFindings:0`, `sameSeedByteIdentical:true`; baselines git-diff EMPTY unless a dumped store intentionally changed).

═══ TWO-LANE OPTION (JK approved) ═══
LANE A = living season (winner-honors #18, the L-SIM harness, Tiers 4/6). LANE B = draft UI / Stream B (#1 archetype converter FIRST, then assembly merges #26-28, then screen wiring #11-17). Verify disjoint file surfaces first; SERIALIZE heavy vitest/L-SIM gates; contracts on each lane's PROMPT_CONTRACTS.md.
