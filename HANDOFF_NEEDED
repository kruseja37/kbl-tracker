next_ticket: **A1.5d hop-2 FAME SWAP (§6) — the first `changes[]` CONSUMER + the records→living-season centerpiece.** The DETECT chain is COMPLETE through hop-1c (hop-1a `86ffea39` changes[] + hop-1b `8ae5e683` per-game tap+flag + hop-1c `a97b50b8` §4 fame-bearing catalog). §1 (the fame-fluctuation prerequisite) is **CONFIRMED ALREADY-MET** (no build needed — see below). Continue the UNATTENDED AUTH-4 captain loop on `codex/franchise-v1-next` (MAIN). Proven loop: ground FRESH from source → Captain RE-verifies the make-or-break → write+commit contract → dispatch Codex (`run_in_background`+`dangerouslyDisableSandbox`, watchdog) → audit the REAL diff → (ultracode: a parallel adversarial-audit subagent earns its keep — it caught 2 real hop-1c defects the green tests missed) → Opus runs his OWN FULL suite + L-SIM → commit branch-only BY PATH → log.

branch/topology: **MAIN** `/Users/johnkruse/Projects/kbl-tracker` on `codex/franchise-v1-next` (HEAD = `64b24c38`). **trackerDb v26** (unchanged). A1.5d uses its own db `kbl-franchise-stadium-records` → NO trackerDb bump. ⚠ **PAUSED — DO NOT TOUCH/MERGE (JK):** `codex/draft-pipeline-fix` + `codex/auction-draft-ux-rehaul`.

resume_note: **SOLE WORKER — claim the baton FIRST:** `mv HANDOFF_NEEDED HANDOFF_DONE_$(date -u +%Y%m%dT%H%M%SZ)_<note>`; confirm no `codex exec` proc + no other `claude` on this branch. **Characterized baseline (ZERO-NEW-REDS gate): FULL suite ~8406 passing**; the ONLY fails = `wpaRuntimeBoundary` (HARD, solo-fails too) + `franchiseManualSmokeFixture` (order-flake, solo-passes) + `franchiseOffseasonGuards` (order-flake — solo-passes 24/24, confirmed this session; it surfaces in some full-suite runs only). **Read the vitest SUMMARY (failed-file LIST), NOT the RC.** ⚠ **TRANSITIVE-MOCK-BREAK is LIVE if hop-2 wires a NEW module into `processCompletedGame`** (already-imported modules are safe): a new import that pulls `gameStorage`/`franchiseManager`/etc. breaks the 3 partial-mock test files at module-LOAD — fix = add the missing export as a `vi.fn()` stub. ⚠ Do NOT flip any Phase-2 flag. ⚠ DOC-worker keeps `spec-docs/FRANCHISE_MODE1_MODE2_PLAYABLE_V1_GAP_ANALYSIS.md` (M) dirty — NEVER stage it; commit BY PATH; leave untracked junk alone. **caffeinate PID 84474** (verify; re-`caffeinate -s -i` if dead). The benign Codex stderr `failed to load skill .../mode2-pilot/SKILL.md: invalid YAML` is NOT a build error.

**✅ §1 (fame-fluctuation prereq) = ALREADY DONE — do NOT rebuild it.** The spec §1 "remove the per-game reachFloor ratchet" already landed in the L6 fame work: `franchiseFameCompute.ts:108` is `const reachFloor = stored.reachFloor;` (pass-through, NO `updateReachFloor` ratchet — that fn is test-only); heat fluctuates via `applyHeatUpdate(stored.heat, breakdown.total)`; the honor floor pins to REGIONAL_STAR (`franchiseHonorReachFloor.ts:34`); `resolveFameTier` floors by `Math.max(heatRank, reachFloor)` so a non-honored player (reachFloor 0) CAN fall = §1's intent. ⇒ hop-2 is UNBLOCKED. (Optional verification-only nicety: an explicit L-SIM `soul.*` invariant that a non-honored tier can fall.)

**➡ hop-2 ARCHITECTURE (Captain-grounded this session; ground the 2 remaining bits before contracting — full detail in the newest AUTONOMOUS_RUN_LOG.md block):**
- **Goal (§6 sign-law):** per stadium-record change, `s = FRANCHISE_STADIUM_RECORD_TYPE_POLARITY[recordType]` (hop-1c exported this map in `franchiseStadiumRecordsStorage.ts`); base `B` (§16: B_set 2.0 / B_break 1.5 / B_overtaken 1.0; farthest-HR + single-play-swing weighted higher). SET → newHolderΔ=+B_set·s. OVERTAKE → newHolderΔ=+B_break·s, each priorLeaderPlayerId Δ=−B_overtaken·s.
- **The bridge (verified):** `persistFameMoraleConsequencesAfterFame` (`processCompletedGame.ts:520`) iterates `playerHeatDeltas:{playerId,heatDelta}[]` (skip 0) → composes `FAME_HEAT_CHANGED` (player+fan morale), dedup'd by `fame:…:checkpoint:playerId:heat-delta` (`:503`). Called at `:1168-1180` inside `if(isFranchisePhase2FameEnabled())` after `fameResult = persistDarkFameRecordsForCompletedGame(...)`. §5.2 = concat the polarity heatDeltas onto `fameResult.playerHeatDeltas`.
- **4 THINGS TO RESOLVE (2 already mapped, 2 need more grounding):**
  1. **Ordering/restructure (mapped):** the hop-1b stadium tap runs LATER (after L12, ~`:1224+`) than the fame block AND returns only `changes:<count>`. hop-2 needs the actual `FranchiseStadiumRecordChange[]` at/before the fame block → surface the DETECT changes[] earlier + have the tap return the ARRAY. Decide restructure-early (right for hops 3–6) vs compute-inside-fame-block (double-compute, build-dark, no reorder).
  2. **HEAT vs MORALE split (GROUND FIRST):** the bridge moves MORALE only; stored fame HEAT is written by `persistDarkFameRecordsForCompletedGame` (`franchiseFameCompute.ts:107-126`). §6 says the swap moves BOTH heat AND morale — confirm whether concat'ing playerHeatDeltas moves stored heat or the bump must also feed the heat write (e.g. into `breakdown.total`). Read `persistDarkFameRecordsForCompletedGame` end-to-end.
  3. **Sentinel collision (GROUND):** the bridge dedups by `fame:…:playerId:heat-delta`; a same-player stadium bump collides. §5.2 mandates a DISTINCT `park-record:{stadiumId}:{recordType}:{recordKey}:{checkpoint}:{holderId}` sentinel → stadium bumps need their own source-event path (or merge same-player deltas pre-bridge). Ground how composeMoraleConsequence/apply keys the sentinel.
  4. **Double-count guard (§6):** holder PRIDE lives ONLY in the fame heat path; hop-3's `PARK_RECORD_SET` morale row must be fan+teammate only (self-delta 0) — note now so hops 2/3 don't double-count self-morale.
- **Gate:** `isFranchisePhase2FameEnabled` (+ StadiumRecords flag for the detect); build-dark at default flags. **FULL suite** (chokepoint) + **L-SIM** (smoke 24g + season 60g, run the STANDARD 60g leg LAST; `git diff` the `lsim-h2-baseline-*.json` → byte-identical = dark/dormant proof).

**🟡 AFTER A1.5d the queue is JK-DECISION-BLOCKED (do NOT build unattended):** re-grade-per-checkpoint STORE · RA-7 park-adjust. **BROWSER/JK-PENDING backlog:** A1.4 L12-6 UI · L4b live LLM emission+wiring+flag · A1.3b fork-2 · A1.5d §8 display + the reporter LLM half (hop-4) · BV backlog (BV-V8, BV-CAP-MISS, A1.5b-2, T-6b).

DISPATCH MECHANICS (proven; watchdog; macOS has no `timeout`; `model_reasoning_effort` ∈ high|xhigh — NEVER very-high):
  cd /Users/johnkruse/Projects/kbl-tracker
  export PATH="$HOME/.nvm/versions/node/v20.20.0/bin:$PATH"
  rm -f /tmp/codex-<ID>.out; : > /tmp/codex-<ID>.stdout
  ( PREV=0; STALL=0; ELAPSED=0
    while [ $ELAPSED -lt 1800 ]; do
      sleep 120; ELAPSED=$((ELAPSED+120))
      pgrep -f "codex exec.*codex-<ID>.out" >/dev/null 2>&1 || exit 0
      CUR=$(wc -c < /tmp/codex-<ID>.stdout 2>/dev/null | tr -d ' '); CUR=${CUR:-0}
      CHANGED=$(git -C /Users/johnkruse/Projects/kbl-tracker diff --name-only -- src 2>/dev/null | wc -l | tr -d ' '); CHANGED=${CHANGED:-0}
      if [ "$CUR" -le "$PREV" ] && [ "$CHANGED" -eq 0 ] && [ ! -s /tmp/codex-<ID>.out ]; then STALL=$((STALL+1)); else STALL=0; fi
      PREV=$CUR
      if [ $STALL -ge 3 ]; then echo "WATCHDOG STALL kill at ${ELAPSED}s" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null; exit 0; fi
    done
    echo "WATCHDOG 30m cap kill" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null ) &
  WDOG=$!
  sed -n '/<!-- ===== CONTRACT: <ID> /,/<!-- ===== END CONTRACT: <ID> /p' /Users/johnkruse/Projects/kbl-tracker/spec-docs/PROMPT_CONTRACTS.md \
    | NODE_ENV= ~/.local/bin/codex exec -C /Users/johnkruse/Projects/kbl-tracker --skip-git-repo-check -s workspace-write -c model_reasoning_effort=high -o /tmp/codex-<ID>.out - >> /tmp/codex-<ID>.stdout 2>&1
  RC=$?; kill $WDOG 2>/dev/null; echo "codex rc=$RC"
  # Launch-sanity (~35s): codex ALIVE + no real 400/401/quota. Captain audits the REAL diff + runs his OWN full suite + L-SIM.

session-done (2026-06-26, UNATTENDED AUTH-4 `/kbl-captain` — fresh session): **1 feature SHIPPED + 1 prereq resolved-without-build.** **A1.5d hop-1c `a97b50b8`** (§4 fame-bearing record catalog — 10 record types + polarity map, computed DIRECTLY from inline `completedGames` atBatEvents+playerWpaTotals, NO foundation/spray/getGameEvents change; an ultracode parallel adversarial-audit caught 2 DETECT-correctness defects → fixed via Codex fix-iter #1 [WPA-eligibility gate + swing-tie co-leaders]; branch-only / Opus-audited-REAL-diff / Opus-ran-INDEPENDENT-FULL-suite / ZERO-NEW-REDS 8406-pass / NO DB change). **§1 fame-fluctuation prereq = CONFIRMED ALREADY-MET** (no build). Handed off at the clean post-hop-1c seam (nothing in flight) because hop-2 is the architecturally-involved fame-swap centerpiece (chokepoint restructure) warranting fresh context — full hop-2 grounding captured above + in the run log.

READ ON RESUME (in order, via Read on exact paths): `spec-docs/AUTONOMOUS_RUN_LOG.md` (newest block = hop-1c + the §1-resolved + hop-2-architecture grounding) · `spec-docs/CURRENT_STATE.md` LIVE HEADER (top block) · `spec-docs/AUTONOMOUS_RUN_PROTOCOL.md` · `spec-docs/SESSION_RULES.md` · `spec-docs/STADIUM_ANALYTICS_SPEC_V2.md` §5-§7 (the hop-2 fame-swap + the hops 3-6 it feeds).
