next_ticket: **⚠ FIRST — a JK/trait-system decision on a PRE-EXISTING L-SIM SEASON BREAK (CRITICAL), THEN A1.5d hop-3 (fan morale).** A1.5d hop-2 FAME SWAP is DONE (`219bdb3a`) — the records→fame living-season centerpiece shipped. While gating it I discovered the canonical L-SIM **season is RED at game 36** on `soul.trait-two-slot-no-offset-hysteresis` (PROVEN pre-existing — NOT hop-2; see below). This blocks a clean 60g season re-bake for ALL further soul-layer work, so it should be resolved (or JK-acknowledged) before piling on hops 3-6. Continue the UNATTENDED AUTH-4 captain loop on `codex/franchise-v1-next` (MAIN). Proven loop: ground FRESH from source → Captain RE-verifies the make-or-break → write+commit contract → dispatch Codex (`run_in_background`+`dangerouslyDisableSandbox`, watchdog) → audit the REAL diff → (ultracode: a parallel adversarial-audit subagent earns its keep) → Opus runs his OWN FULL suite + L-SIM → commit branch-only BY PATH → log.

branch/topology: **MAIN** `/Users/johnkruse/Projects/kbl-tracker` on `codex/franchise-v1-next` (HEAD = `1c194b2d`). **trackerDb v26** (unchanged). A1.5d uses its own db `kbl-franchise-stadium-records` → NO trackerDb bump. ⚠ **PAUSED — DO NOT TOUCH/MERGE (JK):** `codex/draft-pipeline-fix` + `codex/auction-draft-ux-rehaul`.

resume_note: **SOLE WORKER — claim the baton FIRST:** `mv HANDOFF_NEEDED HANDOFF_DONE_$(date -u +%Y%m%dT%H%M%SZ)_<note>`; confirm no `codex exec` proc + no other `claude` on this branch. **caffeinate PID 84474** (verify; re-`caffeinate -s -i` if dead). The benign Codex stderr `failed to load skill .../mode2-pilot/SKILL.md: invalid YAML` is NOT a build error.
- **Characterized FULL-suite baseline (ZERO-NEW-REDS gate): ~8413 passing**; the ONLY fails = `wpaRuntimeBoundary` (HARD, solo-fails too) + `franchiseManualSmokeFixture` + `franchiseOffseasonGuards` (order-flakes — re-run SOLO to confirm). **Read the vitest SUMMARY (failed-file LIST), NOT the RC.**
- **⚠ NEW characterized fact: the L-SIM SEASON is PRE-EXISTING-RED.** `NODE_ENV= npx vitest run -c test-utils/lsim/season.config.ts` STOPS the baseline leg at **game 36** with 1 CRITICAL: `soul.trait-two-slot-no-offset-hysteresis` (5 trait GAINS persisted pending with stored probability 0.717–0.746 < the 0.75 gain threshold). The scenario test still PASSES (asserts games>0, not zero-findings) so READ THE SUMMARY JSON (`stoppedEarly`/`criticalFindings`), not the RC. The **SMOKE (24g)** runs clean (the trip is at game 36, beyond 24g) — so smoke alone does NOT catch it. The committed `lsim-h2-baseline-checkpoint-040/050/060.json` are STALE (Jun 23) — leave them; a clean re-bake is impossible until the trip is fixed.

**🔴 THE PRE-EXISTING CRITICAL (resolve or JK-rule FIRST):** Proven NOT hop-2 — a **pristine-HEAD** season run (hop-2 + harness-fix both reverted) trips at game 36 identically (digest `5763890:53248585`, `sameSeed:true`). So current HEAD (≤ hop-1c `a97b50b8`) already trips it; some post-Jun-23 soul-layer commit introduced it. **Likely root for the trait investigation:** the trait-grant GATE (`src/engines/traitAcquisition.ts:420`, `proposalBase.probability >= gainThreshold`) and the STORED `proposal.probability` DIVERGE — a gain gated at ≥0.75 is stored at <0.75, most likely via the T-3b trend-tilt recent-window recompute (`buildProposalBase` `trendTilt` factor) or `reconcileGainProposals` adjusting probability AFTER the gate. EITHER the grant should not persist a gain whose final stored probability < gainThreshold (a real bug), OR `soul.trait-two-slot-no-offset-hysteresis`'s `gain && probability < gainThreshold` assertion is checking a post-trend probability the grant intentionally stores (stale invariant). **Plan:** bisect the introducing commit (run the season at suspect commits, watch `stoppedEarly`/`criticalFindings`); reconcile the gate vs the stored probability OR ratify/adjust the invariant. **Do NOT weaken the CRITICAL invariant or touch the trait grant unattended without JK** — it's a soul-layer measurement call. To re-confirm the trip cheaply: `NODE_ENV= npx vitest run -c test-utils/lsim/season.config.ts` then `grep -oE '"baseline":\{[^}]*\}' <log>`.

**A1.5d hop-2 (DONE, `219bdb3a`) — what shipped + the design:** §6 polarity sign-law fame swap. Per record, `s = FRANCHISE_STADIUM_RECORD_TYPE_POLARITY[recordType]`; new sole holder `+B_set`(set)/`+B_break`(overtake)·s·weight, each dethroned prior holder `−B_overtaken`·s·weight (iconic farthest-HR/swing weight 1.5; §16 dials). **FOLDED into the single fame heat write** (`franchiseFameCompute.persistDarkFameRecordsForCompletedGame` gained an optional `stadiumChanges` param; `applyHonorHeatBump`, no decay; writes dethroned non-playing holders) so heat moves AND the EXISTING bridge moves morale from the combined per-player delta — **bridge untouched, no new sentinel** (folding keeps the per-player `fame:` sentinel unique; idempotency = DETECT emits empty `changes[]` on re-run). The detect now runs EARLY in `processCompletedGame` (before the fame block) and feeds the fame writer; the late duplicate detect was removed; the tap returns `changeList[]`. NEW pure `franchiseStadiumRecordFame.ts` (`buildStadiumRecordFameHeatBumps`, unit-tested). The spec's literal §5.2 (concat + distinct `park-record:` sentinel, bridge untouched) was found **internally unimplementable** → fold is the documented faithful resolution (adversarial-review-upheld); back-annotate `STADIUM_ANALYTICS_SPEC_V2.md` §5.2/§6 to record this when convenient (deferred — non-blocking).

**🔧 Harness fix (DONE, `19b75104`):** added `kbl-franchise-stadium-records` to `test-utils/lsim/sandbox.ts` `DB_NAMES_TO_DELETE` (it leaked across determinism legs; broke hop-2's same-seed determinism). Pristine HEAD is unaffected.

**➡ NEXT after the CRITICAL is handled:** A1.5d hop-3 (fan morale — a new `PARK_RECORD_SET` MasterMoraleEventType = an EXHAUSTIVE-Record touch in `masterMoraleMatrix.ts` + `narrativeEngine` maps; §6 double-count guard: the `PARK_RECORD_SET` morale row must carry ONLY fan+teammate deltas, self-delta 0, since hop-2's fame heat path already moves self-pride morale) → hop-4 reporter (exhaustive-Record `STADIUM_RECORD` NarrativeEventType + PURE adapter, LLM emission browser-pending) → hop-5 Almanac → hop-6 rivalry edge → §8 display (USER-VISIBLE → flag for JK). hop-3+ can build on the same cadence (full-suite + smoke 24g + determinism-seed 60g) but the canonical season stays RED until the trait trip is fixed. **AFTER A1.5d the queue is JK-DECISION-BLOCKED** (re-grade-per-checkpoint STORE · RA-7 park-adjust). **BROWSER/JK-PENDING backlog:** A1.4 L12-6 UI · L4b live LLM emission · A1.3b fork-2 · A1.5d §8 display + the reporter LLM half · BV backlog (BV-V8, BV-CAP-MISS, A1.5b-2, T-6b).

DISPATCH MECHANICS (proven; watchdog; macOS has no `timeout`; `model_reasoning_effort` ∈ high|xhigh — NEVER very-high):
  cd /Users/johnkruse/Projects/kbl-tracker
  export PATH="$HOME/.nvm/versions/node/v20.20.0/bin:$PATH"
  rm -f /tmp/codex-<ID>.out; : > /tmp/codex-<ID>.stdout
  ( PREV=0; STALL=0; ELAPSED=0
    while [ $ELAPSED -lt 1800 ]; do
      sleep 120; ELAPSED=$((ELAPSED+120))
      pgrep -f "codex exec.*codex-<ID>.out" >/dev/null 2>&1 || exit 0
      CUR=$(wc -c < /tmp/codex-<ID>.stdout 2>/dev/null | tr -d ' '); CUR=${CUR:-0}
      CHANGED=$(git -C /Users/johnkruse/Projects/kbl-tracker diff --name-only -- src 2>/dev/null | wc -l | tr -d ' '); CHANGED=${CHANGED:-0}
      if [ "$CUR" -le "$PREV" ] && [ "$CHANGED" -eq 0 ] && [ ! -s /tmp/codex-<ID>.out ]; then STALL=$((STALL+1)); else STALL=0; fi
      PREV=$CUR
      if [ $STALL -ge 3 ]; then echo "WATCHDOG STALL kill at ${ELAPSED}s" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null; exit 0; fi
    done
    echo "WATCHDOG 30m cap kill" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null ) &
  WDOG=$!
  sed -n '/<!-- ===== CONTRACT: <ID> /,/<!-- ===== END CONTRACT: <ID> /p' /Users/johnkruse/Projects/kbl-tracker/spec-docs/PROMPT_CONTRACTS.md \
    | NODE_ENV= ~/.local/bin/codex exec -C /Users/johnkruse/Projects/kbl-tracker --skip-git-repo-check -s workspace-write -c model_reasoning_effort=high -o /tmp/codex-<ID>.out - >> /tmp/codex-<ID>.stdout 2>&1
  RC=$?; kill $WDOG 2>/dev/null; echo "codex rc=$RC"

session-done (2026-06-26, UNATTENDED AUTH-4 `/kbl-captain` — fresh session): **1 feature SHIPPED + 1 pre-existing harness leak fixed + 1 PRE-EXISTING CRITICAL discovered.** **A1.5d hop-2 `219bdb3a`** (the records→fame swap centerpiece — fold design, adversarial-review-upheld; build 0 / FULL ZERO-NEW-REDS 8413 / fame-morale-idempotency-double-count invariants pass through g36 / determinism-seed 60g clean same-seed; no DB change). **L-SIM harness leak fixed `19b75104`.** **Discovered (rigorously isolated via a pristine-HEAD run): the L-SIM season is PRE-EXISTING-RED at game 36 on `soul.trait-two-slot-no-offset-hysteresis` (5 sub-threshold trait gains) + the committed season baselines are stale — NOT hop-2.** Handed off at a clean seam (hop-2 committed, nothing in flight) to surface the pre-existing CRITICAL for a JK/trait-system decision before continuing hops 3-6 on a broken regression anchor.

READ ON RESUME (in order, via Read on exact paths): `spec-docs/AUTONOMOUS_RUN_LOG.md` (newest block = hop-2 SHIPPED + the pre-existing CRITICAL OPEN-DECISION) · `spec-docs/CURRENT_STATE.md` LIVE HEADER (top block) · `spec-docs/AUTONOMOUS_RUN_PROTOCOL.md` · `spec-docs/SESSION_RULES.md` · `spec-docs/STADIUM_ANALYTICS_SPEC_V2.md` §6-§9 (hop-3+ + the double-count guard).
