next_ticket: **A1.5d — STADIUM RECORDS (build hop-by-hop; START with hop-1 DETECT).** Continue the UNATTENDED AUTH-4 captain loop. A1.5d is the LAST buildable queue item; after it the queue is JK-DECISION-BLOCKED (re-grade store + RA-7 park-adjust OPEN-DECISIONS — do NOT build those unattended). Work all on `codex/franchise-v1-next` (MAIN). The proven loop: ground FRESH from source → Captain RE-verifies the make-or-break → write+commit contract → dispatch Codex (`run_in_background`+`dangerouslyDisableSandbox`, watchdog) → audit the REAL diff → Opus runs his OWN FULL suite (this hop wires into `processCompletedGame` → transitive-mock-break risk) → commit branch-only BY PATH → log.

branch/topology: **MAIN** `/Users/johnkruse/Projects/kbl-tracker` on `codex/franchise-v1-next` (HEAD = `7af4433e`). **trackerDb v26** (A1.3b bumped it 25→26 + registered all touchpoints). ⚠ **PAUSED — DO NOT TOUCH/MERGE (JK):** `codex/draft-pipeline-fix` + `codex/auction-draft-ux-rehaul`. A1.5d uses its OWN db `kbl-franchise-stadium-records` (NOT trackerDb → NO trackerDb bump).

resume_note: **SOLE WORKER — claim the baton FIRST:** `mv HANDOFF_NEEDED HANDOFF_DONE_$(date -u +%Y%m%dT%H%M%SZ)_<note>`; confirm no `codex exec` proc + no other `claude` on this branch. **Characterized baseline (ZERO-NEW-REDS gate): FULL suite ~8392 passing**; the ONLY fails = `wpaRuntimeBoundary` (HARD, fails solo — unrelated WPA subsystem) + `franchiseManualSmokeFixture` (order-flake, solo-passes 4/4; confirmed 3× this session). **Read the vitest SUMMARY (failed-file list), NOT the RC.** ⚠ Do NOT flip any Phase-2 flag (whole wave is build-dark + JK-review-pending). ⚠ A concurrent DOC-worker keeps `spec-docs/FRANCHISE_MODE1_MODE2_PLAYABLE_V1_GAP_ANALYSIS.md` (M) dirty — NEVER stage it; commit BY PATH only; leave untracked junk (HANDOFF_DONE_*, scripts/, *.csv, instructions/, PLAYER_NUMERIC_*) alone. **caffeinate PID 84474** (verify alive; re-`caffeinate -s -i` if not). The benign Codex stderr `failed to load skill .../mode2-pilot/SKILL.md: invalid YAML` is NOT a build error — ignore it.

**✅ A1.5d DEPENDENCIES ALL MET (Captain-verified this session):** A1.5a fame-fix DONE (`0cc319a0` — the per-game `updateReachFloor` ratchet is GONE from `franchiseFameCompute.ts`, grep-confirmed empty) · A1.5b carry converter DONE (`f3b48fbf`) · the storage + foundation ALREADY EXIST (`franchiseStadiumRecordsStorage.ts` 23KB + `franchiseStadiumFoundation.ts`; own db `kbl-franchise-stadium-records`, already in `SYNC_REGISTRY`/`backupRestore`). **The flag `isFranchisePhase2StadiumRecordsEnabled` is NOT YET defined** (L14 is taken — `franchiseRebrandOffer.ts:47`; add it to `franchisePhase2Flags.ts` following the existing pattern, default false).

**➡ A1.5d = the §4 catalog + the 6 §5 hops + the §8 display. BUILD HOP-BY-HOP (the spec itself says "seam + gate per hop"). Spec = `STADIUM_ANALYTICS_SPEC_V2.md` §4-§8 (Captain read it this session). Order + risk:**
- **HOP-1 DETECT (BUILD FIRST — contained, build-dark, no exhaustive-Record touch):** new `persistDarkStadiumRecordsForCompletedGame` gated by the new `isFranchisePhase2StadiumRecordsEnabled()`, in the dark-tap chain at `processCompletedGame.ts:1048-1095`. Build the per-game foundation report (`buildFranchiseStadiumFoundationReport` — input `BuildFranchiseStadiumFoundationInput` `franchiseStadiumFoundation.ts:126` = optional `stadiumSnapshots`/`completedGames`/`atBatEvents`/`fieldingEvents`; assemble from the season loads the way `franchiseL10SweepCompute.ts` already loads `getSeasonGames`/`getGameEvents` — "real hot-path compute, bounded" per §5, JK-accepted); read prior via `listFranchiseStadiumRecords` BEFORE upserting; extend `upsertFranchiseStadiumRecordsFromFoundationReport` (`franchiseStadiumRecordsStorage.ts:527`) to RETURN `changes[]` = `{stadiumId, recordType, recordKey, priorValue, priorLeaderPlayerIds, newValue, newLeaderPlayerIds}` (SET = new identity/no prior; OVERTAKE = newValue beats a DIFFERENT sole holder; ties extend co-leadership SILENTLY — only a strict new SOLE holder fires a change). Changes are detected + persisted but NOT yet consumed (hops 2-6 consume them). FULL-suite gate. GROUND the upsert body (527-610) + the §4 record catalog (10 fame-bearing types, each with a polarity) before contracting.
- **HOP-2 FAME swap (§6, the centerpiece — touches fame):** polarity-signed `heatDeltas` concat onto `fameResult.playerHeatDeltas` at `processCompletedGame.ts:1051`→`resolveFameTap`; sign-law §6 (SET `+B·s`; OVERTAKE new `+B·s` / old `−B·s`); gate `isFramedPhase2FameEnabled` + per-bump sentinel; double-count guard (pride lives ONLY in fame heat).
- **HOP-3 FAN morale (§7 — ⚠ EXHAUSTIVE-RECORD TOUCH):** a NEW `PARK_RECORD_SET` `MasterMoraleEventType` (clone `PLAYER_MILESTONE`, FAN delta only, self-delta 0) → the `satisfies Record<MasterMoraleEventType>` completeness in `masterMoraleMatrix.ts` forces the entry; compose→apply for the stadium HOME team; gate morale flag + sourceEventId dedup. PLUS the home-park-rival 2× (`fanMoraleEngine.ts:250,374,405` — flip `vsRival` + a 2× `rivalMultiplier`; derive each opponent's W-L-at-your-park from the archive).
- **HOP-4 REPORTER (⚠ EXHAUSTIVE-RECORD TOUCH + LLM → browser-pending half):** NEW `STADIUM_RECORD` `NarrativeEventType` (`narrativeEngine.ts:77` + the `hedgingModifier`:592 + the `highStakes`:~640 exhaustive maps) + a `franchiseStadiumRecordNewsAdapter` (clone the L12 award adapter — PURE, build-dark) + a `franchiseStadiumRecordEmission` (clone `franchiseRelationshipFlareEmission.ts`). ⚠ The `generateSeasonNewsTake` LLM call is best-effort/browser-pending (same posture as L4b — build the PURE adapter, FLAG the live LLM emission for JK).
- **HOP-5 ALMANAC:** map the change → a `'park-record'` `AlmanacNarrativeArchiveEntry` + embed the standing record in the persisted season summary (`franchiseSeasonSummaryStorage.ts:701` de-orphan), independent of the LLM take.
- **HOP-6 PLAYER RIVALRY edge (⚠ writes a LIVE relationship edge):** an OVERTAKE stamps a `RelationshipEdgeRow` (old↔new holder) via `putFranchiseRelationshipEdge` (a NEW event-spark writer bypassing the attribute-only formation engine; use the `RIVALRY`/unused `HISTORY` slot), gate `isFranchisePhase2L13Enabled`.
- **§8 STADIUM STAT-DISPLAY (⚠ USER-VISIBLE → FLAG for JK browser sign-off, do NOT close):** per-stadium OPS/ERA/AVG/HR/K splits in the Team Hub stadium-analytics tab (display-only, distinct from §4 fame records).

**🟡 OPEN-DECISIONS FOR JK (logged in the ledger; the queue AFTER A1.5d is blocked on these — do NOT build them unattended):**
- **re-grade-per-checkpoint STORE** — compute ready (`scoreSmb4Player` from evolved ratings); the per-(player,checkpoint) grade STORE is the fork: a dedicated store = a trackerDb bump (JK authorized only A1.3b), reusing `franchiseTrueValueSnapshots` is entangled with the live trueValue writer (`processCompletedGame.ts:362`) + race-standings reader. Forks: (a) authorize a dedicated grade-snapshot store bump, or (b) rule the read-modify-write reuse.
- **RA-7 park-adjust** — the primary/cumulative ratings signal reads park-BLIND season rows; "per-game park-adjust" needs a net-new event re-aggregation that's NOT default-identity, and the spec forbids the 50/50 approx. Forks: (a) full-season event re-tally, (b) per-player weighted-avg park factor, (c) recent-leg-only.

**⚠ BROWSER/JK-PENDING BACKLOG accumulated (verified-complete, NOT closed — JK batched browser sign-off):** A1.4/L12-6 UI surfacing (AwardsWatchlist categories + All-Star UI feed + race standings) · L4b live LLM emission seam + processCompletedGame wiring + an `isFranchisePhase2L4bEnabled` flag · A1.3b fork-2 (the BOTH tax+one-shot ruling — most likely JK-review point) · the older BV backlog (BV-V8 park→WAR, BV-CAP-MISS, BV-RB-13b, A1.5b-2 SVG re-derivation [blocks Power-native], T-6b position-mismatch scout/analyzer flag display).

DISPATCH MECHANICS (proven all session; USE THE STALL-DETECTING WATCHDOG; macOS has no `timeout`; `model_reasoning_effort` ∈ high|xhigh — NEVER very-high):
  cd /Users/johnkruse/Projects/kbl-tracker
  export PATH="$HOME/.nvm/versions/node/v20.20.0/bin:$PATH"
  rm -f /tmp/codex-<ID>.out; : > /tmp/codex-<ID>.stdout
  ( PREV=0; STALL=0; ELAPSED=0
    while [ $ELAPSED -lt 1800 ]; do
      sleep 120; ELAPSED=$((ELAPSED+120))
      pgrep -f "codex exec.*codex-<ID>.out" >/dev/null 2>&1 || exit 0
      CUR=$(wc -c < /tmp/codex-<ID>.stdout 2>/dev/null | tr -d ' '); CUR=${CUR:-0}
      CHANGED=$(git -C /Users/johnkruse/Projects/kbl-tracker diff --name-only -- src 2>/dev/null | wc -l | tr -d ' '); CHANGED=${CHANGED:-0}
      if [ "$CUR" -le "$PREV" ] && [ "$CHANGED" -eq 0 ] && [ ! -s /tmp/codex-<ID>.out ]; then STALL=$((STALL+1)); else STALL=0; fi
      PREV=$CUR
      if [ $STALL -ge 3 ]; then echo "WATCHDOG STALL kill at ${ELAPSED}s" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null; exit 0; fi
    done
    echo "WATCHDOG 30m cap kill" >> /tmp/codex-<ID>.stdout; pkill -9 -f "codex exec.*codex-<ID>.out" 2>/dev/null ) &
  WDOG=$!
  sed -n '/<!-- ===== CONTRACT: <ID> /,/<!-- ===== END CONTRACT: <ID> /p' /Users/johnkruse/Projects/kbl-tracker/spec-docs/PROMPT_CONTRACTS.md \
    | NODE_ENV= ~/.local/bin/codex exec -C /Users/johnkruse/Projects/kbl-tracker --skip-git-repo-check -s workspace-write -c model_reasoning_effort=high -o /tmp/codex-<ID>.out - >> /tmp/codex-<ID>.stdout 2>&1
  RC=$?; kill $WDOG 2>/dev/null; echo "codex rc=$RC"
  # Launch-sanity (~25s): codex ALIVE + no real 400/401/quota before ending the turn. Captain audits the REAL diff (READ IT) + runs his OWN full suite.

session-done (2026-06-26, UNATTENDED AUTH-4 `/kbl-captain`): **3 features SHIPPED, all branch-only / Codex-built / Opus-audited-REAL-diff / Opus-ran-INDEPENDENT-FULL-suite / ZERO-NEW-REDS / build-dark.** **A1.3b `c87ff779`** (trade-demand FULL + the JK-authorized trackerDb v25→v26 bump — new `franchiseTradeDemandState` store + §13 flashpoint tax seam-fill [albatross-precedence] + one-shot `TRADE_DEMAND` morale; all touchpoints registered, parity-test green) · **A1.4/L12-6 data-layer `bcf12d0a`** (`allStarSelections` career counter — new sub-step in `runFranchiseAllStarLockPayouts`, type-routed SP/RP→pitching else batting, idempotent via parent lock-once; UI surfacing flagged) · **A1.5/L4b deterministic core `fee76d37`** (pure matrix-sourced season-take news adapter, reuses `'SEASON_SUMMARY'`, dormant like L10/L11; LLM emission flagged). Each contract committed first; each gated on the Captain's OWN full suite (8378→8392 pass across the session). Handed off at a CLEAN seam (nothing in flight) because A1.5d is a 6-hop multi-subsystem ticket (3 exhaustive-Record touches) that needs hop-by-hop fresh-context building, and the post-A1.5d queue is JK-decision-blocked.

READ ON RESUME (in order, via Read on exact paths — never trust summaries): `spec-docs/AUTONOMOUS_RUN_LOG.md` (newest 3 blocks = A1.3b · A1.4 · L4b, at the very bottom) · `spec-docs/CURRENT_STATE.md` LIVE HEADER (top 3 blocks) · `spec-docs/AUTONOMOUS_RUN_PROTOCOL.md` (AUTH-4 + the 4 safety walls) · `spec-docs/SESSION_RULES.md` · `spec-docs/STADIUM_ANALYTICS_SPEC_V2.md` §4-§8 (the A1.5d spec) · `spec-docs/V1_BUILD_QUEUE.md` (A-W1.5 row A1.5d).
